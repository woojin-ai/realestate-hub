# 2026-07-18 데일리 스탠드업

## ✅ 직전(07-17) 열려있던 액션 아이템 2건 — 이번 라운드에 전부 해소 확인
> 라운드17이 남긴 🔴/🟡 오픈 이슈 2건이 이후 커밋으로 반영됐고, **이번 라운드에 프로덕션 실측으로 종결 확인**했다. 사용자 추가 조치 불필요.
> - **🔴 #1 Vercel Resend 환경변수 (문의 폼 프로덕션 발송)** → **RESOLVED**. 사용자가 `RESEND_API_KEY`/`CONTACT_TO_EMAIL`을 등록하고 재배포를 트리거함(커밋 `af0646a`). 이번 라운드 프로덕션 `POST /api/contact` 실측 → **HTTP 200**(직전엔 설정 미완료로 500 반환하던 엔드포인트). 문의 폼이 배포 사이트에서 실제 동작함을 end-to-end 확인.
> - **🟡 페이지 타이틀 접미사 중복(terms/about/privacy/support)** → **RESOLVED**. 개발팀이 검증 중 발견해 수정(커밋 `3828432`). 이번 라운드 프로덕션 `<title>` 실측: `블로그 | 부동산 실거래가 대시보드`, `문의하기 | 부동산 실거래가 대시보드`, `소개 | 부동산 실거래가 대시보드` — 전부 접미사 1회만, 중복 없음.

---

## 라운드 18 (심야 자동세션): 리프레시 순환 계속 안정 + 직전 오픈 이슈 2건 프로덕션 종결 확인 — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태. 이번 라운드 프리워밍 호출 HTTP 200(30.5s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **51 → 55 전진**(nextIndex:55, processedCount:4). 처리 4개 지역 전부 `monthsCollected:13`(완전 유지, incomplete:false) + `fetchedMonths:1`(현재월 리프레시): 경기 하남시(41450)·용인 처인구(41461)·용인 기흥구(41463)·용인 수지구(41465). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **용인 수지구(41465, 대형)** `/api/data?lawd_cd=41465&months=13`: `source:cache`, 13/13 실데이터, 갭 0. 매매 current_avg 100,169(현재월 202607), 월별 count 324·436·903·811·258·580·637·623·474·692…(202507~) 실값. 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 직전 라운드 오픈 이슈 2건 프로덕션 실측 종결
- **문의 폼(Resend)**: 프로덕션 `POST /api/contact` → **HTTP 200**(테스트 제출 1회, 수신처=계정 이메일 본인함, 스팸 방지 위해 1회만). 라운드16이 남긴 "Vercel 환경변수 미등록 시 500" 조건이 사용자 등록(커밋 `af0646a`)으로 해소됨을 확인.
- **타이틀 중복**: 프로덕션 3개 페이지 `<title>` 실측, 전부 접미사 1회. 커밋 `3828432` 수정이 라이브 반영됨 확인.

### 4. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 시작 시 `git fetch`로 origin 동기 확인(HEAD=origin `af0646a`, 워킹트리 clean). 프로덕션 응답 정상(데이터 API `source:cache` 200, 문의 API 200, 정적 페이지 200). 이번 라운드는 팀 로그만 추가 커밋. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드18 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 55부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- ✅ **문의 폼(Resend) 프로덕션 발송 — RESOLVED**(환경변수 등록+200 확인). ✅ **타이틀 중복 — RESOLVED**(커밋 3828432 라이브). 직전 라운드 오픈 이슈 전부 종결.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(신규 최상위 페이지=IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- 🟢 콘텐츠 여력: `/blog`가 `guide` 카테고리 1개·글 3편으로 라이브(라운드17). 2차 확장(`howto` 등)은 `lib/blog.ts`의 `blogPosts` 배열에 글만 추가하면 목록 탭 자동 확장(코드 변경 불필요). IR 무변경이라 자율 진행 여지 있으나, 이번 라운드는 오픈 이슈 종결 확인에 집중해 신규 콘텐츠는 착수하지 않음 — 다음 라운드 판단.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 19: 크로스 사이트 내비게이션 위젯 이식 (계산기 허브 → 이 프로젝트)

### 배경
사용자가 계산기 허브(`C:\claude code webside`)에 만든 "크로스 사이트 내비게이션 위젯"(계산기 허브/부동산 실거래가 대시보드/청약레이더/대출모아 4개 자매 사이트를 서로 연결, 데스크톱 우측 고정 레일·모바일 우하단 FAB)을 이 프로젝트에도 이식해달라고 마스터를 통해 지시. 원본 스펙(`design/cross-site-nav-widget-spec.md`, 특히 §13 이식 가이드)과 실제 구현 코드(`components/CrossSiteNav.tsx`, `lib/cross-site-links.ts`)를 직접 Read해 스펙 문서가 아니라 **실제(최신) 코드를 기준**으로 이식(대출모아는 스펙 문서엔 `url:null`로 남아있으나 실제 코드엔 이미 `https://loan-compare-ten.vercel.app`로 배포·`status:"live"` 반영돼 있어 이 값을 그대로 가져옴).

### 이 프로젝트 전용 재계산 — 브레이크포인트 (핵심)
계산기 허브는 가장 넓은 컨테이너가 1024px(`max-w-5xl`)라 레일을 `xl`(1280px)부터 노출했지만, 그 값을 그대로 베끼면 안 되는 이유를 직접 검산:
- `grep -rn "max-w-" app/ components/`로 이 프로젝트의 전체 컨테이너 폭을 재확인 — 가장 넓은 것은 `app/page.tsx`(대시보드 홈)의 `max-w-[1200px]`, 나머지(블로그/약관/소개/문의/개인정보)는 전부 `max-w-3xl`(768px)로 더 좁음. `components/`에서 걸리는 유일한 `max-w-`는 `AptDetailModal.tsx`의 모달 폭(560px, 페이지 컨테이너 아님)이라 계산에서 제외.
- 공식(스펙 §4-1): `V_min = C + 2×(오프셋16 + 레일폭80 + 여백목표24)` = `1200 + 240 = 1440px`.
- `xl`(1280px) 검산: 거터 `(1280-1200)/2=40px` < 레일 점유 `16+80=96px` → **여유 -56px(콘텐츠와 겹침, 불가)**.
- `2xl`(1536px) 검산: 거터 `(1536-1200)/2=168px`, 여유 `168-96=72px`(목표 24px보다 넉넉, 안전).
- → **레일 노출 브레이크포인트를 `xl`이 아니라 `2xl`로 확정**(`hidden ... 2xl:flex` / `2xl:hidden`).

### 그 외 이 프로젝트 전용 재계산/치환
- **디자인 토큰**: 이 프로젝트엔 계산기 허브의 `brand-surface`/`brand-border`/`brand-text-secondary`/`brand-text-disabled`/`brand-bg`/`brand-primary`/`brand-text` 토큰이 없음(`app/globals.css`엔 `--color-brand`/`--color-brand-dark`만 존재). `grep -rn "text-gray-\|border-gray-"`로 이 프로젝트의 실제 관례(raw Tailwind gray 팔레트, 130건)를 확인 후 치환: `brand-primary→brand`, `brand-surface→white`, `brand-border→gray-200`, `brand-text-secondary→gray-500`, `brand-text-disabled→gray-400`, `brand-bg→gray-100`. 추가로 마스터 지시에 없던 `brand-text`(기본 텍스트, 계산기 허브 body 태그 토큰) 사용처를 코드 이식 중 직접 발견해 `text-gray-900`(이 프로젝트 h1/h2 등 기존 heading 관례와 동일, `app/terms/page.tsx` 등에서 확인)로 치환 — 그대로 뒀으면 `text-brand-text`가 정의되지 않은 죽은 클래스가 될 뻔한 부분.
- **`lib/focusRing.ts` 없음**: 새 파일을 만들지 않고 Tier A(`FOCUS_RING_CARD`) 값을 `components/CrossSiteNav.tsx` 안에 `brand-primary→brand` 치환한 인라인 상수로 대체.
- **z-index**: `AptDetailModal.tsx`가 오버레이에 `z-[1000]`(다른 곳은 sticky 테이블 헤더용 `z-10/20/30`)을 씀을 확인 — 위젯의 `z-40`이 이보다 낮아 모달이 열리면 자동으로 덮임(의도된 동작), 충돌 없음 확인.
- **`:has()` FAB 자동숨김 규칙 — 이식 중 직접 발견한 이슈**: 계산기 허브 원본 규칙은 `body:has(main input:focus, ...) .cross-site-fab`로 `<main>` 안쪽에 스코프돼 있었으나, `grep "<main" app/`로 확인한 결과 **이 프로젝트는 app/ 전체에 `<main>` 요소가 단 하나도 없다**(모든 페이지가 `<header>`/`<div>`/`<footer>`를 형제로 배치). 그대로 복사했으면 영구히 매치되지 않는 죽은 규칙이 될 뻔해서, `<main>` 조건을 제거하고 `body:has(input:focus, select:focus, textarea:focus) .cross-site-fab`로 재계산해 포팅.
- **마운트 위치**: `SiteFooter` 공용 컴포넌트가 없어 `app/layout.tsx`의 `<body>{children}</body>` 구조에서 `{children}` 바로 다음에 `<CrossSiteNav currentSiteId="realestate-hub" />` 배치(탭 순서상 본문 우선).

### 구현
- `lib/cross-site-links.ts` 신규 — 계산기 허브 실제 데이터 배열 그대로(4개 사이트 이름/설명/URL/순서 동일), 클래스명 토큰만 위 치환 적용.
- `components/CrossSiteNav.tsx` 신규 — 계산기 허브 실제 구현 이식 + `2xl` 브레이크포인트 + 토큰 치환 + 인라인 포커스 링 상수.
- `app/globals.css` 수정 — `:has()` FAB 자동숨김 규칙 추가(위 `<main>` 재계산 반영).
- `app/layout.tsx` 수정 — `<CrossSiteNav currentSiteId="realestate-hub" />` 마운트.

### 검증
- `npm run build` 성공 — 전체 라우트 회귀 없음.
- **브라우저 실측(로컬 dev, 포트 3000이 다른 세션과 충돌해 `.claude/launch.json`을 임시 3102로 변경 후 검증, 종료 후 3000으로 원복·`git diff` 없음 확인)**:
  - 1600px·1536px(`2xl` 경계): 레일 표시(`display:flex`), FAB 숨김. 콘텐츠 우측 끝(예: 1600px에서 x=1392.5)과 레일 좌측(x=1489) 사이 실측 간격 **96.5px**(목표 24px 이상 확보) — `getBoundingClientRect()`로 직접 측정해 겹침 없음 확인.
  - 1535px(경계 -1px)·1400px·1280px(계산기 허브라면 레일이 보였을 폭): 전부 레일 숨김(`display:none`)·FAB 표시(`display:block`) 확인 — **재계산이 실제로 반영됐음을 입증하는 핵심 검증 포인트, PASS**.
  - 모바일(375×812): FAB만 표시(56×56px, 우하단). `<summary>` 클릭(ref 기반 실제 클릭 이벤트)으로 패널 펼침 확인 — 계산기 허브(링크, "새 탭에서 열림"), 부동산 실거래가 대시보드(`aria-current="page"` + "현재 사이트", 옅은 파란 배경, 비-링크), 청약레이더(링크), 대출모아(링크, `href="https://loan-compare-ten.vercel.app"`, "준비 중" 아님) 4개 항목·순서·상태 전부 스펙대로 확인.
  - 지역선택 `<select>`에 실제 클릭 이벤트로 포커스를 준 뒤 `:has()` 규칙 매치 확인: `body.matches("body:has(input:focus, select:focus, textarea:focus)")` = true, `.cross-site-fab`의 `pointer-events`가 즉시 `none`으로 전환, `getAnimations()`로 `opacity 0→` 트랜지션이 실제로 트리거(`playState:"running"`)됨을 확인. **단, 이 세션의 브라우저 탭이 `document.visibilityState:"hidden"`(비전경 상태, 툴 환경 한계로 추정 — 스크린샷 캡처도 동일 원인으로 반복 타임아웃)이라 애니메이션 타임라인이 0에서 진행되지 않아 opacity가 최종 0까지 도달하는 것은 육안 확인 못 함.** 규칙 매치·트랜지션 트리거까지는 명확히 실측했으므로 실제 사용자 브라우저(전경 탭)에서는 정상적으로 150ms 페이드아웃될 것으로 판단하나, 100% 육안 확인은 아니므로 다음 라운드가 일반 브라우저에서 한 번 더 열어보는 것을 권장.
  - 콘솔 에러 0건(전 구간).
- 이번 라운드는 `docs/team-log/2026-07-17.md`가 아니라 **`2026-07-18.md`(이 파일)**에 기록 — 마스터 지시 시점 이후 다른 세션이 이미 "라운드18"을 오늘 날짜 파일(`2026-07-18.md`)에 기록해 두었음을 작업 중 발견, 일일 로그 관례(`AGENTS.md`: "일일 작업 로그")와 실제 최신 상태를 따라 이 파일에 "라운드19"로 이어붙임.

### git
- 시작 시 `git fetch`로 origin 동기 확인(HEAD `83f3147`=라운드18 로그, 워킹트리 clean). 신규 2파일(`lib/cross-site-links.ts`, `components/CrossSiteNav.tsx`) + 수정 2파일(`app/globals.css`, `app/layout.tsx`) 커밋 예정, push 직전 재차 `git fetch`로 충돌 확인. `.claude/launch.json`은 검증 후 포트 3000으로 원복 완료(diff 없음), 이번에도 커밋 대상 아님.

### 다음 라운드가 참고할 상태
- 크로스 사이트 내비게이션 위젯 라이브 — 계산기 허브/청약레이더/대출모아 3곳은 즉시 링크, 자기 자신(부동산 실거래가 대시보드)은 강조 표시. 4개 사이트 데이터·순서는 계산기 허브와 동일하게 유지해야 하므로, 이후 대출모아 등 사이트 정보가 바뀌면 계산기 허브 `lib/cross-site-links.ts`와 같이 갱신할 것.
- 🔵 입력 포커스 시 FAB 숨김(`:has()`)은 규칙 매치·트랜지션 트리거까지 실측 확인했으나 툴 환경의 백그라운드 탭 제약으로 최종 opacity 0 도달은 육안 미확인 — 여유 있을 때 일반 브라우저(모바일 실기기 또는 전경 탭)에서 한 번 더 열어 페이드아웃이 실제로 보이는지 가볍게 재확인 권장(코드/CSS 자체는 계산기 허브에서 이미 운영 검증된 동일 메커니즘이라 리스크 낮음으로 판단).
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트 라이브 유지. `/ranking`은 여전히 사용자 승인 대기. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 20 (심야 자동세션): 블로그 `howto` 카테고리 첫 글 신규 — IR 무변경 콘텐츠 확장

### 1. 프리워밍 — 리프레시 순환 계속
- 라운드 시작 직후 프리워밍 호출 HTTP 200(26.3s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **54 → 58 전진**(nextIndex:58/114, processedCount:4). 처리 지역: 경기 파주시(41480)·이천시(41500)·안성시(41550)·김포시(41570), 앞 3개 `monthsCollected:13`+`fetchedMonths:1`(현재월 리프레시), 김포시는 `incomplete:true`(fetchedMonths:0, monthsCollected:13 유지 — 이번 호출에서 현재월 신규 없음, 콜드 갭 아님). 전국 114개 1차 완주 상태 유지.

### 2. 프로덕션 헬스체크
- `/`(home) 200 · `/blog` 200 · `/api/data?lawd_cd=41465&months=1` 200. 라운드19에서 배포한 크로스 사이트 내비게이션 위젯 포함 전 구간 정상.

### 3. 이번 라운드 작업 — 블로그 `howto` 첫 글 (planning→dev→qa 핑퐁)
- **배경**: `lib/blog.ts` `blogPosts`는 3편 전부 `guide`였고 `howto`(활용법) 카테고리는 타입/라벨/배지만 정의되고 글 0편. `/ranking`(신규 최상위 페이지=IR 변경)은 자동세션에서 착수 보류 규칙이라, 이번 라운드는 IR 무변경인 순수 콘텐츠 확장을 선택.
- **planning-team**: 실제 기능(`AptDetailModal.tsx`/`DealsTable.tsx`/`page.tsx`)을 읽어 "지어낸 기능 없이" 완성 글 1편 반환. 주제=**단지 상세(평수별 실거래가) 모달 활용법**("아파트 이름을 눌러 보세요 …"). 저위험 사용법 안내로 YMYL 안전, content-strategy §4 금지표현 미사용.
- **dev-team**: `lib/blog.ts` `blogPosts` 배열 끝에 `apartment-detail-view-howto`(category:`howto`, publishedAt:2026-07-18) 1개 객체만 append. `DISCLAIMER` 상수 재사용. `npm run build` 성공, `/blog/apartment-detail-view-howto` SSG 편입. **코드 변경 = 이 파일 1개(66행 추가/0 삭제)**. 목록/필터 탭은 `getActiveBlogCategories()`(글 있는 카테고리만)로 동적 도출이라 "활용법" 탭 자동 노출, 컴포넌트 수정 불필요.
- **qa-team 독립검증**: 6개 항목(빌드·slug 유일성·타입 무결성·카테고리 탭 동적도출·콘텐츠 품질/기능 교차검증·회귀 없음) **전체 PASS**. 특히 글이 언급한 모든 기능·수치(점선밑줄 건물명 버튼·평수별 표·지형 경사 4라벨·학교 반경1km·어린이집·편의시설 반경500m 등)를 실제 컴포넌트 코드와 전수 대조해 지어낸 기능/틀린 반경 0건 확인. 비차단 문구 정밀화 제안 3건(선택, 사실오류 아님).

### 4. 마스터 최종 확인 / git
- 마스터 직접 `npm run build` 재확인 성공 + `git diff --numstat`=`66 0`(lib/blog.ts 1파일만). push 직전 `git fetch`로 origin 동기 확인. 이번 라운드 커밋: `lib/blog.ts` + 이 팀로그.

### 다음 라운드가 이어받을 상태 (라운드20 기준)
- ✅ 프리워밍 리프레시 순환 안정(인덱스 58부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 확장.
- 🟢 **블로그 콘텐츠 여력 확대**: 이제 `guide` 3편 + `howto` 1편. `howto` 2편째나 `insight`/`market` 착수 모두 `blogPosts` 배열 append만으로 IR 무변경 자율 진행 가능(카테고리 탭 자동 확장). 자동세션에서 안전한 생산 작업 트랙.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 기획+디자인 초안 준비 완료, 승인 즉시 dev→qa→push 착수 가능. 자동세션 착수 보류 유지.
- 🔵 크로스 사이트 위젯 FAB 페이드아웃(`:has()`) 최종 opacity 육안확인은 라운드19 툴 제약으로 미완 — 일반 전경 브라우저에서 가벼운 재확인 권장(리스크 낮음).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 21 (심야 자동세션): 블로그 `howto` 2번째 글 신규 — IR 무변경 콘텐츠 확장

### 1. 프리워밍 — 리프레시 순환 계속
- 라운드 시작 직후 프리워밍 호출 HTTP 200(26.9s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **58 → 60 전진**(nextIndex:60/114, processedCount:2). 처리 지역: 경기 김포시(41570)·화성시(41590), 둘 다 `monthsCollected:13`+`fetchedMonths:1`(현재월 리프레시)·`incomplete:false`. 전국 114개 1차 완주 상태 유지, 새 콜드 갭 0.

### 2. 프로덕션 헬스체크
- `/`(home) 200 · `/blog` 200 · `/api/data?lawd_cd=41465&months=1` 200. 라운드19~20 배포분 포함 전 구간 정상.

### 3. 이번 라운드 작업 — 블로그 `howto` 2번째 글 (planning→dev→qa 핑퐁)
- **배경**: 라운드20까지 `guide` 3편 + `howto` 1편(단지 상세 모달). `/ranking`은 자동세션 착수 보류(IR 변경)라 이번에도 IR 무변경 순수 콘텐츠 확장을 선택.
- **planning-team**: 실제 컴포넌트(`SummaryCards.tsx`·`MonthlyChart.tsx`·`DealTypeTabs.tsx`·`app/page.tsx`)를 직접 읽어 "지어낸 기능 없이" 완성 글 1편 반환. 주제=**조회 시 표 위에 먼저 뜨는 요약 카드(현재 평균가+전월/3개월/6개월/1년 변동률 4종)·월별 추이 그래프·매매/전세 탭 읽는 법**. 기존 모달 글과 스코프 분리(모달 열기 *전* 상단 요약 영역). 색·화살표 규칙(상승 빨강▲/하락 파랑▼/보합 회색━)·'수집 중' 표기까지 코드 근거로 서술, YMYL 안전(방향 표시일 뿐 좋고 나쁨 아님 명시).
- **dev-team**: `lib/blog.ts` `blogPosts` 배열 끝에 `summary-cards-trend-chart-howto`(category:`howto`, publishedAt:2026-07-18) 1개 객체만 append. `DISCLAIMER` 상수 재사용(body[1] 콜아웃, 하드코딩 아님). `npm run build` 성공, `/blog/summary-cards-trend-chart-howto` SSG 편입(블로그 4→5편). **코드 변경 = 이 파일 1개(+82/-0)**. 카테고리 탭은 `getActiveBlogCategories()` 동적 도출이라 컴포넌트 수정 불필요.
- **qa-team 독립검증**: 6개 항목(빌드·slug 유일성·타입/구조 무결성·카테고리 탭 동적도출·콘텐츠/기능 전수 교차검증·YMYL/회귀) **전체 PASS**. 글이 언급한 모든 라벨·수치·색·화살표(현재 평균 매매가/전세가, 전월/3개월/6개월/1년 대비, '수집 중', text-up #e53935 빨강▲/text-down #1e88e5 파랑▼/text-flat #888 회색━, '월별 평균가 추이'·x축 YYYY.MM·툴팁 'OO만원'/'데이터 없음'·spanGaps 결측월 이어그림·'거래 없음', 매매/전세 2탭, 최근 13개월·마지막 업데이트)를 실제 컴포넌트 코드와 전수 대조해 지어낸 기능/틀린 라벨/틀린 색규칙 0건 확인. sitemap 편입·`<title>` 접미사 1회도 확인. 비차단 제안 1건(y축 눈금에 '만원' 접미사는 툴팁에만 붙음 — 사실오류 아님, 판정 무관).

### 4. 마스터 최종 확인 / git
- 마스터 직접 `npm run build` 재확인 성공(16 static, 블로그 5 slug 전부 프리렌더) + `git diff --numstat`=`82 0`(lib/blog.ts 1파일만). push 직전 `git fetch`로 origin 동기 확인. 이번 라운드 커밋: `lib/blog.ts` + 이 팀로그.

### 다음 라운드가 이어받을 상태 (라운드21 기준)
- ✅ 프리워밍 리프레시 순환 안정(인덱스 60부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 확장.
- 🟢 **블로그 콘텐츠 여력**: 이제 `guide` 3편 + `howto` 2편. `howto` 3편째나 `insight`/`market` 첫 글 모두 `blogPosts` 배열 append만으로 IR 무변경 자율 진행 가능(카테고리 탭 자동 확장). 심야 자동세션 안전 생산 트랙 유지. 남은 실기능 후보(정렬/필터, 데이터 기준일·캐시 동작 등)는 아직 미소진.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 기획+디자인 초안 준비 완료, 승인 즉시 dev→qa→push 착수 가능. 자동세션 착수 보류 유지.
- 🔵 크로스 사이트 위젯 FAB 페이드아웃(`:has()`) 최종 opacity 육안확인은 라운드19 툴 제약으로 미완 — 일반 전경 브라우저에서 가벼운 재확인 권장(리스크 낮음).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 22 (심야 자동세션): 블로그 `howto` 3번째 글 신규 — AI 맞춤 추천 사용법 (IR 무변경) + QA 핑퐁 1회

### 1. 프리워밍 — 리프레시 순환 계속
- 라운드 시작 직후 프리워밍 호출 HTTP 200(27.5s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **60 → 64 전진**(nextIndex:64/114, processedCount:4). 처리 지역: 경기 광주시(41610)·양주시(41630)·포천시(41650)·여주시(41670), 4개 전부 `monthsCollected:13`+`fetchedMonths:1`(현재월 리프레시)·`incomplete:false`. 전국 114개 1차 완주 유지, 새 콜드 갭 0.

### 2. 프로덕션 헬스체크
- `/`(home) 200 · `/blog` 200 · `/api/data?lawd_cd=41465&months=1` 200 · `/blog/summary-cards-trend-chart-howto` 200. 전 구간 정상.

### 3. 이번 라운드 작업 — 블로그 `howto` 3번째 글 (planning→dev→qa 핑퐁, FAIL 1회 후 수정→PASS)
- **배경**: 라운드21까지 `guide` 3편 + `howto` 2편. `/ranking`은 자동세션 착수 보류(IR 변경)라 이번에도 IR 무변경 순수 콘텐츠 확장 선택.
- **planning-team**: 미커버 실기능 3후보(RegionSelector·DealsTable·AI추천)를 실제 코드로 비교 → **AI 맞춤 추천 기능**(`AiRecommendSection.tsx`+`RecommendForm.tsx`+`RecommendCard.tsx`)이 실기능이 가장 풍부하고 미커버라 선택. 예산·기준(이하/이상)·신축 기준연도·최소 세대수 입력, 4종 가중치 슬라이더(가격·역세권·신축·평지), 무호출 즉시 재정렬, 순위 메달·종합점수·항목별 막대 카드, 진행 표시·더 불러오기까지 실코드 근거 대조표와 함께 완성 글 반환. **실제 스키마 불일치도 선제 발견**: 마스터가 준 스펙의 `description`/`readingMinutes` 필드는 실제 `BlogPost` 인터페이스에 없고 `summary` 필드 사용(읽기시간은 `getReadingMinutes()` 자동 계산) — 실 스키마에 맞춰 작성.
- **dev-team**: `lib/blog.ts` `blogPosts` 배열 끝에 `ai-recommend-weights-howto`(category:`howto`, publishedAt:2026-07-18) 1개 객체만 append. 스키마 재확인(존재 필드만, body type 4종 paragraph/heading/callout/list 전부 `BlogSection` 유니온 유효, callout.variant="warning"/list.ordered=false 유효, `DISCLAIMER` 상수 참조) — 불일치 없음. `npm run build` 성공(17/17), `/blog/ai-recommend-weights-howto` SSG 편입(블로그 5→6편). 코드 변경 = 이 파일 1개.
- **qa-team 독립검증 1차 — FAIL 2건**(콘텐츠 정확성): (5-1) 글이 AI 섹션 위치를 "아파트 목록 표 **위쪽**"으로 서술했으나 실제 `app/page.tsx`는 DealsTable(173)→AiRecommendSection(175) 순서라 **아래**가 맞음. (5-2) 슬라이더 상대비율 예시를 "가격 45·역세권 35·신축 20·평지 10 → 합 100%"로 인용했으나 이는 정규화 전 원시값(합 110)이라 옆의 "합 100%" 라벨과 자기모순 — 실제 화면은 `round(w/110*100)`로 정규화한 **41·32·18·9(합 100)** 표시. 나머지 항목 1~4·5(그 외 전수 대조)·6(YMYL)·7(회귀) 전부 PASS.
- **마스터 수정**: 두 문장을 직접 교정 — (5-1) "표 위쪽"→"표 아래쪽", (5-2) 예시를 실제 렌더값 "가격 41·역세권 32·신축 18·평지 9 → 합 100%"로 교체 + "(기본값 45·35·20·10을 합이 100이 되도록 환산한 값)" 보충설명 추가 + "폼 위의"→"폼 안의"(RecommendForm 내부 위치 정합). `npm run build` 재성공(17/17).
- **qa-team 재검증 — 2건 모두 PASS 전환**: (5-1) app/page.tsx 렌더 순서·설계 스펙과 일치 확인. (5-2) 산술 재검산(45/110·35/110·20/110·10/110 반올림 = 41·32·18·9, 합 100)·RecommendForm 157–160 템플릿·"폼 안의" 위치 3자 일치 확인. **항목 1~7 전부 PASS, 커밋 승인.**

### 4. 마스터 최종 확인 / git
- 마스터 직접 `npm run build` 재성공(17 static, 블로그 6 slug 전부 프리렌더) + `git diff --numstat`=`90 0`(lib/blog.ts 1파일만). push 직전 `git fetch`로 origin 동기 확인(HEAD=origin `9522c4d`). 이번 라운드 커밋: `lib/blog.ts` + 이 팀로그.
- 비차단(제품 후속): AI 추천 접힘 배너 부제가 아직 3요소 "예산·역세권·신축 기준"으로 남아 평지 슬라이더 미반영 — 블로그와 무관한 제품 카피 이슈, 개발팀 후속 정리 대상(이번 커밋 스코프 밖).

### 다음 라운드가 이어받을 상태 (라운드22 기준)
- ✅ 프리워밍 리프레시 순환 안정(인덱스 64부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 확장.
- 🟢 **블로그 콘텐츠 여력**: 이제 `guide` 3편 + `howto` 3편. AI추천까지 커버해 남은 미커버 실기능은 RegionSelector(지역 선택, 글감 얇음)·DealsTable(정렬/컬럼, 기존 2글과 일부 중복 주의) 정도 — 다음엔 `insight`/`market` 첫 글로 다양화 검토 권장. 전부 `blogPosts` append만으로 IR 무변경 자율 진행 가능.
- 🟡 **제품 후속(비차단)**: AI 추천 접힘 배너 부제 "예산·역세권·신축 기준"이 4요소(평지 포함)와 어긋남 — 여유 있는 라운드에 dev-team이 카피 갱신 권장(qa가 발견, 이번 커밋 무관). **→ 라운드23에서 RESOLVED(아래).**
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 기획+디자인 초안 준비 완료, 승인 즉시 dev→qa→push 착수 가능. 자동세션 착수 보류 유지.
- 🔵 크로스 사이트 위젯 FAB 페이드아웃(`:has()`) 최종 opacity 육안확인은 라운드19 툴 제약으로 미완 — 일반 전경 브라우저에서 가벼운 재확인 권장(리스크 낮음).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 23 (심야 자동세션): AI 맞춤 추천 카피 ↔ 실제 4종 가중치 슬라이더 전면 정합 (라운드22 비차단 후속 종결) — IR 무변경, dev→qa 3라운드 핑퐁

### 1. 프리워밍 — 리프레시 순환 계속
- 라운드 시작 직후 프리워밍 호출 HTTP 200(30.2s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 랩어라운드 순환 중 **69 → 75 전진**(nextIndex:75/114, processedCount:6). 처리 6개 지역 전부 `monthsCollected:13`+`fetchedMonths:1`(현재월 리프레시)·`incomplete:false`: 인천 부평구(28237)·계양구(28245)·서구(28260)·강화군(28710)·옹진군(28720)·부산 중구(26110). 전국 114개 1차 완주 유지, 새 콜드 갭 0.

### 2. 이번 라운드 작업 — 라운드22가 남긴 🟡 비차단 후속 이슈 종결 (dev→qa 핑퐁 3라운드로 스코프 확장·완결)
- **배경**: 라운드22 QA가 "AI 추천 접힘 배너 부제 '예산·역세권·신축 기준'이 실제 4종 가중치 슬라이더(가격·역세권·신축·평지)와 어긋남 — 평지 누락 + 예산(필터)을 가중치로 오기"를 비차단으로 남겼다. `/ranking`은 자동세션 착수 보류(IR 변경)라, 이번 라운드는 이 IR 무변경 카피 정합 이슈를 종결 트랙으로 선택.
- **정답 기준(마스터 코드 확인)**: 가중치 슬라이더는 `components/RecommendForm.tsx` SLIDERS 4종 = 💰가격(price)·🚇역세권(subway)·🏗신축(new)·⛰평지(slope). **예산(budget)은 슬라이더가 아니라 이하/이상 필터 입력**이므로 '점수화 선호도' 서술에서 빠져야 함.
- **핑퐁 1 (dev→qa)**: dev-team이 `components/AiRecommendSection.tsx` 배너 2문구(200행 collapsed 설명·240행 펼침 부제)를 `가격·역세권·신축·평지`로 수정(+2/-2, 빌드 17/17). qa-team 독립검증 → **직접 수정분 PASS**, 그러나 **같은 유형의 잔존 4건 발견**: `lib/blog.ts:374(제목)/382(본문)/395(배너 축자인용)`, `app/support/page.tsx:53-54, 70-71`. 특히 **395행은 이번에 고친 배너 원문을 그대로 인용**하던 스테일 카피라, 배너 수정으로 오히려 문서↔UI 직접 모순 발생 → 반드시 동반 수정 필요.
- **핑퐁 2 (마스터 수정→qa)**: 마스터가 `lib/blog.ts` 3곳(제목 예산→가격, 본문 예산이나…→가격이나…평지, 395 인용을 라이브 배너와 축자 일치) + `app/support/page.tsx` 2곳(53-54·70-71) 수정(빌드 17/17). qa-team 재검증 → 수정 5건 PASS + **395=라이브 배너 축자 일치 PASS(직전 최우선 FAIL 해소)**, 그러나 **전면 재스캔으로 수정 범위 밖 2건 추가 발견**: `app/terms/page.tsx:89`(방금 고친 support와 직접 모순), `app/about/page.tsx:29-31`(예산을 '직접 조절하는 선호도'로 오기).
- **핑퐁 3 (마스터 수정→qa 종결)**: 마스터가 terms:89(`선호도(가격, 역세권, 신축, 평지)`)·about:29-31(`가격, 역세권, 신축, 평지에 대한 선호도`) 수정. qa-team 최종 재검증 4항목 **전부 PASS**: (1) 두 수정 4종 정확 일치·한국어 자연스러움 (2) **전면 최종 스캔 잔존 0건**(예산을 가중치/슬라이더/선호도로 오기 0·평지 누락 0; 예산이 남은 곳은 전부 RecommendForm 입력라벨·blog 필터설명·recommend/prewarm 시스템용어 등 정당한 필터 문맥) (3) 누적 diff 5파일 전부 텍스트 대칭·로직 무변경 (4) 빌드 17/17. → **"전면 정합 PASS — 커밋 승인".**

### 3. 마스터 최종 확인 / git
- 마스터 직접 `npm run build` 재성공(17 static) + 자체 전면 grep(`예산·역세권`/`예산, 역세권`/`예산이나 역세권`) → **NO STALE MATCHES**. `git diff --numstat` = `about 2/2 · support 4/4 · terms 1/1 · AiRecommendSection 2/2 · blog.ts 3/3`(전부 텍스트 대칭, 로직 무변경). push 직전 `git fetch`로 origin 동기 확인(HEAD=origin `f8d6aad`). 이번 라운드 커밋: 위 5파일 + 이 팀로그.
- **IR(정보구조) 무변경**: 신규 페이지/라우트/컴포넌트 0, 전부 기존 표시 카피 문자열 교체뿐. 자동세션 안전 스코프.

### 다음 라운드가 이어받을 상태 (라운드23 기준)
- ✅ 프리워밍 리프레시 순환 안정(인덱스 75부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 확장.
- ✅ **라운드22 🟡 비차단 후속(AI 추천 배너 카피 불일치) — RESOLVED**. 배너·블로그·소개·약관·문의(FAQ) 5개 파일에 흩어져 있던 "예산 오기 + 평지 누락" 동일 유형 카피를 전수 정합(예산=필터, 가중치 4종=가격·역세권·신축·평지). 전면 스캔 잔존 0 확인.
- 🟢 **블로그 콘텐츠 여력**: `guide` 3편 + `howto` 3편. 다음엔 `insight`/`market` 첫 글로 다양화 검토 권장(전부 `blogPosts` append만으로 IR 무변경 자율 진행 가능). 남은 미커버 실기능은 RegionSelector·DealsTable 정도.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 기획+디자인 초안 준비 완료, 승인 즉시 dev→qa→push 착수 가능. 자동세션 착수 보류 유지.
- 🔵 크로스 사이트 위젯 FAB 페이드아웃(`:has()`) 최종 opacity 육안확인은 라운드19 툴 제약으로 미완 — 일반 전경 브라우저에서 가벼운 재확인 권장(리스크 낮음).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 24 (심야 자동세션): 블로그 `insight`(지역별 인사이트) **카테고리 첫 글** 신규 — IR 무변경, 4번째 카테고리 개통 + QA 정확성 교정 1건

### 1. 프리워밍 — 리프레시 순환 계속
- 라운드 시작 직후 프리워밍 호출 HTTP 200(27.9s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **75 → 78 전진**(nextIndex:78/114, processedCount:3). 처리 지역: 부산 서구(26140)·동구(26170)·영도구(26200), 3개 전부 `monthsCollected:13`+`fetchedMonths:1`(현재월 리프레시)·`incomplete:false`. 전국 114개 1차 완주 유지, 새 콜드 갭 0.

### 2. 프로덕션 헬스체크
- `/`(home) 200 · `/blog` 200 · `/api/data?lawd_cd=41465&months=1` 200. 전 구간 정상.

### 3. 이번 라운드 작업 — `insight` 카테고리 첫 글 (planning→dev→qa 핑퐁, 전항목 PASS + 마스터 정확성 교정 1건)
- **배경**: 라운드23까지 `guide` 3편 + `howto` 3편이지만 `insight`(지역별 인사이트)·`market`(월간 시황)은 타입/라벨/배지만 정의되고 글 0편이었다. 라운드23이 "다음엔 insight/market 첫 글로 다양화 권장"이라 남겨, 이번 라운드는 그중 `insight` 첫 글로 **4번째 카테고리를 개통**(전부 `blogPosts` append만, IR 무변경). `/ranking`은 자동세션 착수 보류 유지.
- **⚠️ YMYL 최우선 제약으로 프레이밍 확정**: `insight`(지역별 인사이트)는 "○○구 상승세"류 특정 지역 시세 방향·미래예측·투자권유·하드코딩 숫자로 흐르기 쉬운 中위험 카테고리다. 그래서 첫 글을 **"특정 지역을 판단하지 않고, 대시보드의 지역 데이터를 오해 없이 읽는 해석 원칙"** 방법론 프레이밍으로 확정(지역명은 구조 설명용 가상 예시로만, 시세 방향 0).
- **planning-team**: 실제 코드(`RegionSelector`·`DealTypeTabs`·`SummaryCards`·`DealsTable`·`AptDetailModal`·`MonthlyChart`·`BuildingTypeToggle`·`lib/analyzer.ts`·`app/page.tsx`)를 전수로 읽어 "지어낸 기능·숫자 없이" 6개 해석 원칙(①집계단위=구 ②평균가보다 거래건수 먼저 ③평균은 구성변화로 섞인 값 ④'수집 중'·최근달 신고지연 감안 ⑤매매/전세 분리 ⑥빈 데이터=지역 특성)+체크리스트 완성 글 1편 + **기능·용어 근거 대조표**(20여 항목, 파일:라인) + YMYL 안전성 근거 반환. 면책 콜아웃 `variant`는 과제 예시(info)가 아니라 **기존 6글 관례·`blog.md §3-2`에 맞춰 `warning`**으로 자기판단 조정(마스터 승인 — 中위험 카테고리에 더 강한 면책 톤이 안전, DISCLAIMER 상수 참조는 준수).
- **dev-team**: `lib/blog.ts` `blogPosts` 배열 끝에 `regional-price-difference-reading-principles`(category:`insight`, publishedAt:2026-07-18) 1개 객체만 append(스키마 준수: 존재 필드만·body 4종 유니온 유효·callout.variant="warning"·list.ordered=false·`DISCLAIMER` 상수 참조). `npm run build` 성공(18/18), `/blog/[slug]` SSG **6→7** 편입. **코드 변경 = 이 파일 1개(+95/-0)**. 카테고리 탭은 `getActiveBlogCategories()` 동적 도출이라 "지역별 인사이트" 탭 자동 노출, 컴포넌트 수정 불필요.
- **qa-team 독립검증 — 6개 항목 전부 PASS**: (1)빌드·SSG 6→7 (2)slug 유일·타입/구조 무결성(스키마 외 필드 0·body 유니온 유효·[1] 면책 DISCLAIMER 상수) (3)카테고리 탭 동적도출("지역별 인사이트" 신규 노출·배지 #00897b/#e0f2f1) (4)**콘텐츠 실코드 전수 대조**(집계단위=구·산술평균·기본정렬 count 내림차순·건물명클릭 평수별·전세 신규계약 기준 갱신제외·추이 빈달/'거래없음'·오피스텔 필터 등 전부 코드 일치, 지어낸 기능/틀린 라벨/틀린 정렬/틀린 집계 0) (5)YMYL(하드코딩 시세 숫자 0·금지표현은 전부 "판단하지 않는다" 부정문 용법·가상예시 명시) (6)회귀 없음(numstat 1파일 순수추가·sitemap 7 slug 편입). **비차단 사실부정확 1건**: 원칙2 "**바로 옆** '거래건수' 열"이 실제 열순서(건물명·연식·평균가·**전월대비**·거래건수·최근거래월)상 평균가와 거래건수가 인접하지 않아 "바로 옆" 위치 서술만 부정확(라벨·정렬·집계는 정확 → 항목4 FAIL 트리거 아님).

### 4. 마스터 최종 확인 / git
- **마스터 정확성 교정**: QA가 비차단으로 남긴 열-인접 부정확을 그냥 넘기지 않고 커밋 전 직접 교정 — "바로 옆 '거래건수' 열"→"**같은 표에 함께 표시되는** '거래건수' 열"(위치 함의 제거, 사실 정확화). `npm run build` 재성공(18/18, 블로그 7 slug 전부 프리렌더) + `git diff --numstat`=`95 0`(lib/blog.ts 1파일, 교정 후에도 순수추가 유지). push 직전 `git fetch`로 origin 동기 확인(HEAD=origin `abaeccb`). 이번 라운드 커밋: `lib/blog.ts` + 이 팀로그.
- **IR(정보구조) 무변경**: 신규 라우트/페이지 파일 0(동적 `[slug]`가 데이터 1건 늘어난 것뿐), 컴포넌트 수정 0. 자동세션 안전 스코프.

### 다음 라운드가 이어받을 상태 (라운드24 기준)
- ✅ 프리워밍 리프레시 순환 안정(인덱스 78부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 확장.
- 🟢 **블로그 콘텐츠 여력 — 3개 카테고리 개통**: 이제 `guide` 3편 + `howto` 3편 + **`insight` 1편**(총 7편). 남은 미개통 카테고리는 `market`(월간 시황) 1개 — 다만 '월간 시황'은 실제 시장 수치·방향 서술이 불가피해 **YMYL 리스크가 가장 높은 카테고리**라, 착수 시 하드코딩 숫자 금지·"어떻게 읽는가" 방법론 유지 등 이번 insight보다 더 강한 가드가 필요(신중 검토 권장). insight 2편째(다른 해석 원칙)나 howto/guide 심화가 더 안전한 다음 후보. 전부 `blogPosts` append만으로 IR 무변경 자율 진행 가능.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 기획+디자인 초안 준비 완료, 승인 즉시 dev→qa→push 착수 가능. 자동세션 착수 보류 유지.
- 🔵 크로스 사이트 위젯 FAB 페이드아웃(`:has()`) 최종 opacity 육안확인은 라운드19 툴 제약으로 미완 — 일반 전경 브라우저에서 가벼운 재확인 권장(리스크 낮음).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 25 (심야 자동세션): 블로그 `insight` **2편째** 신규 — 위치·생활여건 정보 해석 원칙 (IR 무변경) + QA 전항목 PASS

### 1. 프리워밍 — 리프레시 순환 계속
- 라운드 시작 직후 프리워밍 호출 HTTP 200(30.7s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **78 → 82 전진**(nextIndex:82/114, processedCount:4). 처리 지역: 부산 부산진구(26230)·동래구(26260)·남구(26290)·북구(26320), 4개 전부 `monthsCollected:13`+`fetchedMonths:1`(현재월 리프레시)·`incomplete:false`. 전국 114개 1차 완주 유지, 새 콜드 갭 0.

### 2. 프로덕션 헬스체크
- `/`(home) 200 · `/blog` 200 · `/blog/regional-price-difference-reading-principles` 200 · `/api/data?lawd_cd=41465&months=1` 200. 라운드24 배포분(insight 1편) 포함 전 구간 정상.

### 3. 이번 라운드 작업 — `insight` 2편째 (planning→dev→qa 핑퐁, 전항목 PASS)
- **배경**: 라운드24까지 `guide` 3편 + `howto` 3편 + `insight` 1편(=7편). 라운드24 핸드오프가 "다음엔 insight 2편째 또는 howto/guide 심화 권장, `market`(월간 시황)은 YMYL 최고위험이라 신중"이라 남겨, 이번 라운드는 **insight 2편째**를 선택(전부 `blogPosts` append만, IR 무변경). `/ranking`은 자동세션 착수 보류 유지.
- **planning-team**: 후보 (a)insight 2편째 vs (b)howto/guide 심화 중 (a) 선택 — 근거: 기존 insight(`regional-price-difference-…`)는 6원칙 전부가 *가격·거래건수·평균·매매전세 탭* 즉 **시세 숫자 해석**이라, 이번 **위치·생활여건(지하철 거리·지형 경사·학교·병원·편의시설) 해석**과 소재 겹침 0. 기존 howto(모달 여는 *기계적 사용법*)와도 성격 분리(이번은 *같은 값을 시세·가치와 오해 없이 읽는 원칙*). `AptDetailModal.tsx` 한 파일에 3섹션 라벨·임계값·반경·폴백이 실재해 6원칙 전부 실코드 뒷받침 가능 + YMYL 방어적("역세권→가치" 추론을 독자 스스로 차단하도록 가르침). (b)는 미커버 실기능이 조회 시작 흐름뿐이라 3 howto와 중복·얇음, 빌라/단독 토글은 국토부 승인 대기라 부적합. 완성 글 + **17행 기능·용어 근거 대조표**(주장↔파일:라인) + YMYL 안전성 근거 반환.
- **주제/slug**: `location-info-reading-principles`(insight) — 6개 해석 원칙(①위치값=시세지표 아님 ②'가장 가까운 1곳'·'반경 안 개수'만, 목록/등급 아님 ③경사 점수=좌표 기반 상대지표, 공식등급 아님 ④경로 경사 오르막/내리막=고도차 기준 ⑤'없음'=오류/나쁜신호 아님 ⑥입지↔실거래가 별개 출처, 인과로 안 엮음) + 체크리스트. 지역·단지 실명 0(가상 예시조차 안 씀).
- **dev-team**: `lib/blog.ts` `blogPosts` 배열 끝에 객체 1개만 append(스키마 준수: 존재 필드만·body 4종 유니온 유효·callout.variant="warning"·`DISCLAIMER` 상수 참조·list.ordered=false). `npm run build` 성공(19/19), `/blog/[slug]` SSG **7→8** 편입. **코드 변경 = 이 파일 1개(+92/-0)**. 카테고리 탭은 insight가 이미 활성이라 컴포넌트 수정 불필요.
- **qa-team 독립검증 — 6개 항목 전부 PASS**: (1)빌드·SSG 7→8 (2)slug 유일·타입/구조 무결성(스키마 외 필드 0·body 유니온 유효·[1] 면책 DISCLAIMER+warning) (3)카테고리 탭 동적도출(insight 기존 활성) (4)**콘텐츠 실코드 전수 대조 — 17항목 전부 일치, 사실오류 0**: 3섹션 위치(451/599/706)·지하철 도보거리(484-491)·경사 임계 80/60/40 "높을수록 평탄"·좌표 확보 시에만(45-50,515)·경로 경사 고도차 ±2m 오르막/내리막(559-564)·'측정 불가'(573)·학교 반경1km·최근접1곳·"반경 1km 내 없음"(kakao-nearby:166,769)·어린이집/편의시설 반경500m 개수(254,415)·편의시설 6종 순서(128-135)·대형/동물병원 최근접1곳(792,807)·빈상태 3문구(478/627/733)·null필드 줄감춤(634-699)·입지 3종 독립 API(151/191/240) 전부 코드 일치, 지어낸 기능/틀린 라벨/틀린 임계값/틀린 반경 0(라운드24 "바로 옆 열" 류 재발 없음) (5)YMYL(하드코딩 시세 0·지역단지 실명 0·미래예측/투자권유/가치판단 0, 인과·가치 표현 전부 "~하지 않는다" 부정 프레이밍) (6)회귀 없음(numstat 1파일 순수추가·sitemap 8 slug 자동편입). **FAIL 0, 커밋 승인.**

### 4. 마스터 최종 확인 / git
- 마스터 직접 `npm run build` 재성공(19/19 static, 블로그 8 slug 프리렌더) + `git diff --numstat`=`92 0`(lib/blog.ts 1파일, 순수추가) + `git status -s`=lib/blog.ts 1건만. push 직전 `git fetch`로 origin 동기 확인(HEAD=origin `3cf93b9`=라운드24). 이번 라운드 커밋: `lib/blog.ts` + 이 팀로그.
- **IR(정보구조) 무변경**: 신규 라우트/페이지 파일 0(동적 `[slug]` 데이터 1건 증가), 컴포넌트 수정 0. 자동세션 안전 스코프.

### 다음 라운드가 이어받을 상태 (라운드25 기준)
- ✅ 프리워밍 리프레시 순환 안정(인덱스 82부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 확장.
- 🟢 **블로그 콘텐츠 여력 — guide 3 + howto 3 + insight 2 (총 8편)**. 미개통 카테고리는 `market`(월간 시황) 1개뿐인데 **YMYL 최고위험**(실제 시장 수치·방향 서술 불가피)이라 착수 시 하드코딩 숫자 금지·"어떻게 읽는가" 방법론 유지 등 강한 가드 필요(신중). 더 안전한 다음 후보: insight 3편째(또 다른 해석 원칙)·howto/guide 심화. 전부 `blogPosts` append만으로 IR 무변경 자율 진행 가능.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 기획+디자인 초안 준비 완료, 승인 즉시 dev→qa→push 착수 가능. 자동세션 착수 보류 유지.
- 🔵 크로스 사이트 위젯 FAB 페이드아웃(`:has()`) 최종 opacity 육안확인은 라운드19 툴 제약으로 미완 — 일반 전경 브라우저에서 가벼운 재확인 권장(리스크 낮음).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 26: `market`("시황 브리핑") 카테고리 활성화 — 뉴스 종합 + 자사 데이터 교차검증 첫 글 (마스터 직접 지시, ⚠️ push 보류)

### 배경
마스터가 직접 지시. `docs/planning/content-strategy.md` §7(2026-07-18 추가, 뉴스 종합 시황 브리핑 스펙)과 `docs/design/blog.md` §7(참고 기사 출처 링크 블록 구성안)이 이미 준비돼 있었고, 이번 라운드는 그 스펙을 실제로 구현 + 첫 글 작성까지 실행하는 라운드다. **외부 뉴스가 재료로 들어가는 첫 콘텐츠라 마스터 본인이 직접 검수(인용 정확성, 참고 기사 링크 유효성)한 뒤 push하기로 사전 합의 — 이번 라운드는 commit까지만 하고 push하지 않는다.**

### 1. 스키마/컴포넌트 변경 (content-strategy.md §7-5·§7-0, docs/design/blog.md §7-4·§7-5·§7-7 그대로)
- `lib/blog.ts`: `BlogSection` 유니온에 `sourceLinks` 타입 추가 — `{ outlet: string; title: string; url: string; publishedAt?: string }[]` (매체명·제목·URL 필수, 발행일 선택, 설계 스펙과 1:1 일치).
- `lib/blog.ts`: `BLOG_CATEGORY_LABEL.market`을 `"월간 시황"` → `"시황 브리핑"`으로 변경(배지 색 `#6a1b9a`/`#f3e5f5`은 코드 변경 없음, 그대로 유지 확인).
- `components/blog/BlogBody.tsx`: `case "sourceLinks"` 추가 — 구분선(`border-t border-gray-100`) + 작은 라벨(`<h2 className="text-xs font-semibold text-gray-500">참고 기사</h2>`) + 목록(매체명 · 기사제목(링크, `target="_blank" rel="noopener noreferrer"`) · sr-only "(새 탭에서 열림)" · 발행일). `formatBlogDateKo` 기존 유틸 재사용(신규 유틸 없음). `body` 배열 맨 마지막 요소로 넣는 관례라 CTA 박스 앞에 자동 배치됨(별도 분기 로직 추가 없음, `docs/design/blog.md` §7-1 근거 그대로).
- 코드 변경 파일은 이 2개뿐(`lib/blog.ts`, `components/blog/BlogBody.tsx`). 새 컴포넌트 파일·새 라우트·새 카테고리 유니온 값 없음(기존 `market`을 그대로 활성화하는 방식, §7-0 결정 그대로 따름).

### 2. 뉴스 리서치 — 6단계 프로세스 실행
- **1단계(광역 스캔)**: `NaverSearch-search_news`로 `"아파트 매매"`(30건)·`"아파트 전세"`(30건) `sort:"date"` 스캔. 전부 최근 7일 이내(2026-07-11~07-18) 기사. 반복 등장 후보: 경기 화성시 동탄(전국 상승률 1위, 8주 연속), 서울 외곽구(성북·구로·중랑·강서·노원·도봉·강북), 세종시 전세실종, 대출규제·금리인상 여파.
- **2단계(정밀 스캔)**: 후보별 재검색(`"동탄 아파트값 전국 1위"`, `"한국부동산원 7월 둘째 주 아파트가격동향"`, `"노원구 도봉구 강북구 아파트 상승률 외곽"`, `"성북구 아파트 상승률"`, `"세종 아파트 전세 실종"` 등 5개 쿼리 추가, 총 7개 쿼리로 약 155건 스캔). **한국부동산원이 2026-07-16 발표한 "7월 둘째 주(7월 13일 기준) 주간 아파트가격 동향"을 디지털데일리·연합인포맥스·서울경제·서울신문·한국경제·더팩트·경향신문·중앙일보·KBS·SBS·MBC 등 20곳 이상이 각자 별도 기사로(통신사 전재 아님, 매체별 헤드라인·구성·인용이 전부 다름) 독립 보도한 것을 확인 — 서울 0.30%(2주 연속), 성북구 0.49%(서울 1위)·구로구 0.44%(2위) 수치가 5개 이상 매체에서 동일하게 교차확인됨.
- **세종시 전세실종 소재는 조사했으나(뉴시스 단독 확인, 교차 매체 부족) 이번 원고 스코프에서 제외** — 스코프를 "서울 외곽 상승세" 단일 주제로 좁혀 800~1200자 안에서 깊이 있게 다루는 쪽을 선택.

### 3. 최소 3소스 요건 — 충족 (6개 매체, 최소 요건의 2배)
실제로 본문 재료로 쓰고 `sourceLinks`에 실은 매체: **디지털데일리·연합인포맥스·서울경제·서울신문·한국경제·더팩트**(6곳, 전부 서로 다른 회사·독립 취재/작성 — 통신사 재전재 0). "서울 0.30%/성북구 0.49%/구로구 0.44%" 수치는 디지털데일리·연합인포맥스·서울경제 3곳에서 동일 수치로 교차확인, "노원·도봉·강북 등 외곽으로 대출규제발 실수요 이동" 서술은 서울신문·한국경제·더팩트 3곳에서 교차확인.

### 4. 자사 데이터 교차검증 (`/api/data?lawd_cd={code}&months=13&building_type=아파트`, 로컬 dev 서버 포트 3102, 조회 기준일 2026-07-18)
포트 3000은 다른 프로젝트(계산기 허브) dev 서버가 점유 중이라 `npm run dev -- -p 3102`로 이 프로젝트 서버를 별도 실행해 조회(launch.json은 건드리지 않음, 검증 후 서버 종료 완료).

| 지역 | lawd_cd | 매매 전월 | 3개월 | 6개월 | 1년 | 최근월(7월) 건수 / 전월(6월) 건수 | 판정 |
|---|---|---|---|---|---|---|---|
| 성북구 | 11290 | -3.07% | +4.61% | +1.6% | **+15.89%** | 86 / 286 | **방향 일치**(뉴스: 이번 주 서울 1위 0.49%). 전월 마이너스는 7월 신고 건수가 6월의 30%뿐이라 표본 급감에 따른 것으로 판단, 이번 주 정확한 %는 계산방식이 달라 그대로 검증 불가함을 본문에 명시 |
| 구로구 | 11530 | +3.25% | +9.54% | +4.33% | **+22.0%** | 118 / 269 | **방향 일치**(뉴스: 2위 0.44%), 네 구간 전부 플러스로 성북구보다 표본도 안정적 |
| 노원구 | 11350 | +3.58% | +8.47% | +6.32% | **+13.93%** | 187 / 617 | **방향 일치**(뉴스: 대출규제발 외곽 유입) |
| 도봉구 | 11320 | -1.83% | +2.43% | -0.18% | +5.98% | 82 / 260 | **방향은 일치하나 완만** — 노원구만큼 뚜렷하지 않음, 균형 서술로 본문에 반영 |
| 강북구 | 11305 | +1.43% | +1.83% | +8.16% | +19.25% | **15** / 91 | **검증 대상에서 제외** — 7월 표본이 15건으로 지나치게 적어 이 지표로는 어떤 판단도 무리라고 판단, 본문에서 수치 인용 안 함(뉴스가 언급한 지역명 나열에는 포함하되 자사 데이터 검증 주장은 하지 않음) |
| 화성시(동탄 포함) | 41590 | -26.6%대 | -18%대 | -22%대 | -4~-5%대 | 410 / 2597 | **불일치·검증 불가로 판단, 본문에서 아예 제외** — 이 사이트는 화성시 전체를 한 단위로 집계하는데 뉴스의 "동탄구 0.73% 상승"은 화성시 내 특정 지구만 가리켜 지역 단위 자체가 안 맞고, 매매 전월 -26.6%는 신고 건수가 410/2597(16%)로 급감한 데다 화성시 전체(동탄 신축 고가 단지~외곽 저가 지역 혼재)라 구성효과가 매우 커 부동산원 주간지수(동탄 한 지구만의 헤도닉 지수)와 근본적으로 비교 불가 — §7-2 3단계 "불일치·확인 불가면 그 사실은 아예 쓰지 마라" 원칙 그대로 적용, 본문에서 동탄/화성 관련 수치·서술 전부 배제(단, 취재 소재로 사용한 기사 자체(디지털데일리·서울경제·더팩트)는 그 기사의 다른 부분(성북/구로/외곽 서술)을 실제로 재료로 썼으므로 `sourceLinks`에는 포함) |
| 세종시 | 36110 | - | - | - | - | - | 본문 스코프 제외로 교차검증 자체를 진행하지 않음(3항 참고) |

핵심 원칙(§7-2 3단계) 적용: **일치하는 지표(성북·구로·노원)는 자사 데이터를 근거로 명시, 애매하거나(도봉) 검증 불가한 것(강북·화성)은 각각 "완만함"을 병기하거나 아예 소재에서 제외** — 억지로 끼워맞추지 않음.

### 5. 집필 — 완전히 새 문장, 직접인용 0건
3~4단계까지 걸러진 사실만 재료로 새로 작성(기사 문장 재구성 없음). 직접인용은 아예 쓰지 않아 저작권·귀속 문제를 원천 차단(15단어 규칙조차 적용할 대상이 없음). 최종 분량 933자(공백 제외) / 1201자(공백 포함) — 목표 800~1200자에 부합.

### 6. 리스크 체크리스트(content-strategy.md §7-3) 9개 자가 검수
1. **출처 흐리기 금지**: 본문에 "디지털데일리와 서울경제는", "연합인포맥스는", "서울신문은", "한국경제 등 다른 매체도"처럼 구체 매체명을 전부 명시. "언론 보도에 따르면" 류 표현 0건.
2. **동일 소스 재전재 구분**: 6개 매체 전부 독립 취재/작성(헤드라인·구성 전부 다름) 확인 후 채택. 연합뉴스 등 통신사 전재본은 소스로 세지 않음.
3. **특정 매체 논조 대변 금지**: 직접인용 0건, 전부 수치·사실 서술만 사용. 특정 매체의 전망·분석을 이 글의 결론으로 쓴 곳 없음.
4. **시각 갈릴 때 균형 서술**: "도봉구는 1년 5.98%에 그쳐 노원구만큼 뚜렷하지는 않았습니다", "이번 주 0.49%·0.44%라는 수치 자체를 이 데이터로 그대로 검증하기는 어렵습니다" — 애매한 지점을 얼버무리지 않고 그대로 노출.
5. **전망성 인용 배제**: "오를 것"/"하락할 것" 등 미래예측 표현 0건(grep 자가검수로 확인, 6항 참고). 전부 과거·현재 시제 사실 서술.
6. **특정 단지·매물 실명 배제**: 동(정릉·하월곡동, 상계·중계동, 창·쌍문동 등) 이름까지 뉴스에는 나오지만 전부 배제하고 시군구(구) 단위(성북구·구로구·노원구·도봉구)까지만 서술. 단지명 0건.
7. **최소 3소스 미달 시 미발행**: 6개 매체 확보(요건의 2배) — 미발행 사유 없음, 정상 발행.
8. **자사데이터 불일치 시 배제**: 화성시(동탄) 전면 배제, 강북구 수치 배제(표본 15건) — 4항 표 참고.
9. **참고기사 링크 필수**: `sourceLinks`에 6개 항목(매체명+제목+URL+발행일) 전부 포함, 전부 curl 200 확인(아래 8항).

### 7. 검증
- `npm run lint` — 통과(경고 0).
- `npm run build` — 성공. `/blog/[slug]` SSG 8→**9** 편입(`seoul-outer-district-price-briefing-2026-07-18` 추가). 라우트 수 20개, 기존 라우트 전부 정상.
- **dev 서버 실제 렌더링 확인**(포트 3102, `preview_start`로 브라우저 연결): 신규 글 페이지 `get_page_text`로 전체 텍스트 확인(도입부→경고 콜아웃→2개 소제목 섹션→sourceNote 2회→정리 리스트→참고기사 6건→CTA→목록복귀 전부 정상 출력), 콘솔 에러 0건. **JS로 computed style 직접 측정**: 배지 `color: rgb(106,27,154)`(`#6a1b9a`)·`background: rgb(243,229,245)`(`#f3e5f5`) 스펙 그대로, warning 콜아웃 `background: rgb(252,228,236)`(`#fce4ec`)·`border-left: 4px rgb(198,40,40)`(`#c62828`) 스펙 그대로, 참고기사 소제목 `font-size:12px/font-weight:600`(`text-xs font-semibold`) 스펙 그대로, 6개 링크 전부 `target="_blank"`+`rel="noopener noreferrer"`+sr-only "(새 탭에서 열림)" 확인. 블로그 목록 페이지(`/blog`)에서 카테고리 탭이 "전체/용어·제도/지역별 인사이트/활용법/시황 브리핑" **5개 전부(최초로 4개 카테고리 탭 동시 노출)** 자동 렌더 확인(`getActiveBlogCategories()` 동적 로직 그대로 동작).
- **회귀 확인**: 기존 8편 슬러그 전부(`real-price-report-deadline-guide` 등) + 목록 페이지, curl로 HTTP 200 전수 확인. `regional-price-difference-reading-principles`(insight) 상세 페이지를 `get_page_text`로 재확인 — 콜아웃·본문·리스트·CTA 전부 이전과 동일하게 렌더(sourceLinks 섹션 없는 기존 글은 그 블록 자체가 안 나타남, 스키마 확장이 기존 8편에 부작용 없음 확인). `sitemap.xml`에 신규 슬러그 포함, 9개 blog slug 전부 확인.
- **금지 표현 grep 자가검수**: `오를 것\|사야 한다\|추천 단지\|저평가\|고평가\|수익률\|투자가치\|시세차익` — `lib/blog.ts` 전체에서 3건 매치되나 전부 기존 insight 2편(이전 라운드 QA 이미 PASS한 부정문 용법, 라인 484-498·642)이고 **이번에 추가한 신규 글(라인 663 이후) 범위에는 0건** — 라인 번호로 직접 대조해 확인함.
- **sourceLinks URL 접근성**: 6개 URL 전부 `curl -L -A "Mozilla/5.0..." -o /dev/null -w "%{http_code}"` → **전부 200** 확인(리다이렉트 없이 직접 200).

### 8. 최종 파일/커밋 (⚠️ push 안 함)
- 변경 파일 3개: `lib/blog.ts`(스키마+라벨+신규 글), `components/blog/BlogBody.tsx`(sourceLinks 렌더), `docs/team-log/2026-07-18.md`(이 기록).
- git add(파일 지정) + commit 완료. **git push는 실행하지 않음** — 마스터가 직접 인용 정확성·참고 기사 링크 유효성을 검수한 뒤 마스터 본인이 push하기로 사전 합의된 라운드(사용자 지시 원문 그대로 준수).

### 다음 라운드가 참고할 상태
- 🔵 **이번 글은 마스터 검수 대기 중 — push 안 됨.** 마스터가 인용 정확성·링크 유효성 확인 후 직접 push할 것. 다음 자동세션은 이 커밋을 임의로 push하지 말 것(마스터 명시 지시).
- 🟢 `market`(시황 브리핑) 카테고리 활성화 완료, 첫 글 1편 라이브 준비(로컬 검증 완료, push 전). 이제 5개 카테고리(guide 3·howto 3·insight 2·market 1) 구조. `content-strategy.md` §7-4가 제안한 "주 1회, 매주 월요일" 주기는 이번 라운드로 실행력 1회 확보 — 다음 주기 착수 여부는 마스터 확인 후 판단.
- 🟡 **세종시 전세실종** 소재는 조사만 하고 이번 글에서는 제외(교차 매체 부족+스코프 관리) — 다음 `market` 라운드 후보로 남겨둠.
- 🟡 **강북구·화성시(동탄)**는 이번엔 뉴스는 언급하되 자사 데이터 검증은 하지 않음(표본 부족/지역단위 불일치) — 향후 `/ranking`(전국 시군구 랭킹, 여전히 사용자 승인 대기)이 배포되면 지역명→코드 역참조·세분화된 지역 단위가 갖춰져 이런 케이스의 교차검증이 더 쉬워질 것으로 예상(§7-2 3단계 각주와 동일 맥락).
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시). `/ranking`은 여전히 사용자 승인 대기.

---

## 라운드 27: 헤더 내비게이션 메뉴 신규 (블로그/소개/문의하기 footer → 헤더 승격, 마스터 직접 지시)

### 배경
사용자가 실제 배포 사이트를 쓰다가 "블로그·소개·문의하기가 footer 맨 아래에 작은 글씨로만 있어서 있는 줄도 몰랐다, 위로 올려라"라고 직접 지적. 지금까지 이 3개 페이지로 가는 링크는 `app/page.tsx`의 `<footer>` 안 `text-xs text-gray-400` 텍스트뿐이었고, 헤더에는 내비게이션 메뉴 자체가 없었다(홈의 `<header>`는 그라디언트 배경+히어로 일러스트+타이틀뿐인 배너 형태). 이번 라운드는 디자인팀+개발팀을 겸해 기획~구현~검증까지 한 세션에서 수행.

### 시작 전 확인
- `app/page.tsx`: 홈 전용 `<header>`(그라디언트+히어로 일러스트, `<Image>` 우측 46%/40% 배치)에 내비 메뉴 없음, footer에 개인정보처리방침·이용약관·소개·문의하기·블로그 5개 링크.
- `app/layout.tsx`: `CrossSiteNav`(우측 고정 레일 2xl↑/모바일 FAB) 전역 마운트 확인, `<main>` 없이 `{children}` 직후 배치.
- `app/blog/page.tsx`·`app/blog/[slug]/page.tsx`: 자체 `<BlogHeader />`(그라디언트+로고, 내비 없음) 보유.
- `app/about`·`app/support`·`app/terms`·`app/privacy`: 최상위 헤더 자체가 없는 독립 페이지(콘텐츠 div로 바로 시작, 하단 "← 홈으로" 링크만 존재) — 확인.

### 설계/구현
- **`components/SiteHeader.tsx` 신규**: `HeaderNavLinks`(블로그/소개/문의하기 3개 링크, `usePathname()`으로 활성 판정 — `/blog`는 `/blog/[slug]`도 활성 대상)와 기본 export `SiteHeader`(로고+타이틀+`HeaderNavLinks`, 그라디언트 헤더) 둘 다 export. `SiteHeader`는 `pathname === "/"`면 자기 자신을 렌더링하지 않음(홈은 자체 히어로 배너가 대신 통합 — 완전히 다른 스타일 헤더 두 개가 겹치는 것 방지). 활성 링크는 `aria-current="page"` + 흰 배경/브랜드다크 텍스트 강조(`CrossSiteNav`의 `aria-current` 패턴을 재사용하되 다크 그라디언트 배경에 맞게 색상은 별도 계산 — 그대로 복사하면 안 보임).
- **`app/layout.tsx`**: `<SiteHeader />`를 `{children}` 앞에 전역 마운트(탭 순서상 본문보다 먼저, `CrossSiteNav`보다도 먼저).
- **`app/page.tsx`**: 기존 히어로 `<header>`의 `flex items-center gap-3`를 안쪽 wrapper로 옮기고, 그 아래 줄에 `HeaderNavLinks`를 세로로 추가(가로 `justify-between` 대신 세로 스택 선택 — 데스크톱에서 히어로 일러스트가 우측 40~46%를 차지해 겹칠 여지가 있어, 좌측에 세로로 쌓는 편이 항상 안전하다고 판단). 실측으로 확인(아래 검증 참고).
- **`app/blog/page.tsx`, `app/blog/[slug]/page.tsx`**: 자체 `<BlogHeader />` 제거(전역 `SiteHeader`가 대체 — 그대로 두면 그라디언트 헤더 2개가 겹침). 이제 아무도 참조하지 않는 `components/blog/BlogHeader.tsx` 삭제(grep으로 다른 참조 0건 확인 후).
- `/terms`·`/privacy`는 법적 고지 성격이라 헤더 메뉴로 올리지 않고 footer 유지(마스터 지시 그대로). 기존 footer 5개 링크는 삭제하지 않음(상단 메인 진입 + 하단 보조 진입, 중복 무해).

### 검증
- `npx tsc --noEmit` / `npm run build` 둘 다 성공, 라우트 20개 전부 정상(회귀 없음).
- **dev 서버 실측**: 이 세션의 cwd(계산기 허브)가 아닌 이 프로젝트 자체 dev 서버가 필요해, `preview_start`(name 기반)로 새로 띄우려던 시도는 Windows 경로 공백·harness 프로세스 관리 이슈로 반복 실패 — 우회책(`cmd`/`.bat` 래퍼, npm `--prefix`+8.3 short path)까지 시도한 끝에, **세션 시작 전부터 이미 떠 있던 이 프로젝트의 별도 dev 서버(포트 3102, PID 27564)**를 발견해 그걸로 검증(브라우저 탭이 이미 그 포트에 연결돼 있었음). 관련 임시 조치(다른 프로젝트의 `.claude/launch.json`에 테스트 항목 추가 등)는 전부 검증 직후 원상복구, `git diff` 없음 확인.
  - **데스크톱(1280/1600px)**: `/`·`/blog`·`/about`·`/support`·`/terms`·`/privacy` 전부 JS로 DOM 직접 확인 — 헤더 정확히 1개(중복 없음), 3개 링크 정상 렌더, 현재 페이지에 `aria-current="page"` 정확히 부여(예: `/about`에서는 "소개"만, `/terms`·`/privacy`는 셋 다 비활성 — 의도대로). 홈에서는 `HeaderNavLinks` 좌측 32~236px, 히어로 일러스트 좌측 시작 950px로 겹침 없음(`getBoundingClientRect` 실측), `CrossSiteNav` 레일(1488~1568px)과도 겹침 없음.
  - **모바일(375px)**: 홈·블로그·소개·문의하기 전부 3개 링크가 한 화면에 다 보이고(오프스크린 0), `CrossSiteNav` FAB(우하단 303~359px)와도 안 겹침.
  - **클릭 이동 확인**: 홈에서 "블로그" 링크를 실제 클릭 이벤트로 눌러 `/blog`로 클라이언트사이드 이동 확인.
  - **기존 기능 회귀 확인**: 시/도(서울특별시)→구(강남구) 캐스케이드 선택 정상, "조회하기" 클릭 → 로딩 배너 → 실데이터(현재 평균 매매가 32억 4,659만원 등) 정상 수신까지 end-to-end 확인. 헤더 변경으로 인한 기능 저하 없음.
  - **다만 `/blog/[slug]` 상세 페이지는 이 dev 서버에서 지속적으로 500(Turbopack "Jest worker … exceeding retry limit")** — 시스템 메모리 여유 2.8GB/15.7GB(약 18%)뿐인 환경 자원 부족이 원인으로 판단(이 서버가 오래 떠 있어 워커 풀이 소진된 것으로 추정, 코드 문제 아님). 근거: ①`npm run build`가 이 라우트를 포함한 9개 slug 전부 SSG 성공 ②동일 패턴(자체 헤더 제거)을 적용한 형제 페이지 `/blog`는 이 서버에서 정상 확인 ③해당 파일 변경은 import 1줄 + JSX 1줄 제거뿐. 다음 라운드가 여유 있을 때 이 dev 서버를 재시작해 `/blog/[slug]`도 한 번 더 육안 확인 권장(코드 자체의 리스크는 낮다고 판단).

### git
- 커밋 `ceede15`(`components/SiteHeader.tsx` 신규, `components/blog/BlogHeader.tsx` 삭제, `app/layout.tsx`·`app/page.tsx`·`app/blog/page.tsx`·`app/blog/[slug]/page.tsx` 수정 — 6파일). push 전 `git fetch`로 origin 동기 확인(HEAD=origin `2f4c25f`=라운드26, 충돌 없음) 후 push 완료.

### 다음 라운드가 참고할 상태
- ✅ **헤더 내비게이션(블로그/소개/문의하기) — 사용자 지적 사항 RESOLVED.** 전 페이지(홈 포함) 헤더에서 3개 메뉴 노출, 활성 페이지 강조, 데스크톱/모바일 모두 확인 완료.
- 🔵 `/blog/[slug]`는 이번 라운드 dev 서버(포트 3102)에서 워커 풀 소진으로 500 지속 — 코드 자체(빌드 성공·패턴 동일)는 문제 없다고 판단하나, 여유 있는 라운드에 서버 재시작 후 한 번 더 육안 확인 권장.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 자동세션 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 전부 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).
