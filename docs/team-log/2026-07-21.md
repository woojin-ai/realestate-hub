# 2026-07-21 팀 로그 (부동산 실거래가 대시보드)

## 라운드 33 — 시작 마커 (동시세션 방지책 ③ 선커밋)

- **계획**: 07-20 이월 1순위 = **부분월(최신월) 공시 P1(SummaryCards)·P2(MonthlyChart) 구현**. 기획 검토(`docs/planning/2026-07-20-partial-month-disclosure-review.md`)·디자인 구성안(`docs/design/partial-month-disclosure-spec-2026-07-20.md`) 둘 다 커밋 완료 상태 → 개발팀에 그대로 전달.
- **착수 전 제약 (스펙 재확인)**: ①표시 수치 불변(텍스트 추가 + 선/점 스타일만) ②문구가 값 변화 방향 단정 금지 ③"집계 중/수집 중" 표현 금지, 배지 확정안 `신고 진행 중`.
- **주의(2라운드 연속 FAIL 대응)**: 개발 산출물 받으면 **주석의 사실 주장을 별도 항목으로 검증**(특히 "왜 이렇게 안 했나" 문장의 발명된 기술 장애물).
- 프리워밍: 라운드 시작 시 인덱스 **57/114** 확인(HTTP 200, incomplete:false, 504 없음).

## 라운드 33 — 종료 (부분월 공시 P1·P2 완료 배포)

### 결과: ✅ 배포 완료 (commit `UI: 부분월 공시 P1·P2`, push 완료)

**개발팀 → QA 독립검증 → 마스터 직접 diff 검증** 전 단계 통과. 승인 스펙(`docs/design/partial-month-disclosure-spec-2026-07-20.md` §2 P1, §3 P2)을 그대로 구현.

**변경 3파일 (+78/−19):**
- `components/SummaryCards.tsx` — `isPartialMonth = current_ym != null && current_ym === getMonthKey(0)`일 때만 (A) 라벨 옆 `신고 진행 중` 배지, (B) 카드 하단 부분월 본문. **매매·전세 공통**(전세 모집단 공시만 전세 조건부 유지). `ymLabel`은 라벨·본문 공유(문자열 조립, 값 불변).
- `components/MonthlyChart.tsx` — `currentYm: string｜null` prop 추가(`summary.current_ym` 단일 소스). 마지막 구간만 `segment.borderDash [6,4]`, 최신월 점 반경5·흰속채움·테두리2, 캡션 + 툴팁 접미 `(신고 진행 중)`. `lastIdx=-1`이면 점선·강조·캡션·툴팁 전부 미노출.
- `app/page.tsx` — `<MonthlyChart ... currentYm={summary?.current_ym ?? null} />`.

**QA 판정: 7항목 + 개발팀 자가보고 2세부 전부 PASS, FAIL 0.** tsc exit 0 / build 22 페이지 QA가 직접 재현.

**🟢 주석 사실성 (2라운드 연속 FAIL 이슈) — 이번엔 PASS.** 추가 주석 5개 전부 코드 동작과 1:1 일치, 지어낸 기술 이유 없음. 마스터도 diff에서 직접 확인.

**마스터 직접 검증:**
- diff 전문 확인 — `values`/`data: values`/`current_avg` 렌더 경로 무변경, 스타일 배열·텍스트만 추가(표시 수치 불변 성립).
- 배포 후 라이브 실측 `/api/data?lawd_cd=36110`: 매매 `current_ym=202607 avg=47658`(최신월 count 167), 전세 `current_ym=202607 avg=27237`(count 318), 13개월 키 정상. **current_ym === getMonthKey(0)(202607)이라 부분월 조건이 실제로 라이브에서 참** → 배지/점선이 실렌더된다. 홈 HTTP 200.
- (클라 컴포넌트라 배지의 픽셀 렌더는 curl로 못 봄 — 코드 레벨 + 데이터 전제로 확인. §5 체크리스트 4·7·9·10 육안 항목은 미완, 다음 라운드/사용자 스팟체크 몫.)

### 프리워밍
라운드 중 2회 호출: 인덱스 **57 → 64** 전진(HTTP 200, incomplete:false, **504 없음**). 경기 진입 진행(용인·파주·이천 → 광주·양주·포천).

---

## 🔴 사용자 판단 필요 (전부 07-20에서 이월, 이번 라운드 신규 없음)

부분월 공시 P1·P2가 이 이월 결정들과 **독립적으로 방향 중립 설계**돼 있어(스펙 §1-2 자체 검증), 아래 결정이 나기 전에 배포해도 문구를 다시 쓸 필요 없음. 그래도 아래는 여전히 미결:

1. **전세 건수 모집단 통일 (A/B/C)** — 07-19 이월. 세종 블로그가 (B) 전제로 작성됨.
2. **자연키 확장 (A: 258→265) vs 현행 유지 (B)** — (A) 채택 시 발행된 `sejong-jeonse-briefing-2026-07-19` 본문 수정 동반 필요.
3. **해제(취소)거래 필터 (a 제외 / b 컬럼보존+공시 / c 현행)** — 세종 202607 매매 5건/133건=3.8%가 해제인데 미필터. 위 1·2와 성격 같아 묶어 결정 권장.
4. **DealsTable avg_price(최근3개월) vs count(13개월) 모집단 상이** — 매매 탭 공시 0. 위와 묶음 권장.
5. **직전월도 신고기한 미도래** — 이번엔 승인범위(당월)만. 직전월 확대 여부 별건.
6. **`/ranking` 신규 최상위 페이지** — 승인 대기, 착수 보류.
7. **스케줄 세션 중복 근본 대응**(주기 60분 확대 등) — 방지책 ③(시작 마커 선커밋) 계속 시행 중, 이번 라운드도 중복 없음.

## 다음 라운드 참고 상태

- 🟡 **범위 밖 발견 (별건 유지)** — `SummaryCards.tsx:38`… 아니라 현재는 배지 추가로 라벨 구조가 바뀜. **원 이슈: 변동률 null일 때 `ChangeCard`가 렌더하는 `수집 중`(주체 오독)이 새 `신고 진행 중` 배지와 한 화면에 병존.** P1으로 배지가 들어왔으므로 다음에 `수집 중` 표현 정리 시 우선 검토. (승인 범위 밖이라 이번엔 미변경.)
- 🟡 **부분월 공시 후속 P3~P5**(DealsTable.mom_pct / AptDetailModal / 추천) + **공시 문구 상수화 `lib/disclosures.ts`** — 이번 구현으로 하드코딩 공시문이 3곳→5곳. 통일 결정 나면 수정 지점 증가. 상수화 착수는 사용자 판단 후 권장.
- 🟡 **§5 육안 검수 미완** — 375px 배지·월표기 비잘림(4), 비부분월 점 외형 픽셀 동일(7·9), 스크린리더 캡션(10). 다음 라운드 dev 서버 스팟체크 또는 사용자 확인.
- 🟡 CS FAQ 8문답 / CS UX 제안(대시보드 화면 내 "진행 중인 달" 안내) — 위 사용자 판단이 정해져야 문구 확정.
- ✅ 연 경계 KST(라운드 32) / 월·날짜 경계 KST(31) 완료. 클라 3곳 의도적 예외 유지(`lib/kst.ts` 헤더 사유) — "일관성" 명목으로 건드리지 말 것.
- ✅ 빌라/단독·알림 계속 보류(2026-07-14 지시). "준비중" 유지.
