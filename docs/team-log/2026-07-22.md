# 2026-07-22 팀 로그 (부동산 실거래가 대시보드)

## 라운드 35 — 시작 마커 (동시세션 방지책 ③ 선커밋)

- **계획 (07-21 라운드34 인계 기준)**:
  1. **공시 문구 상수화 `lib/disclosures.ts`** — 하드코딩 공시문이 5곳으로 늘어난 상태(라운드33에서 3→5). **표시 문자열 바이트 단위 불변**을 조건으로 한 순수 추출 리팩터. 사용자 판단 대기 7건 중 어느 쪽으로 결정이 나도 수정 지점이 1곳으로 모여 이득이 크고, 문구 자체를 바꾸지 않으므로 승인 범위를 침범하지 않는다.
  2. **§5-6·7·8 픽셀 육안 재시도**(라운드34 환경 제약분) — 캔버스 스크린샷/리드백 가능하면 마무리, 안 되면 다시 사용자 스팟체크로 넘김.
- **제약**: ①렌더 문자열 1바이트도 바뀌면 FAIL ②조건부 로직(isPartialMonth 등) 손대지 않음 ③주석에 지어낸 기술적 이유 금지(3라운드 전 FAIL 이력).
- **사용자 판단 대기 7건**: 이번에도 신규 결정 없음 → 착수 안 함.
- 프리워밍: 라운드 시작 시 인덱스 **69 → 76/114** 전진(HTTP 200, incomplete 1건은 부산 동구 fetchedMonths:0, 504 없음).

## 라운드 35 — 종료 (공시 문구 상수화 배포 완료)

### 결과: ✅ 배포 완료 (commit `cafc084`, push 완료). 렌더 결과 무변경 리팩터.

**개발팀 → QA 독립검증 → 되돌림(주석 FAIL 1건) → 재수정 → 마스터 직접 검증** 전 단계 통과.

**신설 `lib/disclosures.ts` (7 export):**
`REPORTING_IN_PROGRESS_BADGE` / `CHART_TOOLTIP_PARTIAL_SUFFIX` / `partialMonthNotice(ymLabel)` / `partialMonthChartCaption(ymLabel)` / `JEONSE_POPULATION_NOTICE_SUMMARY`·`_TABLE`·`_MODAL`.

**수정 4파일 (+18/−18)** — `SummaryCards` 3곳, `MonthlyChart` 2곳, `DealsTable` 1곳, `AptDetailModal` 1곳. 문자열만 `{상수}`/`{함수(ymLabel)}`로 치환. `isPartialMonth`·`dealType === "전세"`·`lastIdx` 로직, className, DOM 구조 전부 무변경. 기존 주석의 사실 정보(스펙 §번호, `analyzer.ts avgJeonseDeposit`·`buildMonthlyStats` 출처)도 원문 보존.
- 추출 대상이 예상보다 2곳 많았다 — `DealsTable`·`AptDetailModal`의 전세 공시 문단을 개발팀이 발견해 함께 이동(공시문 하드코딩 5곳 → 실제 7곳).

**QA 판정: 제약 1·2·3 PASS, 제약 4(주석 사실성) FAIL 1건 → 되돌림 후 해소.**
- QA가 개발팀 스크립트를 재실행하지 않고 **독립 오라클**을 새로 구성: 원본/신규를 각각 `tsc --jsx react-jsx`로 트랜스파일해 JSX 접힘을 컴파일러가 확정시킨 뒤, `acorn`으로 AST의 한글 리터럴을 전량 덤프해 대조. 보간 케이스는 센티넬 문자열로 접두/접미까지 검사. **7/7 바이트 동일, 한글 리터럴 전량 diff에서 곁다리 변경 0건.**
- **FAIL 내용**: 헤더 주석의 "사용자에게 노출되는 공시 문구의 단일 출처"가 거짓 — 같은 주제(신고 지연·데이터 출처) 공시가 `app/about/page.tsx:34-41`, `app/support/page.tsx:23-35`, `app/page.tsx:195`에 여전히 하드코딩돼 있다. 나중에 이 주석을 믿고 "여기만 고치면 된다"고 판단하면 그 3곳이 어긋난다. → 범위를 명시한 문구로 재작성 완료(현 `lib/disclosures.ts:1-4`).
- **주석 사실성 이슈는 3라운드 중 2번째 PASS 흐름** — 이번 FAIL은 지어낸 기술 장애물이 아니라 **범위 과대주장** 유형(성격이 다름). 되돌림 1회로 해소.

**마스터 직접 검증:**
- `git diff` 전문 확인 — 순수 치환. 표시 수치 경로·className 문자열 무변경.
- 7개 문자열의 JSX 공백 접힘을 직접 재대조(보간 앞뒤 `달(`↔`)은`, `달(`↔`)입니다.`에 공백 없음, 툴팁 접미 선행 공백 1칸 보존) — QA 결론과 일치.
- `npx tsc --noEmit` exit 0, `npm run build` exit 0 / 22 페이지·라우트 목록 동일을 마스터가 직접 재실행.

### ⚠️ 인계 필요 — 이번 리팩터가 만든 결합
- `CHART_TOOLTIP_PARTIAL_SUFFIX`가 `REPORTING_IN_PROGRESS_BADGE`를 참조하도록 바뀌었다(원본은 독립 리터럴 2개). 현재 값이 같아 렌더 영향 0이지만, **승인 대기 결정에서 배지 텍스트만 바꾸면 차트 툴팁도 함께 바뀐다.** 결합은 풀지 않고 상수 옆 주석에 사실만 기록해 뒀다(`lib/disclosures.ts:9-13`).
- 보간 2곳은 children이 배열 3개 → 문자열 1개가 되어 SSR에서 React `<!-- -->` 텍스트노드 구분자가 사라진다. 보이는 문자·태그·스타일은 동일하고, 해당 문단은 클라이언트 렌더라 프리렌더 HTML에 원래 없다. **HTML 스냅샷을 바이트 비교하는 테스트가 생기면 이 점을 감안할 것.**

### 미착수 (사유 명시)
- **§5-6·7·8 픽셀 육안 재시도 — 이번 라운드도 안 함.** 라운드34에서 확인된 원인이 **프리뷰 페인의 캔버스 리드백/스크린샷 차단**(getImageData 전량 0, screenshot 30s 타임아웃)이라 같은 환경에서 재시도해도 결과가 같다. 환경이 바뀌거나 **사용자 스팟체크**로만 마무리 가능. (캡션 렌더로 `lastIdx>=0`이 참임은 확인돼 설정 경로 활성은 이미 성립.)
- **사용자 판단 대기 7건**: 신규 결정 없어 전부 미착수(아래 목록 그대로 유효).

### 프리워밍
라운드 중 2회 호출: 인덱스 **69 → 79/114** 전진(HTTP 200, **504 없음**). 인천 마무리(계양·서·강화·옹진) → 부산 진입(중·서·동·영도·부산진구). 1회차에 부산 동구가 `fetchedMonths:0, incomplete:true`로 나왔으나 **2회차에서 정상 수집(monthsCollected 13)** — 일시적 미수집이며 재호출로 해소됨.

### 배포 후 라이브 확인
- 홈 HTTP **200**.
- 배포된 클라이언트 청크(`/_next/static/chunks/265yktdb02333.js`)에서 `신고 진행 중` 문자열 존재 확인 → 상수 추출 후에도 번들에 문구가 정상 포함(임포트 경로 정상).

---

## 🔴 사용자 판단 필요 (전부 이월, 이번 라운드 신규 없음)

1. **전세 건수 모집단 통일 (A/B/C)** — 07-19 이월. 세종 블로그가 (B) 전제로 작성됨.
2. **자연키 확장 (A: 258→265) vs 현행 유지 (B)** — (A) 채택 시 `sejong-jeonse-briefing-2026-07-19` 본문 수정 동반.
3. **해제(취소)거래 필터 (a 제외 / b 컬럼보존+공시 / c 현행)** — 위 1·2와 묶어 결정 권장.
4. **DealsTable avg_price(최근3개월) vs count(13개월) 모집단 상이** — 매매 탭 공시 0.
5. **직전월도 신고기한 미도래** — 현재 승인범위는 당월만.
6. **`/ranking` 신규 최상위 페이지** — 승인 대기, 착수 보류.
7. **스케줄 세션 중복 근본 대응**(주기 60분 확대 등) — 방지책 ③(시작 마커 선커밋) 계속 시행, 이번 라운드도 중복 없음.

> 참고: 이번 상수화로 **1~4번 결정이 확정되면 수정 지점이 `lib/disclosures.ts` 한 파일로 모인다**(단, about/support/푸터 3곳은 별도 — 헤더 주석에 명시).

## 다음 라운드 참고 상태

- 🟡 **§5-6·7·8 픽셀 육안**만 미완 — 환경 제약이 원인이므로 **사용자 스팟체크 또는 캔버스 리드백 가능한 브라우저**에서만 마무리 가능. 같은 프리뷰 페인 재시도는 무의미.
- 🟡 **§5-3 무거래 지역 실측** — 07-22 기준 드롭다운 전 지역이 당월 거래 보유. 월초 또는 무거래 지역 발생 시 재시도.
- 🟡 **`SummaryCards.tsx:43`의 `수집 중`** — `신고 진행 중` 배지와 한 화면 병존(주체 오독 가능). 승인 범위 밖이라 미변경, 문구 정리 시 우선 검토. 상수화됐으므로 정리 시 `lib/disclosures.ts`로 함께 옮기면 됨.
- 🟡 **부분월 공시 후속 P3~P5**(DealsTable.mom_pct / AptDetailModal / 추천) — 사용자 판단 후.
- 🟡 용어 혼용 발견(기존 이슈, 이번 변경과 무관): "준비중"(`BuildingTypeToggle`, `app/page.tsx:113`) vs "준비 중"(`CrossSiteNav`).
- ✅ 연 경계 KST(라운드32) / 월·날짜 경계 KST(31) 완료. 클라 3곳 의도적 예외 유지(`lib/kst.ts` 헤더 사유) — "일관성" 명목으로 건드리지 말 것.
- ✅ 빌라/단독·알림 계속 보류(2026-07-14 지시). "준비중" 유지.
