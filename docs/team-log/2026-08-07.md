# 2026-08-07 (금) 팀 로그 — 라운드70

> 인계 정본은 `docs/team-log/2026-08-06.md`의 **라운드69 §11**이다(같은 날짜 파일에 라운드65~69가 함께 있다).

## 0. 상태 파악

- `git log`: HEAD가 **`960c898`(라운드68 종결)** 이었다. 즉 **라운드69는 작업을 끝내고 커밋 없이 종료**됐다.
- `git status`: 동결 3파일(`lib/db-cache.ts`·`lib/molit-api.ts`·`supabase/schema.sql`) **외에** `lib/blog.ts`(신규글 196줄)·`docs/team-log/2026-08-06.md`(라운드69 절)·`docs/planning/blog-deals-table-coverage-2026-08-06.md`(untracked)가 미커밋으로 남아 있었다.
- `git fetch origin`: `main...origin/main` 동기.
- `docs/planning/2026-07-22-pending-decisions-brief.md`: 신규 미결 추가 없음.

### 0-1. 라운드69 미커밋분 인수 — 검증 후 커밋

라운드69는 본문 5판·QA FAIL 2건 정정까지 마치고도 커밋을 남기지 않았다. 라운드70이 **직접 재검증한 뒤** 인수했다.

- `npx tsc --noEmit` **exit 0** / `npm run build` **exit 0** / `/blog/[slug]` **21경로**(static 33).
- 경로 명시 스테이징으로 3개 파일만 커밋 → **`f8ef94f`**, push 완료.
- 동결 3파일은 스테이징하지 않았다(**22라운드째**).

> ⚠️ 커밋 메시지를 Bash에서 PowerShell here-string(`@'...'@`)으로 넣어 앞에 `@`가 섞였다. push 전 `--amend`로 정정했다. **Bash 도구에 PowerShell here-string을 쓰지 마라**(사용자 전역 표준 ㋐).

## 1. 야간 프리워밍 — 인덱스 96 → **114**, 실패 0, 회차 처리량 11개

```json
{"ok":true,"processedCount":11,"failedCount":0,"attemptedCount":11,
 "nextIndex":114,"totalRegions":115,"elapsedMs":26271}
```

- 처리: **대전 대덕구(30230) → 울산 울주군(31710) 11개.** 27.2초. 잔여 **1개 지역**(114/115).
- 검산식 성립: `attemptedCount 11 == processedCount 11 + failedCount 0`.
- 96 + 11 = 107인데 114가 나왔다 → **표준 ㉹ 보강**(라운드69 §1-1)대로 **값이 커졌으므로 지연 크론 끼어듦**이다(갭 7). `incomplete` 원인이면 값이 작아진다.
- 이 회차에 **광주 5개 구가 전부 `fetchedMonths:1, incomplete:false, monthsCollected:13, statsGapMonths:0`으로 "정상 완료"** 보고됐다. 이것이 §2의 출발점이다.

---

## 2. 🔴 이번 라운드의 본체 — **10라운드 이월된 "광주 캐시 리셋" 티켓의 진단이 틀렸다**

### 2-1. 모순 발견

프리워밍은 광주를 "정상 완료"라고 하는데 `/ranking`은 광주 5개 구를 여전히 "집계 준비 중"(isPending)에 둔다. 둘 중 하나는 거짓이다.

라이브 `/api/data` 실측(마스터 직접):

| 지역 | `summary.매매.monthly` 13개월 | `current_ym` | `aptStatsMaeMae` |
|---|---|---|---|
| 울산 남구(31140) | 202605=383 / 202606=375 / 202607=161 / 202608=16 | 202608 | 30행 |
| 광주 동구(29110) | **13개월 전부 `{avg:null, count:0}`** | null | 0행 |
| 광주 서구(29140) | 동일 | null | 0행 |

세 지역 모두 `source="cache"`. 즉 **행은 있는데 값이 비어 있다.**

### 2-2. 원천을 직접 쟀다 — 국토부 API 직접 호출

`.env.local`의 `MOLIT_API_KEY`로 `RTMSDataSvcAptTradeDev`를 직접 호출했다.

- **광주 5개 구 전부 × 202603·202604·202605·202606·202607 = 25콜 전부 `resultCode=000 OK` + `totalCount=0`.**
- 대조군 같은 달(202606): 서울 강남 11680=**223** / 부산 강서 26440=**134** / 대구 수성 27260=**370** / 대전 대덕 30230=**116** / 울산 남구 31140=**385** / 세종 36110=**371**. 전부 정상.
- 아파트 **전월세** 엔드포인트도 동일: 광주 북구 29170=**0** vs 울산 남구 31140=**286**.

**→ 광주의 빈 값은 우리 캐시의 오염이 아니라 원천 부재다. 캐시를 리셋해도 다음 수집이 같은 0건을 다시 받아 `ready/13`으로 굳는다(1회 만에 원상복귀).**

### 2-3. 팬아웃 후보도 아니다 — 29xxx 전수 스윕

개발팀이 `lib/molit-api.ts`의 `LAWD_FANOUT`을 지목했다. 부천(41190)·화성(41590)이 **똑같은 증상**("정상 응답인데 totalCount=0")인데 하위 레거시 코드에 자료가 있어 팬아웃으로 풀린 사례다. 광주도 같은 경우인지 실측했다.

- **`29100~29299` 200개 코드 전수 스윕(아파트 매매 202606) → nonzero 0건.** 광주용 하위 코드는 존재하지 않는다.
- 🔴 **계측기 타당성 대조(표준 ㉼)**: 같은 스크립트로 `41590~41599`를 돌려 화성 하위 4개를 정확히 집어냈다 — **41591=122 / 41593=160 / 41595=423 / 41597=1979**, 나머지 6개는 0. **위 "0건"은 측정기가 죽어서 나온 0이 아니다.**
- ⚠️ 첫 스윕은 5단위(`29100, 29105, …`)로 돌려 **하마터면 잘못된 결론을 낼 뻔했다.** 부천·화성의 하위 코드는 `41192`·`41591`처럼 5의 배수가 아니다. 격자 간격이 대상의 주기와 어긋나면 전수처럼 보이는 표본이 된다(사용자 전역 표준 ㋉와 같은 함정).

### 2-4. 왜 10라운드나 잘못된 티켓으로 남았나

개발팀이 저장소 안에서 찾아냈다: **`docs/team-log/2026-07-28.md`의 QA 기록이 이미 같은 결론을 원문 대조로 내놓았다** — 29170이 202412·202301까지 0이라 **우리 캐시가 개입할 수 없는 과거 달도 0**이라는 결정적 근거까지 포함해서. 그런데 이후 라운드 문서들(`2026-08-04-round58.md`, `docs/qa/dead-region-audit-2026-08-06.md`)은 그 측정을 이어받지 못하고 "원인 미판별 / 사용자 캐시 리셋 1회"로 되돌아가 있다.

> **표준 ㊄ — 원천 측정 결과는 팀로그가 아니라 그 사실이 쓰이는 코드 옆에 남겨라.** 팀로그는 라운드가 지나면 읽히지 않는다. 07-28의 측정은 정확했는데 **전달 매체 때문에** 10라운드 동안 세 번이나 "원인 미판별"로 되돌아갔다. 그래서 이번엔 결론을 `lib/regions.ts`의 광주 코드 블록 바로 위에 적었다.

### 2-5. 이번 라운드가 한 조치

- `lib/regions.ts` 광주 블록에 **결론·측정치·계측기 대조·"팬아웃 시도 금지"**를 주석으로 고정.
- **코드 동작은 바꾸지 않았다.** 지역을 목록에서 빼거나 칩 문구를 새로 쓰는 것은 사용자 판단 사안이라 손대지 않았다(§6-1).
- 국토부가 왜 광주를 안 주는지는 **코드로 알 수 없다**. 주석에 추측을 쓰지 않았다.

---

## 3. 🔴 라이브 사실오류 1건 적발·정정 — H2 색 범례

기획팀이 카테고리 조사 중 발견했고 마스터가 코드로 재확인했다.

`lib/blog.ts` `summary-cards-trend-chart-howto`(H2) 색 범례 문단이 이렇게 닫혀 있었다:

> "…국내 시세 표기에서 흔히 쓰는 방식을 그대로 따른 것으로, **색만 봐도 방향을 빠르게 구분할 수 있습니다.**"

**08-04 표본 하한 배포로 거짓이 됐다.** `components/SummaryCards.tsx:83`이 저표본 카드를 `text-gray-600`(회색)으로 렌더하면서 `:66`의 화살표(▲/▼)와 부호는 **그대로 유지**한다(`:75-80` 주석이 그 의도를 명시). 즉 화면에 **상승인데 회색인 카드**가 존재한다.

단순 부정확이 아니다 — 같은 문단이 "변동이 없으면 회색"을 색 범례로 제시하므로, 독자는 그 카드를 **보합으로 오독**한다. 라운드67 §4-1·라운드69 FAIL-1과 **동일 유형**(같은 화면 안의 모순)이다.

**정정 방법**: 설명을 늘리지 않고 **지시 대상을 좁혔다**(라운드69 §4-3의 해법). 저표본 회색 표시 자체는 본문에서 설명하지 않는다 — 라운드69 마스터 판정으로 "본문 0문장"이 확정된 사안이고, 노출률이 1.1%(631장 중 7장, `analyzer.ts:336-338` 실측)라 설명 비용이 편익을 넘는다. 되돌림 금지 주석을 함께 달았다.

---

## 4. 이월 부채 정리 — 승인 완료 항목 3건 착수·종결

| 라운드69 §11 항목 | 상태 |
|---|---|
| **§11-5** `buildMonthlyStats` 주석 추가 | ✅ 완료 |
| **§11-6** `lib/analyzer.ts` 줄번호 포인터 전멸 (**5라운드 연속 미착수**) | ✅ 완료 |
| **§11-4** 의미 게이트 M1~M5를 `content-strategy.md`에 절 신설 | ✅ 완료 |

### 4-1. `buildMonthlyStats` 주석 (§11-5)

라운드69 FAIL-1의 발원지가 이 함수를 "무필터"로 읽은 오독이었다. 주석으로 못 박았다 — ① 건물 단위 필터(`isRealApartment`)는 전이폐포에 0건이지만 ② **레코드 단위 필터는 있다**(매매 `price>0`, 전세 `deposit>0` + 갱신 제외). 그리고 🔴 **같은 `MonthlyStat` 안에서도 `avg`와 `count`의 모집단이 다르다** — `count`는 거르지 않은 전체 건수다.

### 4-2. 줄번호 포인터 (§11-6)

`lib/blog.ts` 주석 → `lib/analyzer.ts` 포인터 **11개 전수**(`analyzer.ts:NNN` 형태 8개 + `vs :661`처럼 파일명이 생략된 형태 3개)를 현재 줄로 갱신하고, **갱신 후 전수 재대조**했다. 라운드68이 "6/6 전멸"로 잰 것이 실제로는 11개였다.

> ⚠️ 이번 라운드가 `buildMonthlyStats`에 주석을 넣어 `analyzer.ts` 줄이 **+26 밀렸다.** 포인터 갱신은 그 편집 **이후에** 했다. 순서를 반대로 하면 갱신하자마자 다시 죽는다.
>
> 그리고 갱신 작업 자체에서 같은 실수를 했다 — `count: records.length`를 `:316`으로 적었는데 **그 주석을 쓰는 행위가 그 줄을 :318로 밀었다.** 자기참조 포인터는 쓰고 나서 반드시 다시 재라(표준 ㋎의 또 다른 실례).

### 4-3. 의미 게이트 M1~M5 (§11-4)

`content-strategy.md`에 **§8 별도 절**로 신설했다(라운드69 승인대로 §4 하위가 아니다 — §4는 "쓰지 마라"(사전), §8은 "고칠 때 돌려라"(사후)라 성격이 다르다). §8-3에 표준 ㊀·㊃를, §8-4에 축자 게이트 병행과 어간 단위 금지어를 실었다. §8-5에 이번 라운드 적용 실적(§3)을 기록.

---

## 5. 콘텐츠 판단 — **오늘은 신규 발행 없음, 정정 1건으로 갈음**

기획팀이 카테고리 현황을 **코드로 재측정**했다(라운드69 수치를 승계하지 않고 반증 시도):

| 카테고리 | 편수 | 판정 |
|---|---|---|
| guide | 6 | 라운드69 수치와 **일치, 드리프트 0** |
| howto | 5 | 〃 |
| insight | 6 | 〃 |
| market | 4 | 〃 |
| **합계** | **21** | 최상위 `slug:` 21개 ↔ `category: "…"` 21개 교차 확인 |

- `market`은 주 1회 규칙상 **2026-08-10(월) 이후**.
- 기획팀이 라운드69의 "howto 표면 포화" 주장을 검증 → **결론은 참, 단 표면 목록은 6개가 아니라 9개**(RegionSelector·BuildingTypeToggle·AptDetailModal 누락). 미커버 표면 4개는 전부 실격(PendingRegions는 인용 금지 대상, ContactForm·CrossSiteNav·CategoryTabs는 소재 부적격).
- 🔴 **그런데 기획팀이 라운드69 포화 판정의 구조적 구멍을 지적했다** — 라운드69는 "표면이 글에 매핑돼 있는가"만 쟀고 **"그 글이 지금 코드와 맞는가"는 안 쟀다.** 그 결과가 §3의 라이브 거짓이다. **표면 포화는 신규 편수를 막을 뿐 정비 부채를 지우지 않는다.**

→ 오늘은 **신규 1편보다 라이브 거짓 정정이 우선**이라고 판단했다. 신규 글은 §6-2에 완전한 설계로 인계한다.

---

## 6. 다음 라운드가 이어받을 상태

### 6-1. 🔴 광주 티켓의 성격이 바뀌었다 — 재분류 필요

- **폐기**: "광주 5개 구 캐시 리셋 [10라운드째 사용자 조치 대기]". **이 조치는 광주를 고치지 못한다**(§2-2). 사용자에게 계속 요청하면 안 된다.
- **신규**: 광주는 **국토부 원천에 아파트 자료가 오지 않는 지역**이다. 코드로 할 수 있는 일이 없다. 남은 선택지는 셋이고 **전부 사용자 판단 사안**이다:
  1. 공공데이터포털/국토부에 광주 자료 미제공 문의 (사이트 밖 조치)
  2. `/ranking`·`PendingRegions`에서 광주를 **"집계 준비 중"이 아닌 다른 표시**로 옮기기 — ⚠️ **새 문구 창작이라 승인 사안**이다. 라운드68 §8-1의 옹진군 건과 **같은 범주**이므로 묶어서 판단하는 편이 낫다("자료가 곧 들어온다"는 기대를 만드는 칩을, 영구 희소/영구 부재 지역에도 쓰는 것이 맞는가).
  3. 그대로 두기.
- 참고: 프리워밍 응답은 **행 수를 한 번도 보지 않기 때문에** 완전히 빈 지역을 영구히 "정상"으로 보고한다(개발팀 규명). 로직 버그가 아니라 **지표 설계의 구멍**이고, 고치려면 새 지표가 생기므로 기획/설계 판단이 필요하다 — 임의로 손대지 않았다.

### 6-2. 🆕 다음 insight 1편 — 설계 완료, 집필만 남음

기획팀이 근거 조사·중복 회피·집필 제약까지 마쳤다. **slug 권고 `transaction-count-unit-reading-principles`.**

- ⚠️ **기존 이름 `transaction-count-population-reading-principles`를 버린다.** `population`(모집단)이 정확히 G4 `jeonse-new-vs-renewal-contract-guide`가 선점한 축이라 이름부터 중복을 부른다.
- 🔴 **"후보 B" 레이블을 폐기한다.** 기획문서 3개가 같은 이름으로 **서로 다른 글**을 1순위라 부르고 있다(`blog-aggregation-period-…md:54`는 "선 모양", `blog-deals-table-coverage-…md:352`는 이 글). 다음 라운드가 반드시 사고를 낸다.
- **핵심 명제**: 화면에 '건수'라고 적힌 값은 한 종류가 아니라 자리마다 **무엇을 한 건으로 세는지가 다른 여러 값**이며, 그래서 서로 더하거나 빼거나 나란히 견줄 수 없다.
- **원칙 5블록**: ①'건수'는 한 종류가 아니다 ②세는 단위가 자리마다 다르다(지역/건물/평수 묶음) ③평균을 만든 건수 ≠ 전체 신고 건수 ④기간·계약 구분이 겹쳐 든다(**I4·G4로 위임, 재서술 금지**) ⑤두 건수를 더하거나 빼지 않는다 — 맞춰 보는 순서
- **중복 회피**: G4 `blog.ts:945`가 전세 축을 **선점**(인접이 아니라 선점)했고, I4 `:1679`가 그 주제를 G4로 **종결 위임**해 뒀다. 원칙 4에서 지칭 1회로만 끊고 `신규`·`갱신`·`모집단` 어휘를 발행 텍스트 0건으로 QA 게이트에 건다. 내부링크 3곳 = G4/I4/I1로 소진, I6는 링크하지 않는다.
- **집필 제약**: `SAMPLE_FLOOR_N`(현재 5) 리터럴 본문 금지(`analyzer.ts:350-352`가 단일 소스 선언 — 화면 공시문이 `{n}` 보간이라 **축자 인용하면 숫자가 딸려온다**). 저표본 회색 표시는 본문 0문장. 노출률 1.1%·631장 등 실측치는 기획 근거로만. 지역명·단지명·금액 리터럴 0건.
- ⚠️ 라운드69가 든 근거("08-04 표본 하한이 라이브 글에 0건")는 **참이지만 주근거로 부적합**하다 — 노출률이 1.1%라 독자 99%가 못 본 화면 요소다. **보조 근거로 강등**하고 상시 노출되는 여러 '건수'를 주근거로 삼아라.

### 6-3. 프리워밍

**인덱스 114 / 115**, 잔여 **1개 지역**. 다음 회차에 1바퀴가 끝난다. `--max-time 110` 유지.

### 6-4. 이월 (그대로 유효)

- **동결 3파일 미커밋 22라운드째** — 결정 #3(b) DB 마이그레이션(`deals`에 `cdeal_type`·`cdeal_day`). **DB 적용이 코드 배포보다 먼저여야 한다**(`PGRST204`). 가장 오래된 차단이다.
- 라운드69 §11-7 미착수분 그대로: 라운드66 §7-①(카드 영구 10개 상한 오해, 문구 창작이라 승인 사안), 라운드63 §7(읽기시간 여유 경고, OBS-B, 블로그 링크 3단계 **9라운드째**, QA D-3·**O-5 10라운드째**, 건물유형 토글 동일유형 클릭 가드).
- 라운드68 §8-1 옹진군 칩 문구 승인 사안 — **§6-1-2와 묶어서 판단 권고**.
- 기획팀 부수 적발: 팀로그(라운드65 §3)가 PendingRegions 집필 금지 위치를 `lib/blog.ts:1426`이라 적었으나 실제는 **`:1435-1441`**(`:1426`은 닫는 중괄호). 팀로그는 과거 기록이라 정정하지 않았고 여기에만 적어 둔다.

---

## 7. 이번 라운드에서 새로 세운 표준

- **㊄** 🔴 **원천 측정 결과는 팀로그가 아니라 그 사실이 쓰이는 코드 옆에 남겨라.** 07-28의 정확한 측정이 전달 매체 때문에 10라운드 동안 세 번 "원인 미판별"로 되돌아갔다(§2-4).
- **㊅** 🔴 **격자 스윕의 간격이 대상의 주기와 어긋나면 전수가 아니라 편향 표본이다.** 5단위 스윕은 부천·화성형 하위 코드(41192·41591)를 원리적으로 못 잡는다. **전수를 주장하려면 간격 1로 돌려라**(§2-3).
- **㊆** **자기참조 줄번호 포인터는 쓰고 나서 다시 재라.** 포인터를 적는 행위 자체가 대상 줄을 민다(§4-2).

## 8. 사용자 판단/조치 대기

> ℹ️ **푸시 알림을 보내지 않았다.** 라운드59가 3회 연속 미전달(`Remote Control inactive`)을 실측한 이래 라운드60·62·63·65·66·67·68·69가 따랐고 이번에도 따른다.

1. 🔴 **[성격 변경] 광주 5개 구** — "캐시 리셋"은 **취소**다(효과 없음이 실측으로 확정). §6-1의 세 선택지 중 판단 필요. 특히 (2)는 라운드68 옹진군 건과 **묶어서** 결정하는 것이 낫다.
2. **[22라운드째, 가장 오래된 차단] 결정 #3(b) DB 마이그레이션** — `deals`에 `cdeal_type`·`cdeal_day` 추가. DB 적용이 코드 배포보다 먼저여야 한다.
3. **[신규, 비차단] 프리워밍 지표에 "행 수" 축이 없다** — 완전히 빈 지역을 영구히 "정상"으로 보고한다. 새 지표 신설이라 기획/설계 판단 사안.

---
---

# 2026-08-07 (금) 팀 로그 — 라운드71

> 인계 정본은 위 **라운드70 §6**이다. 라운드70도 라운드69와 똑같이 **작업을 끝내고 커밋 없이 종료**했다(2라운드 연속).

## 9. 상태 파악 + 라운드70 미커밋분 인수

- HEAD가 `f8ef94f`(라운드69분)에 머물러 있고, 라운드70의 성과물 5개 파일이 워킹트리에만 있었다.
- 마스터가 **직접 재검증한 뒤** 인수했다(dev/QA 보고를 승계하지 않음):
  - `npx tsc --noEmit` **exit 0** / `npm run build` **exit 0** / `/blog/[slug]` **21경로**(static 33)
  - 🔴 **`lib/analyzer.ts` 포인터 37개 전수 재대조 → 전건 일치.** 라운드70이 갱신한 11개뿐 아니라 인용된 모든 줄(`:143`·`:214`·`:216`·`:224-227`·`:235`·`:237`·`:246-255`·`:257`·`:278`·`:318`·`:336-338`·`:350-352`·`:384`·`:399-401`·`:436-442`·`:542`·`:565-569`·`:582-583`·`:598-608`·`:661`·`:673-674`)을 실제로 읽어 대조했다. 표준 ㊆(자기참조 포인터는 쓰고 나서 다시 재라)가 지켜졌음을 확인.
  - §3 색 범례 정정의 근거도 원문 확인: `components/SummaryCards.tsx:83`(`lowSample ? "…text-gray-600"`)·`:66`(arrow 유지)·`:75-80`(그 의도 명시 주석).
- 경로 명시 스테이징으로 5개 파일만 커밋 → **`7fd5fce`**, push 완료.
- **동결 3파일은 스테이징하지 않았다(23라운드째).** `.claude/settings.json`(untracked)도 제외.
- 커밋 메시지는 Bash heredoc(`git commit -F - <<'EOF'`)으로 넣었다 — 라운드70이 밟은 PowerShell here-string 함정(전역 표준 ㋐)을 피했다.

### 9-1. 라이브 배포 확인 (마스터 직접)

`/` · `/blog` · `/blog/deals-table-coverage-reading-principles` · `/ranking` 전부 **HTTP 200**. §3 정정문이 라이브에 반영된 것도 원문 대조로 확인했다("…그대로 따른 것입니다. 다만 방향을 확인할 때는 색보다 숫자 앞의 화살표를…").

## 10. 🟢 야간 프리워밍 — **초회 순회 완주**. 인덱스 114 → 0으로 넘어가 2바퀴 시작(7/115)

```json
{"ok":true,"processedCount":8,"failedCount":0,"attemptedCount":8,
 "nextIndex":7,"totalRegions":115,"elapsedMs":26594}
```

- 처리: **세종 36110(마지막 = 인덱스 114) → 서울 종로구 11110(인덱스 0) ~ 중랑구 11260 8개.** 27.7초, 실패 0.
- 검산식 성립: `attemptedCount 8 == processedCount 8 + failedCount 0`.
- 🟢 **인덱스가 114에서 7로 되돌아왔다 = 115개 전 지역 초회 순회가 이번 회차에 끝났다.** 라운드70 §6-3이 예고한 "다음 회차에 1바퀴가 끝난다"가 실현됐다. 이제부터는 재순회(갱신)다.
- ⚠️ **건수 표기 정정(표준 ㋢)**: 문서 여러 곳이 대상을 "114지역"이라 적고 있으나 런타임 `regions.length`는 **115**다(`app/api/cron/prewarm/route.ts:706`, 같은 파일 `:59` 주석도 115로 적고 있다). 앞으로 **115(런타임 기준)**로 인용해라. (참고: `lib/regions.ts`의 5자리 코드 리터럴 grep은 **132**건인데 이는 모집단이 다르다 — 프리워밍 대상 목록과 같지 않으므로 이 값으로 순회 진행률을 말하지 마라.)

## 11. 🔴 초회 순회 완주가 **미결 판단 #13의 착수 조건을 충족시켰다** — 사용자 판단 대기 항목의 상태 변경

`docs/planning/2026-07-22-pending-decisions-brief.md` **§8 결정 #13**("신고 기한이 안 지난 달을 재수집할 것인가")은 2026-07-27에 추가된 항목이라 **07-25 "권고안대로" 전체 승인의 범위 밖**이고, 지금도 사용자 판단 대기다.

그 §8-4 권고가 조건을 달아 두었다:
> "프리워밍이 아직 56/114로 초회 순회 중이라, 재수집 부하를 지금 늘리면 완주가 더 밀린다 — **115지역 초회 순회 완료 이후 착수**를 함께 권고한다."

**그 조건이 오늘 충족됐다(§10).** 즉 #13은 "조건 미충족으로 대기"가 아니라 **이제 순수하게 사용자 결정만 남은 상태**로 바뀌었다. 코드는 손대지 않았다(선택지 (가)·(다) 모두 라이브 공시 문구 신설/변경이라 승인 사안).

- 남은 선택지 그대로: **(가)** 현행 유지 + 공시 한 줄 보강 / **(나)** 직전 달까지 상시 재수집 / **(다)** 신고 기한이 남은 동안만 직전 달 재수집. 기획팀 권고는 **(다), 단 (가)를 먼저**.
- 차단성 아님(현행으로도 사이트 정상).

---
---

# 2026-08-07 (금) 팀 로그 — 라운드72

> 인계 정본은 위 **라운드71 §9~§11**이다. 라운드71도 커밋 없이 종료했다 — **3라운드 연속**(69·70·71). 이제 이것은 우발이 아니라 패턴이므로 §16-1에 별도 항목으로 올린다.

## 12. 상태 파악 + 라운드71 미커밋분 인수

- HEAD는 `7fd5fce`(라운드70분). 라운드71의 성과물이 워킹트리에만 있었다:
  - `lib/blog.ts` **+206행** — insight 7편째 신규 글(라운드70 §6-2 설계의 실행분). 글 위에 붙은 긴 주석 블록에 **QA FAIL-1~FAIL-6을 정정한 흔적**이 남아 있다 = 라운드71 안에서 QA 1회가 이미 돌았다.
  - `docs/team-log/2026-08-07.md` +46행(라운드71 §9~§11).
- `git fetch origin`: `main...origin/main` 동기.
- `docs/planning/2026-07-22-pending-decisions-brief.md`: 신규 미결 추가 없음(#13이 라운드71에서 "조건 충족"으로 상태만 바뀐 상태 그대로).
- ⚠️ **라운드71 로그에 §12 "다음 라운드가 이어받을 상태" 절이 없다.** 집필·QA·정정까지 마치고 인계 절을 쓰기 전에 끊긴 것으로 보인다. 그래서 라운드72는 인계문 없이 워킹트리만 보고 상태를 복원했다.

### 12-1. 🔴 프리워밍 시크릿 — 스케줄 작업 파일의 값이 라이브와 다르다

스케줄 작업 정의(`SKILL.md` §1)에 **평문으로 박힌 `secret` 값이 `.env.local`의 `CRON_SECRET`과 서로 다르다.** 작업 정의 자체가 "정확한 값은 `.env.local`을 Read해서 확인하라"고 단서를 달아 두었으므로 그대로 따랐고, **`.env.local` 값으로 호출해 HTTP 200을 받았다**(§13).

- 즉 작업 정의에 적힌 값을 그대로 쓰면 **401로 프리워밍이 조용히 실패**한다. 지금까지의 라운드들이 `.env.local`을 읽는 절차를 지켜 왔기 때문에 드러나지 않았다.
- 값은 여기에 적지 않는다(운영 원칙 4: 시크릿 평문 금지). **조치는 사용자 몫**이므로 §18에 올린다.

## 13. 야간 프리워밍 — 2바퀴째 진행, 인덱스 7 → **23**, 실패 0

```json
{"ok":true,"processedCount":9,"failedCount":0,"attemptedCount":9,
 "nextIndex":23,"totalRegions":115,"elapsedMs":25402}
```

- 처리: **서울 강서구(11500) → 송파구(11710) 9개.** 26.7초, 실패 0.
- 검산식 성립: `attemptedCount 9 == processedCount 9 + failedCount 0`.
- **갭 7이 2회 연속 재현됐다.** 이번 회차의 첫 처리 지역이 인덱스 22−8=**14**이므로 시작점이 7이 아니라 14였다 → 그 사이 **7개를 다른 실행이 처리**했다. 라운드70(96→114, 갭 7)과 **같은 크기**다.
  - 관측 사실은 "갭 = 7, 2회 연속 동일"까지다. 지연 Vercel 크론이 회차당 7개를 처리한다는 해석은 **아직 추론**이며 3회째 관측으로 확인할 값이다. 다음 라운드는 시작 인덱스를 **처리 목록 첫 지역에서 역산**해 갭을 반드시 기록해라(응답의 `nextIndex`만 보면 갭이 보이지 않는다).
- ⚠️ **신규 관측 — 송파구(11710)가 `fetchedMonths:0, incomplete:true`.** 최근 로그들에서 처음 나온 `incomplete:true`다. 나머지 8개는 전부 `fetchedMonths:1, incomplete:false, monthsCollected:13, statsGapMonths:0`. **`nextIndex`는 송파를 넘어 23으로 전진했다** — 즉 미완 지역을 재시도하지 않고 지나간다. 다음 순회에서 다시 잡히므로 즉시 결함은 아니지만, **라운드70 §6-1이 지적한 "프리워밍 지표에 행 수 축이 없다"와 같은 계열**(완주 여부를 인덱스 전진으로만 판정)이다. 코드는 손대지 않았다(지표 신설 = 기획 판단 사안, §18-3).

## 14. 라운드71 신규 글 인수 — 마스터 직접 재검증 + QA 독립 1라운드 → FAIL 1건 정정

라운드71의 QA 결과를 **승계하지 않고** 다시 돌렸다(FAIL-1~6이 실제로 반영됐는지 + 새 결함 여부).

### 14-1. 마스터 직접 측정

- `npx tsc --noEmit` **exit 0** / `npm run build` **exit 0** / `/blog/[slug]` **22경로**(static 33→**34**). 21편 → **22편**으로 정확히 1편 증가.
- **표준 ㋥ 준수 — QA가 보고한 핵심 4건을 relay하지 않고 직접 원문을 열어 재측정했다**:
  - `components/SummaryCards.tsx:124` = `{lowSample && (` → 캡션이 **조건부 노출**임을 확인. 본문이 "적히는 자리도 있는데"라는 존재 서술로 끊은 것이 맞다(상시 서술이면 라이브 거짓).
  - `components/AptDetailModal.tsx:419` `pct = s.count / total` vs **`:420` `barW = s.count / maxCount`** → 분모가 실제로 다르다. 본문이 백분율 숫자만 가리키고 막대를 언급하지 않은 것이 맞다.
  - `lib/analyzer.ts:601` `if (!area) continue;` → 원칙 5의 "평수 정보가 비어 있는 거래는 그 표에서 빠진다"가 참.
  - `lib/analyzer.ts:318`·`:661` 둘 다 `count: records.length` → 원칙 3의 비대칭 서술이 참.
- **발행 텍스트만 뽑아 게이트를 직접 재측정**(주석 줄 제거 후 문자열 리터럴 추출, **28 units / 3,498자**):
  - 금지 어휘·리터럴 **0건** — `신규`·`갱신`·`모집단`·`거래량`·`거래건수`·`비율`·`막대`·`저표본`·`회색`·`SAMPLE_FLOOR_N`·`그대로`·`전부`·`모두`·`빠짐없이`·`언제나`·`%`·`만원`·`억원` 전부 0.
  - 지역명 리터럴 **0건**(15개 토큰 전수). 아라비아 숫자는 **`원칙 1`~`원칙 5` 서수 8회뿐** = 실측치·기간 숫자 0.
  - 내부링크 **정확히 3곳**(I1·I4·G4), `deals-table-coverage` 링크 **0건**(주석 ⑤ 준수). `sourceNote`/`sourceLinks` 0, `DISCLAIMER` callout 1.
  - `git diff -U0` 훅 헤더 **단일 hunk / 206 insertions·0 deletions** → 기존 21편 **회귀 0** 실증.

### 14-2. ❌ FAIL-A (일반) — 열거를 개수로 닫는 표현 잔존 1개소, 정정 완료

라운드71이 FAIL-4로 본문 원칙 4의 "두 축이 더"·"세 축이 함께"를 걷어내면서 **체크리스트 4번째 항목은 놓쳤다**:

> "건수에는 기간이나 계약의 종류 같은 다른 축도 함께 걸려 있다 — 원칙 4에서 가리킨 **두 글이** 각각 다룬다."

앞절이 `같은`으로 축을 열어 두었는데 뒷절이 `두`로 개수를 닫는다. 값 자체는 현재 참이지만(원칙 4가 가리키는 글 = 2편) **링크가 하나 늘거나 줄면 즉시 거짓이 되는 자기참조 카운트**이고, 주석 ⑨가 금지한 유형(선례 `blog.ts:1875-1879`, 라운드57 (C))이다.

**정정**: `두 글이` → `글이`. 주석 ⑨의 "빠진 축을 설명해 메우지 말고 **세지 않는 표현**을 쓴다"대로 **설명을 늘리지 않고 숫자만 뺐다.** 12자 미만 변경이라 축자 중복 게이트 재측정은 불필요.

> 🔴 **표준 ㋎ 11연속.** FAIL-4를 고치는 정정문이 같은 종류의 오류를 같은 글 안에 남겼다 — 본문은 고치고 체크리스트는 안 고쳤다. **본문과 체크리스트는 같은 명제를 두 번 적는 구조라, 한쪽을 고치면 반드시 다른 쪽도 재라.** 이 글의 체크리스트 6항목은 원칙 1~5의 재진술이므로 구조적으로 항상 쌍이다.

정정 후 `npx tsc --noEmit` **exit 0** / `npm run build` **exit 0** 재확인. 개수로 닫는 표현 재측정 → 검출 4건은 전부 `한 종류가 아니다`·`한 곳이 아니다`·`붙잡는 것은 한 가지입니다`로 **전칭을 부정하거나 논지를 세우는 구문**이며 열거를 닫지 않는다(이 글의 중심 명제 자체다). 게이트 ⑨ 통과.

### 14-3. OBS-1 정정 — 주석 줄번호 범위의 경계가 한 줄씩 어긋나 있었다

주석이 areaBuckets 적재 루프를 `:597-605`, 표 생성을 `:607-609`로 적었다. 직접 재서 보니 **`:597`은 주석 줄·`:607`은 빈 줄**이고, 실제는 적재 루프 **`:598-606`** / 표 생성 **`:608-630`**(`.map()` 끝까지)이다. 참조 대상 구문은 맞아서 오지시는 아니었으나 **표준 ㊆**(자기참조 포인터는 쓰고 나서 다시 재라)의 취지대로 경계까지 정정하고, 왜 어긋났는지를 주석에 남겼다. 라운드70 §6-4의 `:1426`은 닫는 중괄호였다 건과 **같은 유형**이다.

### 14-4. QA가 남긴 관찰 2건 — 판정과 처리

- **OBS-2(원칙 2 "그렇게 넓게 묶지 않고"의 포함관계)**: 순위 건수는 **지역×1개월**, 목록 표 건수는 **표의 한 줄×13개월**이라 두 값은 집합 포함관계가 아니다 → 작은 지역에서는 목록 표 값이 더 클 수 있다. **라이브 거짓은 아니라고 판정했다** — 본문은 "무엇을 한 덩어리로 묶는가"라는 개체 층위만 말하고, 바로 다음 문장이 값 대소 추론을 부정하며(`어느 쪽 값이 더 크다고 해서 … 뜻이 되지는 않습니다`) 원칙 5가 뺄셈까지 금지한다. **문장을 늘리는 정정은 하지 않았다**(라운드69 §4-3·라운드70 §3의 해법 = 설명 추가가 아니라 주장 축소, 이미 축소돼 있다).
- **라벨 인용 노선의 코퍼스 불일치**: 이 글은 라벨을 인용하지 않는데(주석 ③-2) 라이브 I4 `:1698`·`:1755`는 `거래건수(13개월)`을 괄호까지 인용하며 "그렇게 확인하라"고 가르친다. **이 글 단독 결함이 아니라 코퍼스 전체의 정책 층위 사안**이라 이번 라운드에서 손대지 않았다 → §16-2에 신규 부채로 올린다.

## 15. 콘텐츠 — 블로그 **22편째** 발행(insight 7편)

| 카테고리 | 라운드70 | 라운드72 |
|---|---|---|
| guide | 6 | 6 |
| howto | 5 | 5 |
| **insight** | 6 | **7** |
| market | 4 | 4 |
| **합계** | 21 | **22** |

- slug `transaction-count-unit-reading-principles` / 축 = **"건수의 세는 단위"**. 기존 insight 6편의 축과 겹치지 않는다.
- `market`은 주 1회 규칙상 **2026-08-10(월) 이후**.
- 경로 명시 스테이징으로 **2개 파일만** 커밋(`lib/blog.ts`·`docs/team-log/2026-08-07.md`). **동결 3파일은 스테이징하지 않았다 — 24라운드째.** `.claude/settings.json`(untracked)도 제외.
- 커밋 메시지는 Bash heredoc(`git commit -F - <<'EOF'`)으로 넣었다(전역 표준 ㋐ — Bash에 PowerShell here-string 금지).

## 16. 다음 라운드가 이어받을 상태

### 16-1. 🔴 신규 — "작업은 끝내고 커밋 없이 종료"가 3라운드 연속이다

라운드69·70·71이 전부 성과물을 워킹트리에 남기고 끝났다. 매번 다음 라운드가 **재검증 비용을 다시 지불**하고 인수했다(이번 라운드도 tsc+build+QA 1라운드를 새로 돌렸다). 우발이 아니라 패턴이므로 원인 가설을 적어 둔다:

- 라운드 소요가 회차 간격을 넘긴다(계산기 허브 쪽에서 실측된 것과 같은 계열의 문제). 라운드71은 **인계 절(§12)을 쓰기 전에** 끊겼고, 라운드70·69도 마지막 단계가 커밋이었다.
- **대책 권고**: 검증이 끝난 산출물은 **인계 절 집필보다 먼저 커밋해라.** 지금 순서(작업 → 로그 → 커밋)는 가장 값비싼 산출물을 가장 마지막에 두고 있다. 로그가 없는 커밋은 다음 라운드가 `git show`로 복원할 수 있지만, 커밋 없는 작업은 워킹트리를 뒤져야 한다.
- ⚠️ 이 항목은 **사용자 조치가 아니라 다음 라운드의 실행 순서 문제**다. §18에 올리지 않는다.

### 16-2. 🆕 신규 부채 — 라벨 인용 노선이 코퍼스 안에서 갈린다 (비차단)

- 노선 A(인용): I4 `:1698`·`:1755`가 `거래건수(13개월)`을 **괄호까지 축자 인용**하며 그렇게 확인하라고 가르친다.
- 노선 B(비인용): 라운드57 상주 지시(`:1863-1870`)와 이번 글(주석 ③-2)은 **라벨을 인용하지 말고 자리로 지칭**한다.
- 둘 다 각자 근거가 있다(A는 독자가 화면에서 찾기 쉽고, B는 라벨이 바뀌면 죽지 않는다). **어느 쪽으로 통일할지는 기획 판단 사안**이고, 통일하면 라이브 글 본문이 바뀌므로 **QA를 거쳐야 한다.** 지금은 어느 글도 거짓이 아니라 비차단이다.

### 16-3. 프리워밍

**인덱스 23 / 115**, 2바퀴째. 다음 라운드는 ① `.env.local`에서 시크릿을 읽고(§12-1) ② **처리 목록 첫 지역에서 시작 인덱스를 역산해 갭을 기록**하고(§13, 갭 7이 3회째 재현되는지) ③ 송파구(11710)가 이번엔 `incomplete:false`로 잡히는지 확인해라. `--max-time 110` 유지.

### 16-4. 다음 글 후보 — 아직 설계 없음

라운드70 §6-2가 인계한 insight 설계는 **이번 라운드에서 소진됐다**(§15). 다음 글은 설계부터 새로 시작해야 한다. 라운드70 §5의 판정("표면 포화는 신규 편수를 막을 뿐 정비 부채를 지우지 않는다")이 그대로 유효하므로, **미감사 표면 QA 전수감사**(스케줄 작업 §2-3 노선)를 우선 검토할 것을 권고한다 — 라운드70 §3(색 범례 라이브 거짓)과 이번 §14-2가 모두 "발행된 글이 코드와 어긋난 자리"에서 나왔다.

### 16-5. 이월 (그대로 유효)

- **동결 3파일 미커밋 24라운드째** — 결정 #3(b) DB 마이그레이션. 가장 오래된 차단. §18-2.
- 🔴 **광주 5개 구** — 라운드70 §2가 "캐시 리셋은 효과 없음"을 실측 확정. 성격이 바뀐 채로 사용자 판단 대기(§18-1).
- **미결 판단 #13** — 라운드71 §11이 착수 조건(115지역 초회 순회 완주) 충족을 확인. 순수 사용자 결정만 남음. 비차단.
- 라운드68 §8-1 옹진군 칩 문구 승인 사안 — 광주 (2)와 **묶어서 판단 권고**.
- 라운드69 §11-7 미착수분 그대로: 라운드66 §7-①(카드 영구 10개 상한 오해), 라운드63 §7(읽기시간 여유 경고, OBS-B, 블로그 링크 3단계 **10라운드째**, QA D-3·**O-5 11라운드째**, 건물유형 토글 동일유형 클릭 가드).

## 17. 이번 라운드에서 새로 세운 표준

- **㊇** 🔴 **본문과 체크리스트는 같은 명제의 쌍이다 — 한쪽을 고치면 반드시 다른 쪽도 재라.** 라운드71이 본문의 "개수로 닫기"를 걷어내고 체크리스트의 같은 오류를 남겼다(§14-2, 표준 ㋎ 11연속).
- **㊈** **검증이 끝난 산출물은 인계 절 집필보다 먼저 커밋해라.** 3라운드 연속으로 가장 값비싼 산출물이 가장 마지막 단계에 놓여 유실 직전까지 갔다(§16-1).
- **㊉** **프리워밍 갭은 `nextIndex`가 아니라 처리 목록 첫 지역에서 역산해야 보인다.** 응답만 읽으면 다른 실행이 끼어든 구간이 드러나지 않는다(§13).

## 18. 사용자 판단/조치 대기

> ℹ️ **푸시 알림을 보내지 않았다.** 라운드59가 3회 연속 미전달(`Remote Control inactive`)을 실측한 이래 라운드60·62·63·65·66·67·68·69·70·71이 따랐고 이번에도 따른다.

1. 🔴 **[성격 변경, 라운드70부터] 광주 5개 구** — "캐시 리셋"은 **취소**(효과 없음 실측 확정). 라운드70 §6-1의 세 선택지 중 판단 필요. (2)는 옹진군 건과 **묶어서** 결정 권고.
2. **[24라운드째, 가장 오래된 차단] 결정 #3(b) DB 마이그레이션** — `deals`에 `cdeal_type`·`cdeal_day` 추가. **DB 적용이 코드 배포보다 먼저여야 한다**(`PGRST204`).
3. **[라운드70부터, 비차단] 프리워밍 지표에 "행 수" 축이 없다** — 완전히 빈 지역을 영구히 "정상"으로 보고한다. 이번 라운드에 **같은 계열 관측이 하나 더 붙었다**: 미완(`incomplete:true`) 지역을 재시도 없이 지나간다(§13 송파구). 새 지표 신설이라 기획/설계 판단 사안.
4. 🆕 **[신규] 스케줄 작업 정의의 프리워밍 시크릿이 라이브와 다르다** — `SKILL.md` §1에 평문으로 박힌 값과 `.env.local`의 `CRON_SECRET`이 불일치다. 작업 정의의 단서("`.env.local`을 Read해서 확인하라")를 지킨 라운드만 성공하고, 박힌 값을 그대로 쓰면 **401로 조용히 실패**한다. 값을 문서에 적을 수 없으므로 **사용자가 직접 작업 정의를 갱신하거나 그 줄에서 값을 지우고 `.env.local` 참조만 남기는 편이 안전**하다. (비차단 — 이번 라운드는 `.env.local` 값으로 정상 호출됨.)
5. **[라운드71부터, 비차단] 미결 판단 #13** — 착수 조건 충족 완료. 선택지 **(가)/(나)/(다)** 중 결정 필요(기획 권고: (다), 단 (가)를 먼저). 선택지 전부 라이브 공시 문구 신설/변경이라 승인 사안.
