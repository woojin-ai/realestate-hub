# 2026-07-27 (월) 팀 로그 — 라운드52

## 0. 상태 파악
- `git status`: `lib/db-cache.ts` · `lib/molit-api.ts` · `supabase/schema.sql` 3파일 미커밋(라운드48부터 5라운드째). **의도된 보류**이며 이번 라운드도 유지한다 — 운영 DB에 컬럼이 아직 없다(오늘 재확인: `GET /rest/v1/deals?select=cdeal_type` → HTTP 400 `42703 column deals.cdeal_type does not exist`). 사용자에게 07-26에 이미 통지했으므로 **재통지하지 않는다**(어제 로그 지시).
- 어제(07-26) 로그의 "다음 라운드 이어받을 상태"를 승계했다. 오늘이 **월요일**이라 `market`(시황 브리핑) 주 1회 발행 대상 라운드다.

## 1. 야간 프리워밍
```
HTTP:200 TIME:32.0s
{"ok":true,"processedCount":3,"nextIndex":56,"totalRegions":114,
 "processed":[용인시 기흥구 41463, 용인시 수지구 41465, 파주시 41480]}
```
- 세 지역 모두 `monthsCollected:13` · `incomplete:false` · 504 없음. 진행률 **56/114**(어제 50 → 오늘 56; 사이에 새벽 크론 1회가 더 돈 것으로 보인다).

## 2. 🔴 신규 적출 — `/ranking` "전월대비"가 전월이 아닌 달과 비교되고 있다 (라이브 오표시)

콘텐츠용 원자료를 직접 조회하다 발견했다. **라이브에 잘못된 숫자가 노출 중**이라 이번 라운드 최우선 작업으로 올렸다.

### 증상
`lib/ranking.ts:75`
```ts
const prev = sorted.find((r) => r.deal_ym < current.deal_ym && r.avg_price !== null);
```
`prev`는 "current보다 과거인 **가장 가까운 유효 행**"일 뿐 **직전 달이라는 보장이 없다.** 그런데 화면 라벨은 전부 전월이라고 단정한다 — `components/ranking/RankingTable.tsx:25` 컬럼 `"전월대비"`, `components/ranking/RankingHighlights.tsx:67-68` `"▲/▼ TOP 10 (전월 대비)"`.

### 실측 (2026-07-27, `monthly_stats` 아파트, `deal_ym>=202604` = `app/ranking/page.tsx:42` `getMonthKey(3)` 창)
- 변화율이 계산된 계열 **129개 중 10개가 202607 vs 202604(3개월 간격)**. 나머지 119개만 정상 1개월 간격.
- 해당 지역(매매·전세 각각): 부산 서구(26140) · 부산 동래구(26260) · 성남시 중원구(41133) · 이천시(41500) · 대전 중구(30140).
- **라이브 노출 확인**: 배포본 `/ranking` HTML의 매매 "▲ 상승 TOP 10 (전월 대비)" **10위 = 대전 중구 +6.25%** — 이 값의 비교 기준월은 2026-04다. 전세에서는 부산 서구 **+16.20%**가 상승 4위에 해당한다(같은 원자료로 재구성해 확인).

### 근본 원인 — `monthly_stats`의 커버리지 구멍 (`deals`는 멀쩡하다)
- 전세 기준 **108개 지역 중 47개에 결측 월이 있고, 결측 월 총합 345개월**. 최악은 13개월 창에 **2개월만** 있는 지역(41360·41115·28245·26710·26500·26470·11620 등).
- **원자료는 정상이다.** 금천구(11545)는 `monthly_stats`에 2026-04·05·06 행이 **없는데**, `deals`에는 각각 **87 / 109 / 77건**이 들어 있다.
- 원인 지점: `app/api/cron/prewarm/route.ts:252`의 `upsertMonthlyStats(..., allData)`에서 `allData`가 **그 실행에서 fetch한 달만** 담는다. 이미 `months_collected:13`인 지역은 매 실행 최신월 1개만 fetch하므로(`fetchedMonths:1`) **그 달의 stats 행 1개만 갱신**된다. 창이 매달 미끄러지는데 지역별 재방문 주기는 약 38라운드(114÷3)라, 그 사이 달들은 `deals`에만 남고 `monthly_stats`에는 영영 안 생긴다.

### 이번 라운드 처리 / 다음 라운드로 넘기는 것
- **이번 라운드**: 표시 정정만 한다 — `prev`가 **직전 달일 때만** 채택하고 아니면 `changePct`/`changeDiff`/`prevYm`을 `null`로. (3개월 변화율을 1개월 변화율들과 같은 순위표에 섞으면 순위 자체가 왜곡되고, 행마다 라벨을 바꾸는 대안은 TOP 10 카드 제목과 여전히 모순된다. 값을 비우는 쪽이 기존 `isPending`("집계 준비 중") 정직성 패턴과도 일치한다.)
- **다음 라운드 최우선**: `monthly_stats` 백필. `deals`에 원자료가 있으므로 **재수집(국토부 재호출) 없이 재계산만으로** 복구된다. 프리워밍의 upsert 범위를 "이번에 fetch한 달"이 아니라 "창 전체"로 넓히는 수정이 같이 필요하다. 라이브 순위 숫자가 실제로 바뀌는 변경이라 dev→qa 정식 경로로 진행할 것.

## 3. ⚠️ 신규 확인 — 완결월 숫자는 "수집 시점 스냅샷"이고 다시 갱신되지 않는다

콘텐츠 교차검증 과정에서 코드로 확정한 사실이다(버그로 단정하지 않고 정책 사안으로 올린다).

`app/api/data/route.ts:111-121`
```ts
const canUseDb = withinCollectedWindow && (!isCurrentMonth || cacheFreshToday);
```
- 신선도 조건은 **최신월(index 0)에만** 붙는다. 나머지 달은 `months_collected` 창 안이면 무조건 DB 캐시로 서빙된다. 프리워밍도 같다(`fetchedMonths:1`).
- 결과: 2026-06 계약분 **신고 기한이 2026-07-30까지 남아 있는데도**, 6월을 07-15에 수집한 지역은 그 뒤 들어온 신고분을 영원히 반영하지 못한다. 실제로 서울 자치구별 6월 stats의 `computed_at`이 **07-15 ~ 07-25로 제각각**이다.
- 영향 범위: 완결 직전 달(신고 기한이 아직 안 지난 달)의 건수·평균가가 지역마다 다른 시점의 스냅샷이라, **지역 간 비교가 엄밀하지 않다.** 오래된 달은 기한이 지났으므로 문제되지 않는다.
- 판단 필요: "신고 기한이 안 지난 달(당월 + 직전월)은 재수집 대상으로 본다"는 정책으로 바꿀지. 프리워밍 비용이 늘어나므로 **사용자 판단 대기 항목으로 등록**한다(§6).

## 4. 콘텐츠 — `market` 3편째(시황 브리핑)
→ 기획 확정은 §6, QA·정정은 §7, 배포 확인은 §8에 정리했다.

## 5. 랭킹 수정 — 팀 핑퐁 경로와 검증 결과

**경로: 마스터(적출) → dev → qa(FAIL 2건, 단 핵심 수정은 PASS) → dev(콘텐츠·스펙 정정) → qa(재검증) → 마스터 확인 → push.**

### 5-1. 개발팀 수정 (`lib/ranking.ts` 한 파일, +39/-2)
- 신규 순수함수 `prevYmOf(ym)` — `YYYYMM` 문자열의 직전 달. **`new Date()`/`setMonth()`를 일부러 쓰지 않았다**(`lib/kst.ts` `getKstYm` 주석에 기록된 말일 롤오버 사고와 같은 부류를 재도입하지 않기 위해). `getKstYm`은 "지금으로부터 N개월 전"이라 재사용 불가 — 여기 필요한 건 "임의의 `YYYYMM` 키의 직전 달"이다.
- `prev` 선정을 `deal_ym === prevYmOf(current.deal_ym)` **정확 일치**로 교체. 없으면 `changePct`/`changeDiff`/`prevYm` 전부 `null`.
- `isPending` 경로 무변경. 라벨·UI 요소 무변경.

### 5-2. QA 독립 검증 — 핵심 수정 **PASS**
- **A/B diff 독립 재현**(Supabase 원자료 630행을 직접 조회해 수정 전/후 로직에 같은 입력): 매매 64→59, 전세 65→60. **합계 129→119, 변경 10계열**, 바뀐 필드는 `changePct`/`changeDiff`/`prevYm` 3개뿐(`avgPrice`·`dealCount`·`refYm`·`isPending` 전부 동일). 행 유실 0, `isPending` 17개 그대로.
- **불변식**: 수정 후 `prevYm !== refYm-1`인 행 **0개**(수정 전 매매·전세 각 5개).
- **경계 13케이스 실행**: 연 경계(202601→202512), 12월, 갭 3개월, 직전달 `avg=null`, 잘못된 입력 6종(`"2026"`·`"20261"`·`"202613"`·`"202600"`·빈 문자열·`"000001"`) 전부 예외 없이 `null` 처리 + 행 보존.
- **"기존 코드가 이미 안전"이 실행된 적 없는 경로인지 따로 확인** → 아니었다. 수정 전에도 `changePct===null && isPending===false` 조합이 매매·전세 각 41행 존재했고, 라이브 HTML에 `데이터 없음`이 정확히 41회 박혀 있었다. 설계 스펙 `ranking-board-spec-2026-07-17.md:118`도 원래부터 이 동작을 규정하고 있었다.
- **TOP 10 실제 변동**: 매매 상승 10위 `대전 중구 +6.25%` → **`서울 강북구 +6.17%`**(1~9위 불변), 전세 상승 4위 `부산 서구 +16.20%` 제거 후 10위에 **`대구 중구 +8.45%`** 신규 진입. **하락 TOP 10은 매매·전세 모두 before/after 문자열 완전 동일.**
- 빌드 산출물 실물: `대전 중구 | 3억 3,514만원 | 데이터 없음 | 86 | 2026.07` — 평균가·거래량·기준월 보존, `colspan` 0회, 셀 630개=105행×6열로 정확히 떨어짐.

### 5-3. 🔴 QA FAIL 2건 — 코드가 아니라 **주변 산출물이 옛 동작을 설명하고 있었다**
개발팀 잘못이 아니라 "코드만 고치고 그 코드를 설명하는 글은 그대로 둔" 데서 나온 어긋남이다. **둘 다 같은 커밋에 함께 고쳐야 배포 순간의 자기모순을 막는다** — 그래서 랭킹 수정을 단독으로 먼저 push하지 않았다.

- **FAIL-1 (라이브 블로그)**: 어제 발행한 `ranking-board-reading-principles`의 원칙 5가 "중간에 거래가 없던 달이 끼어 있으면 **두 달 전과 견준 값일 수도 있습니다**"라고 옛 동작을 설명한다(라이브 `curl`로 해당 문장 실재 확인). 수정본이 배포되면 즉시 거짓이 된다. 원칙 2의 "**직전 집계월** 평균과 견준 백분율"도 부정확해진다.
- **FAIL-2 (설계 스펙)**: `docs/design/ranking-board-spec-2026-07-17.md:210`이 폐기된 규칙("`prev` = current 바로 아래 `deal_ym`")을 정본으로 남기고 있다. 다음 라운드가 스펙을 근거로 되돌릴 위험이 있다.

### 5-4. 마스터 판단
- 원칙 5는 문단 전체를 교체한다 — 새 문안은 ① 기준월이 지역마다 다르다(여전히 참: 전세 105행 중 2행이 `refYm=202606`), ② 변화율은 **항상 직전 달 한 달 간격**, ③ 직전 달이 없으면 **'데이터 없음'으로 비우고 표에는 남는다**, ④ 그래도 기준월이 행마다 달라 지역 간 변화율을 같은 두 달로 단정할 수 없다 — 네 가지를 모두 담는다. QA가 지적한 "표에 남은 채 변화율만 비는 행"에 대한 설명 공백도 이 문단이 함께 메운다.
- 갱신 계약 서술은 **새 법령 주장을 만들지 않고** 이미 발행된 `jeonse-new-vs-renewal-contract-guide`(신고 대상 기준·무변동 갱신 제외를 조문 근거와 함께 서술)와 일치시킨다. 조문 번호·시행일은 본문에 넣지 않는다(썩는 정보 배제 원칙).

## 6. 콘텐츠 — `market` 3편째 기획 확정

**소재**: 지난주(7/22) 여러 매체가 보도한 **서울시 6월 아파트 임대차 거래 집계**(전세 7,477건·전월 대비 -12.5%, 월세 비중 54.1%, 매매 -48.0%)를 **같은 국토부 실거래 신고 자료 계열을 쓰는 이 대시보드**로 확인하는 편. market 편 중 **처음으로 원자료 계열이 같은 사례**다.

### 6-1. 초판 축이 한 번 폐기됐다 (중요)
기획팀 초판은 "시간차 재조회로 6월 건수가 움직이는지 관측"을 축으로 삼았다. 마스터가 코드로 확인한 결과 **관측 자체가 불가능**하다(§3). 안 움직이는 게 아니라 **못 움직인다.** 초판의 대체 문안 [8-을]("남은 기간에 값이 달라질 수 있는 상태입니다")을 그대로 뒀다면 **거짓 서술로 발행될 뻔했다.** 기획팀이 갑·을 두 문안을 **모두 폐기**하고 축을 바꿨다.

**새 결론**: "6월 건수는 어느 출처에서 보든 아직 확정된 값이 아니다 — 서울시가 22일에 집계한 값도, 이 대시보드가 15~25일에 수집한 값도 각자 그 시점까지 신고된 만큼을 센 결과다." 우리 한계와 상대 한계를 **대칭으로** 놓아 자사 데이터 우위 서술을 피했다.

### 6-2. 3소스 요건 판정 (§7-2 2단계 · §7-3-7)
- ✅ 채택: 6월 거래 집계 — **헤럴드경제·한국경제·청년일보 7/22**(각자 바이라인, 통신사 전재 아님)
- ❌ 배제: **전세 매물 감소** — 뉴시스·뉴스핌 **2곳뿐**으로 3소스 미달. 마스터가 넘긴 실마리("전세 씨가 말랐다")가 여기 해당해 **재료에서 탈락**했다. 지표도 **매물(listings)**이라 우리 **거래 건수(transactions)**와 다른 계열이다(라운드29 FAIL-1 재발 방지).
- ❌ 배제: KB 평균 전세 7억 돌파 — 단일 소스 + 자사 교차검증 불가.
- ⚠️ 파이낸셜뉴스 2건은 HTTP 403(마스터 환경에서도 접근 불가) → **미확인 표기 + 소스 카운트 제외.**
- 시계열 예외(최대 3주)는 **미사용** — 7일 창 안에서 3소스가 채워졌다.
- **WebSearch 요약문 오염 실사례**: 2025-08 기사와 2026-07 기사를 한 문단에 섞어 제시했고 자치구별 수치가 3·5·7월 값이 뒤섞여 나왔다. 채택 수치는 전부 원문 fetch로만 확정했다. (이 세션에는 `NaverSearch-search_news` MCP가 없어 `WebSearch`+`WebFetch`로 대체했다 — §7-2 절차의 도구 대체 첫 사례.)

### 6-3. 자사 교차검증 실측 (마스터 직접 조회, `monthly_stats` 아파트)
- **5·6월 두 달 자료가 모두 있는 서울 자치구는 25개 중 16개.** 선정은 자의가 아니라 "두 달 자료가 모두 있는 곳 전부"라는 기계적 규칙이고, 이 규칙을 **본문에 그대로 공개**한다(체리피킹 방어).
- 전세 합계 **4,645건 → 3,957건**. 보도의 감소 방향과 일치. **다만 16곳 중 14곳 감소·2곳(강북구·마포구) 증가** — "서울 전역 일제 감소"로 읽히지 않게 본문에 명시.
- 매매 합계 **5,248건 → 2,996건**(16곳 전부 감소). 보도의 매매 -48.0%와 방향 일치.
- ⚠️ **우리 감소율(%)은 본문에 쓰지 않는다.** 모집단이 다르므로(16개 구 부분집합 · 갱신 포함 · 수집 시점 상이) 서울시 발표 %와 나란히 놓일 여지 자체를 없앴다.
- **[D4] 2026-07 건수(25개 구 4,300건)는 본문에서 생략** — 이 값만 25개 구 기준이라 16개 구 기준 수치와 섞이면 "7월이 6월보다 많다"로 오독된다. 기획팀 역제안을 채택했다.
- **[D8] `/ranking` 안내는 넣지 않는다** — 라이브 헤더 기준월이 **2026.07(진행 중인 달)**이라 6월 확인 용도로 독자를 보낼 수 없다.

### 6-4. 마스터 확정(기획안 §8 회신)
제목 (a)안 / D4 생략 / 16개 구 명단은 `paragraph` 유지 / `internalLink` 타입 도입은 **별도 라운드로 분리**(13편 일괄 작업) / market 3편째도 **마스터 직접 검수 유지**(자사 데이터의 구조적 한계를 처음 공개 서술하는 편이라 오히려 검수 필요성이 크다) / 참고 기사 링크 **7건 전부 HTTP 200 확인 완료**.

## 7. 발행 직전 QA 재검증 — PASS 2건 + 적출 3건(마스터 직접 정정)

작업 1(발행글 문안 정정)·작업 2(설계 스펙)는 **전 항목 PASS**. 특히 새 문안이 코드와 일치하는지를 `lib/ranking.ts:108-117` ↔ 원고 문장 단위로 대조했고, 옛 서술이 `lib/blog.ts` 전체에서 0건임을 문자열로 확인했다(`두 달 전과 견준`·`그보다 앞선 달 중에서 찾습니다`·`직전 집계월`·`행마다 다른 달을 기준으로` 전부 0). 새 문안의 "각 행의 변화율은 언제나 한 달 간격"(간격)과 "기준월이 행마다 다를 수 있다"(원점)가 서로 다른 변수라 양립한다는 점까지 논증으로 확인됐다.

작업 3(신규 발행)은 기획안 §5-2 23블록 **축자 일치**, 자사 수치 **독립 재검산 전부 일치**(전세 4,645→3,957 / 매매 5,248→2,996 / 증가 2곳=강북·마포 / 16개 구 명단·순서 / 수집시점 07-15~07-25), 보도 수치 **내적 정합 전부 일치**(8,819/16,296=54.118%→54.1% 등), §5-3 금지 목록 **위반 0건**, 기존 12편 수정 라인 **0**. 그럼에도 **3건이 적출**됐고 마스터가 직접 정정했다.

### 7-1. 🔴 FAIL-A — 커버리지 과대 서술 (사실 정밀도)
원고 [7]이 "나머지 9개 구는 **두 달 중 한 달의** 집계가 아직 없어"라고 썼으나, 실측상 그 9개 구(양천·강서·금천·영등포·동작·관악·서초·송파·강동)는 **2026-05·06이 둘 다 없다**(`monthly_stats` 114행 = 16구×2유형×2개월 + 25구×2유형×202607로 정확히 분해). "둘 중 하나는 있다"로 읽혀 우리 커버리지를 실제보다 넓게 서술했고, 이는 기획안 §6이 "커버리지 과대 서술 금지(팀로그 07-26 A-2 재발 방지)"로 잡아둔 리스크에 정면으로 걸렸다.
→ **"두 달치 집계가 아직 없어"로 정정.** 원고·기획안 §4-1·§5-2 세 곳 모두.
→ **근본 원인은 마스터 회신에 없던 정보를 기획안이 추정으로 채운 것**이다. 마스터 회신은 "5·6월 두 달이 모두 있는 구는 16개"라고만 했고 나머지 9개의 **결측 형태**는 말하지 않았다. 라운드51이 세운 "분기의 else 쪽을 끝까지 따라간다"와 같은 부류의 실패다 — 다음부터 **회신에 없는 사실은 기획안에 쓰지 말고 재질의**한다.

### 7-2. 🔴 FAIL-B — 화면에서 각주와 헤딩이 충돌
`components/blog/BlogBody.tsx:62-64`가 `sourceNote`를 "…**조회 기준일** YYYY-MM-DD" 고정 문구로 렌더하는데, [12] `sourceNote`가 "이 숫자들은 **조회한 날짜가 아니라** 수집한 날짜의 기록입니다" 섹션 **끝**에 붙어 설명을 정면으로 뒤집었다.
→ **[12] 삭제(3회 → 2회).** 헤딩의 대비는 이 편의 핵심이라 살리고 각주 인접만 제거했다. 남은 [9]는 그 헤딩 **앞**에 오므로 "각주 → 그 각주 표기를 곧바로 설명하는 헤딩" 순서가 되어 모순이 아니다(마스터가 빌드 산출물로 순서 실측 확인). [19]도 유지되어 출처 표기 자체는 남는다.
→ 기존 12편에 없던 충돌이다. 이번 편이 "수집 시점 스냅샷"을 처음 전면에 내세우며 생겼다. **`sourceNote` 라벨("조회 기준일") 재설계는 13편 전체에 영향이 있어 디자인팀 별건 안건으로 남긴다.**

### 7-3. ⚠️ WARN-C — 데스크톱 요약 clamp 여유 1.4%
summary 123자가 발행 13편 중 최장이라 데스크톱 `md:line-clamp-2` 2줄째가 696px 중 686px를 썼다(모바일은 이상 없음). 폰트 메트릭 오차로 잘릴 수 있어 **113자로 축약**(기존 최장 116자보다도 짧다). 축약 후 실측: 카드 요약 정상 렌더, meta description 113자(160자 이내), 읽기시간 9분 유지.

### 7-4. 분량 목표 사후 정정
확정 원고 실측이 **3,936자·9분**으로 기획안 §5-1의 "8분" 목표를 벗어났다. 개발팀이 **기획팀 확정 원고를 임의로 줄이지 않고 보고한 판단이 옳다.** 마스터 결정: **9분 그대로 발행**(insight 12편 4,112자·9분과 같은 구간). 다음 market 편 기준선을 **3,900~4,100자(9분)**로 갱신했다 — 목표치에 원고를 맞추는 게 아니라 필요한 설명을 담고 나온 실측을 기준선으로 삼는다.

### 7-5. 마스터 최종 확인
`npm run build` 직접 재실행 통과(25 static, `/blog/[slug]` **13편**, `/ranking` ISR 6h 유지). 빌드 산출물에서 정정 3건 실물 확인(`두 달 중 한 달` 0건 / [11] 문단 뒤 각주 소멸 / 요약·읽기시간). `git diff -U0 lib/blog.ts` 훅 정확히 3개(`@@ -1069`, `@@ -1093`, `@@ -1128,0 +1129,169`)로 **기존 12편 수정 라인 2줄뿐**임을 재확인 후 커밋.

## 8. 커밋·배포·라이브 확인

- 커밋 `dd42060` — `lib/ranking.ts`(+39/-2) · `lib/blog.ts`(+171/-2) · `ranking-board-spec`(+2/-1) · `pending-decisions-brief`(+38) · `blog-market-briefing-2026-07-27.md`(신규 448행). **보류 3파일(`lib/db-cache.ts`·`lib/molit-api.ts`·`supabase/schema.sql`)은 스테이징에서 제외**한 것을 `git status`로 확인 후 커밋했다.
- `git push origin main` 완료(`5806f35..dd42060`) → Vercel 자동 배포.

### 라이브 실측 (배포 후)
- `/blog/seoul-jeonse-transaction-count-briefing-2026-07-27` **HTTP 200**(1차 시도 404 → 배포 완료 후 200).
- `/ranking` **HTTP 200**. 매매 "▲ 상승 TOP 10 (전월 대비)" **10위 = 서울 강북구 +6.17%**(수정 전 대전 중구 +6.25%). 문서 전체에서 `6.25` **소멸**, 전세 `16.2`(부산 서구) **소멸**. `데이터 없음` 41회 → **46회**(+5 = 매매 쪽 영향 계열 수와 일치). 1~9위는 수정 전과 동일.
- `/blog/ranking-board-reading-principles` 라이브에서 옛 서술 **전부 0건**(`두 달 전과 견준`·`그보다 앞선 달 중에서 찾습니다`·`직전 집계월`), 새 서술 `바로 직전 달` 4회·`데이터 없음` 2회 노출 확인.

→ **QA가 예측한 TOP 10 변동이 라이브에서 그대로 재현됐다.**

## 9. 다음 라운드 이어받을 상태

- 프리워밍 **56/114**.
- 블로그 **13편**(guide 4 · howto 3 · insight 3 · **market 3**). 카테고리 최소는 이제 howto·insight 3편 동률. `market`은 **다음 월요일(2026-08-03) 첫 라운드**까지 쉰다.
- **🔴 최우선 작업 — `monthly_stats` 백필** (§2 근본 원인). 전세 기준 108개 지역 중 47개에 결측, 총 345개월. **`deals`에 원자료가 있으므로 국토부 재호출 없이 재계산만으로 복구된다.** 함께 고칠 것: `app/api/cron/prewarm/route.ts:252`의 `upsertMonthlyStats(..., allData)`가 "이번 실행에서 fetch한 달"만 담아 구멍이 계속 생긴다 — 창 전체로 넓혀야 한다. **라이브 순위 숫자가 실제로 바뀌므로 planning→dev→qa 정식 경로로 진행할 것.** 백필 후에는 이번 라운드에 `데이터 없음`이 된 10개 계열이 정상 변화율을 되찾는지 확인한다.
- 승인 없이 착수 가능한 잔여분(라운드44~51에서 이월, 변동 없음): 미감사 표면 읽기전용 전수 감사(홈 대시보드 상호작용 · `/support` 문의 폼 · AI 추천 결과 문구), 비가시 SEO 잔여(canonical이 블로그 상세에만 있고 `/`·`/about`·`/blog`·`/privacy`·`/support`·`/terms`에는 없음), `lib/site.ts`에 `SITE_NAME` 상수 부재, `app/blog/[slug]/page.tsx:67` 주석 오기.
- 신규 이월 안건 2건(둘 다 승인 불필요, 착수 가능):
  1. **`sourceNote` 라벨 재설계** — "조회 기준일"이라는 고정 문구가 "수집 시점" 서술과 충돌한다(§7-2). 13편 전체 영향이라 디자인팀 설계 후 진행.
  2. **`internalLink` 섹션 타입 도입** — 링크 수요 3건 누적으로 라운드29 합의 조건이 충족됐다. 13편을 한 번에 손보는 별도 라운드로 분리하기로 이번 라운드에 결정했다.

## 10. 사용자 판단/조치 대기

1. **[기존] 결정 #3(b) DB 마이그레이션** — `deals` 테이블에 `cdeal_type`·`cdeal_day` 컬럼 추가(Supabase SQL Editor). 오늘도 미적용 확인(HTTP 400 `42703`). **07-26에 이미 통지했으므로 재통지하지 않았다.** 실행되면 다음 라운드가 보류 3파일을 즉시 커밋·검증한다.
2. **[신규] 결정 #13 — 신고 기한이 안 지난 달을 재수집할 것인가** (§3). `docs/planning/2026-07-22-pending-decisions-brief.md` §8에 선택지 3개와 권고((다), 단 114지역 초회 순회 완료 후)를 정리했다. **차단성 아님** — 회신이 없어도 다른 작업은 계속 진행된다. 이 사안 때문에 이번 편 블로그가 자사 데이터의 한계를 처음으로 공개 서술하게 됐다.

## 11. 이번 라운드에서 남길 교훈

1. **콘텐츠 작업이 버그를 찾아냈다.** `/ranking`의 전월대비 오표시는 기능 QA가 아니라 **블로그용 원자료를 직접 조회하다** 발견됐다. 자사 데이터로 교차검증하는 `market` 편의 부수 효과이며, 이 카테고리를 유지할 근거가 하나 더 늘었다.
2. **코드를 고칠 때 "그 코드를 설명하는 글"을 같이 찾아야 한다.** 이번엔 QA가 잡아냈지만, 랭킹 수정만 먼저 push했다면 라이브 블로그가 즉시 거짓이 됐다. **동작을 바꾸는 커밋은 `lib/blog.ts`·`docs/design/` 전문 검색을 기본 절차로 넣는다.**
3. **회신에 없는 사실을 추정으로 채우지 않는다.** FAIL-A는 마스터 회신에 없던 "결측 형태"를 기획안이 추정으로 적으며 생겼다(§7-1). 모르면 재질의한다.
4. **도구가 없으면 절차를 대체하되 기준은 낮추지 않는다.** `NaverSearch-search_news` MCP가 없어 `WebSearch`+`WebFetch`로 대체했는데, WebSearch 요약문이 실제로 2025-08 기사와 2026-07 기사를 섞어 내놨다. **3소스 요건·7일 창·원문 확인은 그대로 적용**했고, 그 결과 마스터가 넘긴 실마리 3건 중 2건이 재료에서 탈락했다.
