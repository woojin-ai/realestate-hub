# 2026-08-04 (화) 팀 로그 — 라운드57

> ⚠️ **같은 날 두 번째 라운드다.** 라운드56 마지막 커밋 `ef050ba`가 **01:56**, 이 라운드 시작이 **01:57**이다(24분 뒤 본격 착수). 라운드56 로그는 `2026-08-04.md`에 따로 있다 — **두 파일을 함께 읽어야 오늘이 완성된다.**

## 0. 상태 파악

- `git status`: 동결 3파일(`lib/db-cache.ts`·`lib/molit-api.ts`·`supabase/schema.sql`) 미커밋 **11라운드째**. 변동 없음.
- `git fetch origin` 동기. 라운드56 커밋 3건(`f0f881e`·`7b140f1`·`c76fc6d`+`ef050ba`) 반영 상태.
- 라운드56 §9-1의 인계: *"16편째 market은 기획 완료·미집필, 미결 8건 전부 확정 → 다음 라운드는 묻지 말고 곧장 집필해라."* → **그대로 이행했다.**
- 라이브 헬스: 홈 HTTP 200 / 1.3s, `/blog` 정상.

## 1. 야간 프리워밍

```
HTTP:200 TIME:29.3s
{"ok":true,"processedCount":3,"nextIndex":103,"totalRegions":114,"elapsedMs":28299,
 "processed":[대전 서구 30170, 대전 유성구 30200, 대전 대덕구 30230]}
```
- 진행률 **103/114**(라운드56 97 → 오늘 103). 3개 지역 모두 `monthsCollected:13` · `statsGapMonths:0` · `incomplete:false` · 504 없음.
- ⚠️ **관찰**: 라운드56이 `nextIndex:97`로 끝났는데 이 라운드가 인덱스 **100부터** 처리했다. 사이에 97·98·99를 처리한 실행이 있었다는 뜻이다. 라운드 로그에 없는 실행이 존재한다 — §9-2의 스케줄 중복 의심과 같은 방향의 증거다.

## 2. 🔴 1차 출처를 뚫었다 — 라운드56이 "확보 실패"로 넘긴 것

라운드56 §11-3은 미분양 원 통계 확인을 **실패**로 기록하고 다음 라운드에 재시도를 남겼다. **이번에 뚫렸고, 그 과정에서 발행 예정 원고의 사실 하나가 바뀌었다.**

### 2-1. 막힌 경로와 뚫린 경로

| 경로 | 결과 |
|---|---|
| `www.molit.go.kr/USR/NEWS/m_71/dtl.jsp`(보도자료) | ❌ **307 리다이렉트 루프**(`--max-redirs 20` 소진, URL 불변). 라운드56과 동일 재현 |
| `apis.data.go.kr` | ❌ `400 Request Blocked`(기존) |
| `korea.kr` 정책브리핑 검색 | ❌ 아래 §2-3 참조 |
| **`stat.molit.go.kr` 통계누리** | ✅ **뚫림** |

### 2-2. 재현 가능한 접근 경로 (다음 라운드가 다시 포기하지 않도록)

1. 메타 페이지 `stat.molit.go.kr/portal/cate/statMetaView.do?hRsId=32` → **200**
2. 첨부 다운로드는 `downLoadFile.do?oFileName=…&rFileName=…&midpath=/stat_file/` — 🔴 **파라미터명이 `fileName/filePath`가 아니다.** POST는 500, **GET**이어야 한다
3. `미분양주택현황(2026년6월말)_통계누리.xlsx` **956,672바이트**
4. 시트2 `A6="전 국"`, `B6=67464`, `H3="(단위 : 호)"` / 시트1 `DZ5(26.5)=65239` → `EA5(26.6)=67464`

**파일명에 대상 월이 박혀 있어 연도 혼입 함정이 이 경로에서는 원천 차단된다.** 개발팀·QA가 **각각 독립적으로** 내려받아 같은 셀을 확인했다.

### 2-3. 🔴 연도 혼입이 세 번째로 재현됐다 — 이번엔 더 나빴다

기획팀이 KDI 2021년 자료를 걸러냈고(라운드56 §6-2), 마스터가 `molit.go.kr` 도메인 검색에서 16,289호·27,910호 혼입을 겪었다(라운드56 §11-3). 이번엔 **`korea.kr`로 도메인을 좁혔는데도** 검색이 **2021-08-05 보도자료**를 2026년 질의의 답으로 요약해 돌려줬다(16,289호). 

**교훈: 도메인을 좁히는 것으로는 연도 혼입을 막지 못한다.** 막은 것은 **파일명에 기간이 박힌 원본 파일을 직접 여는 것**이었다.

### 2-4. 🔴 그 결과 단위가 바뀌었다 — "가구"가 아니라 "호"

원본 xlsx가 `(단위 : 호)`다. 기사 3건·기획안·마스터 지시서가 전부 **"가구"**로 쓰고 있었다. 본문이 **1차 출처에 귀속**하는 형태(`「2026년 6월 주택통계」에 따르면`)이므로, 1차 출처를 이름으로 대면서 그 출처가 쓰지 않는 단위를 붙이면 귀속과 표기가 어긋난다. → **본문 전량 "호"로 교체.**

- 개발팀이 **지시 범위 밖 사안으로 보고**했고 마스터가 확정했다.
- 부수 효과로 `[9]`의 단위 대비가 선명해졌다 — *"한쪽은 주택을 '호'로 세고, 다른 쪽은 거래를 '건'으로 셉니다."*
- `sourceLinks` 기사 제목 안의 "3만 가구"는 **원문 축자라 유지**한다. 본문이 그 단어를 쓰지 않으면 이 사이트의 주장이 되지 않는다.
- 🔴 **"언론은 가구로 썼지만 원문은 호다" 류의 메타 서술은 넣지 않았다** — 독자 가치가 없고 매체 표기를 지적하는 모양이 되어 `content-strategy.md` §7-3-3에 걸린다.

## 3. 🔴 마스터가 기획안을 덮어쓴 것 4건

기획안(라운드56 작성)은 승인 수준이었지만, **이번 라운드에 조건이 바뀌었거나 기획안 자체가 틀린 지점**이 있었다.

### 3-1. D-1 — 8-2를 부분 뒤집었다
기획안 §11 8-2는 "1차 출처 미확보"를 이유로 미분양의 정의·집계경로 문장을 **전부 삭제**하기로 했다. §2에서 1차 출처를 확보했으므로 **집계 경로 문장만 복원**했다(*"분양시행자 → 시·군·구 → 시·도 → 국토교통부"*, *"행정보고내용을 집계"*). **정의 문장은 여전히 금지** — 그 페이지에 명시적 정의가 **없음**을 QA가 `"정의"` 문자열 0건으로 확인했다.

### 3-2. 🔴 D-2 — 기획안 §11-2의 예문이 거짓이었다
기획안이 자사 수치 사용 예문으로 *"이 대시보드가 2026년 6월에 집계한 아파트 매매는 **전국** 29,608건"*을 적어 두었다. **그대로 썼으면 거짓이다.**

마스터 실측(`monthly_stats`, 202606, 아파트, 매매):
```
Content-Range: 0-111/112   → 112행, 합계 29,608
lawd_cd 29xxx(광주) = 4행(29110·29140·29155·29200) 전부 deal_count=0, avg_price=null
29170(광주 북구)는 행 자체가 없다 ← 202606이 112행, 202607이 114행인 차이의 원인
```
**광주 5개 구가 통째로 0인 채 합계에 들어가 있다.** "전국"이라 쓰면 없는 지역을 있다고 말하는 것이 된다. → **자사 수치 서술에 `전국` 금칙어 추가.** 단 미분양 서술의 "전국"은 허용(국토부가 전국 기준으로 발표한 값이고, 통계누리 조사대상 원문도 *"전국의 민간 미분양 주택"*이다).

### 3-3. 🔴 D-5 — 발행일이 미래 날짜였다
기획안 §11 8-8이 발행일을 `2026-08-05`로 확정했다. 근거는 *"집필이 라운드57로 넘어갔다. 발행일이 실제 발행일과 어긋나면 그 자체가 사실 오류다"* — 옳은 원칙인데 **"라운드57은 다음 날에 돈다"는 전제가 깨졌다.** 라운드57은 **같은 날 24분 뒤**에 떴다.

그대로 뒀으면 8-8이 막으려던 바로 그 오류를 **미래 날짜 쪽으로** 저지르고, `slug`(`-2026-08-04`)와도 자기모순이었다. → `publishedAt`·`asOfDate` 모두 **`2026-08-04`**.

### 3-4. D-4 — 단위 "호" (§2-4)

## 4. 🔴 QA가 배포를 막았다 — FAIL 3 / WARN 5

형식·수치·1차 출처는 전부 통과했는데 **화면 사실 서술**이 걸렸다. 셋 다 실재했고 개발팀이 코드로 전건 재확인했다.

### 4-1. FAIL-1 — 화면에 없는 UI를 있는 것처럼 가리켰다
본문이 *"이 대시보드의 **거래건수**는 그 달에 계약된 거래를 센 값"*, *"**6월 칸**에 들어가는 것은…"*이라고 썼다.
1. 화면에서 `거래건수` 라벨이 붙은 유일한 값은 `DealsTable.tsx:31`의 **`"거래건수(13개월)"` = 13개월 누적**이다.
2. **3일 전 발행한 `aggregation-period-reading-principles` [5]가 정확히 반대로 서술한다** — *"한 행에 나란히 놓인 두 값이 서로 다른 길이의 기간을 세고 있다."* 다른 4편도 전부 "목록 표의 열"로 쓴다. **16편 중 이 글만 다르게 부르게 될 뻔했다.**
3. **"6월 칸"은 존재하지 않는다.** `MonthlyChart.tsx:46-58`의 dataset은 1개, label이 `"평균 매매가(만원)"`뿐이다. **월별 건수를 그리는 화면이 어디에도 없다.**

→ 화면 라벨과 겹치는 호칭을 버리고 **집계 층위**로 바꿨다("이 대시보드가 달마다 모아 세는 매매 건수"). "6월 칸" 삭제. 부수 효과로 `[9]`와 `[15]`(29,608 = `monthly_stats.deal_count` 월 버킷)의 **층위가 비로소 맞았다** — 수정 전에는 화면 열(13개월 누적) ↔ 월 합계로 어긋나 있었다.

### 4-2. FAIL-2 — 화면 열거를 **닫아 놓고** 하나를 빠뜨렸다
*"화면에 나오는 것은 정해져 있습니다 … 이 가운데 어디에도"*로 썼는데, `app/page.tsx:247-261`의 실제 렌더에 **`AiRecommendSection`**이 있다. `AiRecommendSection.tsx:189`가 `buildingType==="아파트" && lawdCd`에서 **항상 렌더**한다 — 본문이 가정한 바로 그 조건이다. 게다가 발행본 `ai-recommend-weights-howto`가 이미 축자로 그 영역을 서술하고 있어 **정면 충돌**이었다.

→ `'AI 맞춤 추천'`을 열거에 넣고(노출 조건까지 반영), **열거의 완전성 주장을 버렸다**(`"화면 어디에도 미분양에 해당하는 항목은 없습니다"`). **부재를 주장하는 문장은 참으로 유지되지만, 목록이 완전하다는 주장은 화면이 하나 늘 때마다 거짓이 된다.**

### 4-3. 🔴 FAIL-3 — 결함의 근원이 마스터 지시서였다
본문 `[3]`이 *"미분양은 팔리지 않고 남아 있는 주택을 센 값"*이라 썼다. **마스터가 D-1에서 이 폴백 문안을 명시적으로 승인했다.**

그런데 같은 라운드에 확보한 1차 출처 조사대상은 *"…**전국의 민간 미분양 주택**(준공 전 + 준공 후)"* — **사업자가 분양한 민간 신축 물량 중 미계약분**으로 한정된다. "팔리지 않고 남아 있는 주택"은 기존 매물·재고 전반으로 읽혀 **모집단 과대 서술**이고, 하필 이 글의 논지("이 통계는 무엇을 세는가") 한복판이다.

→ *"사업자가 분양한 민간 주택 가운데 계약되지 않고 남은 물량"*으로 축소. **1차 출처를 확보하고도 그 전에 만든 폴백 문안을 그대로 승인한 것이 마스터의 실수다.**

> ⚠️ QA는 이 건을 "D-1 사양 위반"으로도 판정했는데 그 부분은 **오판**이다(마스터가 승인한 문안이므로 개발팀은 지시를 따랐다). **두 번째 논거(모집단 과대)가 정확했고 결론은 옳았다.**

### 4-4. QA 부수 관찰 1건은 오탐이었다
QA가 라이브 HTML에서 푸터 크로스사이트 내비가 `계계산기 허브`·`청청약레이더`·`대대출모아`로 첫 글자를 중복 출력한다고 보고했다. **오탐이다.** `CrossSiteNav.tsx:37-48` `SiteBadge`가 `h-8 w-8 rounded-full` **원형 배지**에 이니셜(`계`)을 렌더하고 그 아래 라벨(`계산기 허브`)이 온다. `aria-hidden="true"`라 스크린리더도 건너뛴다. **화면·접근성 모두 정상이고, 텍스트만 추출하면 시각 요소의 인접이 중복으로 보일 뿐이다.** 티켓 만들지 않는다.

### 4-5. WARN 처리
| # | 내용 | 처리 |
|---|---|---|
| W-1 | `[7]` 필드 열거가 **닫혀 있는데 불완전**(`cdeal_type` 등 6개 누락) | ✅ **열거 개방.** 필드를 다 적지 않는다 — 늘면 또 거짓이 된다. 해제거래는 결정 #3-(b) 동결 영역이라 본문에서 설명하지 않음 |
| W-2 | `[13]`이 market 3편과 **의미** 인접(축자는 15자 미만) | 유지. 구분 문장이 붙어 있고 기획안 §5 [13]-3 승인 서술 |
| W-3 | `sourceLinks` 제목 문장부호가 축자 아님(`'경고등'`, `…`) | ✅ **U+2018/U+2019 + U+00B7×3으로 교정.** "원문 축자"는 우리가 세운 규칙이다 |
| W-4 | 동결 3파일 배포 결합 위험 | 커밋 범위를 `lib/blog.ts`로 한정 |
| W-5 | 내부링크 3건이 **하이퍼링크가 아니라 평문**(라이브 `<a href="/blog/…">` 0건) | 사이트 전체 패턴. `internalLink` 섹션 타입 도입이 이미 이월 안건(§9-4) — **이 글에서 손대지 않음** |

### 4-6. 개발팀이 지시 목록 밖에서 같은 결함 2건을 더 잡았다 (자진 보고 **7라운드 연속**)
- `[13]`에도 *"이 대시보드의 **거래건수**와 종류가 같아 보이지만"*이 있었다 — FAIL-1과 동일 결함인데 QA도 마스터도 지목하지 않았다. 전수 재판정 중 스스로 적출해 함께 고치고 **보고했다.**
- `[3]`을 좁히자 `[9]`-1(*"미분양은 어느 시점에 남아 있는 **주택의 수**"*)이 **두 번째 정의문**이 되어 다시 넓어졌다. → *"어느 한 시점에 남아 있는 **물량**을 센 값"*으로 `[3]`의 어휘를 되받게 수정.
- 축자 게이트에서 새로 생긴 16자 매치 1건(`"입니다. 반면 이 대시보드가 "` ↔ market 2편 `:923`)을 `반면`→`한편`으로 해소.
