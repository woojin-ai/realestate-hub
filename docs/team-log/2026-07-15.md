# 2026-07-15 팀 작업 로그 (마스터 오케스트레이터)

> ## ⚠️ 사용자 액션 필요 (최우선, 변화 없음)
> 아래 3건은 **코드로 해결 불가**하며 사용자 액션 대기 상태가 계속된다(2026-07-14부터 이월):
> 1. **Vercel 환경변수 등록 + 재배포** — `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`MOLIT_API_KEY`/`KAKAO_API_KEY`. 미등록이라 프로덕션 `/api/data`가 매번 `source=live`(캐시 미동작), `/api/recommend`는 타임아웃. **로컬은 정상.**
> 2. **프로덕션 Supabase 스키마 적용** — `supabase/schema.sql` + `supabase/apt_geo.sql`. (아래 신규 위치 섹션도 apt_geo를 공유하므로, 적용 시 상세모달 위치도 웜 캐시로 즉시응답. 미적용이어도 카카오 라이브 폴백으로 동작.)
> 3. **텔레그램 봇 토큰** — `TELEGRAM_BOT_TOKEN` 여전히 빈값 → Stage 4(자동수집+알림) 계속 보류.
>
> **최우선 지시 상태(2026-07-14 사용자):** "빌라/단독·알림 확장 금지, 원본 핵심기능부터 복원." ①상세보기·②AI추천은 기존 dev+QA PASS·배포 완료. 이후 ①의 **원본 전체 복원(위치/단지정보/주변시설 3섹션)** 중 **위치 섹션(Stage 1)**·**단지정보 섹션(Stage 2)**을 완성했다(둘 다 dev+QA PASS·push). 다음은 **주변시설 섹션(Stage 3, 학교 MVP)**. 빌라/단독·알림은 계속 보류.
>
> **신규 사용자 조치 권장(이번 라운드 추가):** ④ `supabase/apt_info.sql`을 프로덕션 Supabase에 적용해야 단지정보 캐시 성능(MOLIT 재호출 0) 실효 발생. **미적용이어도 라이브 폴백으로 기능은 정상 동작**(QA가 테이블 없는 상태에서 실호출 PASS 실증). ⑤ `elev_per_hh` 표기(은마 0.009대/세대) 비직관 — 후순위 필드라 릴리스 블로커 아니나 정식 노출 전 표기 방향(예 "N세대당 1대") 재검토 권고.

---

## 라운드: 아파트 상세 모달 — 위치 섹션(Stage 1) 완성 (dev+QA PASS → 커밋/push)

### 배경 — 동시 세션이 남긴 미완성 작업 인수
라운드 시작 시 워킹트리에 직전(동시) 세션이 2026-07-14 21:52경 남긴 **미완성 in-progress 작업**이 있었고, **빌드가 깨진 상태**였다:
- `app/api/apt-location/route.ts`(신규, untracked) — 위치 섹션 API. apt_geo 캐시-우선 + `lib/recommender.ts fillGeo` 라이브 폴백. **완결 상태였음.**
- `components/AptDetailModal.tsx`(수정) — 위치 fetch 상태머신(`locState`/`loc`/`locReloadKey`/`retryLocation`/fetch useEffect)과 **필수 prop `lawdCd`/`gu`**를 추가했으나, ①위치 섹션 **렌더 JSX 누락**, ②두 호출측(`DealsTable`, `AiRecommendSection`) **prop 미배선** → `npx tsc --noEmit` 2개 에러(TS2739)로 빌드 불가.
- `docs/planning/apt-detail-full-restore.md`(신규), `docs/design/apt-detail-modal.md`(§9~§15 추가) — 3섹션 전체 복원 기획/구성안.

기획·디자인 문서가 이미 있고(planning→design 완료), 위치 섹션은 **apt_geo 캐시 재활용·신규 스키마 0·리스크 최소**의 독립 배포 단위(기획안 §5 1단계)라, 폐기하지 않고 **dev→qa→마스터 게이트→push**로 완성했다.

### 이번 범위 (위치 섹션 A "만")
단지정보(B)·주변시설(C)은 이번 범위 밖(다음 라운드). `app/api/apt-location/route.ts`는 완결이라 미변경.

### 구현 (dev-team)
1. `components/AptDetailModal.tsx` — 평수별 표 아래 `<hr>` + **위치 섹션 렌더 JSX**(design §10). `<h3>📍 위치`(이모지 aria-hidden), 지하철 줄 `🚇 {역명} · 도보 {거리}m`(미확인 규약 `subway_name==="-" || subway_dist>=9999` → "지하철 정보 없음"), 좌표 `toFixed(4)` + 카카오맵 링크(`target=_blank rel=noopener noreferrer` + aria-label, 좌표 null이면 줄 생략), loading=회색 pulse+`sr-only`, empty/error=회색 텍스트, error에 "다시 시도"(retryLocation, 해당 섹션만 재fetch). 기존 팔레트만 사용(신규 색 0), 기존 접근성(role=dialog/aria-modal/ESC/스크롤락/포커스) 유지.
2. `components/DealsTable.tsx` + `app/page.tsx` — `DealsTableProps`에 `lawdCd: string|null`·`gu: string` 추가, page가 `<DealsTable>`에 전달, DealsTable이 `<AptDetailModal lawdCd={lawdCd ?? ""} gu={gu}>` 배선.
3. `components/AiRecommendSection.tsx` — 추천 카드 모달에도 `lawdCd={lawdCd ?? ""} gu={gu}` 배선(budgetMax 전달 유지).
- prop 이름은 코드 일관성상 `lawdCd`(카멜)로 유지(design §14-1은 `lawd_cd` 표기였으나 코드 통일 우선, 무영향).

### 검증
- **QA팀 독립검증 PASS**(필수 7항목 전부, LOW 3). 빌드게이트 3종 직접 재실행, 범위(코드 4파일)·prop 배선·design §10 대조·엣지케이스(빈 lawdCd/null 폴백, loc null 가드, abort, retry 격리)·접근성 회귀·시크릿 0. **라이브 실호출**: `/api/apt-location?lawd_cd=11680&gu=강남구&name=은마&dong=대치동` → `{found:true, lat:37.4974…, lng:127.0653…, subway_name:"대치역 3호선", subway_dist:374}` 계약 일치, 비5자리/빈파라미터/미존재단지 모두 NOT_FOUND graceful.
  - LOW(코드결함 아님, 커밋범위 관련): L1 `docs/design/apt-detail-modal.md`도 수정(§9~15 순수 추가), L2 route.ts·planning.md는 untracked(신규) → 커밋 시 동반 스테이징 필요(누락 시 프론트가 없는 API 호출), L3 위치 wrapper `aria-live="polite"`는 향후 B/C 추가 시 묶음 재검토.
- **마스터 재검증**: `npx tsc --noEmit` exit 0, `npx next build` exit 0(`/api/apt-location` ƒ Dynamic 등록), 변경 4파일 시크릿 grep 0(유일 매치 public `map.kakao.com` URL). `git status`에 동시세션 오염 파일 없음(정확히 예상 세트).

### 커밋 범위 (Stage 1 일관 세트)
`components/AptDetailModal.tsx`, `components/DealsTable.tsx`, `app/page.tsx`, `components/AiRecommendSection.tsx`(코드 4), `app/api/apt-location/route.ts`(신규 API), `docs/design/apt-detail-modal.md`(§9-15 구성안), `docs/planning/apt-detail-full-restore.md`(기획안), 본 팀로그. QA LOW L2 반영해 untracked 라우트·기획문서를 함께 스테이징(프론트가 호출하는 API 누락 방지).

### 다음 라운드가 이어받을 상태
- **원본 상세모달 전체 복원 진행률**: 평수별 표(완료·기존) + **위치 섹션(이번 완료)** / **단지정보(B)·주변시설(C) 미착수**.
  - **Stage 2 — 단지정보 섹션**(`/api/apt-info`, 신규 `apt_info` 테이블): 세대수/동수/복도/난방/총주차/주소 MVP6. MOLIT Bass/Dtl 2콜 + kaptCode 영속화. 기획안 §1-B·§2-B·§5-2단계, design §11. 용적률/건폐율/승강기는 후순위(null 행 숨김).
  - **Stage 3 — 주변시설 섹션**(`/api/apt-nearby`, 신규 `apt_nearby` 테이블): **학교(초/중/고)만** MVP(카카오 16~19콜 부담 회피). 기획안 §1-C·§2-C, design §12.
- **경사도/평지점수는 제외 확정**(기획안 §3, ai-recommend와 일관). 사용자가 원본 경사 표기를 강하게 원하면 별도 Cron 트랙 승인 필요(기획안 §6-1).
- **사용자 확인 권장(기획안 §6):** ①주변시설 MVP를 학교로 좁히는 단계 축소 수용 여부, ②apt_info/apt_nearby 신규 스키마의 프로덕션 Supabase 적용(로컬은 라이브 폴백으로 검증 가능하나 캐시 성능은 프로덕션에서만 실측).
- **여전히 사용자 액션 대기**(상단 배너 1·2·3): Vercel env vars, 프로덕션 Supabase 스키마, 텔레그램 토큰.
- 빌라/단독·알림 확장은 사용자 명시 확인 전까지 계속 보류(2026-07-14 지시, 매 라운드 재확인 원칙 유지).

---

## 라운드: 아파트 상세 모달 — 단지정보 섹션(Stage 2) 완성 (dev+QA PASS → 커밋/push)

### 이번 범위 (단지정보 섹션 B "만")
원본 Flask `/api/aptinfo`(app.py 1276-1414) 복원. 위치 섹션(A)·평수별 표는 미변경(diff 실측: AptDetailModal.tsx +175/-0, 삭제 0줄). 주변시설(C)은 다음 라운드(Stage 3).

### 구현 (dev-team)
1. `supabase/apt_info.sql`(신규) — planning §2-B 스키마 그대로(자연키 `(lawd_cd,name,dong)` unique, kapt_code 영속·음성캐시 컬럼, RLS 공개읽기, "적용은 사용자 몫·테이블 없어도 라이브 폴백" 주석). apt_geo.sql 스타일 계승.
2. `lib/molit-aptinfo.ts`(신규) — `resolveKaptCode`(getSigunguAptList3 페이지네이션+정규화 완전/부분일치)·`fetchAptInfo`(getAphusBassInfoV4+getAphusDtlInfoV4 2콜, 원본 `iv()` 헬퍼 포팅). MVP6(세대수/동수/복도/난방/총주차/주소)+후순위(elev_pass/elev_per_hh/park_above/park_under). **far_ratio/cov_ratio는 null 고정**(건축HUB getBrRecapTitleInfo 미호출 — planning §1-B 후순위, 번지파싱 실패율·추가콜/타임아웃 리스크 회피).
3. `app/api/apt-info/route.ts`(신규) — apt-location 패턴 계승: 캐시-우선(apt_info 조회) → found=true 즉시반환(MOLIT 0), found=false 음성캐시 **30일 TTL**(이내 즉시반환/초과 재시도), 미스 시 라이브+upsert(kapt_code 포함). Supabase 미구성/조회·upsert 실패 전부 try/catch 무중단. 콜드 예외 시 500 아닌 `{found:false}`. `dynamic=force-dynamic`, `runtime=nodejs`.
4. `components/AptDetailModal.tsx`(수정) — 위치 섹션 상태머신과 동일 패턴으로 `infoState`/`info`/`infoReloadKey`/`retryInfo` + 별도 useEffect 병렬 lazy fetch(위치와 독립). design §11 UI: `<dl>/<dt>/<dd>` 그리드(데스크톱 2열/모바일 1열), 주소 `sm:col-span-2`, **null 필드 행 숨김**, 전부 null이면 "단지 상세 정보가 등록되어 있지 않습니다", 로딩 회색 pulse+`sr-only`, error `[다시 시도]`(해당 섹션만), 이모지 aria-hidden, h3, 신규 색 0. 기존 props(lawdCd/gu) 재사용.

### 검증
- **QA팀 독립검증 PASS**(9항목 전부, 블로커 0, LOW 1). 빌드게이트 재실행, diff 범위(수정 1·신규 3), 응답계약 §14-2 일치, 원본 필드매핑 1:1(iv 헬퍼 빈값/"0"/"null"/"None" 제외), 캐시/폴백(테이블 미적용 상태에서 조회 PGRST205 throw→라이브 폴백→upsert throw 스왑→**전부 HTTP 200 정확데이터** 실증), 엣지케이스(비5자리·소수점·빈name·미존재단지 모두 `{found:false}` graceful), UI, 시크릿 0(서버로그 grep 0). **실호출** `/api/apt-info?lawd_cd=11680&gu=강남구&name=은마&dong=대치동` → `households:4424, buildings:28, park_total:3021, addr:"…대치동 316 은마", hallway:"복도식", heating:"지역난방", elev_pass:42`.
- **마스터 재검증**: `npx tsc --noEmit` exit 0, `npx next build` ✓ Compiled(`ƒ /api/apt-info` Dynamic 등록), `git status`=정확히 예상 4파일(수정 1+신규 3), 변경파일 시크릿 grep 0.
- **LOW 1건(블로커 아님)**: `elev_per_hh` 은마 0.009(=42/4424, 원본 app.py 1406줄 그대로·값 정확)가 "세대당 0.009대"로 비직관. planning §1-B에서 이 필드는 이미 후순위(🔶 "검증 후 정식 노출") — 정식 노출 전 표기 방향("N세대당 1대" 반전 등) 재검토 권고. 상단 배너 ⑤에 기록.

### 커밋 범위
`components/AptDetailModal.tsx`(수정), `app/api/apt-info/route.ts`·`lib/molit-aptinfo.ts`·`supabase/apt_info.sql`(신규 3), 본 팀로그. QA가 지적한 대로 untracked API/lib/스키마를 함께 스테이징(프론트가 호출하는 API 누락 방지).

### 다음 라운드가 이어받을 상태
- **상세모달 복원 진행률**: 평수별 표 + 위치(A)·**단지정보(B, 이번 완료)** / **주변시설(C) 미착수**.
  - **Stage 3 — 주변시설 섹션**(`/api/apt-nearby`, 신규 `apt_nearby` 테이블): **학교(초/중/고)만** MVP. 기획안 §1-C·§2-C, design §12. 카카오 학교검색(SC4 1콜+폴백 최대3), payload jsonb 캐시(90일 TTL 권고). 병원·편의시설·어린이집은 후순위(카카오 16~19콜 부담).
- **후순위(2차)**: 단지정보 건축HUB(far/cov)·elev_per_hh 정식노출, 주변시설 확장.
- **사용자 조치 대기**(상단 배너): ① Vercel env vars, ② 프로덕션 Supabase 스키마(apt_geo + **apt_info 신규**), ③ 텔레그램 토큰, ④ apt_info.sql 적용(캐시 실효), ⑤ elev_per_hh 표기 재검토.
