# 2026-07-17 데일리 스탠드업

## 🔴 사용자 액션 필요 #0 — 화성시(41590) 캐시 리셋 (신규, 이번 라운드 발견)
프로덕션에서 **경기 화성시(lawd_cd 41590)가 매매·전세 13개월 전부 count=0/avg=null로 오염**된 것을 실측 발견(이웃 안성 41550·김포 41570·파주 41480은 정상). 근본원인을 코드에서 규명해 **재발 방지 fix를 배포**(commit `63391ab`)했으나, **이미 `months_collected=13/status=ready`로 굳어버린 41590의 기존 오염은 자동 치유되지 않는다**(read 경로가 `i < months_collected`면 재-fetch 없이 오염 캐시를 그대로 서빙). → **Supabase에서 41590 캐시 리셋 1회 필요**(부천 41190과 동일 성격):
- `fetch_cache_status`에서 lawd_cd='41590' 행 삭제(또는 months_collected를 0으로) + `deals`/`monthly_stats`의 41590 행 삭제.
- 리셋 후에는 이번 fix 로직으로 재-fetch·재시도·**자가치유 정상 동작**(QA 논리검증 완료). 실패해도 이제는 완료 마킹이 안 되므로 다음 크론/조회가 재시도한다.
- (부천 41190 캐시 리셋 액션도 여전히 미해소 상태 — 아래 참조)

---

## 라운드: 프리워밍 전진 + 캐시 포이즈닝 버그 규명·수정·배포(dev→qa→push)

### 1. 프리워밍 진행
- 시작 인덱스 **57 → 종료 60** 전진. 3회 호출 전부 HTTP 200(28~33s, 60s 하드리밋 안전마진, **504 재발 0**).
  - 안성시(41550) 완료, 김포시(41570) 콜드 지역 6→11→13 여러 호출 이어받기로 완료, 화성시(41590) monthsCollected:13 보고(단, 아래 오염 이슈 — 실제 데이터 없음).
- 전국 114개 중 **60개 통과**(잔여 54).
- 완료 지역 실데이터 실측 검증: 안성 41550(202607 count 64, 13/13), 김포 41570(202607 count 80, 13/13) **정상**. 파주 41480도 13/13 실값 재확인.

### 2. 🐛 캐시 포이즈닝 버그 — 발견→진단→수정→검증→배포 (이번 라운드 핵심)
**발견**: 화성 41590이 프리워밍 "완료"(monthsCollected:13, incomplete:false) 보고에도 실제 조회 시 매매·전세 13개월 전부 0건. 화성은 월 수백~수천 건 거래되는 대형 지역이라 명백한 오류. 이웃 지역은 정상 → **화성만 고립 오염**.

**근본원인(코드 레벨 규명)**:
- `lib/molit-api.ts`의 `fetchTradeSingle`/`fetchRentSingle`이 하드 실패 4경로(callApi throw/네트워크·타임아웃·레이트리밋, 비-XML 응답, 파싱오류, resultCode≠"00"/"000")를 전부 `console.error` 후 `break`하고 그때까지 모은 `results`(대개 `[]`)를 반환 → **실패를 삼킴, throw 안 함**.
- 따라서 `collectMonth`도 throw 안 하고 `route.ts`의 try/catch 미발동 → **"fetch 실패로 인한 빈 결과"와 "정상적으로 0건인 달"을 구분 불가**.
- `route.ts`가 그 빈 결과를 영속화하고 **실패 여부와 무관하게 무조건** `status:"ready"`, `months_collected:13`으로 완료 마킹 → 이후 조회가 `withinCollectedWindow`+`cacheFreshToday`로 재조회를 건너뛰어 **오염 고착**.
- 대형 지역일수록 프리워밍 중 국토부 레이트리밋 확률↑ → 화성이 오염된 것으로 추정. 부천 41190도 동일 계열.

**수정(dev-team)** — commit `63391ab`, 4파일:
- `FetchOutcome<T>{records,failed}` / `CollectMonthResult{data,failed}` 도입. 실패 4경로 전부 `failed:true` 전파, 정상 완주(정상 0건 포함)는 `failed:false`. 불변식: `failed:false && records:[]`=정상0건, `failed:true`=fetch깨짐.
- 팬아웃 래퍼(부천 등) `failed: some(r=>r.failed)`, `collectMonth`는 `trades.failed||rents.failed`.
- `data/route.ts`·`prewarm/route.ts`: 실패 달은 deals/stats 영속화 스킵, **실패가 하나라도 있으면 status:ready·months_collected 확장 차단** → 다음 조회/크론이 그 지역 재시도. 응답은 실패 여부 무관 200 유지(폴백 무결성).
- `recommend/route.ts` 시그니처 동기화.

**검증(3중)**:
- dev-team: `npx tsc --noEmit` exit 0 + `npm run build` 성공.
- qa-team **독립 검증 PASS(7/7)**: 핵심 불변식/완료마킹 게이트/회귀안전성/폴백무결성/호출부 전수(구 시그니처 잔존 0)/빌드 재실행 exit 0/엣지케이스(팬아웃 일부 실패·전세만 실패·EMPTY_MONTH). 
- 마스터 독립: `npm run build` 성공(6개 API 라우트 정상), diff 직접 리뷰(4개 실패경로 `failed:true`, 팬아웃 `some`, `collectMonth` OR 전파 확인).

**회귀 안전성**: 정상 지역·정상 0건 지역은 `failed:false` → `anyFetchFailed=false` 분기로 기존 경로와 등가. 동작 무변화.

### 3. 헬스게이트 GREEN
- tsc exit 0(dev/qa/마스터 3회), `npm run build` 성공(dev/qa/마스터 3회). 프로덕션 홈 HTTP 200(0.13s).

### 4. 부천(41190) 캐시 리셋 — 부분 진전 관찰
- `/api/data?lawd_cd=41190&deal_ymd=202606` 실측: **202607(현재월) count=262/avg=52,793 실데이터 복구**, 그러나 과거 12개월(202507~202606)은 여전히 count=0. → 현재월은 `cacheFreshToday` 아닐 때 라이브 재-fetch되어 살아났으나, 과거월은 완료 마킹된 오염 캐시라 재-fetch 안 됨. **기존 오염 자동치유 불가 재확인 — 41190도 SQL 캐시 리셋 필요**(액션 유지).

### git
- 시작 시 origin 동기(직전 `ade4b97`), 워킹트리 clean(디자인 라운드 커밋됨). 이번 라운드 fix 4파일 커밋·push 완료 → `63391ab`. Vercel 자동배포 트리거됨.
- `.claude/launch.json`(로컬 dev 서버 설정)은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태
- **프리워밍 인덱스 60부터 이어받기**(화성시 다음 지역부터, 전국 114 중 60 완료·잔여 54).
- 🔴 **화성시 41590 캐시 리셋(위 액션 #0) 사용자 실행 여부 모니터링** — 실행되면 fix 로직으로 자가치유되므로 `/api/data?lawd_cd=41590&...`로 13/13 실데이터 복구 확인.
- 🔴 **부천 41190 캐시 리셋도 미해소** — 과거 12개월 0건. 실행 시 13/13 복구 확인.
- 배포된 `63391ab` fix 실효 확인: 배포 후 새로 프리워밍되는 지역이 레이트리밋 실패 시 완료 마킹 안 되고 재시도되는지 관찰(로그로 monthsCollected가 실패 지점에서 멈추는지).
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).
