# 2026-07-17 데일리 스탠드업

## 🔴 사용자 액션 필요 #1 — Vercel에 Resend 환경변수 추가 필요 (문의 폼 프로덕션 발송 전제조건)
> **라운드 16(dev-team)**: `/support` 문의 폼을 mailto에서 실제 접수(Resend 발송)로 전환했다. 계산기 허브와 계정/키를 공유하기로 해 로컬 `.env.local`에는 `RESEND_API_KEY`/`CONTACT_TO_EMAIL`(sss159228@gmail.com)이 이미 있고, 로컬 dev 서버에서 실제 제출 1회로 Resend API 왕복(200 OK)까지 확인했다.
> **그러나 Vercel 프로덕션 환경변수에는 이 두 값이 아직 등록되어 있지 않다(로컬 `.env.local`에만 존재).** 사용자가 Vercel 프로젝트 설정 → Environment Variables에 `RESEND_API_KEY`, `CONTACT_TO_EMAIL`을 직접 추가해야 배포된 사이트에서 문의 메일이 실제로 발송된다. 추가 전까지 프로덕션에서 폼을 제출하면 500(문의 전송 설정 미완료) 응답을 받는다. 상세는 아래 "라운드 16" 참고.

## ✅ 사용자 액션 필요 #0 — 전부 해소됨 (2R 심야 자동세션에서 코드로 종결, 수동 SQL 불필요)
> **원래 이슈**(화성 41590·부천 41190 "캐시 오염 → 사용자가 Supabase 수동 SQL 리셋 필요")는 **이번 날짜 2라운드에서 코드 레벨로 완전 종결**했다. 사용자 조치 불필요.
> - **부천 41190**: admin 리셋 엔드포인트로 캐시 무효화 → 자가치유, 13/13 실데이터 복구.
> - **화성 41590**: "오염"이 아니라 **MOLIT 팬아웃 누락**이 진짜 원인이었음(통합코드 41590은 totalCount=0). 하위 4코드(41591·41593·41595·41597) 팬아웃 추가(commit `d4ed89d`) + 리셋으로 13/13 실데이터 복구.
> - **재발 대비**: `/api/admin/reset-cache`(CRON_SECRET 게이트) 라이브 — 앞으로 어떤 지역이든 이 엔드포인트 한 방으로 리셋→자가치유(수동 Supabase SQL 대체). 상세는 아래 라운드2 섹션.

---

## 라운드: 프리워밍 전진 + 캐시 포이즈닝 버그 규명·수정·배포(dev→qa→push)

### 1. 프리워밍 진행
- 시작 인덱스 **57 → 종료 60** 전진. 3회 호출 전부 HTTP 200(28~33s, 60s 하드리밋 안전마진, **504 재발 0**).
  - 안성시(41550) 완료, 김포시(41570) 콜드 지역 6→11→13 여러 호출 이어받기로 완료, 화성시(41590) monthsCollected:13 보고(단, 아래 오염 이슈 — 실제 데이터 없음).
- 전국 114개 중 **60개 통과**(잔여 54).
- 완료 지역 실데이터 실측 검증: 안성 41550(202607 count 64, 13/13), 김포 41570(202607 count 80, 13/13) **정상**. 파주 41480도 13/13 실값 재확인.

### 2. 🐛 캐시 포이즈닝 버그 — 발견→진단→수정→검증→배포 (이번 라운드 핵심)
**발견**: 화성 41590이 프리워밍 "완료"(monthsCollected:13, incomplete:false) 보고에도 실제 조회 시 매매·전세 13개월 전부 0건. 화성은 월 수백~수천 건 거래되는 대형 지역이라 명백한 오류. 이웃 지역은 정상 → **화성만 고립 오염**.

**근본원인(코드 레벨 규명)**:
- `lib/molit-api.ts`의 `fetchTradeSingle`/`fetchRentSingle`이 하드 실패 4경로(callApi throw/네트워크·타임아웃·레이트리밋, 비-XML 응답, 파싱오류, resultCode≠"00"/"000")를 전부 `console.error` 후 `break`하고 그때까지 모은 `results`(대개 `[]`)를 반환 → **실패를 삼킴, throw 안 함**.
- 따라서 `collectMonth`도 throw 안 하고 `route.ts`의 try/catch 미발동 → **"fetch 실패로 인한 빈 결과"와 "정상적으로 0건인 달"을 구분 불가**.
- `route.ts`가 그 빈 결과를 영속화하고 **실패 여부와 무관하게 무조건** `status:"ready"`, `months_collected:13`으로 완료 마킹 → 이후 조회가 `withinCollectedWindow`+`cacheFreshToday`로 재조회를 건너뛰어 **오염 고착**.
- 대형 지역일수록 프리워밍 중 국토부 레이트리밋 확률↑ → 화성이 오염된 것으로 추정. 부천 41190도 동일 계열.

**수정(dev-team)** — commit `63391ab`, 4파일:
- `FetchOutcome<T>{records,failed}` / `CollectMonthResult{data,failed}` 도입. 실패 4경로 전부 `failed:true` 전파, 정상 완주(정상 0건 포함)는 `failed:false`. 불변식: `failed:false && records:[]`=정상0건, `failed:true`=fetch깨짐.
- 팬아웃 래퍼(부천 등) `failed: some(r=>r.failed)`, `collectMonth`는 `trades.failed||rents.failed`.
- `data/route.ts`·`prewarm/route.ts`: 실패 달은 deals/stats 영속화 스킵, **실패가 하나라도 있으면 status:ready·months_collected 확장 차단** → 다음 조회/크론이 그 지역 재시도. 응답은 실패 여부 무관 200 유지(폴백 무결성).
- `recommend/route.ts` 시그니처 동기화.

**검증(3중)**:
- dev-team: `npx tsc --noEmit` exit 0 + `npm run build` 성공.
- qa-team **독립 검증 PASS(7/7)**: 핵심 불변식/완료마킹 게이트/회귀안전성/폴백무결성/호출부 전수(구 시그니처 잔존 0)/빌드 재실행 exit 0/엣지케이스(팬아웃 일부 실패·전세만 실패·EMPTY_MONTH). 
- 마스터 독립: `npm run build` 성공(6개 API 라우트 정상), diff 직접 리뷰(4개 실패경로 `failed:true`, 팬아웃 `some`, `collectMonth` OR 전파 확인).

**회귀 안전성**: 정상 지역·정상 0건 지역은 `failed:false` → `anyFetchFailed=false` 분기로 기존 경로와 등가. 동작 무변화.

### 3. 헬스게이트 GREEN
- tsc exit 0(dev/qa/마스터 3회), `npm run build` 성공(dev/qa/마스터 3회). 프로덕션 홈 HTTP 200(0.13s).

### 4. 부천(41190) 캐시 리셋 — 부분 진전 관찰
- `/api/data?lawd_cd=41190&deal_ymd=202606` 실측: **202607(현재월) count=262/avg=52,793 실데이터 복구**, 그러나 과거 12개월(202507~202606)은 여전히 count=0. → 현재월은 `cacheFreshToday` 아닐 때 라이브 재-fetch되어 살아났으나, 과거월은 완료 마킹된 오염 캐시라 재-fetch 안 됨. **기존 오염 자동치유 불가 재확인 — 41190도 SQL 캐시 리셋 필요**(액션 유지).

### git
- 시작 시 origin 동기(직전 `ade4b97`), 워킹트리 clean(디자인 라운드 커밋됨). 이번 라운드 fix 4파일 커밋·push 완료 → `63391ab`. Vercel 자동배포 트리거됨.
- `.claude/launch.json`(로컬 dev 서버 설정)은 untracked 유지(커밋 안 함).

---

## 라운드 2 (심야 자동세션): 프리워밍 60→66 전진 + admin 캐시 리셋 엔드포인트 착수

### 1. 프리워밍 진행
- 시작 인덱스 **60 → 종료 66** 전진. 6회 호출 전부 HTTP 200(26~31s, 60s 하드리밋 안전마진, **504 재발 0**).
  - 경기 광주시(41610) 13/13, 양주시(41630) 콜드 3→11→13 이어받기 완료, 포천시(41650) 13/13, 여주시(41670) 13/13 완료.
  - 인천으로 진입: 중구(28110) 6→13 이어받기 완료, 동구(28140) 13/13 완료, 미추홀구(28177) 3/13 진행중(다음 라운드 이어받음).
- 전국 114개 중 **66개 통과**(잔여 48).
- **63391ab fix 실효 확인(핵심)**: 새로 프리워밍된 완료 지역 실데이터 실측 — 포천 41650(매매 current_avg 19,269, 월별 count 48~65 실값), 광주 41610(current_avg 46,541, count 173~276 실값) **정상**. 레이트리밋 실패가 없던 지역은 정상 완주, 양주/중구처럼 콜드 지역은 monthsCollected가 실제 확보분만큼만(3→11→13, 6→13) 점진 마킹되는 것 확인 — 완료-게이트 로직 정상 동작.

### 2. 🔴 화성 41590 / 부천 41190 — 여전히 미해소(사용자 수동 SQL 리셋 안 됨) → **자동화 엔드포인트 착수**
- 실측: 41590 매매·전세 13개월 전부 null/0(오염 그대로), 41190 현재월(202607 avg 52,793)만 라이브 복구·과거 12개월 여전히 0.
- 두 건 모두 "사용자가 Supabase에서 수동 SQL 삭제" 액션이 라운드마다 반복 요구되는 상태 → **근본 개선: 시크릿 게이트 admin 리셋 엔드포인트 신설**로 수동 SQL 없이 자가치유 트리거하도록 결정(자율 판단, 아래).
- 설계: `resetRegionCache(supabase, lawdCd, buildingType="아파트")` → deals/monthly_stats/fetch_cache_status에서 해당 지역 행 삭제 후 삭제 건수 반환. `app/api/admin/reset-cache/route.ts`(GET/POST, prewarm과 동일 CRON_SECRET 게이트). 삭제=캐시 무효화이므로 다음 /api/data 조회가 63391ab 로직으로 재-fetch·자가치유(복원적, 비파괴적).
- **구현·검증·배포 완료** (commit `cdeb6ca`, 2파일 +139줄):
  - dev-team 구현: service-role 클라이언트(RLS 우회) 확인, tsc/build exit 0. checkAuth는 prewarm 것 그대로 복붙(바이트 동일, qa 확인).
  - qa-team 독립검증 **PASS(7/7)** — 인증게이트/삭제범위(lawd_cd+building_type 이중 필터, 오삭제 없음)/파라미터/회귀안전성(기존 함수 수정라인 0)/빌드/엣지케이스 전부 PASS. **MINOR 1건**: `.select().length` 카운트가 PostgREST 1000행 상한에 걸려 대형 지역서 과소표시(삭제 자체는 완전, 자가치유 무영향). → **마스터가 즉시 반영**: `{ count: "exact" }` 방식으로 교체(행 안 되받아 상한 무관·더 가벼움).
  - 마스터 재검증: 수정 후 tsc exit 0 + build exit 0(`/api/admin/reset-cache` dynamic 라우트 등록 확인). push→Vercel 자동배포.
- **배포 완료 확인**: push 후 폴링 1회만에 배포됨(엔드포인트 no-secret 프로브 HTTP 401 = 인증게이트 정상). 404→401 전환 확인.

### 3. ✅ 부천 41190 — RESOLVED (엔드포인트 실전 검증 겸 자가치유 완료)
- `POST /api/admin/reset-cache?secret=…&lawd_cd=41190` → `{deleted:{deals:518,monthlyStats:26,cacheStatus:1}}` (count 정확, 1000 상한 안 걸림 — 수정 실효 확인).
- 직후 `/api/data?lawd_cd=41190&deal_ymd=202607` → **13개월 전부 실데이터 복구**(매매 202507 count 459 … 202606 738 / 202607 262, avg 47,834~55,126). 과거 12개월이 전부 0이던 오염이 **완전 자가치유**. **🔴 부천 액션 종결.**

### 4. 🟠 화성 41590 — "포이즈닝"은 오진, 진짜 원인은 **팬아웃 누락** (신규 규명)
- `POST reset-cache lawd_cd=41590` → `{deleted:{deals:0,monthlyStats:26,cacheStatus:1}}`. deals가 애초에 0건이었다(= 실데이터가 저장된 적 없음, 오염이 아니라 원천 무데이터).
- 리셋 후 깨끗한 상태에서 `/api/data?lawd_cd=41590` **3회 재호출 전부 13개월 count=0**. 즉 **MOLIT가 41590 통합코드로 실거래를 0건 반환**한다 — 부천 41190과 **정확히 동일 증상**.
- 근본원인: `lib/molit-api.ts:48` `LAWD_FANOUT`에 **화성이 누락**. 부천은 폐지 자치구 레거시코드(41192/41194/41196)로 팬아웃해 병합하는데, 화성은 팬아웃 엔트리가 없어 41590 그대로 조회→0건→빈 데이터. (화성특례시 일반구 신설 등으로 MOLIT가 하위 코드로만 데이터 제공하는 것으로 추정.)
- **reset 엔드포인트는 완벽 동작**(부천으로 입증) — 오히려 이 딥이슈를 정확히 노출시킨 것. 캐시 리셋으로 고칠 수 있는 문제가 아니었음(코드 팬아웃 수정 필요).
- **규명 완료(dev-team 실측)**: 화성 하위 4개 코드 확정 → `41591=147 / 41593=194 / 41595=262 / 41597=946`(매매 202506, 합 1549), 전월세 교차검증 `613/537/471/1819`(합 3440). umdNm 실측: 41591=서부권(남양·향남 등)/41593=봉담·비봉/41595=병점·진안/41597=동탄신도시. 41592/94/96/98/99는 정상 0건이라 제외.
- **수정·배포**: `LAWD_FANOUT["41590"]=["41591","41593","41595","41597"]` 추가(commit `d4ed89d`, 실측 근거 주석 포함). 마스터 diff 리뷰·tsc/build exit 0 후 push→Vercel 자동배포.
- ⚠️ **미묘한 함정 기록**: 팬아웃 누락 상태의 41590 조회는 MOLIT가 `resultCode=000/totalCount=0`을 주므로 63391ab 불변식상 "정상 0건(failed:false)"으로 판정 → **ready로 캐시가 굳는다**(포이즈닝 방지 로직으로도 안 막힘, 원천적으로 빈 게 맞다고 오판). 그래서 팬아웃 배포 후 **41590 캐시를 한 번 더 리셋**해야 팬아웃 재-fetch가 실제로 돈다.
- ✅ **화성 41590 — RESOLVED (실전 end-to-end 검증 완료)**: 팬아웃 배포 후 reset→/api/data 재-fetch로 **매매·전세 13/13 전월 실데이터 복구 확인**.
  - 매매(count): 202507→202607 = 749·664·1030·1506·1602·1142·1450·1233·1604·1545·1925·2597·381(현재월도 실값).
  - 전세(count): 1550·1251·1186·1014·1269·1240·1369·1117·1321·1098·982·845·378.
  - (참고: 첫 폴링에서 현재월 202607만 보고 0으로 오판할 뻔 — 7월 초라 현재월 거래가 적었을 뿐, 완결월은 전부 실데이터. 대형 지역 4코드×13월 라이브 재-fetch는 무거워 /api/data가 한두 번 타임아웃했으나 63391ab로 ready 안 굳고 재시도해 결국 완주.)
- (curl 팁) data.go.kr WAF가 curl 기본 UA를 400 차단 → `-A "Mozilla/5.0"` 필요. Node fetch(프로덕션)는 무관.

### 다음 라운드가 이어받을 상태
- **프리워밍 인덱스 67부터 이어받기**(인천 연수구 28185 6/13 진행중부터, 전국 114 중 67 완료·잔여 47).
- ✅ **부천 41190 — RESOLVED**(reset+자가치유로 13개월 실데이터 복구 완료). 재발 시 `POST /api/admin/reset-cache?secret=…&lawd_cd=41190` 한 방이면 됨.
- ✅ **화성 41590 — RESOLVED**(팬아웃 fix d4ed89d 배포 + reset+heal로 13/13 실데이터 복구 검증 완료). 사용자 수동 SQL 불필요.
- 🆕 **admin 캐시 리셋 엔드포인트 라이브**(`/api/admin/reset-cache`, CRON_SECRET 게이트). 앞으로 오염/빈캐시 지역 발견 시 이 엔드포인트로 즉시 리셋→자가치유(수동 Supabase SQL 대체). 대형 지역은 count 정확 반환.

---

## 라운드 3 (심야 자동세션): 프리워밍 66→67 + 화성 41590 "조용한 재오염" 발견 → /api/data 연속 마킹 근본수정

### 1. 프리워밍 진행
- 시작 인덱스 66 → **67** 전진. HTTP 200(29.8s, 504 재발 0). 인천 연수구(28185) 6/13 진행중(monthsCollected 11, incomplete). 다음 라운드 67부터 이어받기.

### 2. 🔴 화성 41590 — "RESOLVED"였는데 **조용히 재오염**돼 있었음 (핵심 발견)
- 라운드 시작 시 실측: 직전 라운드가 13/13 실데이터로 "RESOLVED" 기록(위 라운드2 로그)했음에도, **매매·전세 모두 중간 6개월(202601~202508, 인덱스 6~11)이 count=0/avg=null**로 다시 비어 있었다(인덱스 0~5·12는 실값). 즉 조회/크론 트래픽이 랜딩되는 사이 **자연 재오염**됨.
- **근본원인 규명(코드 정독)**: `/api/data`의 캐시 마킹이 **all-or-nothing**이었다 — `if(!anyFetchFailed){ months_collected=max(existing,months); status="ready" }`. 화성은 4개 하위코드 팬아웃이라 한 요청에서 13개월×4코드×2종=최대 104회 국토부 호출 → **레이트리밋 벽**에 걸린다. 벽 너머 달들이 (a) `failed:true`면 진행이 통째로 버려져 months_collected가 0에서 못 올라가 영영 미수렴, (b) 레이트리밋이 드물게 `resultCode=000/totalCount=0`(throttled-empty)로 오면 `failed:false`로 오판→그 빈 달까지 collected=13/ready로 굳음(=인덱스 6~11 갭 오염). 부천(41190, 518건)은 소형이라 벽에 안 걸려 무사, 화성(3.9만건)만 병리적.
- **실측 재현**: reset 후 콜드 상태의 `/api/data?months=13` 단일 조회는 **55s 타임아웃**(104콜이 함수 시간·레이트리밋 초과). months=1→13 사다리 조회는 **1~5개월은 깨끗이 채워지나 6~7개월에서 레이트리밋 벽**에 부딪혀 0 반환. `months=3` 재조회는 `source:cache` 3/3 실값 → **마킹은 정상 persist됨**(초기 "마킹 안 됨" 가설은 오판, 선형 시간증가는 라이브 fill이었음).
- **현재 상태**: 안전한 클린 사다리로 **화성=최근 5개월(202607~202603) 실값 캐시**(current_avg 매매 52,973)로 복원. months_collected=5, 오염 없음. 나머지 8개월은 프리워밍(연속 마킹)이 이후 사이클에 채운다. 현재월(최중요 지표)은 정확.

### 3. 🔧 근본수정 — `/api/data` 연속(contiguous) 마킹 이식 (prewarm과 동일 불변식)
- **변경**(`app/api/data/route.ts` 마킹 블록): all-or-nothing → **연속 마킹**. months_collected = "인덱스 0부터 연속으로 정상 확보된(DB히트 ∪ 라이브 성공(!failed)) 개월 수"(Math.max로 절대 감소 안 함). 요청창 전체가 깨끗할 때만(`!anyFetchFailed && contiguous>=months`) status="ready", 부분 진행은 "collecting"으로 저장.
- **효과**: 대형 지역이 일부 달 실패해도 **앞의 연속 구간까지 진행을 저장** → 조회/프리워밍 반복으로 결국 수렴(기존엔 실패 시 진행 통째 버림 → 영영 미수렴). **정상 지역은 contiguous===months가 돼 기존과 동일하게 한 번에 ready 마킹 → 회귀 0**(prewarm 검증된 패턴).
- **검증**: 마스터 `npx tsc --noEmit` exit 0 + `npm run build` 성공(/api/data dynamic 등록). **qa-team 독립검증 6 PASS / 1 FAIL** — 회귀안전성(정상지역 콜드조회 old와 동일 결과 코드 논증)·연속성 불변식(중간 failed 시 앞 연속구간까지만 저장)·감소방지(Math.max)·status 다운그레이드(자가치유 정상)·0개월/폴백 엣지 PASS. **FAIL 1건**: `last_deal_ym: ymList[newMonthsCollected-1]`이 `months_collected > 요청 months`일 때(예: 13수집 지역을 months=3 조회) out-of-bounds→undefined(old는 `ymList[length-1]`로 항상 in-bounds였음, 회귀). → **마스터 즉시 수정**: `ymList[Math.min(newMonthsCollected, ymList.length)-1]` 상한 클램프. 재검증 tsc exit 0 + build 성공.
- ⚠️ **잔여 한계(다음 라운드 과제)**: 연속 마킹은 (a)"진행 유실→미수렴"과 (b)"failed:true 갭점프"는 막지만, **(c) throttled-empty(failed:false)로 오는 레이트리밋은 여전히 갭 오염 가능**. 화성 완전 면역엔 **콜드 지역 라이브 호출 상한(정확히는 국토부 하위콜 예산 캡 ~40~48콜/요청)** 이 필요 — 정상 지역 회귀 없이(1코드는 13콜) 화성만 5~6개월/요청으로 제한. molit-api에 콜 카운터 스레딩이 필요해 이번 라운드 범위 밖(신중). 우선 연속 마킹으로 수렴성/부분진행부터 확보.

### 다음 라운드가 이어받을 상태 (라운드3 기준)
- **프리워밍 인덱스 67부터**(인천 연수구 28185, 전국 114 중 67 완료·잔여 47).
- 🟡 **화성 41590 정정**: 라운드2가 "RESOLVED(13/13)"로 기록했으나 **자연 재오염 확인** — 완전 해결 아님. 현재 최근 5개월 실값 캐시(현재월 정확), 나머지는 프리워밍 연속 마킹이 사이클마다 채움. `/api/data` 연속 마킹 fix로 **재오염 대부분 차단 + 수렴 보장**(단 throttled-empty 갭은 잔존 — 콜 예산 캡이 다음 과제).
- ✅ 부천 41190 — RESOLVED 유지(소형이라 병리 없음).
- **커밋 대기**: `app/api/data/route.ts` 연속 마킹 fix — qa PASS 확인 후 커밋·push 예정(이 로그 갱신 시점 기준). 회귀 0(정상 지역 동작 불변), prewarm 검증 패턴 이식.
- 향후: 대형 팬아웃 지역 발견 시(완주했는데 count 0/갭) → admin reset + 연속 마킹으로 점진 수렴. 근본 면역은 콜 예산 캡.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).
- 향후 관찰: 다른 통합코드 지역도 41590/41190처럼 팬아웃 필요할 수 있음(예: 최근 구 신설/폐지 도시). 프리워밍 중 "완주했는데 count 전부 0"인 대형 지역 발견 시 동일 실측 절차로 팬아웃 추가.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 5 (심야 자동세션): 프리워밍 82→95 전진(13개) — 부산 완주 + 대구 6개 구, 완료지역 실데이터 검증

### 1. 프리워밍 진행 (순수 운영 라운드)
- 시작 인덱스 **82 → 종료 95** 전진. **14회 호출 전부 HTTP 200**(25~34s, 60s 하드리밋 안전마진, **504 재발 0**).
- 부산 완주: 해운대구(26350, 콜드 6→13 이어받기)·사하구(26380)·금정구(26410)·강서구(26440, 6→13)·연제구(26470, 12→13)·수영구(26500, 12→13)·사상구(26530, 6→13)·기장군(26710, 12→13) 전부 13/13 → **부산광역시 전 구·군 완주**.
- 대구 진입·연속 완주: 중구(27110)·동구(27140, 9→13)·서구(27170, 9→13)·남구(27200)·북구(27230) 전부 13/13.
- 전국 114개 중 **95개 통과**(잔여 19). 다음 라운드는 인덱스 95(대구 잔여 방면)부터 이어받기.
- **연속(contiguous) 마킹 정상 동작 재확인**: 콜드 지역 6→13, 9→13, 12→13 점진 확장 후 완주 패턴 다수 — `9163395` fix 그대로 작동, 진행 유실 없이 수렴.

### 2. 완료 지역 실데이터 실측 검증
- **해운대구(26350, 대형)** `/api/data?months=13`: `source:cache` 13/13 실데이터, 갭 0. 매매 월별 count 404·364·472·502·506·406·459·392·376·344·366·326·101(현재월), current_avg 50,591. 전세도 실값(count 312·258·290…). 대형 지역이 이번 라운드 콜드 6→13 이어받기로 온전 수렴함을 입증.
- **대구 북구(27230)** 13/13 실데이터, 갭 0. 매매 count 350·317·403·396·458·424·435·372·475·361·388·324·109, current_avg 29,374.

### 3. 헬스게이트 / git
- **코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.61s). 워킹트리 clean(직전 HEAD `5be722a` 라운드4 로그, origin 동기). `.claude/launch.json`은 untracked 유지(커밋 안 함).
- 이번 라운드는 팀 로그 갱신만 커밋.

### 다음 라운드가 이어받을 상태 (라운드5 기준)
- **프리워밍 인덱스 95부터 이어받기**(대구 잔여 → 이후 광주·대전·울산·세종·강원·충청·전라·경상·제주 계열 추정, 전국 114 중 95 완료·잔여 19). **완주 임박** — 앞으로 몇 라운드면 전국 프리워밍 1차 완료 예상.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지(연속 마킹으로 대형 팬아웃 지역도 자연 수렴 검증됨).
- 🆕 admin 리셋 엔드포인트(`/api/admin/reset-cache`, CRON_SECRET 게이트) 라이브 유지 — 오염/빈캐시 지역 발견 시 리셋→자가치유.
- 향후 관찰: 완주했는데 count 전부 0인 대형 통합코드 지역 발견 시 → `LAWD_FANOUT` 하위코드 추가 + reset 절차(41590 선례). 근본 면역(콜 예산 캡)은 미착수 과제이나 수렴성 확보돼 우선순위 낮음.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 7 (심야 자동세션): 리프레시 순환 안정화 검증 + 기획팀 전환(다음 화면 제안 착수)

### 1. 프리워밍 — 리프레시 순환 단계 (1차 완주 후 첫 안정화 라운드)
- 인덱스 랩어라운드 상태에서 시작(직전 라운드6이 114 완주). 이번 라운드 **4회 호출 전부 HTTP 200**(26~30s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **2 → 24 전진**.
- 처리 지역 전부 `fetchedMonths:1`(현재월만 리프레시) + `monthsCollected:13`(완전 유지) — 서울 용산·성동·광진·동대문·중랑·성북·강북·도봉·노원·은평·서대문·마포·양천·강서·구로·금천·영등포·동작·관악·서초·강남·송파 등 순차 리프레시. **1차 완주 후 리프레시 순환이 설계대로 동작**(콜드 갭 재발 0, 빠른 현재월 갱신).
- 전국 114개 1차 완주 상태 유지 — 새 콜드 갭/재오염 지역 발견 없음.

### 2. 완료 지역 실데이터 실측 검증 (리프레시 무결성)
- **강남구(11680, 최대형)** `/api/data?deal_ymd=202607`: `source:cache`, 매매 current_avg 324,659, 월별 count 338·117·225·289·269…(13개월 실값, 갭 0), 단지별 집계(aptStatsMaeMae) 상위 30단지 정상. **리프레시가 기존 13개월 실데이터를 훼손 없이 현재월만 갱신함을 입증**.

### 3. 헬스게이트 / git
- **코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.81s). 워킹트리 clean(직전 HEAD `0271500` 라운드6 로그, origin 동기). `.claude/launch.json`은 untracked 유지.

### 4. 🆕 기획팀 전환 — 다음 화면 제안 문서 완료 (라운드6 권고 반영)
- 라운드6이 "프리워밍 1차 완주로 운영 안정화 → planning/design 또는 marketing/cs로 여력 전환 검토 가능"을 권고 → 이번 라운드 **기획팀이 '다음 화면/기능 3개 우선순위 제안' 문서 완성**(`docs/planning/next-features-2026-07-17.md`, 코드 구조 실측 기반, 초안·구현/배포 없음).
- **핵심 통찰**: 지금까지 모든 화면이 "한 지역 드릴다운"이었는데, 전국 프리워밍 완주로 `monthly_stats` 테이블(114시군구×아파트×매매/전세×13개월 ≈ 3천행, public read RLS)이 상주 → **"전국 가로 비교/랭킹"이 국토부 API 콜 0건**으로 가능해짐(레이트리밋/콜예산 무관).
- **제안 우선순위**:
  1. **전국 시세 랭킹 보드 `/ranking`** — 0콜(monthly_stats만), 난이도 中, 리스크 낮음. `analyzer.pctChange`/`formatPrice` 재사용, SEO 신규 인덱싱+내부 딥링크 순환. (주의: 시군구 평균가는 면적구성 좌우 → "평균가≠평단가" 툴팁 필수; collecting 지역 null avg "집계중" 표기 필요.)
  2. 관심지역 즐겨찾기+나란히 비교 — 0~미미 콜, 난이도 下~中.
  3. 단지별 시세 추이 차트(모달 확장) — 0콜(deals만), 난이도 中.
- 세 제안 모두 `building_type='아파트'` 고정 → **빌라/단독 금지 제약 저촉 없음**.
- **🔵 사용자 확인 필요(자율 진행 안 함)**: 1순위 `/ranking`은 **신규 최상위 페이지 = 사이트 정보구조(IR) 변경**이라 기획 문서가 사전 컨펌을 요청함. 심야 자동세션 범위 밖으로 판단 → **구현 착수 보류, 사용자 결정 대기**. 사용자 승인 시 다음 라운드부터 planning→design→dev→qa→push 파이프라인 진행. (승인 요청 3건: ①랭킹 보드 우선 진행 ②초기 지표=평균가+변화율+거래량 3종 ③신규 `/ranking` 최상위 추가.)

### 다음 라운드가 이어받을 상태 (라운드7 기준)
- ✅ **프리워밍 1차 완주 유지 + 리프레시 순환 안정 동작 검증**. 다음 라운드도 매 시작 프리워밍 호출 계속(대부분 fetchedMonths:1 빠른 순환), 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **기획 산출물 완료(`docs/planning/next-features-2026-07-17.md`) → 사용자 결정 대기**: 1순위 `/ranking` 랭킹 보드는 신규 최상위 페이지(IR 변경)라 사용자 승인 필요 → 자동세션에서 구현 보류. 사용자가 승인하면 design→dev→qa→push 파이프라인 착수(0콜 설계라 레이트리밋 무관, 빌라/단독 제약 무저촉). 승인 없이도 진행 가능한 대안: 2순위 즐겨찾기/비교는 IR 변경 없이 기존 화면 확장이라 상대적으로 자율 진행 여지 있음(단 이 역시 다음 라운드에 판단).
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 8 (심야 자동세션): 리프레시 순환 계속 안정 + `/ranking` 디자인 스펙 초안 준비(승인 대기 중 무배포 선행작업)

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드7이 2→24). 이번 라운드 프리워밍 호출 HTTP 200(29.9s, **504 재발 0**), 인덱스 **24 → 28 전진**. 처리 전부 `fetchedMonths:1`(현재월 리프레시) + `monthsCollected:13`(완전 유지): 서울 강동(11740), 수원 장안(41111)·권선(41113)·팔달(41115). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **수원 팔달구(41115)** `/api/data?months=13`: `source:cache` 13/13 실데이터, 갭 0. 매매 월별 count 156·123·195·199·118·163·171·269·234·317·285·236·86(현재월), current_avg 69,609, 변화율(전월 +3.48%/1년 +23.51%) 정상 산출. 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 🆕 기획→디자인 파이프라인 전진 — `/ranking` 화면 구성안 초안 작성 (무배포 선행작업)
- 라운드7이 남긴 "1순위 `/ranking`은 IR 변경이라 사용자 승인 필요, 승인 시 디자인팀에 와이어프레임 전달" 상태를 이어받아, **승인을 선점하지 않는 범위에서 준비만** 진행 판단(코드/라우트/배포 일절 없음, 문서 1개만). 사용자 승인 대기 상태는 그대로 유지.
- **산출물**: `docs/design/ranking-board-spec-2026-07-17.md` (디자인팀, 신규 1개 문서). 헤더에 "디자인 초안 — 사용자 승인 전, 구현/배포 없음" 명시. 기존 재사용 자산(`analyzer.pctChange`/`formatPrice`/`formatChange`, DealsTable 정렬 토글 관례, 색상 토큰, DealTypeTabs, `monthly_stats` 실제 컬럼)을 **실측 근거**로 작성. 레이아웃 와이어프레임/컴포넌트 트리(RankingBoard·RankingTable·RankingRow·SortToggle·SidoFilter·RankingHighlights·PendingRegions)/데이터 흐름(서버 ISR revalidate 21600s, monthly_stats 직접 read, molit 0콜)/반응형(첫 열 sticky 가로 스크롤)/중립 카피("평균가≠평단가" 주석 3곳)/빌라·단독 미노출 포함.
- **🔴 디자인 실측 중 발견한 선행 이슈 2건(개발 착수 시 같은 스코프로 묶어야 함)**:
  1. **lawd_cd→지역명 역참조 맵 부재.** `lib/regions.ts`의 `LAWD_TO_SIGUNGU`는 건축HUB 코드 매핑이라 표기용으로 못 씀 → `REGION_CODES`를 뒤집는 신규 헬퍼(`{[lawdCd]:{sido,gu}}` + 시/도 축약) 선행 필요.
  2. **딥링크 미동작.** `app/page.tsx`가 URL `?lawd_cd`를 안 읽고 버튼 클릭으로만 조회 → 랭킹 행 클릭 `/?lawd_cd=...`가 빈 대시보드로 감. **메인이 searchParams로 초기 지역 세팅**하는 선행 작업이 랭킹 핵심 가치(내부순환)의 전제.

### 4. 헬스게이트 / git
- **런타임 코드 변경 없음(문서만).** 프리워밍/리프레시 정상, 프로덕션 응답 정상. 워킹트리는 직전 HEAD `048fe53`(라운드7 로그·기획문서 커밋됨)에서 이번 라운드 팀 로그 + 디자인 초안 문서만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드8 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 28부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 이제 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) **둘 다 준비 완료** → 사용자 승인 즉시 dev→qa→push 착수 가능. 단, 개발 착수 시 위 선행 이슈 2건(역참조 맵·searchParams 딥링크)을 같은 스코프에 포함할 것.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 4 (심야 자동세션): 프리워밍 67→82 대약진(15개 전진) + 화성 41590 완전 자가치유 검증

### 1. 프리워밍 진행 (이번 라운드 핵심 성과)
- 시작 인덱스 **67 → 종료 82** 전진. **14회 호출 전부 HTTP 200**(26~38s, 60s 하드리밋 안전마진, **504 재발 0**). 한 세션 15개 지역 완주는 최근 최대폭.
- 인천 완주: 연수구(28185)·남동구(28200)·부평구(28237)·계양구(28245)·서구(28260)·강화군(28710)·옹진군(28720) 전부 13/13.
- 부산 진입·연속 완주: 중구(26110)·서구(26140)·동구(26170)·영도구(26200)·부산진구(26230)·동래구(26260)·남구(26290)·북구(26320) 전부 13/13.
- 전국 114개 중 **82개 통과**(잔여 32). 다음 라운드는 인덱스 82(부산 해운대구 방면)부터 이어받기.
- **연속(contiguous) 마킹 정상 동작 실측**: 콜드 지역이 monthsCollected 11→13, 6→13, 9→13, 3→13 등 **점진 확장 후 완주** 패턴 다수 관찰 — `9163395` fix 그대로 작동, 진행 유실 없이 수렴.

### 2. ✅ 화성 41590 — 완전 자가치유 검증 (라운드3 잔여 이슈 종결)
- 라운드3은 화성을 "자연 재오염 → 최근 5개월만 실값 캐시(나머지는 프리워밍이 채움)" 상태로 남겼음.
- **이번 라운드 실측 `/api/data?lawd_cd=41590&months=13`**: `source:cache`로 **13개월 전부 실데이터 복구 확인**. 매매 월별 count 202507=749·202508=664·202509=1030·202510=1506·202511=1602 … current_avg 52,973(현재월 202607 정확). 갭(count 0/null) 없음.
- → **`9163395`(연속 마킹) fix가 대형 팬아웃 지역의 수렴을 실제로 보장함이 end-to-end로 입증**. 화성=5개월(라운드3) → 13/13(라운드4), 수동 개입 0. throttled-empty 갭 잔존 우려가 있었으나 프리워밍/조회 반복 사이클로 자연 수렴됨.

### 3. 헬스게이트 / git
- 코드 변경 없음(순수 운영 라운드). 워킹트리 clean, HEAD `9163395`(라운드3 fix 이미 커밋·push됨, origin 동기). `.claude/launch.json`은 untracked 유지(커밋 안 함).
- 이번 라운드는 팀 로그 갱신만 커밋.

### 다음 라운드가 이어받을 상태 (라운드4 기준)
- **프리워밍 인덱스 82부터 이어받기**(부산 진행 중, 전국 114 중 82 완료·잔여 32). 남은 지역: 부산 잔여 + 대구·광주·대전·울산·세종·강원·충청·전라·경상·제주 계열 추정.
- ✅ **화성 41590 — RESOLVED (13/13 실데이터, 연속 마킹으로 자연 수렴 검증 완료)**. 부천 41190도 RESOLVED 유지.
- 🆕 admin 리셋 엔드포인트(`/api/admin/reset-cache`, CRON_SECRET 게이트) 라이브 유지 — 오염/빈캐시 지역 발견 시 리셋→자가치유.
- 향후 관찰: 완주했는데 count 전부 0인 대형 통합코드 지역 발견 시 → `LAWD_FANOUT` 하위코드 추가 + reset 절차(41590 선례). 근본 면역(콜 예산 캡)은 여전히 미착수 과제이나, 연속 마킹으로 수렴성은 확보돼 우선순위 낮음.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 6 (심야 자동세션): 프리워밍 95→114 완주 🎉 — **전국 114개 전 지역 1차 프리워밍 완료**

### 1. 프리워밍 진행 — 전국 완주 (이번 라운드 핵심 마일스톤)
- 시작 인덱스 **95 → 114 완주 → 인덱스 랩어라운드(nextIndex 2)**. **~21회 호출 전부 HTTP 200**(25~42s, 60s 하드리밋 안전마진, **504 재발 0**).
- 대구 완주: 수성구(27260, 콜드 6→11→13 이어받기)·달서구(27290, 6→13)·달성군(27710) 전부 13/13 → **대구광역시 완주**.
- 대전 완주: 동구(30110)·중구(30140)·서구(30170, 9→13)·유성구(30200, 3→11→13)·대덕구(30230, 6→13) 전부 13/13 → **대전광역시 완주**.
- 광주 완주: 동구(29110)·서구(29140)·남구(29155)·북구(29170, 3→13)·광산구(29200) 전부 13/13 → **광주광역시 완주**.
- 울산 완주: 중구(31110, 6→13)·남구(31140, 3→11→13)·동구(31170)·북구(31200, 12→13)·울주군(31710, 12→13) 전부 13/13 → **울산광역시 완주**.
- 세종 완주: 세종시(36110, 콜드 6→11→13) 13/13.
- 인덱스 114 도달 후 **자동 랩어라운드** → 서울 종로구(11110)·중구(11140) 재진입(이미 13/13, fetchedMonths:1 = 현재월만 리프레시). 즉 **전국 114개 지역 1차 프리워밍 사이클 완료**, 이후는 현재월 리프레시 순환 단계로 진입.
- **연속(contiguous) 마킹 정상 동작 재확인**: 콜드 지역 6→11→13, 3→11→13, 9→13 등 점진 확장 후 완주 패턴 다수 — `9163395` fix 그대로 작동, 진행 유실 없이 수렴.

### 2. 완료 지역 실데이터 실측 검증
- **대전 서구(30170)** `/api/data?months=13`: 13/13 실데이터, 갭 0. 매매 월별 count 419·407·452·476·565·520·750·522·629·451·407·451·177(현재월), current_avg 38,405.
- **울산 남구(31140)** 13/13 실데이터, 갭 0. 매매 count 429·344·478·464·518·390·503·364·468·366·383·375·110, current_avg 45,171.

### 3. 헬스게이트 / git
- **코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.57s). 워킹트리 clean(직전 HEAD `82bf073` 라운드5 로그, origin 동기). `.claude/launch.json`은 untracked 유지(커밋 안 함).
- 이번 라운드는 팀 로그 갱신만 커밋.

### 다음 라운드가 이어받을 상태 (라운드6 기준)
- ✅ **전국 114개 지역 프리워밍 1차 완주** — 이제 프리워밍은 "빈 지역 채우기"에서 "현재월 리프레시 순환"(인덱스 2부터 재순환) 단계로 전환. 다음 라운드는 프리워밍 호출을 계속하되 대부분 fetchedMonths:1(현재월만 갱신)로 빠르게 순환할 것. 새 콜드 갭(신규 재오염) 발견 시에만 이어받기 확장.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지(연속 마킹으로 대형 팬아웃 지역도 자연 수렴 검증됨).
- 🆕 admin 리셋 엔드포인트(`/api/admin/reset-cache`, CRON_SECRET 게이트) 라이브 유지 — 오염/빈캐시 지역 발견 시 리셋→자가치유.
- 향후 관찰: 완주했는데 count 전부 0인 대형 통합코드 지역 발견 시 → `LAWD_FANOUT` 하위코드 추가 + reset 절차(41590 선례). 근본 면역(콜 예산 캡)은 미착수 과제이나 수렴성 확보돼 우선순위 낮음.
- **다음 우선순위 제안**: 프리워밍 1차 완주로 운영 안정화 달성 → 다음 라운드부터는 리프레시 순환을 유지하면서, marketing/cs팀 리서치·초안 또는 신규 화면 기획(planning→design) 쪽으로 여력 전환 검토 가능.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 9 (심야 자동세션): 리프레시 순환 계속 안정(인덱스 28→32, 분당구 13/13 무결성 실측) — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드8이 24→28). 이번 라운드 프리워밍 호출 HTTP 200(32.1s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **28 → 32 전진**. 처리 4개 지역 전부 `fetchedMonths:1`(현재월 리프레시) + `monthsCollected:13`(완전 유지, incomplete:false): 경기 수원 영통구(41117)·성남 수정구(41131)·중원구(41133)·분당구(41135). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **성남 분당구(41135, 최대형)** `/api/data?months=13`: `source` 캐시 응답, 13/13 실데이터, 갭 0. 매매 current_avg 164,743, 월별 count 235·344·1043·635·88·265·359·275·239·517·517·405·101(현재월), 변화율(전월 +0.17%/3개월 +6.74%/1년 +29.15%) 정상 산출. 전세도 실값(current_avg 70,884, count 968·665·802…). 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 프로덕션 응답 정상. 워킹트리는 직전 HEAD `c84e7fc`(라운드8 로그·기획/디자인 문서 커밋됨, origin 동기)에서 이번 라운드 팀 로그만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드9 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 32부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 10 (심야 자동세션): 리프레시 순환 계속 안정(인덱스 32→37, 부천 41190 13/13 무결성 실측) — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드9가 28→32). 이번 라운드 프리워밍 호출 HTTP 200(29.1s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **32 → 37 전진**. 처리 5개 지역 전부 `monthsCollected:13`(완전 유지, incomplete:false): 경기 의정부시(41150)·안양 만안구(41171)·안양 동안구(41173)·부천시(41190)·광명시(41210). 대부분 `fetchedMonths:1`(현재월 리프레시), 부천 41190은 `fetchedMonths:0`(당일 이미 신선 → 재fetch 스킵). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **부천 41190(전-오염 이력 지역, 재확인)** `/api/data?months=13`: 13/13 실데이터, 갭 0. 매매 월별 count 428·339·494·561·676·549·678·487·698·712·787·725·262(현재월), current_avg 52,793, 변화율(전월 −2.92%/3개월 +4.55%/1년 +10.23%) 정상 산출. 전세도 실값(current_avg 33,368, count 575·545·581…). **라운드2에서 reset+자가치유로 복구한 부천이 이후 리프레시 순환에서도 오염 재발 없이 13/13 유지됨을 재입증.**

### 3. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.77s). 워킹트리는 직전 HEAD `9e503a7`(라운드9 로그 커밋됨, origin 동기)에서 이번 라운드 팀 로그만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드10 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 37부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 11 (심야 자동세션): 리프레시 순환 계속 안정(인덱스 37→41, 안산 단원구 41273 13/13 무결성 실측) — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드10이 32→37). 이번 라운드 프리워밍 호출 HTTP 200(30.9s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **37 → 41 전진**. 처리 4개 지역 전부 `monthsCollected:13`(완전 유지, incomplete:false) + `fetchedMonths:1`(현재월 리프레시): 경기 평택시(41220)·동두천시(41250)·안산 상록구(41271)·안산 단원구(41273). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **안산 단원구(41273)** `/api/data?lawd_cd=41273&months=13`: `source:cache`, 13/13 실데이터, 갭 0. 매매 월별 count 167·165·186·249·246·190·245·221·289·272·307·236·79(현재월), current_avg 47,323. 변화율(changes/diffs) 정상 산출. 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.77s). 워킹트리는 직전 HEAD `d4f85cc`(라운드10 로그 커밋됨, origin 동기)에서 이번 라운드 팀 로그만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드11 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 41부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 12 (심야 자동세션): 리프레시 순환 계속 안정(인덱스 40→44, 고양 덕양구 41281 13/13 무결성 실측) — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드11이 37→41). 이번 라운드 프리워밍 호출 HTTP 200(26.9s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **40 → 44 전진**(nextIndex:44). 처리 4개 지역 전부 `monthsCollected:13`(완전 유지, incomplete:false): 경기 고양 덕양구(41281)·일산동구(41285)·일산서구(41287)는 `fetchedMonths:1`(현재월 리프레시), 과천시(41290)는 `fetchedMonths:0`(당일 이미 신선 → 재fetch 스킵, incomplete 플래그는 진행 표시일 뿐 데이터 완전). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **고양 덕양구(41281)** `/api/data?lawd_cd=41281&months=13`: `source:cache`, 13/13 실데이터, 갭 0. 매매 current_avg 57,288, 월별 count 257·221·328·419·423·347·428·391·479·476·563·513·165(현재월), avg 53,826~60,939 실값. updatedAt 2026-07-16T21:37:55Z. 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 프로덕션 응답 정상(HTTP 200). 워킹트리는 직전 HEAD `4bb0ad9`(라운드11 로그 커밋됨, origin 동기)에서 이번 라운드 팀 로그만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드12 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 44부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 13 (심야 자동세션): 리프레시 순환 계속 안정(인덱스 44→48, 남양주시 41360 13/13 무결성 실측) — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드12가 40→44). 이번 라운드 프리워밍 호출 HTTP 200(30.9s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **44 → 48 전진**(nextIndex:48, processedCount:4). 처리 4개 지역 전부 `monthsCollected:13`(완전 유지, incomplete:false) + `fetchedMonths:1`(현재월 리프레시): 경기 과천시(41290)·구리시(41310)·남양주시(41360)·오산시(41370). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **남양주시(41360, 대형)** `/api/data?lawd_cd=41360&months=13`: `source` 캐시 응답, 13/13 실데이터, 갭 0. 매매 current_avg 58,780, 월별 count 458·465·535·739·753·655·909·685·847·772·928·942·385(현재월), avg 47,012~58,780 실값. 변화율(전월 +1.36%/3개월 +10.69%/6개월 +2.63%/1년 +25.03%) 정상 산출. 전세도 실값(current_avg 43,857, count 879·790·833…). 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.81s). 워킹트리는 직전 HEAD `8d1cf5f`(라운드12 로그 커밋됨, origin 동기)에서 이번 라운드 팀 로그만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드13 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 48부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 14 (심야 자동세션): 리프레시 순환 계속 안정(인덱스 48→51, 시흥시 41390 13/13 무결성 실측) — 순수 운영 라운드

### 1. 프리워밍 — 리프레시 순환 계속
- 시작 시 인덱스 랩어라운드 상태(라운드13이 44→48). 이번 라운드 프리워밍 호출 HTTP 200(26.8s, 60s 하드리밋 안전마진, **504 재발 0**), 인덱스 **48 → 51 전진**(nextIndex:51, processedCount:3). 처리 3개 지역 전부 `monthsCollected:13`(완전 유지, incomplete:false) + `fetchedMonths:1`(현재월 리프레시): 경기 시흥시(41390)·군포시(41410)·의왕시(41430). 리프레시 순환 설계대로 동작, 새 콜드 갭 0.
- 전국 114개 1차 완주 상태 유지.

### 2. 리프레시 무결성 실측 검증
- **시흥시(41390, 대형)** `/api/data?lawd_cd=41390&months=13`: `source:cache`, 13/13 실데이터, 갭 0. 매매 current_avg 44,470, 월별 count 328·357·451·448·508·423·490·426·590·582·567·528·230(현재월), avg 38,260~44,470 실값. 변화율(전월 +7.05%/3개월 +14.75%/6개월 +10.23%/1년 +16.23%) 정상 산출. 전세도 실값(current_avg 26,652). 리프레시가 기존 13개월을 훼손 없이 현재월만 갱신함 재확인.

### 3. 헬스게이트 / git
- **런타임 코드 변경 없음(순수 운영 라운드).** 프로덕션 홈 HTTP 200(0.85s). 워킹트리는 직전 HEAD `2154a9f`(라운드13 로그 커밋됨, origin 동기)에서 이번 라운드 팀 로그만 추가. `.claude/launch.json`은 untracked 유지(커밋 안 함).

### 다음 라운드가 이어받을 상태 (라운드14 기준)
- ✅ **프리워밍 1차 완주 + 리프레시 순환 안정 유지**(인덱스 51부터 이어받기, 대부분 fetchedMonths:1). 새 콜드 갭 발견 시에만 이어받기 확장.
- 🔵 **`/ranking` — 여전히 사용자 승인 대기**(IR 변경). 기획(`docs/planning/next-features-2026-07-17.md`) + 디자인 초안(`docs/design/ranking-board-spec-2026-07-17.md`) 둘 다 준비 완료 → 사용자 승인 즉시 dev→qa→push 착수 가능. 개발 착수 시 선행 이슈 2건(lawd_cd→지역명 역참조 맵 신설·메인 searchParams 딥링크) 같은 스코프 포함 필수. 자동세션에서는 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 — RESOLVED 유지. admin 리셋 엔드포인트(`/api/admin/reset-cache`) 라이브 유지.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 15: CS팀 문의/FAQ 페이지 신규 (`/support`) — 사용자 지적 대응

### 배경
사용자가 "CS팀은 여기서 뭐해? 문의 들어오는 곳이 없잖아"라고 직접 지적. 실제로 사이트에 문의/FAQ 페이지가 전혀 없었다(`docs/cs/2026-07-15-faq-draft.md`에 로컬 초안만 있었고 발행 전이었음). `.claude/agents/cs-team.md` 원칙(데이터 지연 설명 우선, 투자조언 아님 명시, 없는 기능 안내 금지 등) 확인 후 CS+디자인+개발 겸임으로 직접 구현.

### 구현
- **`app/support/page.tsx` 신규**: FAQ 8개(실거래가 신고지연, 빌라·단독 준비중, 투자조언 아님, AI 추천 경사도 점수 산출방식, 개인정보 미저장, 캐시 자동 갱신 주기, 빈 결과 안내, 지원 지역) + mailto 기반 문의 버튼(제목/본문 사전 채움, 오류 제보 시 지역·유형·화면 요청 문구 포함).
  - FAQ 작성 전 실제 코드 확인: `app/api/recommend/route.ts`/`lib/recommender.ts`에 **경사도(평지) 점수가 실재**함을 확인(`getSlopeScore`, opentopodata SRTM30m 우선·open-elevation.com 폴백, w_slope 가중치) — 2026-07-15 CS FAQ 초안의 "경사도 언급 금지" 체크리스트는 그 시점 이후 기능 추가로 이미 갱신 필요 상태였음. 이번 FAQ는 현재 코드 기준으로 정확하게 반영.
  - 문의 이메일은 사이트 내 기존 확정 채널이 없어 계정 이메일(sss159228@gmail.com) 사용. 정식 이메일 수신 백엔드(Resend 등)는 아직 없음 — mailto 링크로만 우선 제공.
- **`app/page.tsx`**: 하단 footer 네비게이션에 "문의하기" 링크 추가(개인정보처리방침 · 이용약관 · 소개 · 문의하기).
- **`app/sitemap.ts`**: `/support` 항목 추가(monthly, priority 0.4, 기존 `/about`·`/privacy`·`/terms` 패턴과 동일).

### 검증
- `npm run build` 성공 — `/support` static(○)로 정상 생성, 타 라우트 회귀 없음.
- 로컬 dev(포트 3101, 기존 3000은 calculator-hub가 점유 중이라 별도 포트 사용) 브라우저 실측: `/support` 렌더·FAQ 8개 아코디언 정상, mailto href `mailto:sss159228@gmail.com?subject=...&body=...` 정상 인코딩 확인, 메인 페이지 footer에 "문의하기" 링크 노출 확인.

### 다음 라운드가 참고할 상태
- `/support`는 mailto 임시 방편 — 실제 문의 접수(폼+이메일 발송, 또는 Resend 등) 도입 시 이 페이지의 버튼 로직만 교체하면 됨.
- `docs/cs/2026-07-15-faq-draft.md`(미발행 로컬 초안)는 이번 `/support` 발행과 별개 문서로 남아있음 — 향후 초안 갱신 시 이번 페이지의 실제 반영 여부를 함께 확인할 것(특히 "경사도 언급 금지" 체크리스트 항목은 현재 코드 기준 더 이상 유효하지 않음을 주의).
- 위 CS FAQ 초안 파일의 금지 체크리스트를 갱신하는 후속 작업은 이번 라운드 범위 밖으로 남겨둠.

---

## 라운드 16: 문의 폼 mailto → 실제 접수(Resend) 전환 — 라운드15 핸드오프 이행

### 배경
라운드15가 `/support`를 mailto 임시방편으로 만들며 "실제 문의 접수(폼+이메일 발송, 또는 Resend 등) 도입 시 이 페이지의 버튼 로직만 교체하면 됨"이라고 남긴 핸드오프를 이행. 사용자 지시: 문의 이메일 발송은 계산기 허브(`C:\claude code webside`)에서 쓰던 Resend 계정/API 키를 그대로 재사용하되, 어느 사이트에서 온 문의인지는 구분 가능해야 함. 마스터가 사전에 `.env.local`에 계산기 허브와 동일한 `RESEND_API_KEY`/`CONTACT_TO_EMAIL`(sss159228@gmail.com)을 추가해둔 상태에서 시작.

### 구현
- **`app/api/contact/route.ts` 신규**: 계산기 허브(`C:\claude code webside\app\api\contact\route.ts`)의 Resend REST API 연동 로직을 그대로 포팅(느슨한 이메일 검증, `from: onboarding@resend.dev`, `reply_to`=문의자 이메일, Resend 상세 에러는 서버 로그로만·클라이언트엔 일반화 메시지만 노출). **subject만 `"[부동산 실거래가 대시보드 문의]"`로 하드코딩**해 계산기 허브(`"[계산기 허브 문의]"`)와 메일함에서 구분 가능하게 함(사용자 핵심 요구사항). 이 프로젝트의 다른 `route.ts` 전부가 갖는 `export const dynamic="force-dynamic"`/`runtime="nodejs"` 하우스 스타일을 동일 적용(로직 변경 아님, 컨벤션 일치).
- **`components/ContactForm.tsx` 신규**: 계산기 허브 `components/SupportInquiryForm.tsx`의 UX 패턴(이름 선택·이메일/문의내용 필수, idle→submitting→success/error 상태머신, 필드 단위 에러 메시지, 제출 중 버튼 비활성화, 성공 시 필드 초기화)을 이 프로젝트의 색상 토큰(`brand`, gray-*, 기존 에러 배너 `#fce4ec`/`#c62828` 팔레트, `min-h-[44px]` 터치타깃 관례)에 맞춰 이식. `"use client"`.
- **`app/support/page.tsx`**: mailto 버튼 및 관련 죽은 코드(`SUPPORT_EMAIL`, `MAILTO_SUBJECT`, `MAILTO_BODY`, `MAILTO_HREF`) 전부 제거, `<ContactForm />`으로 교체. 안내 문구도 "버튼을 누르면 이메일 앱이 열리며..." → "아래 양식을 작성해 보내주시면 담당자가 확인 후 입력하신 이메일로 답변드립니다"로 폼 방식에 맞게 수정.

### 검증
- `npm run build` 성공 — `/api/contact`가 dynamic(ƒ) 라우트로 신규 등록, `/support`는 static(○) 유지, 다른 라우트 회귀 없음.
- 로컬 dev 실측: 포트 3000이 계산기 허브 세션의 dev 서버와 충돌해 `.claude/launch.json`을 임시로 포트 3002로 바꿔 별도 실행(검증 후 3000으로 원복 완료, git diff 없음 확인). 브라우저로 `/support` 접속 → 이름·이메일·문의내용을 실제 입력해 제출 **1회**(스팸 방지를 위해 반복 제출 없음). 결과: 네트워크 탭에서 `POST /api/contact → 200 OK` 확인, dev 서버 로그에도 `POST /api/contact 200`(Resend 에러 로그 없음), 화면에 성공 메시지("문의가 접수되었습니다. 빠른 시일 내 답변드리겠습니다.") 렌더 및 폼 필드 초기화까지 확인. 즉 Resend API까지 실제로 왕복 성공했음을 end-to-end로 확인(다만 sss159228@gmail.com 수신함 도착 자체는 미확인).
- API 키는 로그·문서·커밋 어디에도 평문 노출 없음(`.env.local`은 `.gitignore`의 `.env*` 규칙으로 커밋 대상 아님).

### 🔴 다음 라운드/사용자 액션 필요
- **Vercel 프로덕션에 `RESEND_API_KEY`/`CONTACT_TO_EMAIL` 환경변수가 아직 없음**(로컬 `.env.local`에만 존재) — **사용자가 Vercel 프로젝트 설정에서 직접 추가해야** 배포된 사이트에서 문의 메일이 실제로 발송된다(파일 최상단 배너 참고). 추가 전까지 프로덕션 폼 제출은 500(문의 전송 설정 미완료) 응답.
- git: 커밋 `266f330`(`app/api/contact/route.ts`, `components/ContactForm.tsx`, `app/support/page.tsx` 3파일) push 완료. push 전 `git fetch`로 origin 동기 확인(충돌 0). `lib/blog.ts`(다른 세션의 untracked 산출물로 추정)는 이번 커밋 범위 밖이라 손대지 않음. `.claude/launch.json`은 이번에도 untracked 유지(포트 3000 원복 확인 완료).
