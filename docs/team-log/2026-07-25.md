# 2026-07-25 팀 로그 (부동산 실거래가 대시보드)

## 라운드 47 — 결정 #11(모바일 line-clamp 완화)·#12(한글 폰트스택+word-break) 검증 완료 + 문서 라운드번호 오표기 정정

- **배경**: 직전 세션이 API 세션 한도로 중단되며 `app/globals.css`(34줄)·`components/blog/BlogCard.tsx`(13줄)·`docs/design/blog.md`(16줄)에 결정 #11·#12 구현을 이미 작업트리에 남겨둔 채 미커밋 상태로 남겼다(팀로그 append 전에 중단됨). 이번 라운드는 처음부터 재구현하지 않고, 남은 변경사항의 완결성·정확성을 검증한 뒤 이어서 마무리했다.
- **동시성 확인**: 시작 시 `git fetch` → `origin/main`이 로컬(`de2c216`, 라운드46까지 반영)과 동기, 충돌 없음. 종료 직전 재확인 완료.

### 발견 1 — 문서 라운드 번호 오표기 (정정)

세 파일의 주석/문서에 "2026-07-25 라운드45 결정#11/#12"·"라운드45 정정"이라고 적혀 있었으나, 실제 커밋된 라운드45(`faae9da`)는 세종 시황 수치·블로그 푸터 부재만 다뤘고 그 로그가 명시적으로 "11번(모바일 카드 잘림)·12번(한글 조판)은... 그대로 사용자 판단 대기로 남겨둔다"고 적어 #11·#12를 범위 밖에 뒀다. 라운드46(`de2c216`)도 마찬가지로 #11·#12를 범위 밖에 남겼다. 즉 "라운드45"라는 표기는 중단된 세션이 실제 커밋 이력과 어긋나게 붙인 라벨이었다(다른 병렬 세션이 라운드45·46을 먼저 커밋한 뒤 이어받은 것으로 추정).

**정정**: `app/globals.css`(3곳) · `components/blog/BlogCard.tsx`(1곳) · `docs/design/blog.md`(5곳) 총 9곳의 "라운드45"를 이번 라운드 번호인 "라운드47"로 정정했다. 다른 파일(`components/SiteFooter.tsx`·`app/layout.tsx`·`lib/blog.ts`·이 로그·`docs/planning/2026-07-22-pending-decisions-brief.md`)의 "라운드45" 표기는 실제 라운드45(세종·푸터) 작업이 맞아 손대지 않았다(`grep -rn "라운드45"`로 전수 대조 확인).

### 발견 2 — 결정 #11·#12 구현 자체는 스펙과 정확히 일치 (코드 변경 없음, 검증만 수행)

- `components/blog/BlogCard.tsx`: 제목 `line-clamp-3 md:line-clamp-2`(구 `line-clamp-2` 고정), 요약 `line-clamp-6 md:line-clamp-2`(구 `line-clamp-2` 고정) — `docs/design/blog.md` §1-3·§1-4 스펙과 정확히 일치.
- `app/globals.css`: `--font-sans`에 `var(--font-geist-sans)` 다음 `"Apple SD Gothic Neo", "Malgun Gothic", "맑은 고딕", "Noto Sans KR"` + 기존 `Arial, Helvetica, sans-serif` 순서로 추가, `body`의 하드코딩된 `font-family: Arial, Helvetica, sans-serif`를 `var(--font-sans)` 참조로 교체(기존엔 `@theme inline`의 `--font-sans`가 정의만 되고 `body`가 이를 안 써 Geist가 사실상 무력화돼 있던 문제 해소). `body`에 `word-break: keep-all; overflow-wrap: break-word;` 추가. 추가로 `table td, table th { word-break: normal }` 보호 규칙을 선제적으로 넣어 숫자 위주 표 컬럼을 keep-all 영향에서 제외해뒀다(이번 검증 지시가 우려한 지점을 이미 선반영한 상태였음).

### 검증 방법과 결과

- `npm run lint` — 경고 0. `npm run build` — 성공(중간에 동시 세션의 빌드와 락 충돌 1회 발생, 20초 대기 후 재시도로 해소). 라우트 트리: 정적 9(`/`·`/about`·`/blog`·`/privacy`·`/ranking`·`/robots.txt`·`/sitemap.xml`·`/support`·`/terms`, `/ranking`은 별도 병렬 세션의 미커밋 작업) + SSG `/blog/[slug]` 11편 + 동적 API 8.
- **환경 제약 발견**: 이 세션의 브라우저 미리보기 도구는 컴포지팅 디스플레이가 연결되어 있지 않아(`screenshot`/`read_page`가 "Browser pane is not displayed" 오류로 실패) 실제 픽셀 스크린샷이나 `window.innerWidth` 기반 뷰포트 판정이 불가능했다(`resize_window`로 375px를 설정해도 실제 페이지의 `window.innerWidth`는 반영되지 않고 항상 540 보고). 이를 우회해 **카드 DOM 요소 자체에 `style.width`를 375px 실측 카드 내부 폭(`311px = 375 − 페이지 px-4(32) − 카드 p-4(32)`, 라운드44/45 QA가 이미 확정한 값)으로 강제 지정**한 뒤 `scrollHeight`/`clientHeight` 비교로 실제 잘림 여부를 판정하는 방식을 썼다(뷰포트/미디어쿼리 판정과 무관하게 요소 자체의 실제 레이아웃 폭을 검사하므로 더 결정론적).
  - **측정 기법 자체의 유효성 검증**: 동일한 311px 폭에서 구값(`line-clamp-2`/`line-clamp-2`)을 임시로 재현했더니 제목 5/11편·요약 11/11편이 `scrollHeight > clientHeight`(실제 잘림)로 나왔다 — 라운드44 QA가 적발한 원래 버그(제목 7/11·요약 11/11 잘림)와 정성적으로 일치해, 이 측정 기법이 실제로 잘림을 검출할 수 있음을 먼저 확인했다.
  - **신값 검증**: 같은 311px에서 신값(`line-clamp-3`/`line-clamp-6`)으로는 발행 11편 전부 `scrollHeight === clientHeight`(잘림 없음) — 요약 최장 116자(`ai-recommend-weights-howto`)도 6줄 중 5줄만 사용해 여유 있게 들어가고, 제목 최장 55자(`jeonse-new-vs-renewal-contract-guide`)도 정확히 3줄로 들어맞는다. **11/11편 모두 모바일 잘림 해소 확인.**
- **폰트·word-break 실측**(`getComputedStyle`, dev·production 서버 양쪽): `body`의 `font-family`가 `Geist, "Geist Fallback", "Apple SD Gothic Neo", "Malgun Gothic", "맑은 고딕", "Noto Sans KR", Arial, Helvetica, sans-serif`로 정확히 계산됨을 확인(`html`의 `geistSans.variable` → `@theme inline --font-sans` → `body font-family: var(--font-sans)` 연쇄가 실제로 작동). `body`(및 상속받는 `h1`)의 `word-break`가 `keep-all`, `overflow-wrap`이 `break-word`로 계산됨을 확인.
- **표 회귀 검증(숫자 컬럼 중심)**: 공유 dev 서버(포트 3001)가 다른 세션의 동시 편집 여파로 홈 대시보드에서 응답 없음 상태였던 탓에(콘솔 에러 0, 단 Suspense 상태로 무한 대기 — 내 변경과 무관함, 같은 서버에서 `/blog`는 정상 로드됐고 CSS만 바꾼 내 변경이 데이터 fetch를 멈출 이유가 없음), 현재 작업트리로 만든 **별도 production 빌드**(포트 3878, 검증 후 프로세스 종료)로 우회 검증했다. 강남구 아파트 실데이터(가격 "37억 1,741만원", 등락률 "▲ +9.87%", 연식 "2023", 건수 "87" 등)로 `DealsTable`을 채운 뒤 모든 `td`/`th`의 `word-break`가 `normal`(보호 규칙 적용)임을 확인, `scrollWidth>clientWidth` 오버플로 0건. `AptDetailModal`의 평수별 표(가격·건수·비율 포함)도 동일하게 `word-break:normal` + 오버플로 0건 확인. 표 밖 `<dl>`(세대수·동수·용적률 등)·주변시설 목록도 렌더 텍스트 추출로 깨짐 없음 확인.
- **데스크톱 확인**: `font-family`/`word-break` 규칙은 미디어쿼리 조건이 전혀 없는 전역 `body` 규칙이라 데스크톱·모바일에 동일하게 적용됨(뷰포트 무관, 별도 데스크톱 전용 회귀 경로 없음). `BlogCard`의 `md:line-clamp-2`(데스크톱)는 이번 결정 대상이 아닌 기존 값 그대로라 변경 없음.

### 다음 라운드 이어받을 상태
- 승인 대기 목록: **5건 → 3건**(#11·#12 이번 라운드 해소, #1·#4·#6 잔존 — #1·#4는 다른 병렬 세션이 이미 작업 중으로 추정되어 계속 관찰 필요, #6(`/ranking`)은 미커밋 상태로 이미 코드가 존재해 다음 라운드에서 진행 상황 확인 권장).
- 이번 라운드는 `app/globals.css`·`components/blog/BlogCard.tsx`·`docs/design/blog.md` 3파일만 `git add`로 지정해 커밋 — 동시 작업 중인 타 세션의 미커밋 변경(`app/page.tsx`·`app/sitemap.ts`·`components/AptDetailModal.tsx`·`components/DealsTable.tsx`·`components/SiteHeader.tsx`·`lib/analyzer.ts`·`lib/db-cache.ts`·`lib/disclosures.ts`·`lib/molit-api.ts`·`lib/regions.ts`·`supabase/schema.sql`, 미추적 `app/ranking/`·`components/RankingBoard.tsx`·`components/ranking/`·`lib/ranking.ts`·`.claude/settings.json`)은 손대지 않았다.
- **환경 메모**: 이 세션의 브라우저 미리보기 도구는 컴포지팅이 비활성화돼 있어(screenshot/read_page 불가) 향후 라운드도 비슷한 시각 검증이 필요하면 DOM 강제폭+scrollHeight 비교 기법(위 참고)이나 별도 production 빌드 우회가 필요할 수 있다.

---

## 라운드 46 — 사용자 "권고안대로" 전체 승인 반영: 현행유지 4건 종결 + "준비 중" 표기 통일 (커밋 분리)

- **배경**: 마스터가 `docs/planning/2026-07-22-pending-decisions-brief.md`와 이 로그(라운드44)의 "🔴 사용자 판단 필요" 목록 전체에 대해 "권고안대로" 승인했다고 전달. 이번 라운드는 그중 **코드 변경이 전혀 없는 "현행 유지" 4건(#2·#3·#5·#7)의 문서 상태를 확정으로 갱신**하는 작업(작업1)과, **실제 코드 수정이 필요한 #8("준비중"/"준비 중" 표기 통일)을 구현**하는 작업(작업2) 두 가지만 맡았다. #1(전세 병기)·#4(DealsTable 라벨)·#6(`/ranking`)·#11(모바일 카드 잘림)·#12(한글 조판)은 코드 변경이 수반되는 별건이라 이번 라운드 범위 밖으로 남겨둔다.
- **동시성 확인**: 시작 시 `git fetch` + `git log origin/main -5` → `origin/main`이 로컬과 동기(`faae9da`), 충돌 없음. 단 `git status`에서 `components/AptDetailModal.tsx`·`components/DealsTable.tsx`·`lib/analyzer.ts`·`lib/db-cache.ts`·`lib/disclosures.ts`·`lib/molit-api.ts`·`supabase/schema.sql`가 **이미 작업트리에서 수정된 상태(미커밋)**로 발견됨 — 다른 병렬 세션이 같은 워킹 디렉터리에서 결정 #1·#3·#4 관련 코드를 동시 작업 중인 것으로 판단, **이 파일들은 손대지 않고 `git add`도 하지 않음**(내 커밋에는 내가 수정한 파일만 명시적으로 지정). 실제로 dev 서버 실측 시 `DealsTable` 헤더가 이미 "평균 매매가(최근 거래 3개월)"·"거래건수(13개월)"·"직전 거래월 대비"로 갱신되어 있어(결정 #4 관련) 다른 세션의 진행 상황과 일치했다.

### 작업 1 — 현행 유지 4건 문서 종결 (코드 변경 없음)

결정 #2(자연키 확장)·#3(해제거래 필터)·#5(직전월 신고기한)·#7(스케줄 중복 방지)을 "이대로 유지"로 확정하고, 삭제 대신 취소선+확정 주석으로 이력을 남겼다.
- `docs/planning/2026-07-22-pending-decisions-brief.md`: 개정 이력(문서 상단)에 2026-07-25 항목 추가, §0 체크리스트 표의 결정 #2·#3 "회신" 칸을 확정 표기로 갱신, 신규 `### 3-5. ✅ 확정`·`### 7-6. ✅ 확정` 두 서브섹션 추가(각각 결정 #3·#2). 이 문서는 애초에 4건 중 #2·#3만 다루므로(#5·#7은 이 문서 범위 밖), 두 결정에만 서브섹션을 달았다.
- `docs/team-log/2026-07-25.md`(이 파일)의 "🔴 사용자 판단 필요" 목록: 항목 2·3·5·7을 취소선 처리하고 "✅ 확정(2026-07-25, 사용자 승인: 권고안대로)" + 선택지 요약을 덧붙였다. 결정 #3은 "(b) 컬럼 보존만"이 확정됐을 뿐 **`cdealType`/`cdealDay` 파싱·스키마 추가 구현 자체는 이번 확정에 포함되지 않는다**는 점을 명시(브리프 §3-3 기준 규모 "中"의 실제 착수는 별도 라운드 판단). 항목 4·6은 이번 라운드 범위 밖임을 짧게 표시만 하고 취소선 처리는 하지 않았다(아직 미해결).

### 작업 2 — 결정 #8: "준비중"/"준비 중" 표기 통일 (`739bc86`)

**문제**: 라운드44 QA 감사가 적출한 불일치 — `app/page.tsx:113`(히어로 부제) · `app/support/page.tsx:43`(FAQ 답변) · `components/BuildingTypeToggle.tsx:13,32,49`(주석·title 속성·배지)는 "준비중"(붙여쓰기), `lib/blog.ts:550`·`app/support/page.tsx:42`·`components/CrossSiteNav.tsx:86,148`은 이미 "준비 중"(띄어쓰기). 같은 `support` 페이지 안에서도(42행 vs 43행) 혼용돼 있었고, 홈 히어로와 블로그 본문이 같은 "빌라·단독" 기능을 다르게 표기했다.

**구현**: 표준을 "준비 중"(띄어쓰기)으로 통일 — `app/page.tsx` 히어로 부제 1곳, `app/support/page.tsx` FAQ 답변 1곳, `components/BuildingTypeToggle.tsx` 3곳(비활성 사유를 적은 코드 주석·`title` 툴팁·화면에 보이는 `(준비중)` 배지)을 수정했다. `lib/blog.ts`·`components/CrossSiteNav.tsx`는 이미 표준 표기라 변경하지 않았다. 지시받은 3파일 외에 `grep -rn "준비중" app/ components/ lib/`로 저장소 전수를 확인해, 위 5곳이 전부였음을 확인(빠짐없음).

**검증**:
- `grep -rn "준비중" app/ components/ lib/` → **0건**(수정 후 재확인). `grep -rn "준비 중"`로 남은 표기 8곳 전부 띄어쓰기 통일 확인.
- `npm run lint` — 경고 0. `npm run build` — 성공, 라우트 트리 불변(정적 6 + SSG 11편 + 동적 API 8 + robots/sitemap).
- **dev 서버 실측**: 이미 실행 중이던 다른 세션의 `next dev`(포트 3000, PID 42780 — Next 16 dev 서버 잠금 정책상 중복 기동 불가라 이 프로세스에 그대로 접속)에 HMR로 반영된 화면을 확인. 홈(`/`)에서 히어로 부제 "…빌라·단독 조회 준비 중" 확인, 건물유형 토글의 빌라·단독 배지 둘 다 "(준비 중)"으로 렌더. `/support`에서 접근성 트리로 FAQ 두 번째 항목("빌라/단독주택은 왜 조회가 안 되나요?") 답변 본문에 "…준비 중입니다"·"&quot;(준비 중)&quot;"이 정상 반영됨을 확인. 콘솔 에러 0.

**커밋**: 코드 3파일(`app/page.tsx`·`app/support/page.tsx`·`components/BuildingTypeToggle.tsx`)만 `git add`로 지정해 커밋(`739bc86`) — 동시 작업 중인 타 세션의 미커밋 변경(위 7파일)과 완전히 분리. 문서(브리프+이 로그)는 별도 커밋으로 분리 예정(마스터 지시).

### 다음 라운드 이어받을 상태
- 승인 대기 목록 갱신: **10건 → 5건**(#2·#3·#5·#7·#8 이번 라운드 해소, #1·#4·#6·#11·#12 잔존 — 이 중 #1·#3·#4 관련 코드는 다른 병렬 세션이 이미 작업 중인 것으로 관측됨).
- 프리워밍은 이번 라운드에 별도 호출 없음(문서·표기 통일 라운드라 대시보드 데이터 경로 무관).

---

## 라운드 45 — QA 감사 적출 2건, 사용자 승인 없이 버그로 재분류해 즉시 수정 (커밋 분리)

- **배경**: 마스터가 라운드44 QA 감사 적출 12건 중 "🆕 라운드 44 QA 감사에서 추가된 판단 필요 4건"의 9번·10번을 검토한 결과, 이 둘은 기획·디자인 재량이 필요한 "결정 사항"이 아니라 **명백한 버그**(수치 자기모순, 스펙에 명시된 요소의 부재)로 판단해 사용자 승인 없이 이번 라운드에 직접 수정했다. 성격이 다른 두 수정이라 커밋을 분리했다(작업1 `3ccabed`, 작업2 `5149a7b`). 11번(모바일 카드 잘림)·12번(한글 조판)은 가시 텍스트/레이아웃을 바꾸는 진짜 결정 사항이라 그대로 사용자 판단 대기로 남겨둔다.
- 시작 시 `git status`/`git fetch`/`git log -5` 확인 — 클린, `origin/main`과 동기, 동시 세션 충돌 없음(작업 종료 시점에도 재확인 예정).

### 작업 1 — 세종 시황 글 전세 누적/갱신/신규 수치 재조회 정정 (`3ccabed`)

**문제**: `lib/blog.ts:963,967`(`sejong-jeonse-briefing-2026-07-19`)가 전세 누적 8,430건 / 갱신 2,885건(34.2%) / "신규만 5,446건"을 함께 적었는데 8,430−2,885=5,545로 **99건** 어긋났다(라운드44 QA #9). 임의 보정 대신 원본 국토부 세종(`lawd_cd=36110`) 데이터를 **지금 시점에 재조회**해 3수치를 동일 스냅샷으로 재확정했다.

**재조회 방법(재현 가능, 스크립트는 세션 scratchpad에 임시 작성 후 저장소에는 커밋하지 않음)**:
1. production `/api/data?lawd_cd=36110&months=13&building_type=아파트`를 호출(2026-07-25 11:02:31~11:02:54 UTC)해 캐시를 최신화하고, 응답 JSON의 `summary.전세.monthly`/`summary.매매.monthly` 13개월치를 합산.
2. 곧바로(11:02:54 UTC) Supabase `deals` 테이블을 **직접 count(*)** 쿼리 — `lawd_cd=36110 AND building_type=아파트`로 전세 총건수, 전세 중 `contract_type='갱신'` 건수, 매매 총건수를 각각 집계(2026-07-22 브리프 §2-1의 "Q6" 측정과 동일 방법론 — 이 프로젝트의 자연키 unique 제약상 `deals`엔 이미 자연키로 접힌 값만 존재하므로 원본 API 합계와 항상 일치해야 함).
3. `fetch_cache_status`(`lawd_cd=36110, building_type=아파트`)로 수집 상태 확인 — `months_collected:13, status:"ready"`. 즉 "조회 시점까지 쌓인 자료"는 원래 글과 동일하게 **2025-07~2026-07 13개월 롤링 윈도우**(이 프로젝트의 `/api/data`·프리워밍 모두 `MONTHS=13` 고정이라 창 자체는 이동하지 않음).

**결과(두 측정 방법이 정확히 일치 — 교차검증 성공)**:

| 항목 | 07-19 원문(자기모순) | 07-25 재조회(1번 방법: API 13개월 합) | 07-25 재조회(2번 방법: deals 직접 count) |
|---|---|---|---|
| 전세 누적 | 8,430 | **8,553** | **8,553** |
| 매매 누적 | 5,028 | **5,100** | **5,100** |
| 전세 갱신 | 2,885(34.2%) | — | **2,945(34.4%)** |
| 전세 신규(=누적−갱신) | "5,446"(불일치) | — | **5,608** |
| 검산 5,608+2,945 | — | — | **=8,553 (누적과 완전 일치)** |

- 완결월 예시("2026년 6월은 전세 605건, 매매 352건")는 재조회해도 **동일**(두 방법 모두 605/352) — 이미 완결된 달이라 드리프트가 없음을 재확인, 문장 수정 불필요.
- "완결된 최근 12개월은 매달 전세>매매"라는 별도 주장도 재조회한 12개월 전체(202507~202606)를 개별 대조해 **여전히 12/12 성립** 확인(전세 692/598/626/608/825/826/867/662/714/590/559/605 vs 매매 238/290/350/436/546/443/599/420/426/404/393/352, 모든 달 전세>매매).
- 반영: `lib/blog.ts` "보도와 어긋난 부분" 섹션의 두 문단(구 963/967행)만 수정 — 누적 8,430→8,553·5,028→5,100, 갱신 2,885→2,945·34.2%→34.4%, 신규 5,446→5,608. 그 문단 뒤 `sourceNote`의 `asOfDate`도 `2026-07-19`→`2026-07-25`로 갱신(이 섹션 수치만 재검증했으므로 이 섹션 담당 sourceNote 1개만 변경 — 가격 흐름을 다루는 나머지 두 `sourceNote`는 미검증 상태로 `2026-07-19` 유지).
- **의도적으로 건드리지 않은 것**: 같은 글의 "진행 중인 2026년 7월 전세 258건·매매 131건"(별도 문단, 07-19 시점 partial-month 스냅샷)은 자기모순 없는 단일 수치 쌍이라 이번 QA 지적(#9) 범위 밖이다. 재조회 시점(07-25)의 2026-07 값은 전세 381·매매 203으로 이미 더 늘어 있었으나, 이는 "진행 중인 달은 며칠 사이에도 달라질 수 있다"는 그 문단 자체의 서술과 정확히 부합하는 정상 동작이라 수정 대상이 아니다(건드리면 오히려 §7-3-11 취지에 안 맞는 범위 확장이 된다).
- `content-strategy.md` §7-3-11("진행 중인 달은 건수까지만, 금액은 쓰지 않는다") 계속 준수 — 이번 수정은 완결월·누적 건수만 다뤘고 금액 수치는 손대지 않았다.

### 작업 2 — 블로그 12개 URL 전역 푸터 추가 (`5149a7b`)

**문제**: `docs/design/blog.md:32,125` 스펙은 블로그 목록·상세 최하단에 `[Footer]`(최소 개인정보처리방침·이용약관)를 명시했으나, 실제 `<footer>`는 `app/page.tsx:194-217`(홈) 안에만 있어 블로그 12개 URL에서 `/privacy`·`/terms` 도달 경로가 0이었다(라운드44 QA #10). `components/SiteHeader.tsx:8` 주석("법적 고지는 footer에만 유지")의 전제 자체가 깨져 있었다.

**구현**: `components/SiteHeader.tsx`가 헤더를 전역화한 것과 동일한 패턴(`usePathname` + `pathname==="/"`면 자기 자신 미렌더링)으로 `components/SiteFooter.tsx`를 신설하고 `app/layout.tsx`에 `{children}` 다음·`CrossSiteNav` 이전으로 마운트했다. 소개·문의하기·부동산 정보 링크는 이미 `SiteHeader`(`HEADER_NAV_ITEMS`)에 있어 중복 배치하지 않고, 개인정보처리방침·이용약관 2개 링크 + 기존 홈 footer와 동일한 데이터 출처 표기(`데이터 출처: 국토교통부 실거래가 공개시스템(공공데이터포털)`)만 담았다. `<body>`가 이미 `min-h-full flex flex-col`이라 footer에 `mt-auto`만 추가해, 콘텐츠가 짧은 페이지(`/privacy` 등)에서도 각 페이지 래퍼를 건드리지 않고 뷰포트 하단에 붙게 했다.

**검증(dev 서버, `npm run dev` localhost:3000, 브라우저 실측)**:
- `/blog`(목록)·`/blog/sejong-jeonse-briefing-2026-07-19`(상세) 모두 `contentinfo` footer 렌더 + `/privacy`·`/terms` 링크 href 확인.
- `/about`·`/support`·`/privacy`에도 동일하게 footer 렌더 확인(전부 `app/layout.tsx`를 공유하므로 나머지 미확인 블로그 10편·`/terms`도 동일 템플릿으로 동작 확실).
- **홈(`/`) 회귀 없음** — `contentinfo`가 정확히 1개(기존 자체 footer, 개인정보처리방침·이용약관·소개·문의하기·부동산 정보 5링크 그대로)이고 `SiteFooter` 이중 렌더 없음 확인.
- 콘솔 에러 0(전 페이지). `CrossSiteNav`(데스크톱 레일 `fixed`, 모바일 FAB `fixed`)와 DOM 구조상 겹치지 않음 — `fixed` 요소는 in-flow 아니라 footer의 `mt-auto` 계산에 영향받지 않음.

### 공통 검증
- `npm run lint` — 통과(경고 0).
- `npm run build` — 성공. 라우트 트리 불변: 정적 6(`/`·`/about`·`/blog`·`/privacy`·`/support`·`/terms`) + SSG `/blog/[slug]` 11편 + 동적 API 8 + robots/sitemap.
- 두 작업 모두 헤더·`CrossSiteNav`와 레이아웃 충돌 없음.

### 다음 라운드 이어받을 상태
- 승인 대기 목록은 **12건 → 10건**으로 감소(9·10번 해소, 8건 이월 + 11·12번만 잔존). 위 "🆕 라운드 44..." 목록의 9·10번 항목에 해소 표시 완료.
- 프리워밍은 이번 라운드에 별도 호출 없음(재조회 스크립트가 세종 1개 지역만 `/api/data`로 갱신 — 대시보드 전체 프리워밍 진행률과는 무관).

---

## 라운드 44 — 블로그 SSG 표면 첫 독립 감사(12건 적출) + 승인 불필요 3건 수정 배포 (4라운드 정체 해소)

- **프리워밍:** 2회 호출 → **nextIndex 36→42 전진**(경기도 광명·평택·동두천·안산 상록/단원·고양 덕양, `monthsCollected` 각 13, `incomplete:false`, HTTP 200, 27s/32s, **504 없음**). 42/114. 라운드당 1회 규정이나, 6라운드째 신규 개발이 막혀 유휴 시간이 남은 상황이라 2회로 전진 폭을 키웠다(과호출 방지선인 "전체 완료"까지는 한참 남음).

- **문제의식:** 라운드 40~43이 "프리워밍 200 + 정적 페이지 200 + 빌드 성공"만 반복 확인하며 4라운드째 정체했다. 그런데 이 지표들은 **"200은 뜨는데 내용이 틀린" 결함을 구조적으로 못 잡는다.** 그래서 이번엔 한 번도 감사하지 않은 표면(블로그 SSG 12개 URL = 목록 + 11편)을 qa-team에 읽기전용 전수 감사로 돌렸다.

- **QA 독립 감사 결과 — 신규 결함 12건 적출.** 기존 라운드가 봐온 범위(HTTP·프리워밍)는 전부 정상이었고, 12건 전부 내용·표면 품질 영역이다.
  - HIGH 3: ① `app/layout.tsx` `lang="en"`(전 페이지 한국어인데 영어 선언, 같은 파일 `locale:"ko_KR"`와 자기모순) ② 세종 시황 본문 수치 자기모순(`lib/blog.ts:963,967` — 8,430−2,885=5,545인데 "5,446건", 역산 시 8,331로도 안 맞음) ③ 블로그 12개 URL에 푸터가 없어 `/privacy`·`/terms` 도달 경로 0(footer가 `app/page.tsx`홈에만 존재)
  - MEDIUM 5: og 미분화(11편 og:title·og:url이 전부 사이트 기본값/루트) / canonical·JSON-LD 0건 / 모바일 375px 카드 요약 11편 전부 잘림 / `word-break:keep-all` 부재 + 본문 폰트스택에 한글 폰트 없음 / 본문 상호참조가 평문(링크 타입이 `BlogSection` 유니온에 아예 없음)
  - LOW 4: "준비중"/"준비 중" / "만원"vs"만 원"·22.0% / 시황 제목 "이번 주" 신선도 만료 / 브레드크럼 2·3번째 동일 href + 탭 ARIA 상태 없음
  - PASS도 근거와 함께 다수 확보: 외부 출처 링크 10/10 200, 읽기시간 독립 재검산 일치, YMYL 법령 인용(주임법 6조의3·6조1항·7조+시행령 8조·신고기준액) 전수 정확, 오탈자 0건, 헤딩 위계 정상.

- **수정 착수 판단 — 승인 대기 8건과 겹치지 않는 것만.** 12건 중 ①`lang` ②og 미분화 ③canonical·JSON-LD **3건만** 골라 dev-team에 배정했다. 선정 기준은 "기획·디자인 결정이 필요 없고, 사용자 가시 텍스트를 바꾸지 않는 명백한 결함". 나머지 9건은 결정 사항이라 아래 승인 대기에 편입.

- **구현(dev-team, 2파일 65삽입/1삭제):** `app/layout.tsx:58` `lang="ko"` / `app/blog/[slug]/page.tsx` per-post `openGraph`(`type:"article"`+`publishedTime`)·`twitter`·`alternates.canonical`·`BlogPosting` JSON-LD. 베이스 URL은 하드코딩 없이 기존 `lib/site.ts`의 `SITE_URL` 재사용. `node_modules/next/dist/docs/`의 `generate-metadata.md`·`json-ld.md`를 먼저 읽고 구현(AGENTS.md 지시 준수) — 공식 가이드가 `next/script` 대신 네이티브 `<script>`+`<` 이스케이프를 권장해 그대로 따랐다.

- **QA 독립 검증 — PASS(전 8항목).** 개발팀 보고를 재확인하지 않고 직접 검증:
  - **가시 텍스트 무변경이 이번 작업의 계약**이었고, 라이브 프로덕션(=변경 전 상태)을 기준선으로 3중 대조 → 가시 텍스트·DOM 골격·**body HTML 바이트 단위** 전부 11/11 완전 일치. 삽입 요소는 ld+json `<script>` 1종뿐.
  - 삭제 줄 전량이 `lang="en"` **한 줄**임을 `git diff -U0`로 확인(범위 이탈 0).
  - JSON-LD 11/11 `JSON.parse` 통과 + `headline`/`description`/`datePublished`가 `lib/blog.ts` 원문과 문자 단위 일치(지어낸 값 0).
  - **XSS 격리 재현 테스트**: 실데이터에 `<`가 없어 미검증 구간이라, `</script><img src=x onerror=...>` 주입 입력으로 표현식을 재현 → 리터럴 `<` 0건, `<` 무해화 확인.
  - 비블로그 6개 페이지 회귀 없음(메타 차이는 `lang` 하나뿐, 사이트 기본 og 정상), `npx tsc --noEmit` exit 0, `next start` 런타임 200 + 없는 slug 404 정상.
- **마스터 최종 확인:** `npm run build` 직접 재실행 성공, 라우트 트리 정상(정적 6 + SSG 11 + 동적 API 8 + robots/sitemap), diff 2파일 65/1 확인 후 커밋·푸시.

- **다음 라운드 이어받을 상태:** 프리워밍 42/114. 아래 승인 대기가 **8건 → 12건**으로 늘었다(신규 4건은 이번 감사 적출분 중 결정 필요한 것). 감사 방식이 유효했으므로, 다른 미감사 표면(홈 대시보드 상호작용, `/support` 문의 폼, AI 추천 결과 문구)에도 같은 읽기전용 전수 감사를 돌릴 만하다 — 이건 승인 없이 가능하다.

---

## 라운드 43 — 프리워밍 전진 + 프로덕션 실측 헬스체크 + 빌드 재검증 (신규 개발 무, 승인 대기 지속)

- **프리워밍:** 라운드 시작 직후 1회 호출 → **nextIndex 32→36 전진**(경기도 의정부시·안양시 만안구·안양시 동안구·부천시 처리, `monthsCollected` 각 13, `incomplete:false`, HTTP 200, TIME 31s, **504 없음**). 새 주기 index 36/114, 정상 진행. 서울 25개구 구간을 지나 경기도 진입.

- **프로덕션 실측 헬스체크(이번 라운드 신규):** 라운드 40~42가 "프리워밍 200 = 배포 정상"이라는 간접 증거만 남겨서, 이번엔 실제 라우트를 직접 때려 확인했다.
  - 정적/SSG 페이지 6종 전부 **HTTP 200**: `/`(0.12s), `/about`(0.52s), `/blog`(0.73s), `/privacy`(0.59s), `/support`(0.57s), `/terms`(0.56s).
  - `/api/data?lawd_cd=11680&months=1`(강남구) **HTTP 200 + 실데이터 반환** — 202607 매매 avg 308,265만원/64건, 전세 avg 96,676만원/318건, `aptStatsMaeMae` 단지별 집계 정상. 캐시·집계 경로가 프로덕션에서 살아있음을 확인.
  - ⚠️ **오탐 기록(다음 라운드 주의):** 최초 점검에서 `/region/11680`·`/api/health`를 찍어 404가 났으나, 둘 다 **이 프로젝트에 존재하지 않는 경로를 내가 임의로 지어낸 것**이었다(실제 라우트는 `app/` 기준 `/`,`/about`,`/blog`,`/blog/[slug]`,`/privacy`,`/support`,`/terms` + `/api/{admin/reset-cache,apt-info,apt-location,apt-nearby,contact,cron/prewarm,data,recommend}`). 회귀 아님. `/api/data`도 `sido`/`gu`가 아니라 **`lawd_cd`(5자리)** 가 정규 파라미터라 첫 400은 정상적인 입력검증 동작이었다. 헬스체크 시 라우트 목록을 먼저 확인할 것.

- **빌드 재검증:** 라운드 39 이후 소스 드리프트가 없어 40~42는 빌드를 생략했는데, 무변경 상태가 길어져 이번 라운드에 1회 `npm run build` 실행 → **성공**. 라우트 트리 정상(정적 6 + SSG `/blog/[slug]` 11편 + 동적 API 8 + robots/sitemap).

- **상태 확인:** 시작 시 `git status` 클린(추적 대상 변경 없음, `.claude/settings.json`은 머신-로컬이라 커밋 제외), `git fetch` 후 `origin/main`과 동기.

- **생산성 판단:** 아래 8건 사용자 판단 대기가 **4개 라운드째(40·41·42·43)** 모든 신규 개발(새 화면/기능·자연키·필터·라벨·`/ranking`)을 막고 있다. 승인 없이 가능했던 결정 #2 읽기전용 실측은 라운드 39에서 이미 완료(브리프 §7). 이번 라운드도 승인 없이 착수할 실질 개발 작업이 없어 6개 팀 배정은 생략했고, 대신 검증 자산(실측 헬스체크·빌드)을 갱신했다.

- **다음 라운드 이어받을 상태:** 동일 — 사용자가 8건 중 하나라도 회신하면 즉시 dev→qa 착수 가능. 회신 없으면 프리워밍(현재 36/114) 유지 + 상태 보고 반복. 헬스체크는 매 라운드 반복할 필요 없이 소스 변경 또는 배포 발생 시 재실행 권장.

---

## 🔴 사용자 판단 필요 (전부 이월 — 라운드 39 #2 실측 이후 변동 없음, 4라운드째 정체)

1. **전세 건수 모집단 통일 (A/B/C)** — 기획 권고 (C) 병기.
2. ~~**자연키 확장 (A1/A2/B)** — 07-23 읽기전용 실측 완료(브리프 §7). (A1) 증가분 ≈+1.3%로 0 아님이나 상당수 중복 의심 → (A2) 우위·(B) 정당성 동시 시사. **사용자 택일 필요.**~~ **✅ 확정(2026-07-25, 사용자 승인: 권고안대로) — (B) 현행 유지.** 자연키 13필드 불변, 코드·DB·표시값 변경 없음. 블로그 수정도 불요(§2-4). 상세는 브리프 §7-6. 라운드46 항목 참고.
3. ~~**해제(취소)거래 필터 (a/b/c)** — 기획 권고 (b) 컬럼 보존만. 표시수치 무변경이라 단독 선승인 가능.~~ **✅ 확정(2026-07-25, 사용자 승인: 권고안대로) — (b) 컬럼 보존만, 집계는 현행 유지.** 표시 수치 변화 없음. ※ `cdealType`·`cdealDay` 컬럼 추가 구현 자체는 이번 확정에 미포함 — 착수는 별도 라운드 판단. 상세는 브리프 §3-5.
4. **DealsTable 평균가·건수 기간 불일치 (가/나/다/라)** — 기획 권고 (가) 라벨 명시. #1과 한 커밋 권장. (이번 라운드 범위 밖 — 별도 세션 진행 중으로 확인됨, dev 서버 실측 시 헤더가 이미 "최근 거래 3개월"/"13개월" 표기로 갱신되어 있었음.)
5. ~~**직전월도 신고기한 미도래** — 현 승인범위는 당월만.~~ **✅ 확정(2026-07-25, 사용자 승인: 권고안대로) — 당월만 표시하는 현행 유지.** 직전월 확대 안 함, 코드 변경 없음.
6. **`/ranking` 신규 최상위 페이지** — 승인 대기. (이번 라운드 범위 밖.)
7. ~~**스케줄 세션 중복 근본 대응** — 시작 마커 선커밋 방지책 유지, 이번 라운드 중복 없음.~~ **✅ 확정(2026-07-25, 사용자 승인: 권고안대로) — 현행(시작 마커 선커밋) 유지.** 추가 대응(주기 확대 등) 없음, 코드 변경 없음.
8. ~~**"준비중"/"준비 중" 표기 통일 여부** — 방향 지정 시에만 착수. **(라운드 44 감사로 실태 확정: `app/page.tsx:113`·`app/support/page.tsx:43`·`components/BuildingTypeToggle.tsx:32,49`는 "준비중", `lib/blog.ts:550`·`app/support/page.tsx:42`·`components/CrossSiteNav.tsx:86,148`은 "준비 중". `support` 페이지는 두 줄 안에서 혼용, 홈과 블로그가 같은 "빌라·단독"을 다르게 표기. 표준 표기는 띄어쓰기 "준비 중".)**~~ **✅ 확정 + 구현 완료(2026-07-25, 사용자 승인: 권고안대로) — "준비 중"(띄어쓰기)으로 통일.** `app/page.tsx`·`app/support/page.tsx`·`components/BuildingTypeToggle.tsx` 3파일 수정(`739bc86`), `lib/blog.ts`·`components/CrossSiteNav.tsx`는 이미 표준 표기라 무변경. `grep -rn "준비중" app/ components/ lib/` 0건 확인. 상세는 라운드46 항목 참고.

### 🆕 라운드 44 QA 감사에서 추가된 판단 필요 4건

9. ~~**세종 시황 본문 수치 자기모순 (HIGH — 발행된 글의 사실오류)** — `lib/blog.ts:963,967`. 전세 누적 8,430건 / 갱신 2,885건(34.2%, 이 비율은 검산 일치) / "신규만 5,446건"인데 **8,430−2,885=5,545**로 99건 어긋난다.~~ **✅ 라운드45에서 해소(사용자 판단 불필요 — 명백한 버그로 재분류).** 원자료 재조회로 3수치 전부 재확정. 상세는 라운드45 항목 참고.
10. ~~**블로그 12개 URL에 푸터 부재 → `/privacy`·`/terms` 도달 불가 (HIGH)** — 스펙 `docs/design/blog.md:32,125`가 목록·상세 최하단 `[Footer]`를 명시했으나 `<footer>`는 `app/page.tsx:194-217` 홈에만 존재.~~ **✅ 라운드45에서 해소(사용자 판단 불필요 — 명백한 버그로 재분류).** `components/SiteFooter.tsx` 신설 + `app/layout.tsx` 전역 마운트. 상세는 라운드45 항목 참고.
11. ~~**모바일 375px 블로그 카드 잘림** — `components/blog/BlogCard.tsx:19,22`의 `line-clamp-2` 기준으로 제목 7/11편·요약 **11/11편** 잘림(요약 62~116자 vs 2줄 ≈44자). 스펙 `blog.md:75`가 "다열 그리드를 안 쓰므로 원천적으로 안 생긴다"고 단언했으나 **데스크톱만 고려한 논증**이었다(768px에서도 3편 잘림). `blog.md:269`의 "요약 2줄 이내" 계약 위반. → (가) 기획팀이 요약 44자 이내로 축약 = 가시 텍스트 변경 / (나) 디자인팀이 모바일 `line-clamp-3` 재규정 = 레이아웃 변경. 둘 다 승인 필요. ※ 코드 기준 산술 추정이라 실기기 육안 확인 권장.~~ **✅ 확정 + 구현 완료(2026-07-25, 사용자 승인: 권고안대로) — (나) 모바일 `line-clamp` 완화.** `BlogCard.tsx` 제목 `line-clamp-3 md:line-clamp-2`, 요약 `line-clamp-6 md:line-clamp-2`로 변경(기획팀 요약 재작성 없이 해소). 375px 실측 카드 폭(311px) 강제 레이아웃 + `scrollHeight`/`clientHeight` 비교로 11/11편 잘림 해소 확인, 구값 재현 시 제목 5/11·요약 11/11 잘림 재현으로 측정기법 자체도 검증. 상세는 라운드47 항목 참고.
12. ~~**한글 조판 기본기 (MEDIUM)** — `app/globals.css:32` `font-family: Arial, Helvetica, sans-serif`에 한글 폰트가 없어 기기별 폴백 불일치(Malgun/Apple SD Gothic/Noto), `app/layout.tsx:59`의 Geist 변수도 이 규칙에 덮여 무력화. `word-break:keep-all`/`overflow-wrap` 규칙 전무라 한글이 어절이 아닌 **글자 단위로 끊김**(#11과 복합 악화). → 전 페이지 렌더링이 바뀌므로 승인 필요.~~ **✅ 확정 + 구현 완료(2026-07-25, 사용자 승인: 권고안대로) — 전역 한글 폰트스택 + word-break 적용.** `--font-sans`에 Geist 다음 순서로 Apple SD Gothic Neo/Malgun Gothic/맑은 고딕/Noto Sans KR 추가, `body`가 이를 실제로 참조하도록 수정(기존엔 하드코딩된 Arial 규칙에 가려 무력화 상태였음). `body`에 `word-break:keep-all`+`overflow-wrap:break-word`, 표 셀은 `word-break:normal`로 보호(숫자 컬럼 회귀 방지). `getComputedStyle` 실측 + `DealsTable`·`AptDetailModal` 실데이터 표로 회귀 없음 확인. 상세는 라운드47 항목 참고.

### 승인 없이 다음 라운드에 착수 가능(참고)
- 미감사 표면 읽기전용 전수 감사 확대(홈 대시보드 상호작용, `/support` 문의 폼, AI 추천 결과 문구) — 라운드 44에서 이 방식이 12건을 적출해 유효성이 입증됨.
- 비가시 SEO 잔여분: canonical이 아직 블로그 상세 11편에만 있고 `/`·`/about`·`/blog`·`/privacy`·`/support`·`/terms`에는 없음.
- `lib/site.ts`에 `SITE_NAME` 상수 부재로 사이트명 문자열이 6곳에 흩어져 있음(현재 값은 전부 일치, 드리프트 위험만).
- `app/blog/[slug]/page.tsx:67` 주석 오기(이스케이프 결과를 `<`로 적었으나 실제는 `<`, 기능 무영향).
