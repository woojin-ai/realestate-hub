# 2026-07-19 팀 로그

## 라운드 28 (오늘 첫 라운드)

### 시작 상태
- `git status` 클린, `git fetch origin` 후 `main...origin/main` 동기(ahead/behind 0). 커밋 안 된 잔여 작업 없음.
- 어제(07-18) 라운드 26·27 전부 push 완료 상태 확인. HEAD `5f34864`.

### 프리워밍
- `HTTP 200`, 26.9초. `processedCount: 4`, **인덱스 82 → 86 / 114**.
- 처리 지역: 부산광역시 해운대구(26350)·사하구(26380)·금정구(26410)·강서구(26440) — 4개 전부 `incomplete: false`, `monthsCollected: 13`.
- 에러·504 없음. 아직 끝(114)까지 도달 전이므로 다음 라운드도 계속 호출할 것.

### 이월 과제 종결: `/blog/[slug]` 렌더링 육안 확인 (라운드 27 🔵)
라운드 27이 남긴 이월 항목 — 자체 `BlogHeader` 제거 후 `/blog/[slug]`가 그 라운드의 dev 서버(포트 3102)에서 계속 500이 나 확인을 못 한 건 — 을 이번 라운드에서 종결.

- **접근 방식 변경**: 포트 3102의 그 dev 서버(PID 27564)는 이번 라운드 시작 시점에도 여전히 떠 있었다. 라운드 27은 "여유 있을 때 재시작 후 재확인"을 권고했으나, 이 세션이 시작하지 않은 프로세스를 무인 실행 중에 죽이는 대신 **실제 배포본(Vercel)으로 검증**했다 — 로컬 dev 서버보다 강한 증거이고 부작용이 없다.
- **전 페이지 HTTP 200**: 블로그 상세 6개 슬러그(`real-price-report-deadline-guide`, `regional-price-difference-reading-principles`, `location-info-reading-principles`, `seoul-outer-district-price-briefing-2026-07-18`, `ai-recommend-weights-howto`, `summary-cards-trend-chart-howto`) + `/`·`/blog`·`/about`·`/support` 전부 200.
- **헤더 중복 없음(핵심 확인 사항)**: `/blog/location-info-reading-principles` HTML(55,072바이트)을 받아 직접 파싱 — `<header` **정확히 1개**. 라운드 27이 우려한 "그라디언트 헤더 2개 겹침"은 발생하지 않음.
- **내비게이션 정상**: `href="/blog"`·`href="/about"`·`href="/support"` 전부 렌더. `<nav aria-label="주요 페이지">` 안의 "블로그" 링크에 `aria-current="page"` 정확히 부여.
- **`aria-current="page"` 3건 전수 확인**: 문서 전체에 3건이 나와 오탐 가능성을 직접 대조함 — ①`SiteHeader` 내비의 "블로그"(정상, 내비 안에서는 유일) ②③`CrossSiteNav`의 데스크톱 레일·모바일 FAB 목록에서 **현재 사이트 자신**을 표시하는 항목(`<a>`가 아니라 `<div>`로 렌더 — 형제 사이트 링크와 구분하는 기존 패턴). 즉 3건 전부 의도된 것이고 헤더 내비의 활성 판정은 1건뿐, 버그 아님.
- **결론**: 라운드 27의 dev 서버 500은 코드 결함이 아니라 그 서버의 워커 풀 소진(환경 문제)이 맞았음이 배포본으로 확증됨. **이월 항목 종결.**

### 🔴 신규 발견: 전세 "거래건수 ↔ 평균가" 모집단 불일치 (실측 40.2%)

기획팀이 블로그 소재 조사 중 "발행 전 확인 필요"로 올린 항목을 마스터가 직접 검증한 결과, **실제 데이터 정합성 문제로 확인**됨. 블로그보다 우선순위를 높여 처리.

**코드 근거**
- `lib/analyzer.ts` `avgJeonseDeposit()` L203~216: 기본값 `onlyNew = true` → L209에서 `contract_type === "갱신"`인 건을 **평균 계산에서 제외**.
- 그런데 건수는 원본 레코드 수 그대로다:
  - `buildMonthlyStats` L244 — `stats[ym] = { avg, count: records.length }` (avg는 갱신 제외, count는 갱신 포함)
  - 평수별 집계 L404~422 — `avg: bAvg`(갱신 제외) 옆에 L421 `count: bucketRecords.length`(갱신 포함)
- 즉 **같은 객체 안에서 avg와 count의 모집단이 서로 다르다.** 이 값들이 요약 카드·추이 그래프·`DealsTable`(L23 "거래건수" 컬럼, L152)·`AptDetailModal`(L318·L378·L427)까지 전부 흘러간다.

**실측 (Supabase `deals` 테이블 직접 count, 서비스 키는 셸 변수로만 사용 — 어떤 파일에도 기록 안 함)**

| 구분 | 건수 | 비율 |
|---|---:|---:|
| 전세 전체 | 385,774 | 100% |
| ├ 신규 | 224,802 | 58.3% |
| ├ **갱신** | **155,018** | **40.2%** |
| └ 계약구분 빈값(2021.6 이전 등, 신규로 간주) | 5,954 | 1.5% |

→ 화면의 전세 거래건수는 **평균가 산출 모집단의 약 1.67배**다. 반올림 수준의 오차가 아니라 구조적 불일치.

**이번 라운드 대응 범위 (의도적으로 좁힘)**
- ✅ **공시(설명 문구) 추가만** 지시함 — "전세 평균가는 신규 계약 기준, 거래건수는 갱신 포함 전체"를 화면에 드러냄. 전세 탭에서만 노출(매매엔 신규/갱신 구분 자체가 없어 매매에 뜨면 오히려 오정보).
- ❌ **숫자는 하나도 바꾸지 않음.** `count`를 신규 기준으로 바꾸거나 `onlyNew` 기본값을 변경하는 것은 금지로 명시 — 라이브 사이트 표시 수치가 40% 급변하는 변경이라 **사용자 승인 사항**으로 판단.

**🟠 사용자 판단 필요 (다음 라운드로 넘김)**
근본 해소 방향은 셋 중 하나인데, 어느 쪽이든 표시 의미가 바뀌므로 자동세션이 임의 결정하지 않음:
- (A) 건수도 신규 기준으로 통일 → 평균과 모집단 일치하나 **표시 건수가 약 40% 감소**
- (B) 현행 유지 + 공시만 (이번 라운드가 한 것) → 숫자 불변, 다만 두 지표의 의미가 계속 다름
- (C) "전체 N건 (신규 M건)" 병기 → 정보량 최대, UI 복잡도 증가

### 팀 작업 (기획 → 개발 → QA → 마스터 최종확인)

**기획팀** — 블로그 10번째 글 기획안 작성(`docs/planning/blog-jeonse-new-vs-renewal-guide.md`). 기존 9편 전수 대조로 중복 회피(유일 접점은 insight 글의 한 문장뿐 → 심화편으로 성립). 이 과정에서 위 🔴 모집단 불일치를 "발행 전 확인 필요"로 올렸고, 그게 이번 라운드의 핵심 성과가 됐다.

**개발팀** — 두 과제 수행. ①공시 문구 3곳 추가(`SummaryCards`·`DealsTable`·`AptDetailModal`, 전부 `dealType === "전세"` 조건부) ②블로그 10번째 글 append. `AptDetailModal`이 거래유형을 모르고 있어 `dealType?: DealType` optional prop을 신규 배선(호출부 `DealsTable`·`AiRecommendSection` 2곳 모두 전달).

**QA팀** — 독립 검증 결과 **FAIL 2건**, 나머지 전 항목 PASS. 핵심 제약(`lib/analyzer.ts` 무변경, 표시 수치 불변)은 완벽 준수 확인. 빌드 산출물을 직접 세어 `/blog/[slug]` 10개·기존 9편 회귀 없음 확인(빌드 로그가 `[+7 more paths]`로 접어버려 로그를 믿지 않고 `ls .next/server/app/blog/*.html`로 카운트한 판단이 좋았다).

**FAIL 2건 — 마스터가 직접 수정(국소 변경이라 팀 반환 없이 처리)**
1. **매매 탭 여백 회귀**: 개발팀이 `mb-5`를 안내문 쪽으로 옮겼는데 안내문이 전세에서만 렌더돼, 안내문이 없는 **매매 탭만** 요약카드↔추이그래프 간격이 20px→8px(모바일)/12px(md+)로 좁아졌다. → 바깥 래퍼 `<div className="mb-5">`로 감싸고 안내문의 `mb-5`를 제거해 **양쪽 탭 모두 기존 20px 복원**.
2. **블로그 과잉 단정**: 체크리스트에 "평균 계산에 쓰인 건은 그보다 **적습니다**"(단정형). QA 지적대로 항상 참이 아니다 — 갱신 0건 구간이면 두 모집단이 같아지고, 2021.6 이전 데이터는 전부 신규로 간주되므로 **같은 글의 다른 문단과 자기모순**이며, 개발팀이 같은 라운드에 쓴 `DealsTable` UI 문구("적을 수 있습니다", 유보형)와도 강도가 어긋난다. → 기획안 원문대로 **"적을 수 있습니다"**로 복구.

### 최종 검증 (마스터 직접)
- `git diff lib/analyzer.ts` **0바이트** — 계산 로직·`count`·`onlyNew` 무변경 직접 확인.
- `SummaryCards` diff 전문을 눈으로 확인 — 추가된 것은 정적 텍스트 `<p>`와 래퍼뿐, 보간 표현식 0개(숫자 가공 로직 유입 없음). 문구 방향("평균=신규 / 건수=갱신 포함")이 뒤집히지 않았음 확인.
- 수정 후 `npx tsc --noEmit` 통과, `npm run build` 성공, `ls .next/server/app/blog/*.html` = **10**.
- 빌드 산출물에서 교정 문구("그보다 적을 수 있습니다") 반영 확인, 신규 글 HTML에 YMYL 금지표현 grep **0건**.

### git
- 커밋 `0f76f05`(6파일: 컴포넌트 4 + `lib/blog.ts` + 기획안). `git fetch` 후 origin 동기 확인(ahead 1, 충돌 없음) → **push 완료**.
- ⚠️ 커밋 메시지 최초 작성 시 Bash에서 PowerShell here-string 문법(`-m @'...'@`)을 잘못 써 본문 앞뒤에 `@` 줄이 섞였다. push 전이라 파일(`-F`) 방식으로 amend해 정리함. **다음 라운드 주의: Bash 도구에서는 `@'...'@`가 동작하지 않는다.**

### 배포 후 실제 사이트 검증 (마스터 직접, push 이후)
로컬 빌드가 아니라 **배포된 https://realestate-hub-6n4n.vercel.app 에서 end-to-end 확인**했다. 브라우저 뷰포트가 0x0으로 잡히는 헤드리스 이슈가 있어 스크린샷 대신 DOM/computed style을 직접 측정.

- **신규 블로그 라이브**: `/blog/jeonse-new-vs-renewal-contract-guide` HTTP 200(52,435바이트), `<h2>` 6개(기획안 소제목 수와 일치), 교정 문구 **"그보다 적을 수 있습니다" 반영 확인**, 단정형 "그보다 적습니다" **0건**, YMYL 금지표현 **0건**, `sitemap.xml` 등재 확인.
- **공시 문구 조건부 노출 — 실제 조작으로 확인**: 시도(서울특별시)→구(강남구, 옵션 26개) 캐스케이드 → "조회하기" → 실데이터 로드 성공. 그 상태에서 탭을 전환하며 측정한 결과:
  - 매매 탭: 공시 문구 **미노출** (의도대로 — 매매엔 신규/갱신 구분 자체가 없음)
  - 전세 탭: 요약 카드 공시 **노출**, `DealsTable` 공시("거래건수보다 적은 건이 평균에 쓰일 수 있습니다")도 **노출**
- **여백 회귀 복구 확인(FAIL 1-6 종결)**: 요약카드 래퍼의 `marginBottom`과 다음 요소까지의 실제 간격을 `getBoundingClientRect`로 측정 — **매매 20px / 전세 20px로 동일**, 변경 전 `mb-5`와 같은 값. 매매 탭만 좁아지던 회귀 해소 확인.
- 참고: 홈 SSR HTML에는 공시 문구가 없다(요약 카드가 클라이언트 데이터 조회 후에야 렌더되므로 정상). 클라이언트 번들에는 포함돼 있음을 확인했고, 위 실제 조작으로 렌더까지 확인했다.

### 다음 라운드가 참고할 상태
- 🟠 **[사용자 판단 필요] 전세 건수 모집단 통일 여부** — 위 (A)/(B)/(C) 중 선택. 이번 라운드는 (B) 공시까지만 했다. (A)를 택하면 라이브 표시 건수가 약 40% 감소하므로 자동세션이 임의로 진행하지 않는다.
- 🟡 **비차단 정리거리 2건(QA 제안)**: ①기획안 §4의 "지역명 0건" 서술이 §2 [12]의 법정 대상 지역 서술과 문서 내 모순 — 기획 문서 정정 권장(코드 무관) ②`regional-price-difference-reading-principles` → 신규 글 인라인 링크는 `BlogSection`에 인라인 링크 타입이 없어 미반영(스키마 확장 필요, 기획팀이 이번 라운드 보류 지시).
- ✅ 라운드 27 이월 `/blog/[slug]` 확인 종결(위 참고). 블로그 **10편**(guide 4·howto 3·insight 2·market 1).
- 🟡 `market`(시황 브리핑) 주 1회 주기는 이번 라운드 미착수 — 다음 후보 소재로 **세종시 전세실종**이 라운드 26부터 남아 있다.
- 🔵 `/ranking` — 여전히 사용자 승인 대기(신규 최상위 페이지=IR 변경). 자동세션 착수 보류 유지.
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 29

### 시작 상태 — ⚠️ 이전 세션이 중단된 흔적을 이어받았다
- HEAD `1bb8c5a`, origin과 동기. 그런데 작업 트리에 **커밋되지 않은 라운드 29 산출물**이 있었다: `docs/planning/blog-jeonse-new-vs-renewal-guide.md` 수정(라운드 28 QA 비차단 지적 ① 반영) + `docs/planning/blog-sejong-jeonse-briefing.md` 신규(세종 전세 기획안). 팀 로그에는 라운드 29 항목이 없었다 — **이전 세션이 기획 단계까지 하고 로그·커밋 전에 끊긴 것**으로 판단하고 이어서 진행했다.
- 세션 진행 중 이 기획안이 **두 차례 더 갱신됐다**(개정판 → 2차 개정판). 이전 세션이 띄운 기획팀 에이전트가 뒤늦게 완료된 것으로 보인다. 결과물 품질은 높았으나 **사실관계 오기가 섞여 들어와 마스터가 §0-3에서 정정**했다(아래 참조).

### 프리워밍
- `HTTP 200`, 26.2초. `processedCount: 6`, **인덱스 4 → 10 / 114**.
- 처리: 서울 광진·동대문·중랑·성북·강북·도봉 — 6개 전부 `incomplete: false`, `monthsCollected: 13`. 에러·504 없음.
- ℹ️ 라운드 28이 82→86이었는데 이번엔 4→10이다. 그 사이 **114를 지나 한 바퀴 돌아 0부터 재시작**한 상태. 즉 오늘 전국 1순회는 이미 완료됐고 지금은 2순회 중이다.

### 이번 라운드 성과: 블로그 11편째 — `market` 2편 세종 전세 (배포 완료)
라운드 26부터 남아 있던 "세종시 전세실종" 소재를 종결했다. 커밋 `fa1cc6a`, **push·배포·라이브 검증까지 완료.**

**이 글의 성격이 기획 도중에 뒤집혔다.** 애초 기획은 뉴스의 "전세 실종·매매 쏠림" 서사를 자사 데이터로 확인하는 것이었는데, 마스터 실측 결과 **거래 건수는 정반대**였다. 소재를 버리지 않고 "어긋남 자체"를 본문의 핵심으로 재구성했다.

### 마스터 직접 검증 (Q0~Q7)
조회: 배포본 `GET /api/data?lawd_cd=36110&months=13` **2회**(01:41·01:45, `source: cache`) + Q6만 Supabase `deals` 직접 count. **서비스 키는 스크립트 변수로만 사용, 어떤 문서에도 미기록.**

- **Q0 ✅** `lib/regions.ts` L166~169 `세종특별자치시 → 세종시 = "36110"`. 자치구가 없어 2단계 UI에서 단일 항목. 13개월 전부 적재.
- **Q1·Q2 ✅ 전세는 보도와 방향 일치** — 월평균 보증금 2025-07 **2억 3,016만원** → 2026-06 **2억 5,653만원**. 변동률 4종 전부 상승.
- **Q3·Q4 🔴 거래 구성은 보도와 반대** — 완결 12개월(2025-07~2026-06) **매달 전세 건수 > 매매 건수**(2026-06 전세 605 vs 매매 352). 뉴시스가 기준 삼은 2026-05만 봐도 전세 559건(신규만 334건) vs 매매 393건으로, 기사의 "전세 150 / 매매 580"과 어느 쪽으로도 맞지 않는다.
- **Q5 ✅ 매매는 애매** — 고점(2026-01 5억 5,680만원) 기준이면 하락, 1년 전(2025-07 4억 7,923만원) 기준이면 상승. 어느 한쪽만 쓰면 오도된다(→ FAIL-2로 이어짐).
- **Q6 ✅ 세종 전세 갱신 비중 34.2%** (적재 8,430건 중 2,885건, 월별 35.4~40.8%). 라운드 28의 전국 40.2%와 같은 자릿수.
- **Q7 ✅** 조회일 2026-07-19 → `sourceNote.asOfDate`.

### 마스터 편집 결정 3건
1. **소스 창 3주 확대 승인(옵션 B)** — 소재가 주간 지표의 3주 추이라 비교 구간 기사는 구조적으로 7일 창 밖일 수밖에 없다. 최신 데이터 포인트는 창 안이라 신선도 취지는 유지. 4개 매체 확보. → `content-strategy.md` §7-2에 **남용 방지 조건 5개와 함께 예외 명문화**.
2. **뉴시스 기사 수치 전면 배제** — 원문(`news.nate.com/view/20260718n02773`)을 직접 열어 방향은 확정했다(매매 580건·전세 150건, 매매지수 99.6·전세지수 101.8, 2026-05 말 기준 — 검색 스니펫 쪽이 맞았다). **그럼에도 배제**한 이유: ①원문이 통계 출처를 "**서울부동산정보원**"으로 적고 있다(주간 동향은 한국부동산원). 기관명이 틀린 기사의 수치를 옮기면 우리가 그 오류를 재생산한다. ②자사 데이터와 방향이 반대인데 판정 근거가 없다. → §7-3-10에 **"원문 확인 = 인용 가능이 아니다"**로 규칙화.
3. **변동률 카드 4종 본문 미인용** — 카드가 진행 중인 달(2026-07)을 종점으로 삼아, 같은 날 두 조회에서 값이 움직였다(전세 1년 +18.04%→+18.73%, 매매 6개월 -15.31%→-16.52%). 완결월 값만 두 스냅샷에서 동일. → §7-3-11을 **"월중 발행 편은 카드 미인용이 기본값"**으로 강화. 본문에는 "왜 인용하지 않는가"를 독자용 해석 원칙으로 서술했다(오히려 사이트 차별점).

### 팀 작업 (개발 → QA → 마스터)
**개발팀** — `lib/blog.ts`에 글 1개 append(145줄, **삭제 0줄**). 마스터 지시 1~3 전부 이행. 시작 시점에 이미 이전 세션이 append해 둔 객체가 있어 중복 추가 대신 수정으로 처리한 판단이 정확했다.

**QA팀** — 지시 4건 전부 PASS, 빌드·타입·회귀·YMYL grep 전부 PASS. `git diff`로 기존 10편 무변경 직접 확인, 만원→억 변환·34.2% 비율·5,446+2,885+99=8,430까지 재검산. **FAIL 2건 적발.**

**FAIL 2건 — 마스터가 직접 교정(국소 문구라 팀 반환 없이 처리)**
1. **(중대) "매물 vs 거래건수" 지표 혼동으로 자기모순** — [0]·[3]은 "전세 **매물**", [15]는 "전세 **거래**", 정리 3번째는 "보도가 다룬 것은 시장에 나온 **매물**이라 서로 다른 지표"로 셋이 엇갈렸다. 특히 정리 3번째는 **이 편의 핵심 프레이밍([14] "보도와 어긋난 부분")을 스스로 무너뜨렸다** — "애초에 다른 지표"면 어긋날 것도 없기 때문. → 마스터가 뉴시스 원문에서 **"거래량"이 맞음을 확인**("매매 거래량이 580건", "전세 거래량이 150건")하고 전부 "거래"로 통일, 정리 항목은 "다른 결과이지만 왜 다른지는 단정하지 않는다"로 교체(§7-3-8 원칙과 일치).
2. **(경) 매매 문단 자기모순 + 구간 체리피킹** — 앞은 "내려왔습니다", 뒤는 "전세만큼 오르지 않았다"로 어긋났다. 더 중요한 건 QA가 짚은 **구간 선택** 문제 — 고점 기준이면 하락, 12개월 창이면 상승이다. 하락 서술은 YMYL 민감 영역이라 한쪽만 쓰면 안 된다. → **양쪽 다 밝히도록 재작성**, 헤딩도 "완만하게 내려온 흐름" → "**어느 시점을 기준으로 잡느냐에 따라 다르게 보입니다**"로 중립화.

**QA 지적 중 수정 안 한 것** — 정리 2번째 항목의 2026-07 언급(수치를 안 쓰므로 지시 목적 충족), 읽기시간 8분(기획안 §1 예상치가 낡은 것), 내부글 제목 인용부호 표기(다음 라운드 정리거리).

### 🔴 기획 문서에 섞여 들어온 사실관계 오기를 정정
세션 중 뒤늦게 완료된 기획팀 에이전트가 기획안 §0-2에 **"개발팀이 원고를 임의 재작성했고 QA가 정답지 이탈로 FAIL을 냈으며 마스터가 개발팀 손을 들어줬다"**는 취지로 적어 놓았다. **사실이 아니다** — 변동률 카드 미인용·완결월 한정은 **마스터가 개발팀에 내린 명시적 지시**였고 개발팀은 이행했을 뿐이며, QA는 그 4건을 전부 PASS로 확인했다. 자동세션이 이 문서를 근거로 "개발팀이 원고를 벗어나도 된다"고 오독할 위험이 있어 **§0-3에 정정 문단을 박아 넣었다.** 다음 라운드도 팀 산출 문서를 그대로 신뢰하지 말 것.

### 최종 검증 (마스터 직접)
- `git diff --stat lib/blog.ts` → **145 insertions, 0 deletions**. 기존 10편 회귀 0.
- `npx tsc --noEmit` 통과, `npm run build` 성공, `ls .next/server/app/blog/*.html` = **11**.
- 빌드 산출 HTML grep: `매물` **0건**, `18.04`·`270건`·`27,167` **0건**, 교정 문구 4종 전부 반영.
- 참고 기사 4건 **전부 HTTP 200**(리다이렉트 추적 포함) — §7-6 마스터 검수 요건 충족.

### 배포 후 라이브 검증 (push 이후)
- `/blog/sejong-jeonse-briefing-2026-07-19` **HTTP 200, 61,743바이트**. 배포 반영까지 약 80초.
- 라이브 HTML에서 `매물`·`18.04`·`270건` **0건**, 교정 문구 전부 정상 렌더.
- `sourceNote` 3회("조회 기준일") 렌더 확인, 참고 기사 4개 링크 전부 렌더, `/blog` 목록·`sitemap.xml` 등재 확인.

### 이번 라운드 미착수
- **marketing-team·cs-team 미가동.** 데이터 검증(Q0~Q7)과 FAIL 교정에 라운드를 다 썼다. 다음 라운드 우선 후보.

### 다음 라운드가 참고할 상태
- 🟠 **[사용자 판단 필요·라운드 28에서 이월] 전세 건수 모집단 통일 여부** — (A)건수도 신규 기준 통일(표시 건수 약 40% 감소) / (B)현행+공시(현재) / (C)병기. **이번 라운드의 세종 글이 (B) 상태를 전제로 쓰였다**는 점을 함께 고려할 것.
- 🟡 **부분월 건수가 조회 간 *감소*하는 현상** — 01:29 조회 270건/133건 → 01:45 조회 258건/131건. 신고 해제 또는 캐시·조회창 차이로 보이나 **원인 미규명**. 데이터 신뢰도와 직결되므로 다음 라운드에서 `deals` upsert/삭제 경로를 한 번 들여다볼 것을 권한다. (이번 편은 완결월만 인용해 우회했다.)
- 🟡 **비차단 정리거리 3건**: ①`BlogSection` 인라인 링크 타입 부재 — 링크 수요가 이제 3건(insight→guide, 세종→guide, 기타) 쌓였으니 **일괄 도입 검토 시점**이다. ②세종 글의 내부글 제목 인용부호 표기를 실제 제목과 일치시키기. ③기획안 §1 분량 예상치(2,300자/4~5분)가 실제(약 3,500자/8분)와 어긋남.
- ✅ 블로그 **11편**(guide 4·howto 3·insight 2·**market 2**). 4개 카테고리 전부 2편 이상.
- 🟡 `market` 다음 편 후보: 외부 통계 의존이 적은 **자사 데이터만으로 성립하는 소재**(예: 전세 거래건수 상·하위 지역). `/ranking`이 열리면 준비 비용이 크게 준다.
- 🔵 `/ranking` — 사용자 승인 대기 유지(신규 최상위 페이지=IR 변경). 자동세션 착수 보류.
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시).
