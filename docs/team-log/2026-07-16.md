# 2026-07-16 팀 작업 로그 (마스터 오케스트레이터)

> ## ⚠️ 사용자 액션 필요 (07-16 2라운드에서 #1·#2 실측 해소 확인 → 잔여만 유지)
> 1. ~~**Vercel 환경변수 등록 + 재배포**~~ — **✅ 프로덕션 실측 해소 확인(07-16 2R).** 프리워밍된 3개 지역(강남 11680·분당 41135·안양만안 41171) 전부 `/api/data`가 `"source":"cache"` 응답, `/api/recommend`(강남)이 지오코딩·평지점수 포함 정상 items 반환(타임아웃 X). Supabase 읽기/쓰기·MOLIT·KAKAO 키가 실제 적용·동작 중임을 확인(프리워밍 cron 자체가 Supabase 저장에 성공해 왔으므로 사실상 이전부터 적용돼 있었고, 배너의 "미적용" 서술이 구식이었음).
> 2. ~~**프로덕션 Supabase 스키마 적용**~~ — **✅ 사실상 확인.** cache 읽기·프리워밍 저장·apt_geo 지오코딩 조회가 모두 동작 → 스키마 적용 완료 상태.
> 3. **텔레그램 봇 토큰** — `TELEGRAM_BOT_TOKEN` 빈값 → Stage 4(자동수집+알림) 계속 보류. (미검증·유지)
> 4. **Vercel `CRON_SECRET` 등록** — 새벽 1시 크론용. **미검증(1시 실행 로그 확인 필요)** — 30분 프리워밍은 별도 secret로 정상 동작 중이라 데이터 수집 자체는 지장 없음.
> 5. `elev_per_hh` 표기(은마 0.009대/세대) 정식 노출 전 재검토 권고. (유지)

---

## 라운드: 야간 프리워밍 진행 + 헬스게이트 모니터링 (오늘 첫 라운드)

### 프리워밍 진행 (이번 라운드 핵심 productive 작업)
- 라운드 시작 인덱스 **29 → 종료 32** 전진. **성남시 수정구(41117 잔여)·수정구(41131)·중원구(41133)·분당구(41135) 완료**(각 monthsCollected=13), 현재 **의정부시(41150) 부분 진행(6개월)**.
- 총 **6회 호출, 전부 HTTP 200**(응답 27~35s, 60s 하드리밋 대비 안전마진 확보). **504 재발 0** — 07-15에 고친 시간예산/이어받기 로직 정상 동작 재확인(콜드 지역 분당구가 6→11→13으로 여러 호출에 걸쳐 완료된 뒤 인덱스 전진).
- 전국 114개 중 32개 통과. 남은 콜드 지역은 향후 라운드/새벽 크론이 이어서 진행.

### 헬스게이트 GREEN
- `npx tsc --noEmit` exit 0.
- 프로덕션 홈 `HTTP 200`(0.64s) 정상 서빙.
- 프로덕션 env 미적용 상태 불변(배너 ①·② 대기) — `/api/data` 캐시 미동작은 코드결함 아님.

### git
- `git fetch` 후 origin과 **동기**, 워킹트리 clean(직전 커밋 `2b1ff60` AI추천 4번째 슬라이더까지 push 완료). 커밋 안 된 완료작업 없음.

### 판단 — dev 신규작업 없음
최우선 코드 트랙 ①(상세보기 3섹션)·②(AI추천 4가중치)는 MVP 완료·배포. 다음 진척은 전부 사용자 조치(배너 ①~⑤) 대기 → dev/qa 파이프라인 idle. 빌라/단독·알림 계속 보류(2026-07-14 지시 재확인). 마케팅·CS·셋업가이드 초안은 이전 라운드에 완료 → 중복 문서 양산 없이 **프리워밍 전진 + 정직한 모니터링**으로 마감.

### 다음 라운드가 이어받을 상태
- **프리워밍 인덱스 32(의정부시부터 이어받기)** — 다음 라운드도 시작 직후 프리워밍 호출로 전진시킬 것.
- 프로덕션 `/api/data` source 재확인해 env 적용 여부 모니터링 — 정상 응답/`source=cache` 감지 시 캐시·추천 프로덕션 실효 재검증 라운드 즉시 진행.
- 빌라/단독·알림 계속 보류(2026-07-14 지시).

---

## 라운드 2: 프리워밍 추가 전진 + ⭐프로덕션 env 실효 재검증(핵심 발견)

### 프리워밍 진행
- 라운드 시작 인덱스 **32 → 종료 34** 전진. **의정부시(41150) 완료**(monthsCollected=13), **안양시 만안구(41171) 완료**(13), 현재 **안양시 동안구(41173) 부분 진행(11개월)**.
- 총 **4회 호출, 전부 HTTP 200**(26~34s, 60s 하드리밋 안전마진). **504 재발 0**. 전국 114개 중 34개 통과.

### ⭐ 핵심 발견 — 프로덕션 env가 실제로는 적용·동작 중 (배너 #1·#2 구식이었음)
- 프리워밍 호출 후 프로덕션 `/api/data`를 소스 필드 기준으로 실측: **강남 11680·분당 41135·안양만안 41171 모두 `"source":"cache"`** 반환 → Supabase 읽기 경로 정상.
- `/api/recommend?lawd_cd=11680`(강남, 45/35/20/10 가중치) → **items 정상 반환**(개포자이프레지던스 등, 지하철거리·slope_score·area_stats 포함). 이전 라운드들이 기록한 "추천 타임아웃"이 재현되지 않음.
- 논리적 확증: 프리워밍 cron 자체가 Supabase에 수집데이터를 저장해 왔고(그래야 `/api/data`가 cache로 응답), recommend의 apt_geo 지오코딩 조회도 성공 → **Supabase(URL+SERVICE_ROLE)·MOLIT·KAKAO 키가 프로덕션에 실제 적용된 상태**. 배너 #1·#2의 "미적용/캐시 미동작" 서술은 구식이므로 이번 라운드에 정정.
- ⚠️ 과신 방지: `CRON_SECRET`(새벽 1시 크론용, #4)과 `TELEGRAM_BOT_TOKEN`(#3)은 이번에 검증 못 함 — 그대로 열린 항목 유지.

### 헬스게이트 GREEN
- `npx tsc --noEmit` exit 0. 프로덕션 홈 `HTTP 200`(0.82s).

### git
- 라운드 시작 시 origin과 동기·워킹트리 clean. 이 로그 갱신분만 커밋·push.

### 판단 — dev 신규작업 없음, 모니터링·정직한 상태정정으로 마감
- 최우선 코드트랙 ①②는 이미 MVP완료·배포. env가 실효 확인됐으므로 다음 리스크는 데이터 커버리지(프리워밍 잔여 80개 지역)·`CRON_SECRET` 검증. dev/qa 파이프라인은 신규 화면 없음 → idle 유지. 빌라/단독·알림 계속 보류(2026-07-14).

### 다음 라운드가 이어받을 상태 (갱신)
- **프리워밍 인덱스 34(안양시 동안구부터 이어받기)** — 시작 직후 프리워밍 호출 지속.
- **env 실효 확인됨** → 이제 모니터링 초점은 (a) 프리워밍 커버리지 전진, (b) 여유 시 `CRON_SECRET`/새벽1시 크론 401 여부 확인(preview_logs 아닌 실제 크론 실행 결과), (c) `TELEGRAM_BOT_TOKEN` 등록 시 Stage4 재개.
- 빌라/단독·알림 계속 보류.

---

## 라운드 3: 프리워밍 추가 전진 + 신규 캐시 지역 실측 검증

### 프리워밍 진행
- 라운드 시작 인덱스 **34 → 종료 37** 전진. **안양시 동안구(41173) 완료**(monthsCollected=13), **부천시(41190) 완료**(13), **광명시(41210) 완료**(콜드 지역 → 3→8→13으로 3회 호출에 걸쳐 이어받기 완료). 현재 인덱스 37(다음 지역부터 이어받기).
- 총 **4회 호출, 전부 HTTP 200**(26~34s, 60s 하드리밋 안전마진). **504 재발 0** — 이어받기 로직 정상(광명이 여러 호출에 걸쳐 완성된 뒤 인덱스 전진). 전국 114개 중 **37개 통과**.

### 신규 프리워밍 지역 캐시 실측
- 갓 프리워밍한 **광명(41210)** `/api/data?deal_ymd=202606` → **실데이터 정상 반환**(months=13, 매매 current_avg=88,490, 월별 avg/count 실값) → Supabase 캐시 읽기·집계 경로 정상 재확인.
- ⚠️ 관찰(비차단): **부천(41190)** `/api/data`(deal_ymd 무param) 호출 시 months=13 구조는 오나 월별 count=0·avg=null. 광명은 무param 호출 시 빈응답이었다가 `deal_ymd` 지정 시 실값 → 무param 기본동작 편차로 추정. monthsCollected=13(행 존재)이므로 데이터 유실 아님. 다음 라운드에 `deal_ymd` 지정 재확인 권고(현 시점 결함 단정 안 함).

### 헬스게이트 GREEN
- `npx tsc --noEmit` **exit 0**. 프로덕션 홈 **HTTP 200**(0.86s).

### git
- 라운드 시작 시 origin과 동기·워킹트리 clean. 이 로그 갱신분만 커밋·push.

### 판단 — dev 신규작업 없음, 프리워밍 전진·정직한 모니터링으로 마감
- 코드트랙 ①②는 MVP완료·배포, env 실효 확인 상태 불변. 신규 화면 없음 → dev/qa 파이프라인 idle 유지. 빌라/단독·알림 계속 보류(2026-07-14 지시 재확인).

### 다음 라운드가 이어받을 상태 (갱신)
- **프리워밍 인덱스 37부터 이어받기** — 시작 직후 프리워밍 호출 지속(전국 114 중 37 완료, 잔여 77).
- 부천(41190) `deal_ymd` 지정 재확인으로 무param null이 표시편차인지 확정.
- `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지. 빌라/단독·알림 보류.
