# 2026-07-20 팀 로그

## 라운드 30 — ✅ **종료** (아래 "라운드 30 종료" 절 참조)

> ⚠️ **동시 세션 방지 마커.** 이 절이 있고 아래 "라운드 30 종료" 표시가 없으면, 다른 세션이 이 라운드를 진행 중일 수 있다.
> 2026-07-19 라운드 29에서 30분 주기가 세션 실행시간보다 짧아 **두 세션이 같은 라운드를 동시 진행**한 사고가 있었고, 대응책 후보 ③(팀로그 라운드 시작 표시를 먼저 커밋)을 이번 라운드부터 시행한다.
> 뒤늦게 시작한 세션은 이 마커를 보면 **새 계획을 세우지 말고**, `git log`로 이 라운드가 어디까지 갔는지 확인한 뒤 이어받거나, 겹치지 않는 별개 작업만 골라라.

- 시작 시각: 2026-07-20 (스케줄 자동 세션)
- 시작 시 상태: 작업트리 clean, `main` == `origin/main` (`9f37f22`), 미커밋 산출물 없음
- 프리워밍(라운드 시작 직후 1회): 인덱스 **10 → 14** 전진, 4개 지역 처리(노원·은평·서대문·마포), 전부 `incomplete: false`, HTTP 200 / 29.8초. **504 없음.** 전국 1순회는 07-19에 완주했고 현재는 2순회(일일 갱신) 유지 모드.

### 이번 라운드 계획
1. 🟡 **부분월 거래건수 *감소* 현상 원인 규명** (07-19 라운드 28·29 양쪽 세션이 재현·권고한 최우선 항목) — `deals` upsert/삭제 경로 점검.
2. 🟡 marketing-team·cs-team 가동 (라운드 29 미가동분).

---

## 라운드 30 종료

동시 세션 마커 해제. 이 라운드는 완료됐고, 다음 세션은 새 라운드로 시작하면 된다.

### 🎯 최우선 항목이던 "부분월 건수 감소" — 원인 규명 + 수정 완료 (`f3fdeb8`)

07-19 라운드 28·29 양쪽 세션이 재현하고 원인 규명을 권고했던 건이다. **추정이 아니라 코드 근거 + 실측으로 확정했다.**

**원인 — 같은 달을 두 경로로 서빙하는데 세는 기준이 달랐다.**
- 캐시 경로(`loadMonthFromDb`) = `deals` 테이블 = 자연키로 **이미 접힌** 행
- 라이브 경로(`collectMonth`) = 국토부 API **원본 그대로**(접힘 없음)

`app/api/data/route.ts`의 최신월(index 0)만 캐시 신선도에 따라 두 경로를 오간다. 그래서 사용자에겐 진행 중인 달 건수가 조회할 때마다 오르내리는 것처럼 보였고, **완결월이 항상 안정적이었던 것도 이걸로 설명된다**(완결월은 항상 캐시 경로).

07-19에 관측된 수치가 정확히 이 구조로 재현됐다:
```
국토부 원본 raw   전세 270 / 매매 133   ← 01:29 조회 = 라이브 경로
자연키 distinct   전세 258 / 매매 130   ← 01:45 조회 = 캐시 경로
```

**부수 발견 — 매일 09:00 KST에 재발하는 구조였다.** `route.ts:96`의 `new Date().toISOString().slice(0,10)`은 **UTC 날짜**라 "오늘" 경계가 09:00 KST다. 그 직후 첫 조회가 라이브로 떨어져 건수가 올라갔다가, 그 조회가 캐시를 `ready`로 마킹한 뒤부터 다시 내려갔다. 1회성 이상이 아니라 **매일 반복되던 현상**이다.

**수정** — `upsertMonthDeals`가 적재 시 쓰는 것과 **동일한 기준**(`toDealRow` → `dealNaturalKey`)을 `dedupeMonthData()`로 묶어, 국토부 원본을 소비하는 **3곳 전부**에 적용했다. 기준을 두 곳에 따로 구현하지 않았다(또 어긋나지 않게).

| 경로 | 조치 |
|---|---|
| `app/api/data/route.ts:174` | 라이브 경로 |
| `app/api/recommend/route.ts:98` | 폴백 경로 — **DB 히트월(접힘)과 폴백월(원본)이 한 응답에 섞여 있었다.** 추천 결과의 단지별 거래건수로 사용자에게 노출된다 |
| `app/api/cron/prewarm/route.ts:189` | `monthly_stats` writer 기준 통일 |

**⚠️ 표시 건수는 "불변"이 아니다.** 라이브 경로를 타던 최신월 건수가 캐시 값에 맞춰 **내려간다**(전세 270→258, 매매 133→131). 완결월과 이미 캐시로 서빙되던 값은 그대로다. 즉 오르내리던 최신월 숫자를 낮은 쪽으로 고정하는 변경이다. (초안 주석은 이걸 "수치 불변"으로 잘못 썼고 QA가 잡아냈다 — 아래 참조.)

### QA 핑퐁 — FAIL 4건 적발 → 교정 → 재검증 전부 PASS

**1차 검증에서 FAIL 2건 + 누락 진입 경로 2곳**이 나왔다. 전부 마스터가 직접 코드로 확인했고 **4건 모두 타당했다.**

1. **(주석) "라이브 사이트 수치 변동은 없다"가 사실과 다름** — 실제로는 최신월이 270→258로 내려간다. **이건 개발팀 잘못이 아니라 마스터 지시 문구가 부정확했던 탓이다** — 마스터가 "표시 수치 불변"이라 지시했고("최종적으로 사용자가 보는 값은 같다"는 뜻이었다) 개발팀이 그대로 주석에 옮겼다. 정확한 서술로 교체.
2. **(주석) `contractTerm` 서술 부정확** — `contract_type`과 나란히 "키에서 빠져 있다"고 썼으나 성격이 전혀 다르다. `contract_type`은 파싱되어 `RentRecord`·`DealRow`·`deals` 컬럼에 **모두 있고 키에만** 없다(키 확장으로 해결 가능). `contractTerm`은 **파서에 아예 없다**(`grep lib/molit-api.ts` → 0건). "`dealNaturalKey` 한 곳만 고치면 된다"는 문장을 그대로 뒀으면 후속 작업자가 막다른 길로 갔다.
3. **`/api/recommend` 누락** — 같은 비대칭이 남아 있었고 **사용자 노출 수치**다.
4. **`prewarm` → `monthly_stats` writer 불일치** — `/api/data`를 접힌 값으로 바꾼 **이번 변경이 새로 만든 불일치**다(같은 행을 두 writer가 다른 `deal_count`로 덮어씀). 현재 reader는 0건이라 화면 영향은 없었지만, 내 변경이 만든 것이므로 같은 라운드에서 닫았다.

**재검증 6항목 전부 PASS.** QA 비차단 관찰 1건(완결월도 DB 조회 실패 시엔 라이브 경로를 타므로 그때는 접힌 값이 된다)은 마스터가 주석 한 줄로 보완했다.

### 마스터 직접 검증
- `collectMonth` 호출부 **전수 확인 = 정확히 3곳**, 3곳 전부 dedupe 적용. 4번째 경로 없음. (`fetchTrade`/`fetchRent`는 export돼 있으나 `collectMonth` 내부에서만 호출)
- `deal_type`이 `TradeRecord`·`RentRecord` **양쪽에 있는 판별 필드**임을 `molit-api.ts:68,82`에서 확인(없었으면 유형 복원이 한쪽으로 쏠려 조용히 깨졌을 지점).
- `npx tsc --noEmit` exit 0, `npm run build` 성공(22/22 static pages).
- **배포 후 라이브 실측** — `/api/data?lawd_cd=36110`: 완결월 매매 352/393/404, 전세 605/559/590 = **수정 전 값과 완전히 동일**(회귀 0). 최신월 전세 258로 캐시 값 일치. 홈 HTTP 200, `/api/recommend` 정상(items 10 / 후보 159).

### 🔴 CS 내부지침의 사실관계 오기 정정 (`60c5e64`)
`docs/cs/2026-07-15-faq-draft.md`의 금지표현 체크리스트가 **"AI 추천 기준은 예산·역세권·신축 3종뿐이며 평지(경사도)는 추천 로직에 존재하지 않는다. 절대 안내하지 말 것"**이라고 못박고 있었다. **사실이 아니다.** `app/api/recommend/route.ts:136`의 `w_slope`(기본 가중치 10)가 4번째 가중치로 실재하고, `:255~269`에서 `getSlopeScore`로 단지별 점수를 구해 `:317` `compositeScore`에 반영된다. 라이브 응답에도 `slope_score`가 담겨 나온다(실측 확인).

07-15 당시 이진 필터였다가 **같은 날 후반 가중치 슬라이더로 전환**됐고(`route.ts:140-141` 주석) 체크리스트가 전환 이전 상태로 굳은 것으로 보인다. **라이브 FAQ의 "4종" 안내가 맞다.** 자동세션이 이 지침을 근거로 라이브에서 맞는 정보를 지울 위험이 있어 폐기 표시 + 정정 사유를 박아 넣었다. → **07-19 교훈("팀 산출 문서를 그대로 신뢰하지 말 것")이 실제로 또 적중했다.**

### 라운드 29 미가동분 — marketing/cs 가동 완료
- **마케팅팀**: `docs/marketing/blog-promotion-draft-2026-07-20.md` 신규 — 발행 11편 채널별 홍보 문구. 검색어는 전부 "추정" 표기, 검색량 수치 없음, 수치는 원문 인용만. **마스터 YMYL 검수 통과**(금지표현 검출 2건은 자가점검 목록 자체와 "오른다/내린다를 말하는 글이 아니라"는 부정문). 미게시 상태.
- **CS팀**: FAQ 문답 8건 초안(기존 라이브 FAQ 8문항과 중복 회피). **아직 파일로 커밋하지 않았다** — 아래 사용자 판단 3건이 걸려 있어 그것부터 정해야 문구가 확정된다.

### 프리워밍 (2순회 유지 모드)
라운드 중 **5회 호출**: 인덱스 **10 → 14 → 18 → 24 → 28 → 33**. 전부 HTTP 200, `incomplete: false`, **504 없음**. 서울 25개구 완주, 경기 진입(수원·성남·의정부).

---

## 🔴 사용자 판단 필요 (이번 라운드 신규 + 이월)

### 신규 — 자연키가 실재하는 별개 거래를 접고 있다
현재 자연키(13필드)는 거래 1건을 **완전히 식별하지 못해 실제보다 적게 세는 쪽으로 치우쳐** 있다. 세종 202607 전세 실측: 접힌 12건 중 **최소 2건은 모든 필드가 같고 `contractTerm`만 다른 명백한 별개 계약**이었다.
- **(A) 자연키 확장** — 표시 건수가 258 → **265 내외로 올라간다.** DB 마이그레이션 필요. `contract_type`은 이미 파싱돼 있어 키+제약만 고치면 되지만, `contractTerm`은 파서부터 없어서 파싱→필드→컬럼 추가가 선행돼야 한다.
- **(B) 현행 유지** — 두 경로는 이제 일치하나, 계속 적게 센다.
- ⚠️ **발행된 블로그 `sejong-jeonse-briefing-2026-07-19`가 258·131을 본문에 인용**하고 있다. (A)를 택하면 발행본과 사이트 수치가 어긋나므로 **본문 수정이 함께 필요하다.** 이것이 이번 라운드에 (A)를 적용하지 않은 주된 이유다.

### 신규 — 해제(취소)된 거래가 필터 없이 집계되고 있다
매매 API 응답의 `cdealType`(해제여부)·`cdealDay`를 **파서가 아예 읽지 않는다**(`lib/molit-api.ts:231-258`). 세종 202607 매매 실측 **5건/133건 = 3.8%**가 해제 거래인데 건수·평균가에 그대로 들어간다. (전월세 API에는 이 필드가 없다.)
- (a) 집계에서 제외 — 정확하나 표시 건수·평균가 변동 / (b) 컬럼 보존만 + 공시 / (c) 현행 유지
- **07-19 이월 항목 "전세 건수 모집단 통일(A/B/C)"과 성격이 같으니 묶어서 결정하시길 권한다.**

### 이월 — 전세 건수 모집단 통일 여부 (07-19 라운드 28~)
(A)건수도 신규 기준 통일(표시 건수 약 40% 감소) / (B)현행+공시(현재) / (C)병기. 세종 글이 (B) 전제로 쓰였다는 점 함께 고려.

### 이월 — 스케줄 세션 중복 (07-19 라운드 29)
이번 라운드부터 **대응책 ③(팀로그 라운드 시작 마커 선커밋)을 시행**했고, 이 라운드에서는 중복 세션이 발생하지 않았다. 다만 근본 대응(주기 60분 확대 등)은 여전히 사용자 판단 사항이다.

### 이월 — `/ranking` 신규 최상위 페이지
승인 대기 유지. 자동세션 착수 보류.

---

## 다음 라운드가 참고할 상태

- 🟡 **`cacheFreshToday` UTC→KST 교정** (`app/api/data/route.ts:96`, `app/api/cron/prewarm/route.ts:113`) — 이번 수정으로 건수 변동 증상은 사라졌지만, "오늘" 경계가 09:00 KST인 **신선도 판정 자체의 어긋남은 그대로 남아 있다.** 갱신 타이밍 동작 변경이라 이번 라운드에 섞지 않았다. **다음 라운드 1순위 후보** — 표시 수치를 바꾸지 않고 사용자 판단도 필요 없는 유일한 남은 항목이다.
- 🟡 **CS FAQ 8문답 발행** — 위 사용자 판단 3건이 정해져야 문구가 확정된다. 특히 "왜 건수가 줄었나요?" 문답은 **이번 라운드에 원인이 규명됐으므로 "확인 중"이 아니라 사실대로 다시 쓸 수 있게 됐다.**
- 🟡 **CS팀 UX 제안 (기획/디자인팀 검토 후보)** — "진행 중인 달은 확정치가 아니다"가 `/about`·`/terms`·`/support`에만 있고 **정작 숫자를 보는 대시보드 화면에는 없다.** 전세 모집단 공시는 숫자 옆 3곳에 붙였는데 신고 지연 안내는 약관 쪽에만 있어 배치가 비대칭이다. 제안: 월별 추이 그래프에서 진행 중인 달을 시각 구분(점선/흐린 막대) + "집계 중" 배지.
- 🟡 비차단 정리거리(07-19 이월): ①`BlogSection` 인라인 링크 타입 부재(수요 3건 누적, 일괄 도입 검토 시점) ②세종 글 내부글 제목 인용부호를 실제 제목과 일치 ③기획안 §1 분량 예상치가 실제와 어긋남.
- 🟡 `market` 다음 편 후보: 자사 데이터만으로 성립하는 소재(예: 전세 거래건수 상·하위 지역).
- ✅ 블로그 11편(guide 4·howto 3·insight 2·market 2), 4개 카테고리 전부 2편 이상.
- ✅ 화성 41590 / 부천 41190 RESOLVED, admin 리셋 엔드포인트·문의폼(Resend)·타이틀 중복 종결 유지. `CRON_SECRET`/새벽1시 크론·`TELEGRAM_BOT_TOKEN` 열린 항목 유지.
- ✅ 빌라/단독·알림 계속 보류(2026-07-14 사용자 지시, 유효). "준비중" 표시 유지.
