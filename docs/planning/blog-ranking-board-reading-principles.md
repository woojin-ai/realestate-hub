# 블로그 기획안 — 전국 시세 랭킹 보드(`/ranking`)의 순위를 오해 없이 읽는 원칙

작성: 기획팀 / 대상: 마스터 → (승인 시) 개발팀
작성일: 2026-07-26
범위: 신규 블로그 1편(`insight` 3편째) 기획안. **코드 변경 없음**(기획 문서만). `lib/blog.ts`의 `blogPosts` 배열에 객체 1개 append로 끝나는 IR 무변경 트랙.

선행 확인 문서: `lib/ranking.ts` · `app/ranking/page.tsx` · `components/RankingBoard.tsx` · `components/ranking/*` 3개 · `lib/analyzer.ts` · `lib/regions.ts` · `lib/db-cache.ts` · `app/api/cron/prewarm/route.ts` · `docs/team-log/2026-07-25.md` 라운드 50 · `docs/planning/content-strategy.md` §2·§4·§7-0 · `lib/blog.ts` 기존 insight 2편 전문.

---

> ## [2026-07-26 정정 · QA FAIL 반영] 원칙 6 사실관계 오류 2건 + 체크리스트 1건
>
> QA팀 독립 검증에서 **FAIL** 판정을 받았다. 개발팀은 기획안을 충실히 옮겼으므로 **1차 책임은 이 기획안에 있다.** 원인은 §3 근거표를 만들 때 `lib/ranking.ts`의 `isPending` 분기와 프리워밍의 `monthly_stats` 기록 동작을 **원문으로 끝까지 확인하지 않고 추정**한 것이다. 아래 3건을 정정했고, 정정 문안은 §2와 §9(개발팀 붙여넣기용 확정 문안)에 반영했다.
>
> | # | 심각도 | 초판(틀림) | 정정 |
> |---|---|---|---|
> | A-1 | MEDIUM(발행 차단) | 원칙 6 ③ "최근 몇 달 사이 자료가 한 건도 없는 지역은 '집계 준비 중' 목록에도 나타나지 않는다" | **실제 동작과 반대.** 거래가 없어 평균가·건수가 비어 있는 지역은 `isPending: true`가 되어 **'집계 준비 중'에 정확히 나타난다**(`lib/ranking.ts:56-73`). 경로를 **3개 → 2개로 축소**하고 ②를 "최근 집계에서 유효한 달을 찾지 못한 모든 경우"로 재정의했다 |
> | A-2 | MEDIUM | 원칙 6 ① "시/도 필터 목록에 있는 범위로 한정" | **커버리지 단위가 틀렸다.** 필터에 뜨는 것은 시·도 이름뿐이고(`components/RankingBoard.tsx:23`) 제외는 **시·군·구 코드 단위**로 일어난다(`lib/ranking.ts:51-52`). 초판 문안은 "목록 밖 시/도만 빠진다"로 읽혀 원칙 6의 취지와 정반대 오해를 만든다. **단위를 시·군·구로 바꿔** 재서술 |
> | C-1 | LOW | 체크리스트 3번 "함께 표시되는 평균가와 거래량을 같이 본다" | 상위 목록에 병기되는 것은 **평균가뿐**이고 거래량은 아래 표에 있다(`components/ranking/RankingHighlights.tsx:39-49`). 본문 [9]는 정확했으나 체크리스트로 압축되며 틀어졌다. 문안 정정 |
>
> **QA가 타당하다고 판정해 유지·수용한 것**(되돌리지 않는다)
> - 개발팀이 "10줄"을 서술형("목록 칸 수보다 적으면")으로 바꾼 것 — 초판 §7 규칙(개수 쓰지 말 것)과 §2 [11] 초안이 서로 모순이었고 **개발팀 판단이 맞다.** §2 [11]에서 "10줄"을 삭제해 모순을 없앴다.
> - 개발팀이 원칙 5에 "'전월대비' 열의 값이 행마다 다른 달을 기준으로 계산된다"를 추가한 것 — 유지(§3 근거표에 이미 있던 사실을 본문으로 끌어올린 정확한 보강).
> - **분량**: 발행본 3,844자·읽기시간 9분. 초판 §1의 "2,700~3,100자(6~7분) — 기존 insight 2편과 같은 급"은 **그 자체로 모순**이었다(기존 2편 실측 3,441자·3,440자·8분). 분량은 현행 유지로 수용하고 §1 수치를 실측에 맞게 정정했다.
>
> **재발 방지**: §3 근거표에 넣는 모든 행은 "그 파일의 해당 줄을 실제로 열어 확인"을 조건으로 한다. 특히 **분기의 else 쪽(값이 없을 때 어디로 가는가)** 은 반드시 원문에서 끝까지 따라간다 — 이번 A-1이 정확히 그 지점이었다.

---

## 1. 확정 사항

| 항목 | 값 |
|---|---|
| 카테고리 | `insight` (지역별 인사이트) |
| slug | `ranking-board-reading-principles` |
| 제목(H1/title) | 순위표의 '1위'는 무엇을 뜻할까 — 전국 시세 랭킹을 오해 없이 읽는 원칙 |
| summary | 전국 시세 랭킹의 순위가 무엇을 기준으로 매겨지고 무엇은 보여주지 않는지, 정렬·필터·집계 범위에 따라 달라지는 순위를 오해 없이 읽는 원칙을 정리했습니다. |
| publishedAt | `2026-07-26` |
| 분량 | **발행본 실측 3,844자 / 읽기시간 9분**(초판 예상치 2,700~3,100자·6~7분은 오차. 기존 insight 2편 실측은 3,441자·3,440자로 둘 다 8분이며, 초판의 "같은 급"이라는 병기와 숫자가 서로 어긋나 있었다 — 정정) |
| 사용 섹션 타입 | `paragraph` / `heading` / `list` / `callout`(warning 1개) — `sourceNote`·`sourceLinks` 미사용 |

> **다음 insight 글의 분량 기준선**: 원칙 6개 + 체크리스트 구조는 **3,400~3,900자(8~9분)** 로 잡는다. 기존 2편·이번 편 실측 3건의 실제 범위이며, 초판처럼 "기존 편과 같은 급"이라는 말과 다른 숫자를 함께 적지 않는다.

### 선정 근거

**(1) 2026-07-25에 새로 배포된 화면인데 짝이 되는 해설 글이 없다.** `/ranking`은 라운드 50에서 배포됐고(`docs/team-log/2026-07-25.md:3-5`), 사이트맵에도 메인 다음가는 우선순위(0.8)로 등록돼 있다(`app/sitemap.ts:16-21`). 그런데 `lib/blog.ts` 11편 어디에도 랭킹 보드를 다룬 글이 없었다.

**(2) 순위표는 구조적으로 오해를 부르는 표면이다.** 개발팀 스스로 화면 안에 "순위가 높다고 더 좋은·투자에 유리한 지역이라는 의미가 아닙니다"라는 안내 박스를 넣어야 했을 정도다(`components/RankingBoard.tsx:122-132`). 화면 안내는 두 문단이 한계이고, 그 뒤에 있는 **집계 규칙**(어느 달을 기준으로, 어떤 표본으로, 어떤 지역만)은 설명할 자리가 없다. 그 자리를 메우는 것이 이 글이다.

**(3) `insight` 카테고리 정의와 정확히 맞는다.** `content-strategy.md` §7-0-4가 정리한 대로 `insight`는 "지역 실명·실제 수치 0, 해석 원칙만 다루는 상시(evergreen) 방법론 글"로 굳어졌다. 이번 글은 특정 지역의 순위를 논하지 않고 **"순위라는 형식 자체가 만드는 오해"** 만 다룬다.

**(4) 카테고리 밸런싱.** 발행 전 guide 4 / howto 3 / insight 2 / market 2 → 발행 후 insight 3.

**(5) 범위 경계 — 이 글은 "사용법"이 아니다.** `/ranking`의 조작법은 `howto` 소재이며 별도 편으로 남긴다(§8).

---

## 2. 글 구조 (`BlogSection` 배열 순서)

관례대로 `[0]`=도입 문단, `[1]`=면책 콜아웃(`DISCLAIMER` 상수 재사용).
**[15]·[19]는 QA FAIL 반영 정정본이다** — 붙여넣기용 최종 문안은 §9에 따로 실었다.

**[0] paragraph — 도입**
표로 줄 세운 숫자는 그 자체로 "위에 있는 것이 더 좋다"는 인상을 준다. 화면 상단에 순위가 우열을 뜻하지 않는다는 안내가 붙어 있지만, 순위표가 만드는 오해는 그것 하나가 아니다. 순위는 **무엇을 기준으로 줄을 세웠는지, 어느 달의 자료인지, 어떤 지역이 애초에 표에 들어와 있는지**에 따라 통째로 달라진다.

**[1] callout(warning)** — `DISCLAIMER` 상수 그대로(`lib/blog.ts:64-65`). 추가 문구 없음.

**[2] heading — 이 글이 다루는 것과 다루지 않는 것**
**[3] paragraph** — ① 특정 지역 평가 안 함 ② 조작법 안 다룸 ③ 실제 순위·금액·변화율 숫자 인용 안 함(갱신될 때마다 바뀌므로 박아두면 그 자체가 오해).

**[4] heading — 원칙 1 — '1위'는 칭호가 아니라 지금 정렬 상태의 줄 번호다**
**[5] paragraph** — 순위 열은 정렬 가능한 값이 아니라 현재 행에 위에서부터 붙인 번호다. 기준 3종 중 하나를 고르고, 같은 기준을 다시 누르면 방향이 뒤집힌다 → **오름차순이면 '1위'는 값이 가장 낮은 지역.** 캡처해 옮길 때 기준·방향이 빠지면 같은 표가 정반대로 읽힌다.

**[6] heading — 원칙 2 — 세 가지 기준은 서로 다른 질문에 답한다, 종합 순위가 아니다**
**[7] paragraph** — 평균가/변화율/거래량은 단위도 재료도 다르며 종합점수 열이 없다. 면적 구성 착시·전세 평균의 모집단은 기존 글로 넘긴다(§6-1).

**[8] heading — 원칙 3 — 변화율 순위는 표본이 적은 지역이 올라오기 쉬운 구조다**
**[9] paragraph** — 상위 목록에 **최소 거래건수 조건이 없다.** 소수 거래가 평균을 흔든다는 원리는 기존 글로 넘기고, 이 글의 논지는 "**순위표는 그 흔들린 값을 위로 끌어올린다**"는 선택 편향. 상위 목록의 평균가와 **아래 표의 거래량 열**을 같이 볼 것.

**[10] heading — 원칙 4 — 상위 목록과 표는 다른 규칙으로 만들어지고, 필터를 바꾸면 순위가 다시 매겨진다**
**[11] paragraph** — 상위 목록은 정렬 버튼과 무관(항상 변화율), 시/도 필터에는 반응. 범위를 좁히면 **목록 칸 수보다 오른 지역이 적을 때 그만큼만 나오고**, 0%는 어느 쪽에도 안 들어간다. 표의 순위도 필터 상태에서 1번부터 다시 매겨진다. 탭을 옮기면 처음부터 다시.
> **[2026-07-26 정정]** 초판에 있던 "10줄보다 짧아질 수 있다"는 §7의 "개수를 쓰지 않는다" 규칙과 모순이었다. 개수를 쓰지 않는 서술형으로 확정(개발팀 발행본이 이미 이 형태).

**[12] heading — 원칙 5 — 같은 열에 있어도 기준이 되는 달은 지역마다 다를 수 있다**
**[13] paragraph** — 기준월은 지역별로 따로 정해지고, 비교 대상도 그 앞쪽에서 찾는다. 신고 시차 때문에 **한 달 뒤처진 달이 기준일 수 있고, 거래 없는 달이 끼면 두 달 전과 비교된 값일 수도 있다.** 표에 기준월 열이 있고 '전월대비' 열이 행마다 다른 달 기준인 이유. 헤더 기준월은 대표값일 뿐.

**[14] heading — 원칙 6 — 순위에 없는 지역이 있다**
**[15] paragraph (QA FAIL A-1·A-2 정정본 — 경로 3개 → 2개)**
- **경로 ①(대상 범위)** — 이 랭킹이 다루는 지역은 **사이트에 등록된 시·군·구 목록**으로 한정된다. 시/도 필터에 나오는 것은 **시·도 이름뿐**이므로, 어떤 시/도를 골랐다고 해서 그 안의 모든 시·군·구가 표에 있는 것은 아니다. 등록되지 않은 시·군·구는 필터를 어떻게 바꿔도 순위에 나타나지 않는다.
- **경로 ②(집계 상태)** — 최근 집계에서 **평균가와 거래 건수가 함께 잡히는 달을 찾지 못한** 지역은 순위에서 빼고 표 아래 '집계 준비 중'에 따로 모은다. 아직 수집이 끝나지 않은 경우도, **최근 몇 달 사이 신고된 거래가 없어 값이 비어 있는 경우도 여기에 해당한다.** 오류가 아니라 빈 값이 정렬에서 1위나 꼴찌로 튀는 것을 막기 위한 의도적 분리다.
- 마무리 — 아파트만 다룬다는 점까지 더하면 표의 첫 줄은 "전국에서 가장 ○○한 곳"이 아니라 "지금 이 표에 올라와 있는 지역 중 첫 줄"이다. 순위표에 없다고 그 지역에 거래가 없다는 뜻도 아니다.

**[16] heading — 순위표는 목록이 아니라 입구로 쓰는 것이 안전하다**
**[17] paragraph** — 지역명 클릭 → 대시보드 딥링크('집계 준비 중' 칩도 동일). 순위를 결론으로 삼지 말고 **입구로 쓰고 확인은 상세 데이터에서.** 일정 주기로 다시 만들어지는 스냅샷이므로 마지막 집계 시각도 확인.

**[18] heading — 정리 — 순위표를 읽을 때의 체크리스트**
**[19] list** (6항목, **3번·6번 정정** — 최종 문안은 §9-2)

> **본문에 넣지 않기로 한 것**(의도적 배제, 개발팀이 임의로 추가하지 말 것)
> - 실제 순위·지역명·금액·변화율·거래건수 값 → insight 관행(§5) 위반.
> - 대상 시·군·구 개수, 시/도 이름, 집계 창의 개월 수, 재생성 주기(시간), 상위 목록 칸 수 → 지역 실명·구현 상세이며 값이 바뀌면 글이 곧바로 낡는다. "등록된 시·군·구 목록", "최근 몇 달", "일정 주기", "목록 칸 수"로만 서술한다.
> - **[2026-07-26 추가] 초판의 경로 ③("창 안에 집계 행 자체가 없어 준비 중 목록에도 안 나타나는 지역")** — 코드상으로는 존재하는 경로다(`app/ranking/page.tsx:41-43` → `lib/db-cache.ts:426` → `lib/ranking.ts:42-47`). 그러나 본문에서 **제외**한다. 이유 2가지: (a) 독자 화면에서 이 지역들은 경로 ①과 **완전히 동일하게 "그냥 없음"** 으로 보여 구분이 불가능하다. (b) 초판처럼 ②와 나란히 세우면 "준비 중 칩에 뜬 지역 = 자료가 있는 지역, 안 뜬 지역 = 자료가 없는 지역"이라는 **정반대 오독**을 만든다. 경로 2개로 줄이는 편이 사실에 더 가깝다.
> - 평균가와 면적 구성의 관계, 거래건수와 평균 안정성의 일반 원리 → 기존 insight 1편 소재. 한 문장 언급 + 참조로만 처리(§4·§6).
> - 전세 평균가의 신규/갱신 모집단 → 기존 guide 1편 소재. "전세 탭은 보증금 기준"까지만.

---

## 3. 각 논지의 코드 근거 (개발팀 집필용 — 여기 없는 사실은 쓰지 말 것)

| 본문 | 사실 주장 | 코드 근거 |
|---|---|---|
| [5] | 순위 열은 정렬 불가 표시 열이고, 값은 현재 행 배열의 인덱스+1이다 | `components/ranking/RankingTable.tsx:22`(`{ key: "rank", sortable: false }`), `:91-93`(`{i + 1}`) |
| [5] | 정렬 기준은 평균가·변화율·거래량 3종 | `lib/ranking.ts:9`, `components/RankingBoard.tsx:25-30`, `RankingTable.tsx:21-28` |
| [5] | 같은 기준 재클릭 시 방향 토글, 다른 기준 클릭 시 내림차순 리셋 | `components/RankingBoard.tsx:99-106` |
| [5] | 초기 상태는 매매 탭·전체 시/도·평균가 내림차순 | `components/RankingBoard.tsx:49-52` |
| [5] | 오름차순이면 값이 낮은 행이 위로 온다 | `components/RankingBoard.tsx:66-77`(`sortDir === "asc" ? av - bv : bv - av`) |
| [7] | 매매 평균가 = 신고 거래금액의 산술평균 | `lib/analyzer.ts:199-203` → `:240-254` → `lib/db-cache.ts:372-396` → `lib/ranking.ts:87` |
| [7] | 전세 평균가 = 보증금 기준(신규 계약만) | `lib/analyzer.ts:205-216`(`avgJeonseDeposit`, `onlyNew=true`), `:247-250` |
| [7] | 변화율 = 직전 집계월 대비 백분율(소수 둘째 자리 반올림, 이전월 0이면 미산출) | `lib/analyzer.ts:225-231`, `lib/ranking.ts:76` |
| [7] | 거래량 = 기준월 한 달의 신고 건수(누적 아님) | `lib/ranking.ts:90`, `lib/analyzer.ts:251` |
| [7] | 종합점수 열이 없다 | `components/ranking/RankingTable.tsx:21-28` |
| [7] | 평균가는 면적 구성에 좌우된다(화면 안내 존재) | `components/RankingBoard.tsx:127-131`, `RankingTable.tsx:47-53` |
| [9] | 상위 목록은 변화율 크기만으로 정렬, **최소 거래건수 조건 없음** | `components/RankingBoard.tsx:79-97`(필터는 `changePct > 0`/`< 0`뿐) |
| [9]·[19]-3 | **상위 목록에 병기되는 것은 평균가뿐이고 거래량은 없다** (C-1 근거) | `components/ranking/RankingHighlights.tsx:39-49` — 렌더 요소는 순번·지역명·`changePct`·`formatPrice(avgPrice)`. `dealCount`는 이 컴포넌트에서 한 번도 참조되지 않는다 |
| [9] | 거래량 열은 아래 표에 있다 | `components/ranking/RankingTable.tsx:26`, `:119` |
| [11] | 상위 목록은 정렬과 무관(의존성에 `sortKey`/`sortDir` 없음), 시/도 필터에는 반응 | `components/RankingBoard.tsx:79-97`(deps `[activeRows]`), `:56-64` |
| [11] | 필터 결과에 따라 목록이 칸 수보다 짧아질 수 있고 0%는 제외 | `components/RankingBoard.tsx:83-95`(주석에 실측 기록), `RankingTable.tsx:30-35`, `docs/team-log/2026-07-25.md:17-19` |
| [11] | 매매·전세는 분리된 데이터셋 | `lib/ranking.ts:28`·`:41-47`, `components/RankingBoard.tsx:54` |
| [13] | 기준월 = `avg_price !== null && deal_count > 0`인 가장 최근 달(지역별) | `lib/ranking.ts:55-56`, 주석 `:32-33` |
| [13] | 비교 대상은 기준월보다 과거이면서 평균가가 있는 첫 달(직전 달이 아닐 수 있음) | `lib/ranking.ts:75`, 주석 `:34` |
| [13] | 기준월 열 존재 / 헤더 기준월은 활성 지역 `refYm`의 최댓값 | `RankingTable.tsx:27`·`:120-122` / `lib/ranking.ts:105-115`, `RankingBoard.tsx:32-35`·`:117-119` |
| [13] | 열 라벨은 "전월대비"지만 실제로는 지역별 직전 집계월 대비 | `RankingTable.tsx:25` ↔ `lib/ranking.ts:75` 대조 |
| **[15] ①** | **대상은 `REGION_CODES`에 등록된 시·군·구뿐이며, 미등록 시·군·구는 수집 단계부터 제외된다** | `app/api/cron/prewarm/route.ts:31`(`import { REGION_CODES }`)·`:70`(REGION_CODES를 순서 그대로 평탄화해 순회) → 미등록 코드는 `monthly_stats` 행 자체가 생기지 않음. 설령 행이 있어도 `lib/ranking.ts:51-52`(`getRegionByLawdCd` 실패 시 `continue`)가 결과에서 제외 |
| **[15] ①** | **필터에 노출되는 것은 시·도 이름뿐 — 시/도를 골라도 그 안의 모든 시·군·구가 표에 있지는 않다** (A-2 근거) | `components/RankingBoard.tsx:23`(`SIDO_NAMES = Object.keys(REGION_CODES)`), `:143-155`(select 옵션이 시·도명) ↔ 제외 단위는 시·군·구 코드(`lib/ranking.ts:51-52`). QA 실측 사례(경기도 가평군·양평군·연천군)는 `lib/regions.ts:43-85` 경기도 목록에 해당 항목이 없음으로 재확인 — **개수·지역명은 본문 금지(§2 배제 목록)** |
| **[15] ②** | **최신 집계에서 유효한 달을 못 찾으면 `isPending: true` → 순위에서 빼고 '집계 준비 중'에 표시** (A-1 정정 핵심) | `lib/ranking.ts:56`(`current = sorted.find(r => r.avg_price !== null && r.deal_count > 0)`) → `:58-73`(`if (!current)` 분기가 `isPending: true`로 push) → `components/RankingBoard.tsx:56-64`(pending 분리) → `components/ranking/PendingRegions.tsx:17-26`(렌더) |
| **[15] ②** | **거래가 없어 값이 비어 있는 지역도 여기에 포함된다** — 프리워밍은 거래 0건인 달도 행을 기록한다 | `app/api/cron/prewarm/route.ts:193`(`allData[ym] = data` — 빈 달도 담김) → `:249-253`(`upsertMonthlyStats(..., allData)`) → `lib/analyzer.ts:245-252`(`allData`의 **모든** ym에 대해 `{avg, count}` 생성, 빈 달은 `avgTradePrice`가 `null`·`count: 0`) → `lib/db-cache.ts:380-389`(`avg_price: null, deal_count: 0`으로 upsert). 즉 행은 있으나 `current`를 못 찾아 `isPending`이 된다 |
| **[15] ②** | 분리 이유가 "빈 값이 1위/최하위로 튀는 것 방지"임 | `components/ranking/PendingRegions.tsx:11-15`(주석), 정렬에서 `null`을 방향과 무관하게 뒤로 보내는 처리 `components/RankingBoard.tsx:71-73` |
| [15] | 대상은 아파트뿐 | `app/ranking/page.tsx:43`(`loadRankingMonthlyStats(supabase, "아파트", ...)`) |
| [17] | 지역명 클릭 → `/?lawd_cd=...` 딥링크, 대시보드가 자동 선택+자동 조회 | `RankingTable.tsx:95-102`, `RankingHighlights.tsx:34-38`, `docs/team-log/2026-07-25.md:13-15`·`:26` |
| [17] | '집계 준비 중' 칩도 딥링크 유지 | `components/ranking/PendingRegions.tsx:15`(주석)·`:29-38` |
| [17] | 일정 주기로 재생성되는 스냅샷 + 마지막 집계 시각 표기 | `app/ranking/page.tsx:9-15`(`revalidate = 21600`), `components/RankingBoard.tsx:185-189` |

**본문에 넣지 않되 개발팀이 알아야 할 사실**
1. 전세 탭에서 평균가의 모집단(신규 계약)과 거래량의 모집단(갱신 포함 전체)이 다르다(`lib/analyzer.ts:247-251`). 기존 guide `jeonse-new-vs-renewal-contract-guide`가 다룬 내용이라 이 글에서는 펼치지 않는다.
2. `monthly_stats`에는 writer가 둘 있다(프리워밍 크론 / `/api/data`). 둘이 같은 기준으로 접어 쓰도록 2026-07-20에 통일됐다(`app/api/cron/prewarm/route.ts:180-192`). 이 글의 서술에는 영향이 없다.

---

## 4. 중복 방지 대조 — 기존 insight 2편(+인접 3편)

### 4-1. `regional-price-difference-reading-principles` (insight, 2026-07-18)

| 기존 원칙 | 다루는 것 | 이번 글 대응 | 판정 |
|---|---|---|---|
| 원칙 1 — 집계 단위는 '구'다 | 대시보드의 조회 단위 | 이번 글은 단위를 전제로 깔고, **그 단위들을 한 화면에 줄 세웠을 때** 생기는 문제를 다룬다 | 겹침 없음 |
| 원칙 2 — 평균가보다 거래건수를 먼저 본다 | 소수 거래가 평균을 흔든다는 **일반 원리** | [9]는 원리를 재설명하지 않고 "**순위표가 그 흔들린 값을 상위로 끌어올린다**"는 선택 편향을 다룬다(최소 거래건수 조건 부재가 근거) | 인접하나 논지 다름 — 원리는 기존 글로 명시적 이관(§6-1) |
| 원칙 3 — 구성 착시 | 평균 자체의 성질 | [7]에서 한 문장 언급 후 이관. [7]의 논지는 "세 열이 서로 다른 질문에 답한다" | 인접하나 반복 없음 |
| 원칙 4 — '수집 중'·신고 지연 | 대시보드 변동률 카드 | [13]은 신고 지연을 **원인으로만** 짚고, 논지는 "행마다 기준월이 달라 같은 열이 같은 기간을 비교한 게 아니다" | 겹침 없음 |
| 원칙 5 — 매매·전세 분리 | 두 탭을 섞지 말 것 | [11] 끝 한 문장. 원칙으로 승격 안 함 | 겹침 없음(의도적 축소) |
| 원칙 6 — 빈 데이터는 지역 특성일 수 있다 | 조회 결과가 비는 경우 | [15]는 "비어 보이는 것"이 아니라 **"표에서 사라지는 것"**(생존편향). 대시보드는 지역을 직접 골라 조회하므로 지역이 사라질 수 없지만, 순위표에서는 두 경로로 사라진다 | **겹침 없음(핵심 차별점).** 단 정정 후에는 ②가 기존 원칙 6과 **주제적으로 인접**하다 — 기존 글은 "값이 비면 그 지역 특성일 수 있다", 이번 글은 "값이 비면 **순위 자체에서 빠져** 별도 칸으로 내려간다"로 논지가 갈린다(원인 해석 vs 표시 규칙) |

### 4-2. `location-info-reading-principles` (insight, 2026-07-18)
전 6원칙이 단지 상세의 위치·생활여건을 다룬다. 이번 글은 시·군·구 단위 시세 집계표 → **소재 접점 0**.

### 4-3. 인접 3편(insight 외)

| 기존 글 | 접점 | 차별 |
|---|---|---|
| `ai-recommend-weights-howto` | "종합점수와 순위는 내가 입력한 조건으로 정렬한 결과일 뿐"(`lib/blog.ts:466`) | **단지 단위·사용자가 만든 순위**(가중치) vs **지역 단위·사이트 고정 규칙 순위**. 종합점수가 있는 화면 vs 없는 화면이라 오히려 대비 |
| `summary-cards-trend-chart-howto` | 변동률 카드 읽는 법 | 단일 지역 4구간 카드. 이번 글은 **지역 간 줄 세우기**에 쓰일 때의 문제이며 기준월 논점이 그 글에 없음 |
| `real-price-report-deadline-guide` | 30일 신고기한 → 최근 달 과소집계 | 제도 설명. 이번 글은 원인으로 인용만 함 |

**결론: 소재·논지 중복 없음.**

---

## 5. YMYL 리스크 점검 (`content-strategy.md` §4 대조)

| 금지 항목 | 점검 결과 |
|---|---|
| 미래 예측·단정 | **없음.** 변화율을 "과거 두 달의 비교값"으로만 서술 |
| "추천", "유망 지역", 지역 우위 매기기 | **없음.** 지역 실명 0건. 글 전체가 "순위 상위 = 우위가 아니다"를 논증하는 구조라 금지 취지에 정면 부합 |
| "저평가/고평가" | **없음** |
| "수익률", "투자가치", "시세차익" | **없음** |
| 외부 수치 인용 | **없음**(시세 수치 0). 구조 표현도 숫자 대신 서술로 처리 |
| 데이터 출처·기준일 | 수치 인용이 없어 `sourceNote` 미사용(기존 insight 2편과 동일) |
| 면책 문구 | `[1]` warning 콜아웃 + 상세 하단 CTA 고정 면책(`app/blog/[slug]/page.tsx:151-154`)로 2회. 수치 인용 0이라 중간 추가 콜아웃 해당 없음 |
| 특정 단지 실명 | **없음** |

**잔여 리스크와 완화**

1. **(낮음) 사양 변경으로 글이 낡을 위험** — 숫자 표현 최소화(§2 배제 목록). 사양 변경 시 [5]·[11] 두 문단만 고치면 되도록 배치.
2. **(낮음) [17]이 행동 권유로 읽힐 여지** — 대상은 화면 사용 방법이며 "확인은 상세 데이터에서"로 한정, 무엇을 판단하라는 말은 쓰지 않는다.
3. **(낮음) '집계 준비 중' 언급이 서비스 결함으로 읽힐 여지** — 화면 문구(`PendingRegions.tsx:23-26`)와 같은 톤으로 "오류가 아니라 의도적 분리"임을 [15]에 명시.
4. **[2026-07-26 신규] (중간 → 정정으로 해소) 커버리지 서술이 정반대 오해를 만들 위험** — 초판 [15]①은 "시/도 목록 밖만 빠진다"로 읽혀 **"내가 고른 시/도 안은 전부 들어 있다"** 는 잘못된 안심을 준다. 실제 제외 단위는 시·군·구다. 정정 문안은 "시/도를 골랐다고 그 안의 모든 시·군·구가 표에 들어 있는 것은 아니다"를 **부정문으로 명시**해 못 박는다. 이 항목은 이 글의 존재 이유(생존편향)와 직결되므로 향후 수정 시 최우선 보존 대상이다.
5. **[2026-07-26 신규] (해소) 준비 중 칩 오독 위험** — 초판처럼 ②와 ③을 나란히 세우면 독자가 "준비 중에 뜬 지역 = 자료가 있는 지역"으로 읽어 **없는 구분을 만들어낸다.** 경로를 2개로 줄여 해소.

---

## 6. 내부링크 계획

### 6-1. 본문 → 기존 글 (텍스트 참조)
**전제**: `BlogSection`에 인라인 링크 타입이 없다(`lib/blog.ts:31-47`). 아래는 **글 제목을 언급하는 텍스트 참조**. 선례: `lib/blog.ts:594`.

| 위치 | 참조 대상 | 처리 |
|---|---|---|
| [7] 끝 | `regional-price-difference-reading-principles` | 면적 구성 착시 → "지역 데이터를 읽는 원칙을 다룬 글에서 따로 설명했습니다"(발행본 반영 완료) |
| [9] 중간 | 같은 글(원칙 2) | 소수 거래의 평균 흔들림 → 동일 방식(발행본 반영 완료) |
| [7] 전세 | `jeonse-new-vs-renewal-contract-guide` | "전세 계약 구분을 다룬 글에서 따로 설명했습니다"(발행본 반영 완료) |

### 6-2. 기존 글 → 이번 글 (마스터 승인 필요, 이번 라운드 필수 아님)
`regional-price-difference-reading-principles` 원칙 6 뒤에 한 문장 + 상호 참조. **기존 발행본 수정이므로 승인 없이 진행하지 않는다.**

### 6-3. `/ranking` 화면 → 이번 글 (개발팀 권장, 코드 1~2줄)
`components/RankingBoard.tsx:122-132` 안내 박스 아래에 "순위표를 읽는 원칙 자세히 보기 →"(`/blog/ranking-board-reading-principles`). `next/link`는 이미 import되어 있다(`:4`). 사이트맵 우선순위가 랭킹 0.8 > 블로그 0.6이라 이 방향의 체류시간 효과가 크다.

### 6-4. 이번 글 → `/ranking` (개발팀 확인)
하단 CTA가 `href="/"` 하드코딩(`app/blog/[slug]/page.tsx:142-155`). (a) 그대로 두기 **[권장]** / (b) `BlogPost` 선택 필드로 분리(스키마 변경) / (c) 본문에서 텍스트로만 지칭 → **(a)+(c) 권장**, (b)는 마스터 판단.

---

## 7. 톤·형식 준수 체크

- 본문 100% 존댓말, 기존 insight 2편 문체 승계.
- 구조 승계: 도입 → 면책 콜아웃 → 범위 선언 → 원칙 6개 → 정리 체크리스트(6항목).
- `BlogSection` 기존 타입만 사용 — **스키마·IR 변경 없음.**
- `DISCLAIMER` 상수 재사용. 표·차트 없음(`docs/design/blog.md` §3-3).
- 화면 명칭은 실제 UI 문구 그대로: "전국 아파트 시세 랭킹"(`RankingBoard.tsx:115`), "시/도 필터"(`:144`), "평균가·변화율·거래량"(`:25-29`), "지역별 순위"(`RankingTable.tsx:48`), "순위·지역·평균가·전월대비·거래량·기준월"(`RankingTable.tsx:21-28`), "집계 준비 중"(`PendingRegions.tsx:22`), "마지막 집계"(`RankingBoard.tsx:187`).
- **개수·수치 서술 규칙(초판 모순 해소)**: 상위 목록을 지칭할 때 개수를 쓰지 않고 "상승·하락 상위 목록", "목록 칸 수"로 쓴다. 화면 라벨을 그대로 인용해야 하는 경우가 아니면 숫자를 넣지 않는다. **초판 §2 [11]이 이 규칙을 스스로 어겼던 것이 QA 지적의 발단이므로, 기획안 안의 예시 문안도 이 규칙을 지켜야 한다.**
- 발행 후 `app/sitemap.ts:52-57`이 자동으로 URL 포함 — 사이트맵 수동 작업 없음.

---

## 8. 마스터 / 개발팀에 넘길 것

**개발팀 — 즉시 반영(QA FAIL 해소, 재발행 필요)**
1. `lib/blog.ts`의 `ranking-board-reading-principles` 본문 중 **원칙 6 문단(현행 `:1101`)** 을 §9-1 문안으로 **전문 교체**. 경로가 3개 → 2개로 줄어드는 변경이다.
2. 같은 글 **체크리스트(현행 `:1119-1125`)의 3번·6번**을 §9-2 문안으로 교체(1·2·4·5번은 그대로 둔다).
3. 교체 외에 본문을 손대지 않는다 — 원칙 5의 "'전월대비' 열이 행마다 다른 달 기준" 보강과 [11]의 서술형 처리는 QA가 타당하다고 판정했으므로 **유지**.
4. §3 표에 없는 사실은 쓰지 않는다. 특히 순위·금액·변화율·거래건수 실측값, 지역명, 시군구 개수, 집계 창 개월 수, 재생성 주기, 상위 목록 칸 수는 전부 제외(§2 배제 목록).

**마스터 확인 필요**
1. §9 정정 문안 승인 여부(본문 1문단 + 체크리스트 2항목 교체, IR 무변경).
2. §6-3 — `/ranking` 안내 박스에 이 글로 가는 링크 추가 여부(라이브 화면 코드 1~2줄).
3. §6-2 — 기존 발행본에 상호 참조를 덧붙일지.
4. §6-4 (b) — 글별 CTA 링크를 `BlogPost` 선택 필드로 뺄지(스키마 변경). 기획팀 권장은 "이번 라운드에는 하지 않음".

**후속 소재(이번 라운드 아님)**
- `howto` — "전국 시세 랭킹 보드 사용법"(탭·시/도 필터·정렬 토글·행 클릭 딥링크 조작 순서). 이번 글이 해석 원칙만 다뤄 조작 안내가 비어 있다.

---

## 9. 개발팀 붙여넣기용 확정 문안 (QA FAIL 정정본)

### 9-1. 원칙 6 문단 전문 — `lib/blog.ts` 현행 `:1101`의 `text` 값을 아래로 교체

```
순위표에서 가장 놓치기 쉬운 것은 표에 없는 지역입니다. 지역이 빠지는 경로는 두 가지입니다. 첫째, 이 랭킹이 다루는 지역은 사이트에 등록된 시·군·구 목록으로 한정되어 있습니다. 시/도 필터에 나오는 것은 시·도 이름뿐이므로, 어떤 시/도를 골랐다고 해서 그 안의 모든 시·군·구가 표에 들어 있는 것은 아닙니다. 등록되지 않은 시·군·구는 필터를 어떻게 바꿔도 순위에 나타나지 않습니다. 둘째, 최근 집계에서 평균가와 거래 건수가 함께 잡히는 달을 찾지 못한 지역은 순위에서 빼고 표 아래 '집계 준비 중' 칸에 따로 모아 둡니다. 아직 수집이 끝나지 않은 경우도, 최근 몇 달 사이 신고된 거래가 없어 값이 비어 있는 경우도 여기에 해당합니다. 이는 오류가 아니라 빈 값이 정렬에서 1위나 꼴찌로 튀어 오해를 만드는 것을 막기 위한 의도적인 분리입니다. 여기에 이 랭킹이 아파트만 다룬다는 점까지 더하면, 표의 첫 줄은 '전국에서 가장 ○○한 곳'이 아니라 '지금 이 표에 올라와 있는 지역 중 첫 줄'입니다. 순위표에 보이지 않는다고 해서 그 지역에 거래가 없다는 뜻도 아닙니다.
```

### 9-2. 체크리스트 6항목 전문 — 현행 `:1119-1125`의 `items` 배열을 아래로 교체
(1·2·4·5번은 현행과 동일한 문안이며 **3번과 6번만 바뀐다.** 순서 사고를 막기 위해 배열 전체를 싣는다.)

```
"'1위'는 고정된 칭호가 아니라 지금의 정렬 기준·방향에서의 줄 번호다 — 방향을 뒤집으면 1위의 의미가 정반대가 된다.",
"평균가·변화율·거래량은 서로 다른 세 개의 순위다 — 하나로 합친 종합 순위는 이 화면에 없다.",
"변화율 순위는 크게 흔들린 값이 위로 올라오는 구조다 — 상위 목록에 함께 표시되는 평균가와, 아래 표의 거래량 열을 같이 본다.",
"시/도 필터를 걸면 순위는 남은 지역 안에서 다시 매겨진다 — 화면의 순위는 언제나 '지금 표에 있는 지역 중 몇 번째'다.",
"행마다 기준이 되는 달이 다를 수 있다 — 변화율을 지역끼리 비교하기 전에 기준월 열을 먼저 확인한다.",
"표에 없는 지역을 기억한다 — 등록되지 않은 시·군·구와 '집계 준비 중'으로 빠진 지역이 있어, 첫 줄이 전국에서 가장 위라는 뜻이 아니다.",
```

### 9-3. 교체 후 확인 사항
- 원칙 6 문단에 "셋째"로 시작하는 서술이 남아 있지 않은지(경로 2개).
- 본문 어디에도 "'집계 준비 중' 목록에도 나타나지 않습니다"라는 취지의 문장이 남아 있지 않은지 — **이 문장이 이번 FAIL의 핵심 오류다.**
- 체크리스트 6번에서 "자료 없음"이라는 표현이 사라졌는지(경로 ③ 제거에 따른 정합).
- 지역명·개수·기간 수치가 새로 들어가지 않았는지.
- `getReadingMinutes`(`lib/blog.ts:1061-1070`)가 본문 글자 수로 계산하므로 교체 후 읽기시간이 1분 단위로 달라질 수 있다 — 정상이며 별도 조치 불필요.
