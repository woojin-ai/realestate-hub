# 블로그 기획안 — 실거래가 통계의 '집계 기간'을 오해 없이 읽는 원칙 (insight 4편째)

작성: 기획팀 / 대상: 마스터 → (승인 시) 개발팀 → QA
작성일: 2026-08-01
범위: 신규 블로그 1편(`insight` 4편째) 기획안. **코드 변경 없음**(기획 문서만). `lib/blog.ts`의 `blogPosts` 배열에 객체 1개 append로 끝나는 **IR 무변경 트랙**.

선행 확인(실제로 열어 확인한 파일·범위):
- `lib/blog.ts` **전문 14편**(본문 블록 전부 — 요약·선언이 아니라 `text` 값을 직접 읽었다). `BlogSection` 유니온 `:31-47`, `DISCLAIMER` `:64-65`, `getReadingMinutes` `:1480-1488`.
- `lib/analyzer.ts` 전문(특히 `buildSummary` `:266-317`, `getAvgAt` `:277-286`, `buildAptStats` `:362-498`, `recentYms` `:388-392`, `mom_pct` `:405-418`, `areaBuckets` `:420-453`).
- `components/DealsTable.tsx`(`COLUMNS` `:26-33`, 주석 `:19-25`, `avgLabel` `:85-88`), `components/SummaryCards.tsx`(`:15-16`·`:41-43`·`:61-69`·`:77-87`), `components/AptDetailModal.tsx`(`:320-346`, 평수별 표 헤더 `:389-403`), `components/MonthlyChart.tsx` 전문, `lib/disclosures.ts` 전문, `lib/ranking.ts`(`:55-58`·`:99-129`), `app/page.tsx`(`:85`·`:127`·`:216`·`:251`).
- `docs/planning/content-strategy.md` §4·§7-0-4, `docs/planning/blog-ranking-board-usage-howto.md` 전문, `docs/team-log/2026-07-29.md` §7 교훈 1~7.

> **07-29 교훈 3 이행 확인**: 인접 글의 "이 글은 ~을 다루지 않습니다" 선언을 근거로 쓰지 않았다. 아래 §4의 중복 판정은 전부 **발행본 본문 블록의 `text` 값**을 근거로 하며, 위험 문구는 줄 번호와 함께 인용해 두었다.

---

## 1. 주제 선정 근거

### 1-0. 전제 (마스터 확정분, 뒤집지 않음)

- 카테고리 = **`insight`**. 현재 guide 4 · howto 4 · **insight 3** · market 3에서 가장 얇은 쪽이고, `market`은 주 1회(월요일) 규칙이라 오늘(금) 제외 — `content-strategy.md` §7-4. → 발행 후 guide 4 · howto 4 · **insight 4** · market 3.
- `insight`의 운영 정의(`content-strategy.md` §7-0-4에 명문화, 발행본 3편이 실제로 지킨 형태): **상시(evergreen) 방법론 글. 실제 지역명·실제 수치 0건, 시점에 묶인 수치는 `market` 전용.** 축은 "데이터를 오해 없이 읽는 원칙".

### 1-1. 기존 insight 3편이 이미 점유한 축

| 발행본 | 축(무엇을 오해하지 않게 하는가) | 원칙 구성 |
|---|---|---|
| `regional-price-difference-reading-principles` (`lib/blog.ts:476-568`) | **집계 단위와 표본 수** — 구 단위 집계, 거래건수, 평균의 구성 변화 | 원칙 1~6 |
| `location-info-reading-principles` (`:571-660`) | **비(非)가격 지표의 성격** — 거리·개수·경사가 무엇을 말하지 않는가 | 원칙 1~6 |
| `ranking-board-reading-principles` (`:1030-1127`) | **'순위'라는 형식** — 정렬·필터·표에 없는 지역 | 원칙 1~6 |

**세 축 어디에도 "기간(집계 창)"이 없다.** 세 글에서 기간이 등장하는 곳은 전부 곁가지다 — regional 원칙 4(`:534`)는 신고 지연을, ranking 원칙 5(`:1093`)는 랭킹 표 한 화면의 기준월만 다룬다.

### 1-2. 후보 3개 비교

#### 🥇 후보 A(추천) — **집계 기간(창)을 오해 없이 읽는 원칙**

이 사이트는 **같은 화면, 심지어 같은 표의 같은 행 안에서 서로 다른 기간을 세고 있고, 그 사실이 화면 라벨에 이미 노출돼 있는데 기존 14편 어디에도 설명이 없다.** 코드 근거:

| # | 사실 | 코드 근거 | 기존 14편 서술 여부 |
|---|---|---|---|
| A-1 | 아파트 목록 표 **한 행 안에서** 평균가 열은 '최근 거래 3개월', 거래건수 열은 '13개월' — 두 열의 기간이 다르다 | `DealsTable.tsx:26-33`(라벨), `analyzer.ts:394-397`(`recent`) vs `:481-483`(`count: records.length`) | **0건** |
| A-2 | '최근 거래 3개월'은 달력의 최근 3개월이 아니라 **거래가 있었던 최근 세 달** — 거래가 뜸한 단지는 훨씬 과거를 덮는다 | `analyzer.ts:388-392`(`recentYms = [...new Set(ym)].sort().reverse().slice(0,3)`), `DealsTable.tsx:20-21` 주석 | **0건** |
| A-3 | '직전 거래월 대비'는 달력상 전월이 아니라 **거래가 있었던 최근 두 달**의 비교라, 두 달 사이 간격이 단지마다 다르다 | `analyzer.ts:405-418`(`prev1=recentYms[0]`, `prev2=recentYms[1]`), `DealsTable.tsx:24-25` 주석 | **0건** |
| A-4 | 상단 변동률 카드는 목표 달에 값이 없으면 **그보다 앞쪽에서 값이 있는 가장 가까운 달로 대신** 계산한다 — 라벨의 기간과 실제 비교 구간이 다를 수 있다 | `analyzer.ts:277-286`(`getAvgAt`의 `older[older.length-1]` 폴백) | **0건** |
| A-5 | 같은 사이트인데 **랭킹 화면은 정반대 규칙** — 직전 달이 비면 대신 가져오지 않고 변화율을 비운다 | `lib/ranking.ts:55-58`·`:99-112` 주석 + `blog.ts:1093`(발행본 insight가 랭킹 쪽만 서술) | 랭킹 쪽만 있음. **두 화면의 규칙이 다르다는 대조는 0건** |
| A-6 | 기준이 되는 '가장 최근 달'은 **값이 잡히는 가장 최근 달**이라 지역·단지마다 다르다 | `analyzer.ts:273-275`(`sortedYms.find(avg !== null)`), `SummaryCards.tsx:64-66`(괄호 월 라벨) | 랭킹 표의 기준월 열만(`:1093`). **대시보드 쪽은 0건** |
| A-7 | 단지 상세는 또 다른 기간 — 모달 meta는 두 기간을 병기하는데, 평수별 표의 평균가·건수·비율은 **넓은 쪽(수집 창 전체)** 기준이라 목록 표의 단지 평균가와 맞아떨어지지 않는다 | `AptDetailModal.tsx:324-346`, `analyzer.ts:421-429`(`areaBuckets`가 `recent`가 아니라 `records`를 순회) | **0건** |

- **evergreen 적합**: 전부 계산 규칙에 관한 서술이라 시점·지역에 묶이지 않는다. 실제 지역명·수치 0건으로 쓸 수 있다.
- **독자 효용이 즉시 발생**: "왜 목록 표 평균가와 단지 상세 평균가가 다르지", "왜 이 단지만 전월 대비가 이상하지"는 실제로 생기는 의문이고, 지금은 답이 사이트 어디에도 없다.
- **YMYL 안전**: 전부 "우리 숫자를 과신하지 말라"는 방향이라 §4 금지 표현(전망·우위·가치판단)과 구조적으로 멀다.

#### 🥈 후보 B(탈락) — 월별 추이 그래프의 '선 모양'을 읽는 원칙

고유 사실은 있다: `spanGaps: true`로 **빈 달을 건너뛰고 앞뒤 점을 이어 그린다**(`MonthlyChart.tsx:52`), `tension: 0.3`이라 점 사이가 직선이 아닌 곡선이다(`:53`), `scales.y`에 `beginAtZero`가 없어 **세로축이 0에서 시작하지 않고 값 범위에 맞춰 잡힌다**(`:95-99` — 같은 변동폭이 더 가팔라 보이는 착시), 가로축은 `autoSkip`+`maxTicksLimit`로 **모든 달 이름이 찍히지는 않는다**(`:89-93`), 마지막 구간 점선·최신월 점 강조·캡션(`:55-65`·`:115-123`).

**탈락 사유 2가지**:
1. **고유 명제의 절반이 발행본 howto와 직접 겹친다.** `summary-cards-trend-chart-howto`(`blog.ts:358`)가 이미 "중간에 비는 달이 있어도 선은 앞뒤 점을 이어 그려주므로, 몇 달치가 비어도 전체 흐름의 모양은 계속 확인할 수 있습니다", 툴팁 '데이터 없음', '거래 없음'까지 서술한다. 07-29 A-1(인접 글과 중복 7건)과 **정확히 같은 실패 형태**를 재생산할 위험이 가장 큰 후보다.
2. 남는 진짜 고유분은 **세로축 스케일 착시 + 곡선 보간** 2건뿐이라 원칙 6개짜리 글을 지탱하지 못한다. → **후속 소재로 보존**(§8-1). 후보 A 발행 후 축이 겹치지 않는다(A=기간, B=시각 표현).

#### 🥉 후보 C(탈락) — "값이 없다"는 표시들을 읽는 원칙(결측 표기 횡단 정리)

사이트 전역에 결측 표기가 8종 흩어져 있다: '수집 중'(`SummaryCards.tsx:43`), '데이터 없음'(`DealsTable.tsx:128`·`165`, `RankingTable`), '거래 없음'(`MonthlyChart.tsx:109`), '집계 준비 중'(`PendingRegions`), '측정불가'·'도보 미확인'(추천 카드), '반경 1km 내 없음', '단지 상세 정보가 등록되어 있지 않습니다'.

**탈락 사유**: **명제 중복이 구조적으로 최대**다. 각 표기의 의미를 이미 4편이 자기 화면 맥락에서 한 번씩 서술했다 — `location-info` 원칙 5(`blog.ts:634` "'없음'으로 나오는 것은 오류도, 나쁜 신호도 아니다"), `ranking-board` 원칙 6(`:1101` '집계 준비 중'), `regional` 원칙 6(`:550` 빈 달·'거래 없음'), `summary-cards howto`(`:342`·`:376` '수집 중'), `ai-recommend howto`(`:470` '측정불가'). 횡단 정리는 정의상 **인접 글 전부와 접하는** 구조라, 15자 게이트를 UI 문자열 예외(②)에 계속 기대야 해서 예외 남발이 된다. 07-29 N-2가 경고한 "규칙이 규칙과 모순되는" 구조에 스스로 걸어 들어가는 선택이다.

### 1-3. 결론

**후보 A를 추천한다.** 고유 사실 7건 전부가 기존 14편 본문에 0건이고, 기존 insight 3편의 축(단위/비가격지표/순위형식)과 겹치지 않는 **네 번째 축(기간)**을 연다.

---

## 2. slug · 제목 후보

| 항목 | 값 |
|---|---|
| **slug(추천)** | `aggregation-period-reading-principles` |
| slug 대안 | `comparison-period-reading-principles` / `data-window-reading-principles` |
| **제목(추천, (c)안)** | **"'전월 대비'는 정말 지난달과 비교한 값일까 — 실거래가 통계의 집계 기간을 오해 없이 읽는 원칙"** |
| 제목 (a)안 | "같은 표에 있어도 세는 기간은 다르다 — 실거래가 통계의 집계 기간을 오해 없이 읽는 원칙" |
| 제목 (b)안 | "평균가와 거래건수는 같은 기간을 세지 않는다 — 집계 기간을 오해 없이 읽는 원칙" |
| category | `insight` |
| publishedAt | `2026-08-01` |
| 사용 섹션 타입 | `paragraph` / `heading` / `list` / `callout`(warning 1개). **`sourceNote`·`sourceLinks` 미사용**(수치 인용 0건, 발행본 insight 3편 전부 미사용과 동일) |

**slug 명명 근거**: 기존 insight 3편이 전부 `*-reading-principles`로 끝나 시리즈 식별자로 굳어 있다(`regional-price-difference-` / `location-info-` / `ranking-board-`). 이 접미를 유지한다.

**제목 (c)안을 추천하는 이유**: '전월 대비'는 화면 라벨(`SummaryCards.tsx:41`, 라벨 텍스트 `{label} 대비`)이자 부동산 기사에서 가장 흔한 검색 표현이라 검색 의도와 직결된다. 의문형이라 "그럼 뭐지"라는 클릭 동기가 생기고, 본문 원칙 4가 그 질문에 정확히 답한다. (a)안은 안전하지만 검색 키워드가 약하고, (b)안은 **모집단 차이(신규/갱신)** 를 다룬 `jeonse-new-vs-renewal-contract-guide`와 제목만 보고 혼동될 소지가 있다(§4-1의 최대 위험 지점과 같은 지점).

> **⚠️ 자수는 이 문서에서 단정하지 않는다**(07-29 B-2 재발 방지). summary 문안 확정 후 개발팀이 PowerShell `("<문안>").Length`로 실측하고, 기존 최장 summary(07-29 QA 실측 기준 `ai-recommend-weights-howto` 116자)와 대조해 목록 카드 `md:line-clamp-2` 잘림 위험이 없는지 확인한다. **기억이나 다른 문서의 인용값을 옮겨 적지 않는다.**

### summary 문안(초안, 실측 전)

> "아파트 목록 표의 평균가와 거래건수, 위쪽 변동률 카드, 단지 상세가 각각 어떤 기간을 세고 있는지와, 라벨에 적힌 기간이 실제 비교 구간과 달라질 수 있는 경우를 정리했습니다."

---

## 3. 섹션 구조 (`lib/blog.ts`의 실제 `BlogSection` 타입명 표기)

관례 준수: `[0]`=도입 `paragraph`, `[1]`=면책 `callout`(`lib/blog.ts:60` 주석). `DISCLAIMER` 상수(`:64-65`) 재사용.
**`heading` 블록은 [2][4][6][8][10][12][14][16][18] = 9개.** (발행본 `ranking-board-reading-principles`와 같은 형태: 범위 1 + 원칙 6 + 마무리 1 + 정리 1)
총 블록 20개.

| # | `type` | 요지 / 담을 내용 |
|---|---|---|
| **[0]** | `paragraph` | **도입.** 이 사이트의 화면에는 기간이 적힌 라벨이 여러 개 있고(평균가 옆 괄호, 거래건수 옆 괄호, 변동률 카드의 '전월'·'3개월'), 얼핏 보면 전부 같은 자료를 다른 각도로 자른 것처럼 보인다. 그러나 **숫자마다 세는 기간이 따로 정해져 있고, 라벨에 적힌 기간과 실제로 견준 구간이 어긋나는 경우도 있다.** 이 글은 어느 지역·단지가 어떻다는 이야기가 아니라, 화면의 숫자를 비교하기 전에 기간부터 맞춰 보는 방법을 정리한 해석 가이드다. |
| **[1]** | `callout` (`variant: "warning"`) | `DISCLAIMER` 상수 **그대로**(발행본 insight 3편 전부 이 형태 — `blog.ts:490`·`:586`·`:1045`. 앞에 덧붙이는 구절 없음) |
| **[2]** | `heading` | 이 글이 다루는 것과 다루지 않는 것 |
| **[3]** | `paragraph` | **범위 선언.** ① 특정 지역·단지를 평가하지 않고 실제 순위·금액·건수도 인용하지 않는다. ② **화면을 조작하는 방법은 다루지 않는다** — 어느 버튼을 어떤 순서로 누르는지는 사용법 글 지칭. ③ 🔴 **가장 중요** — 이 글이 다루는 것은 **'기간'의 차이뿐**이고, 같은 표에서 함께 문제가 되는 **'모집단'의 차이(전세 평균은 신규 계약만, 건수는 갱신 포함)는 이 글의 주제가 아니며** 전세 계약 구분을 다룬 글에 정리돼 있다고 지칭한다. ④ 표본이 적을 때 평균이 흔들리는 문제도 지역 데이터를 읽는 원칙 글 지칭으로 끊는다. <br>⚠️ **07-29 교훈 3 적용**: 이 선언은 장식이 아니라 집필 제약이다. 아래 [5]~[17] 어느 블록에서도 모집단·표본 크기를 **설명하지 않는다**(지칭만). QA는 선언과 본문을 대조한다(§7 QA-4). |
| **[4]** | `heading` | 원칙 1 — 숫자마다 '세는 기간'이 따로 붙어 있다 |
| **[5]** | `paragraph` | 아파트 목록 표의 열 이름에는 괄호로 기간이 적혀 있다 — 평균가 열은 **'최근 거래 3개월'**, 거래건수 열은 **'13개월'**(UI 문자열 축자 인용, §5-2 참조). 즉 **같은 행에 나란히 놓인 두 값이 서로 다른 길이의 기간을 세고 있다.** 건수가 많은 단지라고 해서 그 건수가 전부 평균 계산에 들어간 것이 아니고, 반대로 평균가가 최근 값이라고 해서 옆의 건수도 그 기간의 건수인 것이 아니다. **표를 읽는 첫 동작은 열 이름의 괄호를 확인하는 것**이다. |
| **[6]** | `heading` | 원칙 2 — '최근 거래 3개월'은 달력의 최근 3개월이 아니다 |
| **[7]** | `paragraph` | 이 사이트는 최근 석 달을 달력으로 잘라 오지 않고, **그 단지에서 거래가 실제로 있었던 달만 최근 쪽에서 세 개** 골라 평균을 낸다. 거래가 꾸준한 단지라면 결과적으로 최근 석 달과 비슷해지지만, 거래가 뜸한 단지라면 그 세 달이 훨씬 과거까지 흩어져 있을 수 있다. 그래서 **같은 표에 있는 두 단지의 평균가가 같은 기간을 대표한다는 보장이 없다.** 각 행의 '최근거래월' 열이 그 세 달 가운데 가장 늦은 달이므로, 평균가를 견주기 전에 이 열부터 맞춰 보는 것이 순서다. |
| **[8]** | `heading` | 원칙 3 — '직전 거래월 대비'의 두 달은 나란히 붙어 있지 않을 수 있다 |
| **[9]** | `paragraph` | 목록 표의 변동 배지는 **거래가 있었던 최근 두 달을 견준 값**이다. 두 달이 실제로 이웃한 달일 수도 있고, 사이에 거래가 없는 달이 여러 개 끼어 있을 수도 있다. **열 이름이 '직전 거래월 대비'인 이유가 이것** — '전월'이 아니라 '직전 거래월'이다. 따라서 이 값은 단지마다 **잰 구간의 길이 자체가 다른 값**이라, 두 단지의 배지를 나란히 놓고 어느 쪽이 더 많이 움직였다고 읽는 것은 서로 다른 자로 잰 값을 견주는 셈이다. |
| **[10]** | `heading` | 원칙 4 — 위쪽 변동률 카드의 기간 라벨도 실제 비교 구간과 다를 수 있다 |
| **[11]** | `paragraph` | 화면 위쪽 카드는 '전월 대비'·'3개월 대비'·'6개월 대비'·'1년 대비'로 라벨이 붙어 있다. 계산은 **먼저 달력으로 그만큼 거슬러 올라간 달을 찾고, 그 달에 값이 없으면 거기서 더 과거 쪽으로 내려가며 값이 있는 가장 가까운 달을 대신 쓰는** 방식이다. 대신 쓰더라도 **라벨은 그대로** '3개월 대비'로 남는다. 그래서 거래가 뜸한 지역에서는 라벨이 가리키는 기간보다 더 긴 구간을 잰 값이 표시될 수 있다. 대신 쓸 달조차 없으면 숫자 대신 '수집 중'이 표시되는데, 그 표시의 의미는 지역 데이터를 읽는 원칙 글에 정리돼 있다(**지칭으로 끊고 재설명하지 않는다 — §4-1 위험 #2**). |
| **[12]** | `heading` | 원칙 5 — 같은 사이트라도 화면마다 빈 달을 다루는 규칙이 다르다 |
| **[13]** | `paragraph` | 위의 대신 쓰는 방식은 **지역 대시보드의 변동률 카드에만** 해당한다. 전국 순위 화면의 변화율은 규칙이 반대여서, 견줄 달이 비어 있으면 대신 채우지 않고 그 칸을 비워 둔다(그 규칙과 이유는 순위표를 읽는 원칙 글에서 다뤘다 — **지칭으로 끊는다**). 두 화면이 같은 '변동'이라는 말을 쓰지만 **만들어지는 방식이 다르므로, 한쪽 화면의 값을 다른 화면의 값과 같은 것으로 여기고 옮겨 적으면 어긋난다.** 화면을 옮길 때는 그 화면이 무엇을 견준 값인지 다시 확인하는 것이 안전하다. |
| **[14]** | `heading` | 원칙 6 — 기준이 되는 '가장 최근 달'은 지역마다 다르다 |
| **[15]** | `paragraph` | 현재 평균가 카드의 라벨 옆 괄호에는 그 값이 **어느 달 기준인지**가 적힌다. 이 달은 달력상 이번 달이 아니라 **그 지역에서 평균이 실제로 잡히는 가장 최근 달**이다. 지역마다 자료가 채워지는 속도가 달라 이 달이 서로 다를 수 있고, 그러면 **두 지역의 '현재 평균가'는 같은 달의 값이 아니다.** 변동률 카드 넉 장도 모두 이 달을 기준점으로 삼으므로, 기준이 되는 달이 다르면 변동률끼리도 같은 조건에서 비교한 값이 아니게 된다. 지역을 견주기 전에 괄호 안 달부터 맞춰 보는 것이 순서다. |
| **[16]** | `heading` | 원칙 7 — 단지 상세는 또 다른 기간을 쓴다 |
| **[17]** | `paragraph` | 건물명을 눌러 열리는 상세 화면 위쪽에는 **두 기간이 나란히 적혀 있다**(전체 건수 쪽 기간과 평균 쪽 기간). 그 아래 평수별 표의 평균가·거래건수·비율은 **둘 중 넓은 쪽 기간**을 기준으로 계산된다. 그래서 **목록 표에 보이던 그 단지의 평균가와 상세 화면 평수별 평균가는 애초에 같은 기간의 값이 아니고, 평수별 값들을 이리저리 합쳐도 목록 표의 값으로 떨어지지 않는다.** 이는 어긋난 것이 아니라 각 표가 답하는 질문이 다르기 때문이다 — 하나는 최근 거래 흐름, 다른 하나는 이 단지에서 어떤 평형이 얼마나 거래됐는지다. |
| **[18]** | `heading` | 기간이 다른 값끼리는 빼거나 나누지 않는다 |
| **[19]** | `list` (`ordered: false`, 6항목) | **정리 체크리스트.** ① 숫자를 견주기 전에 **열 이름·라벨의 괄호에 적힌 기간**부터 본다. ② 평균가 쪽 기간은 달력이 아니라 **거래가 있었던 달** 기준이라 단지마다 실제로 덮는 기간이 다르다. ③ 목록 표의 변동 배지는 **직전 '거래월'** 대비이지 직전 달 대비가 아니다. ④ 위쪽 변동률 카드는 견줄 달이 비면 더 과거의 달로 **대신 계산하고 라벨은 그대로** 두므로, 라벨의 기간을 실제 구간으로 단정하지 않는다. ⑤ 화면이 다르면 규칙도 다르다 — **다른 화면의 값을 같은 값으로 옮겨 적지 않는다.** ⑥ 가장 최근 달은 신고 기한(계약일로부터 30일) 때문에 아직 다 반영되지 않아 값이 달라질 수 있다(**공통 의무 고지 — `content-strategy.md:72`. 기존 편과 축자 일치는 게이트 예외 ③, 결함 아님**). |

### 본문에 넣지 않기로 한 것 (개발팀이 임의로 추가하지 말 것)

1. **실제 지역명·단지명·금액·변동률·거래건수 값** — insight 3편 관행 승계(발행본 3편 전부 실제 지역명 0건).
2. **썩는 정보** — `monthly_stats` 행수, 등록 시·군·구 수, 수집 진행률·커버리지, 표 최대 높이(px), 브레이크포인트 수치. §6 참조.
3. **모집단(신규/갱신) 설명** — [3]에서 범위 밖으로 선언했으므로 본문 어디에서도 설명하지 않는다. 지칭만(`jeonse-new-vs-renewal-contract-guide`).
4. **표본이 적으면 평균이 흔들린다는 해석** — `regional-price-difference-reading-principles` 몫. 지칭으로만.
5. **'수집 중'·'데이터 없음'이 무슨 뜻인지에 대한 설명** — 기존 4편이 이미 서술. 이 글은 "그 자리에 그 표시가 뜬다"는 사실까지만.
6. **그래프의 선 모양·세로축 이야기** — 후보 B의 소재이므로 이 편에서 선점하지 않는다(§8-1).
7. **"곧 채워집니다" 류 시점 약속** — 07-29 확립 제약. 이 글은 "지금은 이렇게 계산된다"는 현재형만 쓴다.
8. **기간 규칙이 잘못됐다는 평가·개선 제안** — 사실 서술까지만. 어느 방식이 옳다고 쓰지 않는다.

---

## 4. 중복 회피 근거 (기존 14편 전수 대조)

측정 기준(07-29 N-2 확립 규칙 그대로 승계): **명제(사실 주장) 기준 연속 15자 이상 축자 일치 0건.** 예외 3종은 제외 — ① 내부링크 정형구, ② UI 문자열 축자 인용, ③ 공통 면책·신고기한 고지.

### 4-1. 🔴 위험 지점 사전 지목 (여기가 이번 편 중복 리스크의 전부다)

| # | 위험 | 인접 발행본 위치 | **재사용 금지 문구(축자)** | 이 글의 회피 설계 |
|---|---|---|---|---|
| **#1** 🔴 최고 | **모집단 차이와 기간 차이의 혼동** — "평균에 쓰인 건수 ≠ 표시된 건수"라는 결론이 같아 보인다 | `jeonse-new-vs-renewal-contract-guide` `blog.ts:858` | "평균가의 집계 대상(신규만)과 건수의 집계 대상이 같지 않아, 건수가 많아도 평균 계산에 실제로 쓰인 건은 그보다 적을 수 있습니다." | [3]에서 **범위 밖 선언 + 지칭**, [5]는 "서로 다른 **길이의 기간**을 센다"로 축을 기간에 고정. '신규'·'갱신'·'모집단' 단어를 본문에서 **0회** 사용 |
| **#2** 🔴 | **'수집 중' 재설명** | `regional` `:534` / `summary-cards howto` `:342`·`:376` (3곳 동일 취지) | "이는 오류가 아니라 그 기간을 계산할 만큼의 거래가 아직 반영되지 않았다는 뜻입니다" (`:534`·`:342` 거의 동일) | [11]에서 **표시 사실 1구절 + 지칭**으로 끊는다. "오류가 아니라"로 시작하는 문장 자체를 쓰지 않는다 |
| **#3** 🔴 | **랭킹 변화율의 빈 달 규칙** | `ranking-board-reading-principles` `:1093` | "직전 달 자료가 없으면 더 앞선 달을 대신 가져오지 않고 '데이터 없음'으로 비워 둡니다" | [13]은 **주어를 대시보드로 잡고**, 랭킹 쪽은 "규칙이 반대다 + 그 글에서 다뤘다"로만. "더 앞선 달을 대신 가져오"·"직전 달 자료가 없으면" 어구 **금지**, 대체 표현: "견줄 달이 비어 있으면 대신 채우지 않고" |
| **#4** | **기준월이 다를 수 있다** | `ranking-board` `:1093` 후반 | "두 지역의 변화율이 같은 두 달을 견준 값이라고 단정할 수는 없습니다" / "화면 맨 위에 적힌 기준월은 전체를 대표해 표시한 가장 최근 달이지" | [15]는 **대시보드 요약 카드**(다른 화면)를 대상으로 하고, '기준월'이라는 단어 대신 화면 표기 그대로 "라벨 옆 괄호 안 달"로 지칭. 랭킹 표의 기준월 열은 언급하지 않는다 |
| **#5** | **표본이 적으면 평균이 흔들린다** | `regional` `:514`·`:518` / `apartment-detail howto` `:296` / `ai-recommend howto` `:469` (4곳) | **"소수의 거래에 값이 좌우될 수 있습니다"(18자)** | 본문 전체에서 **표본 크기 논의 금지**([3] 선언). 이 문구·유사 변형 사용 시 즉시 FAIL |
| **#6** | **목록 표 열 구성 나열** | `apartment-detail howto` `:261` | "표에는 건물명, 연식, 평균가, 전월대비 변동, 거래건수, 최근거래월이 열로 표시되며" | [5]는 열을 **나열하지 않고** 평균가·거래건수 **두 열의 괄호**만 짚는다. ⚠️ 아래 4-3의 라벨 불일치 문제도 여기서 발생 |
| **#7** | **평수별 표 대비 목록 표 평균** | `apartment-detail howto` `:269` | "목록 표의 '평균가'는 단지 전체 평균이라 큰 평수와 작은 평수가 섞여 있는데" | [17]은 **평형 구성(섞임)이 아니라 기간**이 다르다는 점만. '섞인다'·'큰 평수와 작은 평수' 어휘 금지(regional `:526`과도 겹친다) |
| **#8** | 카드 라벨 옆 괄호 월 표기 | `summary-cards howto` `:334` | "라벨 옆 괄호 안에 그 값이 어느 달 기준인지가 '2026.06' 같은 형식으로 함께 적힙니다" | [15]는 **예시 형식('2026.06')을 쓰지 않고**, 괄호가 있다는 사실 + "그 달이 지역마다 다르다"는 고유 명제로 바로 넘어간다 |

### 4-2. 나머지 편 대조 결과

| 대조 대상 | 접점 | 판정 |
|---|---|---|
| `location-info-reading-principles`(insight) | 없음 — 위치·생활여건 지표는 기간 개념이 없다 | 겹침 0 |
| `real-price-report-deadline-guide`(guide) | 신고 기한 30일 | [19]-⑥ **공통 의무 고지 1회**(게이트 예외 ③). 본문에서 제도 설명 재서술 금지, 필요하면 지칭 |
| `exclusive-vs-supply-area-guide` / `molit-system-vs-dashboard-guide`(guide) | `:212`가 "최근 13개월 추이"를 이미 언급 | 그 글은 **서비스 비교** 맥락, 이 글은 **열 라벨** 맥락. 축자 회피만 확인 |
| `ranking-board-usage-howto`(howto) | 조작 순서 전담 | [3]에서 지칭으로 분리. 이 글은 버튼·순서 서술 0건 |
| `ai-recommend-weights-howto`(howto) | 추천 카드 평균가 | 언급하지 않는다(추천 화면은 이 글 범위 밖) |
| market 3편 | 시점·지역 브리핑 | 소재 0 접점. 단 축자 위험 어구 **"완결된 최근 12개월"**(`:963`·`:993`), **"며칠 사이에도 달라질 수 있어"**(`:935`)는 사용 금지 |

### 4-3. ⚠️ 부수 발견 — 발행본 2편의 열 라벨이 현재 화면과 어긋난다 (마스터 판단 필요)

본문 대조 중 확인했다. **현재 코드의 목록 표 열 라벨은 `평균 매매가(최근 거래 3개월)` · `직전 거래월 대비` · `거래건수(13개월)`**(`DealsTable.tsx:26-33`·`:85-88`)인데, 발행본 2편은 옛 라벨을 쓰고 있다:

- `apartment-detail-view-howto` `blog.ts:261` — "평균가, **전월대비 변동**, 거래건수, 최근거래월이 열로 표시되며"
- `summary-cards-trend-chart-howto` `blog.ts:350` — "같은 규칙이 아파트 목록 표의 **'전월대비'** 배지에도 동일하게 적용됩니다."

이번 편이 현재 라벨('직전 거래월 대비')을 인용하면 **같은 사이트의 세 글이 같은 화면을 다르게 부르게 된다.** 기획팀은 코드를 고치지 않으므로 처리 방향만 §8-2의 미결 항목 3번으로 올린다.

### 4-4. 결론

소재 중복 없음. 인접 명제 8건은 전부 **범위 선언([3]) + 지칭 + 축자 금지어 목록**으로 사전 차단했다. 07-29 A-1과 달리, 이번에는 인접 글의 **선언이 아니라 본문 `text` 값을 근거로** 위험 지점을 지목했다(§4-1 표의 인용문은 전부 `lib/blog.ts` 실제 문자열).

---

## 5. `content-strategy.md` §4 리스크 체크리스트 적용표

| §4 항목 | 이 글의 회피 방식 |
|---|---|
| "지금 사야 한다", "매수 타이밍", "오를 것이다/떨어질 것이다" 등 **미래 예측·단정** | **통과.** 본문 시제가 전부 계산 규칙 설명(현재형). 미래형이 들어갈 수 있는 유일한 자리였던 "자료가 곧 채워진다"를 §3 배제 목록 7로 **명시 금지**. 가격의 방향(오름/내림)을 서술하는 문장이 0건 |
| "추천 단지", "유망 지역" 등 **우위 매기기** | **통과.** 지역명·단지명 0건. 이 글의 논지는 오히려 "두 값을 견주려면 조건을 맞춰야 한다"는 **비교 억제** 방향이라 금지 취지에 정면 부합 |
| "저평가/고평가" 등 **가치 판단** | **통과.** 사용 0건. 값의 크기를 논하지 않고 "어느 기간을 센 값인가"만 다룬다 |
| "수익률", "투자가치", "시세차익" | **통과.** 사용 0건 |
| **외부 수치 인용 금지**(사이트가 계산하지 않은 값) | **해당 없음.** 외부 소스 0건, `sourceLinks` 미사용. 본문의 모든 사실은 이 사이트 코드가 실제로 하는 계산에 대한 서술 |
| 수치 인용 시 **"국토부 공공데이터 기반, 조회 기준일" 표기** | **해당 없음** — 실거래 수치 인용 0건이라 `sourceNote` 미사용(발행본 insight 3편과 동일). 출처 도달 경로는 기존 화면 하단 표기로 유지 |
| **"최근 달은 신고 지연" 반복 고지** | **적용.** [19]-⑥. `content-strategy.md:72` 의무 고지이며 기존 편과 축자 일치는 **정상**(게이트 예외 ③) |
| **면책 콜아웃 위치**(도입부 직후 1개) | **적용.** [1] `callout`(warning) + 상세 하단 고정 면책(`app/blog/[slug]/page.tsx`) = 총 2회. 수치 인용이 없어 **본문 중간 추가 콜아웃은 해당 없음**(`content-strategy.md` §4 "insight는 수치 인용 직후 1개 추가" 조항의 전제가 성립하지 않음 — 발행본 insight 3편도 동일하게 warning 1개만 둔다: `blog.ts:490`·`:586`·`:1045`) |
| 특정 단지·매물 실명 | **통과.** 0건 |
| 톤 승계(about/terms의 "참고용, 투자자문 아님") | **적용.** 존댓말·기존 insight 3편 문체 그대로 |

### 잔여 리스크와 완화

1. **(중간) "이 사이트 숫자는 못 믿는다"로 읽힐 위험.** 이번 편은 자사 계산의 한계를 7개나 나열한다. → 각 원칙의 마무리를 **"틀렸다"가 아니라 "이렇게 맞춰 보면 된다"** 는 행동 지침으로 닫는다([5]·[7]·[15]의 "…부터 확인하는 것이 순서다"). [17]은 명시적으로 "어긋난 것이 아니라 각 표가 답하는 질문이 다르다"로 정리한다.
2. **(중간) [3] 범위 선언과 본문이 어긋날 위험** — 07-29 A-1의 재발 경로. → QA 항목으로 승격(§7 QA-4): '신규'·'갱신'·'모집단'·'소수의 거래' 어휘를 본문 전문 검색해 0건인지 확인.
3. **(중간) 라벨 인용이 사양 변경으로 낡을 위험.** 열 라벨은 실제로 한 차례 바뀐 전력이 있다(§4-3). → 본문이 인용하는 UI 문자열을 [5]·[9]에 **집중**시켜 영향 범위를 두 문단으로 묶는다. §8-2 미결 2번과 연동.
4. **(낮음) 기간 숫자('13개월'·'3개월')가 구현 상세로 읽힐 위험** — `blog-ranking-board-usage-howto.md` §3 배제 목록 3이 "집계 창 개월 수"를 금지한 선례가 있다. 다만 이번 편이 인용하는 값은 **화면 라벨에 이미 찍혀 있는 문자열**이라 성격이 다르다(랭킹 편은 화면에 없는 내부 값이 문제였다). → §8-2 미결 2번으로 마스터 확정 요청.
5. **(낮음) 원칙 4의 폐이지 확장 사례** — `getAvgAt` 폴백은 극단적으로는 **기준 달과 비교 대상 달이 같아져 변동률이 0%로 보이는** 경우까지 만들 수 있다(`analyzer.ts:273-286` — 지역의 최신 자료 달이 목표 달보다 과거일 때). 독자 효용은 크지만 **결함 보고로 읽힐 소지**가 있어 기획팀이 임의 결정하지 않는다 → §8-2 미결 4번.

---

## 6. 썩는 정보 배제

본문에 **넣지 않는다**(오늘자 실측값 일절 인용 금지):

- `monthly_stats` 행수·잔여 결측 셀 수, 등록 시·군·구 개수, 수집/백필 진행률, 커버리지 비율.
- 특정 지역·단지의 평균가·변동률·거래건수·기준월 실측값.
- px·브레이크포인트·표 최대 높이·ISR 재생성 주기 등 구현 상세 수치.
- "현재 N편", "오늘 기준" 같은 시점 표현.

**허용 대상은 화면 라벨에 찍힌 문자열뿐**이며, 그마저 §8-2 미결 2번 확정 후 사용한다. 시점 한정이 꼭 필요한 문장이 생기면 그 문장은 **삭제**한다(insight는 evergreen 카테고리이므로 `market`의 "시점 한정 문장" 패턴을 쓰지 않는다).

---

## 7. 본문 사실의 검증 위치 (QA 재검산표)

**이 편은 실거래가 수치를 한 건도 인용하지 않는다.** 본문의 모든 사실 주장은 아래에서 재검산한다. **표에 없는 사실은 본문에 쓰지 않는다.**

| 본문 | 사실 주장 | 검증 위치(코드) | 라이브 재검산 |
|---|---|---|---|
| [5] | 목록 표 열 라벨에 기간이 괄호로 적혀 있다(`평균 매매가(최근 거래 3개월)`/`평균 전세가(최근 거래 3개월)`, `거래건수(13개월)`) | `DealsTable.tsx:26-33`·`:85-88`·`:118` | 지역 조회 후 아파트 목록 표 머리글 |
| [5] | 평균과 건수의 계산 범위가 실제로 다르다 | `analyzer.ts:392-397`(`recent`) vs `:481-483`(`count: records.length`) | — |
| [7] | 평균 대상은 "거래가 있었던 최근 세 달" | `analyzer.ts:388-392` | 거래가 뜸한 단지의 '최근거래월'이 최근 달이 아닌지 확인 |
| [7] | '최근거래월' 열이 존재한다 | `DealsTable.tsx:32`·`:189-191` | 표 마지막 열 |
| [9] | 변동 배지 = `recentYms[0]` vs `recentYms[1]` 비교 | `analyzer.ts:405-418` | — |
| [9] | 열 라벨이 '직전 거래월 대비'다 | `DealsTable.tsx:30` (+ 주석 `:24-25`) | 표 머리글 |
| [11] | 카드 라벨 4종 = 전월·3개월·6개월·1년 '대비' | `SummaryCards.tsx:15-16`·`:41` | 조회 직후 상단 카드 |
| [11] | 목표 달이 비면 **더 과거의 가장 가까운 달**로 대체 계산 | `analyzer.ts:277-286` | — |
| [11] | 대체할 달도 없으면 '수집 중' 표시 | `SummaryCards.tsx:42-43` | 자료가 얕은 지역 조회 |
| [13] | 랭킹 화면은 대체하지 않고 변화율을 비운다 | `lib/ranking.ts:99-112` (+ 주석 `:55-58`·`:105-107`) | `/ranking` 표의 '데이터 없음' 행 |
| [15] | 현재 평균가 라벨 옆 괄호에 달이 표시된다 | `SummaryCards.tsx:64-66`·`:77-81` | 상단 첫 카드 |
| [15] | 그 달 = 평균이 잡히는 가장 최근 달(달력상 당월이 아닐 수 있다) | `analyzer.ts:273-275` | 자료가 뒤처진 지역에서 괄호 월 확인 |
| [15] | 변동률 4종이 모두 이 달을 기준점으로 삼는다 | `analyzer.ts:288-305`(`currentAvg` 고정) | — |
| [17] | 상세 화면 위쪽에 두 기간이 함께 적힌다 | `AptDetailModal.tsx:332-337` (+ 주석 `:324-325`) | 건물명 클릭 후 제목 아래 meta 줄 |
| [17] | 평수별 표의 평균가·건수·비율은 넓은 쪽 기간 기준 | `analyzer.ts:421-453`(`areaBuckets`가 `records` 순회), `AptDetailModal.tsx:339-346` | 평수별 표 |

> **집필 규칙**: 분기의 else 쪽(값이 없을 때 화면이 어디로 떨어지는가)은 반드시 원문 코드를 끝까지 따라간 뒤 쓴다 — 2026-07-26 insight 편 QA FAIL이 정확히 그 지점이었다.

### QA 검증 항목

1. **축자 대조** — 발행본 14편과 **명제 기준 연속 15자 이상 일치 0건**. 예외 ①②③. **QA 독립 측정이 정본**(개발팀 보고치 아님, 07-29 실측 교훈).
2. **§4-1 금지 문구 전문 검색** — 8건의 "재사용 금지 문구" 열이 본문에 0건인지.
3. **범위 선언 이행** — '신규'·'갱신'·'모집단'·'소수의 거래'·'표본' 어휘 본문 0건인지([3] 선언과 본문 대조, 07-29 교훈 3).
4. **썩는 정보 0건** — 지역명·금액·건수·행수·px·진행률.
5. **시점 약속 0건** — "곧", "다음 갱신에", "수집이 끝나면".
6. **UI 문자열 인용이 화면과 정확히 일치**하는지(§7 표의 라이브 열). 특히 '직전 거래월 대비'가 라이브에 실제로 찍혀 있는지 **눈으로 확인**.
7. `heading` **9개** = 빌드 산출 HTML `<h2>` 9개, 총 블록 20개.
8. summary 자수 실측치가 기존 최장(116자, 07-29 QA 실측) 이하인지.
9. 기존 14편 라인 무수정, `npm run build`에서 `/blog/[slug]`가 **15편**인지.

---

## 8. 내부링크 / 후속 / 마스터 확정이 필요한 미결 항목

### 8-1. 내부링크 계획 (텍스트 지칭 — `BlogSection`에 인라인 링크 타입 없음, `blog.ts:31-47`)

| 위치 | 참조 대상 | 처리 문안(요지) |
|---|---|---|
| [3] | `ranking-board-usage-howto` 또는 사용법 글 총칭 | "버튼을 누르는 순서는 사용법을 다룬 글에서 따로 정리했습니다." |
| [3]·[5] | `jeonse-new-vs-renewal-contract-guide` | "어떤 계약이 평균에 들어가는지는 전세 계약 구분을 다룬 글에서 설명했습니다." |
| [3]·[11] | `regional-price-difference-reading-principles` | "'수집 중' 표시의 뜻과, 거래가 적을 때 평균이 어떻게 되는지는 지역 데이터를 읽는 원칙 글에 정리돼 있습니다." |
| [13] | `ranking-board-reading-principles` | "전국 순위 화면의 변화율 규칙은 순위표를 읽는 원칙 글에서 다뤘습니다." |
| [17] | `apartment-detail-view-howto` | "상세 화면을 여는 방법은 따로 정리한 글이 있습니다." |

지칭 5곳. 관용구는 **정형성은 유지하되 문구를 매번 똑같이 쓰지 않는다**(07-29 §6-1 주 — 게이트 예외 ①의 취지). 카테고리 라우트 URL은 **0건**(이 사이트에는 `/blog/insight` 같은 라우트가 없다 — 클라이언트 필터 방식, 07-29 §6-0).

### 8-2. 🔴 마스터 확정이 필요한 미결 항목

1. **주제·slug·제목 승인** — 후보 A 채택 여부, slug `aggregation-period-reading-principles`, 제목 (a)/(b)/**(c)** 중 택 1. (기획팀 권고: (c))
2. **UI 라벨 안의 기간 숫자를 본문에 인용해도 되는가** — `'최근 거래 3개월'`·`'13개월'`은 화면에 찍힌 문자열이지만, `blog-ranking-board-usage-howto.md` §3 배제 목록 3이 "집계 창 개월 수"를 금지한 선례가 있다. **(가) 라벨 그대로 인용** / (나) 숫자를 빼고 "괄호에 적힌 기간"으로만 서술. 기획팀 권고는 **(가)** — 숫자를 빼면 원칙 1·2가 성립하지 않는다. 단 마스터가 (나)를 택하면 [5]·[19]-①을 그에 맞춰 재작성한다.
3. **발행본 2편의 옛 열 라벨 정정을 어떻게 처리할지**(§4-3) — 현재 화면은 '직전 거래월 대비'인데 `blog.ts:261`·`:350`이 '전월대비'로 남아 있다. **(가) 이번 라운드에 함께 정정**(기존 편 2줄 수정 → 07-29 이후 "기존 편 라인 무수정" 원칙의 예외 승인 필요) / (나) **별도 티켓으로 분리**하고 이번 편은 현재 라벨을 그대로 인용 / (다) 이번 편이 라벨 인용을 피해 충돌을 미룬다. 기획팀 권고는 **(나)** — 라벨 정정은 사실 정정이라 이번 편과 독립적으로 필요하고, 이번 편을 볼모로 잡을 이유가 없다.
4. **원칙 4의 극단 사례(변동률이 0%로 보일 수 있는 경우)를 본문에 넣을지**(§5 잔여 리스크 5) — 독자 효용은 크지만 결함 보고로 읽힐 수 있고, 개발팀이 폴백 규칙 자체를 손볼 여지도 있다. **(가) 본문에 원칙으로 서술** / (나) 본문에서 빼고 **개발팀 확인 티켓**으로 넘김 / (다) 둘 다. 기획팀은 판단하지 않는다.
5. **원칙 5(화면 간 규칙 차이, [12]·[13])를 남길지** — 자사 두 화면의 계산 규칙이 다르다는 것을 스스로 밝히는 서술이다. 신뢰도 자산이라는 판단(`content-strategy.md` §7-2 "불일치는 최대 자산")과, 사용자에게 혼란을 준다는 판단이 갈릴 수 있어 마스터 확정을 요청한다.
6. **내부링크 지칭 5곳 승인**(§8-1) — 발행본 insight 3편은 지칭이 0~3곳이었다. 5곳은 이 시리즈 최다이므로 개수 승인이 필요하다. 줄여야 한다면 기획팀 권고 우선순위는 #2(전세 계약 구분) > #3(지역 데이터 원칙) > #4(순위표 원칙) 순으로 유지, #1·#5는 삭제 가능.
7. **발행일** — `2026-08-01`로 확정할지(07-29 이후 라운드 공백이 있어 발행 간격이 벌어졌다).
8. **[참고, 결정 불요] 후속 소재 예약** — 후보 B(월별 추이 그래프의 선 모양: 세로축이 0에서 시작하지 않는 점, 빈 달 연결선, 곡선 보간)를 다음 insight 5편째 1순위로 남긴다. 이번 편이 기간 축을 가져가므로 축 충돌이 없다.

---

## 9. [2026-08-01 마스터 확정] §8-2 미결 8건 판정

> 이 절은 **구현 착수 전에** 기록했다(07-29 교훈 4 — 정본 개정이 구현보다 늦으면 "미이행 유령"이 생긴다). 개발팀은 §1~§8과 이 절이 충돌하면 **이 절을 따른다.**

| # | 안건 | 확정 |
|---|---|---|
| 1 | 주제·slug·제목 | **승인.** 후보 A / slug `aggregation-period-reading-principles` / 제목 **(c)안**. 단 QA는 제목이 던진 질문("정말 지난달과 비교한 값인가")에 **본문이 실제로 답하는지**를 검증 항목으로 삼는다 — 답하지 않으면 낚시 제목이 된다 |
| 2 | UI 라벨의 기간 숫자 인용 | **(가) 채택, 단 제약 1개.** 숫자는 **화면 라벨을 축자 인용하는 따옴표 안에서만** 나온다(게이트 예외 ②). "이 사이트는 13개월치를 모읍니다" 같은 **독립 사실 주장 형태로 쓰지 않는다** — 수집 창이 바뀌면 즉시 거짓이 되는 썩는 정보이기 때문이다 |
| 3 | 발행본 2편 옛 라벨 | **(가) 이번 라운드에 함께 정정 — 예외 승인.** 기획 권고 (나)를 뒤집는다. 근거: 이건 소재 이월이 아니라 **현재 라이브에 떠 있는 사실 오류**다(실제 라벨은 `직전 거래월 대비`인데 발행본이 `전월대비`로 서술). 이 편이 올바른 라벨을 인용하는 순간 같은 화면을 세 글이 다르게 부르게 된다. **`lib/blog.ts:261`·`:350` 2줄만** 최소 수정 |
| 4 | 원칙 4 극단 사례(0%) | **(나) 본문 제외 + 개발 티켓으로 분리.** 마스터가 코드로 재확인한 결과 이건 "읽는 원칙"이 아니라 **결함**이다(§9-1). 정상 동작인 척 본문에 쓰면 안 된다 |
| 5 | 원칙 5(화면 간 규칙 차이) | **유지 승인.** `content-strategy.md` §7-2("불일치는 최대 자산") 승계 |
| 6 | 내부링크 지칭 | **5곳 → 3곳으로 축소.** #2·#3·#4만 남기고 **#1·#5 삭제**(기획팀이 삭제 가능으로 표시한 둘). 근거: 지칭 정형구가 07-29 축자 잔여의 주원인이었고, 이월 안건 "internalLink 타입 도입"이 해결되기 전까지는 개수를 늘리지 않는다 |
| 7 | 발행일 | **`2026-08-01` 승인** |
| 8 | 후속 소재 B 예약 | **접수.** insight 5편째 1순위로 보존 |

### 9-1. 미결 4번 관련 — 신규 결함 티켓 (본문에 쓰지 말 것)

`lib/analyzer.ts:276-285` `getAvgAt(offset)`은 목표 달에 값이 없으면 `ym <= target` 중 **가장 최근 유효월**로 대체한다. 이 필터는 **`currentYm` 자신을 배제하지 않는다.** 따라서 어떤 지역의 최신 유효월이 목표 달보다 과거이면 `getAvgAt`이 `currentAvg`를 그대로 돌려주고, `pctChange(currentAvg, currentAvg)` = **0**이 된다.

- **화면 결과**: 비교할 과거 달이 **하나도 없는데** '수집 중'이 아니라 **"0.00% ━"**(변동 없음)으로 표시된다. 사용자는 "전월과 같았다"로 읽지만 실제로는 **자기 자신과 비교한 값**이다.
- **재현 조건**: `currentYm < getMonthKey(offset)`. 2026-08-01 현재 `getMonthKey(1)`=`202607`이라, 최신 유효월이 `202606` 이하인 지역·단지에서 바로 발생한다.
- **권고 방향**(개발팀 판단 필요, 이번 라운드 미착수): `older` 필터에서 `ym < currentYm` 조건을 추가하거나, 대체월이 `currentYm`과 같으면 `null`을 돌려 '수집 중'으로 떨어뜨린다.
- ⚠️ **라이브 표시 수치가 바뀌는 변경**이므로 원자료 재조회 검증 + 계산 근거 team-log 기록이 선행돼야 한다.

### 9-2. 기획안 축자 오기 2건 (마스터 정정)

1. **§4-3의 라벨 인용이 틀렸다.** 기획안은 현재 라벨을 `평균 매매가(최근 거래 3개월)`로 적었으나 **실제는 `평균가(최근 거래 3개월)`**(`components/DealsTable.tsx:29`). "매매" 두 글자가 없다. 본문이 이 라벨을 인용할 때 **기획안이 아니라 코드를 보고** 옮겨라.
2. **§1-0의 요일 표기가 틀렸다.** "오늘(금)"으로 적혔으나 2026-08-01은 **토요일**이다. `market` 주 1회(월요일) 규칙에 따른 제외 결론 자체는 영향 없음.

### 9-3. 확정된 범위 — 오늘 건드리는 파일

`lib/blog.ts` **단 1개**. ① 신규 15편째 append, ② `:261`·`:350` 라벨 2줄 정정. 그 외 파일 무접촉(`components/RankingBoard.tsx`는 별건으로 동시 진행 중이므로 **열지 말 것**).

---

## 10. [2026-08-01] §9-2 정정 1번을 다시 정정 — **마스터가 틀렸다**

§9-2에서 마스터는 "기획안 §4-3이 라벨을 `평균 매매가(최근 거래 3개월)`로 적었으나 실제는 `평균가(최근 거래 3개월)`"라고 정정했다. **이 정정이 틀렸다.** 개발팀이 렌더 경로를 끝까지 따라가 뒤집었다.

```tsx
// components/DealsTable.tsx:85-88
const avgLabel =
  dealType === "매매" ? "평균 매매가(최근 거래 3개월)" : "평균 전세가(최근 거래 3개월)";
// components/DealsTable.tsx:118
{col.key === "avg_price" ? avgLabel : col.label}
```

`avg_price` 열은 **항상** `avgLabel`로 그려진다. 따라서 `COLUMNS[:29]`의 `평균가(최근 거래 3개월)`는 **화면에 도달하지 않는 폴백 상수**이고, 라이브 머리글은 탭에 따라 **`평균 매매가(최근 거래 3개월)` 또는 `평균 전세가(최근 거래 3개월)`** 둘 중 하나다.

- **기획안 §4-3의 원 서술이 맞았다**(매매 탭 기준). 마스터의 §9-2 정정 1번은 **폐기**한다.
- 본문 [5]는 개발팀이 **렌더되는 두 라벨을 탭별로 병기**하는 방식으로 구현했다. QA는 이 기준으로 대조해야 한다.
- §9-2 정정 2번(요일 표기 "금"→토요일)은 그대로 유효하다.

### 10-1. 이 실수에서 남길 것

**상수 배열의 `label` 값을 화면 문구의 근거로 쓰지 마라.** 07-29 교훈 2가 "위치를 서술할 땐 컨테이너의 반응형 클래스부터 확인하라"였는데, 이번 건은 같은 실수의 **문구 버전**이다 — `COLUMNS` 선언만 읽고 렌더 지점(`:118`)의 삼항 분기를 보지 않았다. **UI 문구를 인용할 땐 선언이 아니라 JSX가 실제로 출력하는 지점까지 따라가라.** (07-29 교훈 3 "선언이 아니라 본문 전문을 대조"의 코드판이다.)

또한 이번에도 **개발팀 자진 보고가 마스터 판정을 뒤집었다** — 3라운드 연속이다(07-28·07-29·오늘).

## 11. [2026-08-01 마스터 확정] 개발팀 질의 3건

| # | 안건 | 확정 |
|---|---|---|
| (1) | 라벨 정정 재정정 | **개발팀이 옳다.** §10 참조. 본문 [5]의 탭별 병기 방식 **승인** |
| (2) | [2] heading이 insight 3편과 20자 축자 일치 | **원안 유지 승인.** 축자 게이트는 07-29 N-2에서 **"명제(사실 주장) 기준"**으로 재정의됐고, `이 글이 다루는 것과 다루지 않는 것`은 사실 주장이 아니라 **시리즈 구조 라벨**이다. 3편이 같은 라벨을 쓰는 것은 중복이 아니라 **시리즈 일관성**이며, 오히려 편마다 다르게 쓰면 구조 인지를 해친다. 변형하지 마라 |
| (3) | §3 heading 문구 2건 조정([6] `3개월`→`석 달`, [10] `라벨`→`이름`) | **승인.** 둘 다 §9 제약(기간 숫자는 따옴표 안에서만 / §4-1 #8 어휘 회피)을 정확히 적용한 결과다 |

---

## 12. [2026-08-01] QA 반려 — 기획안 §3 [19]-① 원안 결함

QA가 배포 차단 3건을 적출했고 마스터가 코드로 전부 재확인해 **QA가 옳다고 판정**했다. 그중 **F-3은 이 기획안 자체의 결함**이라 여기 남긴다.

### 12-1. 기획안이 만든 오류 (F-3)

§3 [19]-① 원안: *"열 이름·라벨의 괄호에 적힌 기간부터 본다"*

**괄호에 기간이 적히는 것은 목록 표 열 이름뿐이다.** 변동률 카드 이름은 `components/SummaryCards.tsx:41`의 `{label} 대비`라 **괄호가 없고**, 괄호가 붙는 카드는 현재 평균가 카드 하나뿐인데 그 괄호 안은 `(2026.07)` 형태의 **달**이지 기간이 아니다(`:78-81`).

원안이 "열 이름"과 "라벨"을 한 구절로 묶으면서 괄호를 카드 쪽까지 분배해 버렸고, 개발팀이 원안을 따라 쓴 결과 독자가 카드에서 **존재하지 않는 괄호**를 찾게 되는 문장이 됐다. 같은 글의 도입 [1]과 원칙 6 [15]는 이 구분을 정확히 했는데 마무리 체크리스트만 무너진 것도 원안 구조 때문이다.

→ **다음에 체크리스트형 마무리를 쓸 때, 여러 화면 요소를 한 구절로 묶어 수식어를 공유시키지 마라.** 화면마다 실제 표기가 다르면 그 구절은 반드시 일부 화면에서 거짓이 된다.

### 12-2. 나머지 2건은 구현 단계 오류 (기획안 무관)

- **F-1** — 집계 버킷을 "그 달들에 **신고된** 거래"로 썼다. `analyzer.ts:388`의 `ym`은 `molit-api.ts:282-283`의 `dealYear`/`dealMonth`(국토부 `DEAL_YMD`) = **계약 년월**이다. 발행본 `real-price-report-deadline-guide`의 "최근 달은 신고가 진행 중이라 건수가 적다"는 서술 자체가 계약월 버킷일 때만 성립한다.
- **F-2** — "기준 달은 **달력상 이번 달이 아니라**"는 단정이 거짓. `SummaryCards.tsx:68-69`가 `current_ym === getMonthKey(0)`인 경우를 정면으로 상정하고 "신고 진행 중" 배지·부분월 안내를 띄운다.

### 12-3. 교훈

**"계산 규칙을 서술하는 글"은 문장 하나하나가 사실 주장이다.** 이번 3건은 전부 문체·중복이 아니라 **사실 오류**였고, 축자 게이트·금지문구·회귀·빌드·lint를 전부 통과한 상태에서 나왔다. **형식 게이트를 다 통과해도 사실 검증은 별개**라는 것이 이번 라운드의 핵심이다.
