# 인천 `lib/regions.ts` 변경 독립 검증 — QA 보고서 (2026-08-06)

- 검증팀: QA (구현 미관여)
- 대상: 인천 폐지 3개 제거(28110·28140·28260) + 신설 4개 등재(28125·28155·28275·28290)
- 설계 정본: `docs/planning/incheon-reorg-activation-2026-08-06.md` §3-3·§4-2·§4-3·§4-4
- 검증 기준 커밋: **`4818cc7`**

> ℹ️ **이 파일은 QA팀이 아니라 마스터가 저장했다.** QA팀은 하네스 레벨 시스템 프롬프트에 "보고서/요약/분석 `.md`를 Write하지 말고 최종 메시지로 반환하라"는 지시가 있다며 파일 생성을 거부하고 본문만 반환했다(2회 연속). `.claude/settings.json`에는 그런 설정이 없다(권한 allowlist뿐). 측정값·판정은 전부 QA팀 것이다. QA는 저장소를 **무수정**, 커밋·푸시 0, DB 쓰기 0(전부 select).

## 0. 최종 판정 — ✅ **PASS (6/6)** · FAIL 0 / WARN 5 / 미측정 5

## 1. 🔴 먼저: 검증 중 대상이 커밋됐다 (프로세스 사실)

브리핑은 "미커밋"이었으나 검증 도중 **01:39:14에 커밋 `4818cc7`이 생성**됐고, QA의 첫 스냅샷(01:35) 이후 커밋 전까지 코드 파일이 추가 편집됐다(`lib/blog.ts` +13행 → +18행).

→ **QA는 1차 검증을 폐기하고 커밋본 기준으로 전량 재검증했다.** 아래는 전부 `4818cc7` 기준이며, `git diff HEAD -- lib/regions.ts`가 공집합이라 빌드·렌더로 확인한 바이트와 커밋본이 동일함을 확인했다.
*(원인은 동시 세션 실행 — `docs/team-log/2026-08-06.md` 라운드64 사후 추가 §A~C 참고.)*

## 2. 변경 범위 격리 — **PASS**

```
git --no-pager diff --numstat 8656aea HEAD -- '*.ts' '*.tsx'
3   0  app/api/cron/prewarm/route.ts
3   0  components/SummaryCards.tsx
17  1  lib/blog.ts
6   2  lib/monthly-stats-gaps.ts
19  7  lib/regions.ts
```

코드 파일이 5개다(브리핑의 "1개"와 불일치). **나머지 4개가 주석 전용인지 파서로 증명했다** — TypeScript `transpileModule({removeComments:true})` 산출물 해시 대조:

```
lib/blog.ts                   SAME
lib/monthly-stats-gaps.ts     SAME
app/api/cron/prewarm/route.ts SAME
components/SummaryCards.tsx   SAME
lib/regions.ts                DIFF   ← 대조군
```

🔴 **대조군(`regions.ts`)이 DIFF로 나와 이 방법이 실질 변경을 실제로 판별한다는 것까지 확인했다.** 육안 판독이 아니다. `SummaryCards.tsx` 추가분은 `{/* … */}` JSX 주석 내부라 렌더되지 않는다. 4개 전부 설계 §6-3이 "권고 · 같은 커밋"으로 지정한 항목이므로 **스펙 내**다.

**동결 3파일 — PASS.** 커밋에 미포함(`git show --name-only`). 현재도 M 상태이고 **mtime 전부 2026-07-25**(이번 라운드 무관, 내용은 `cdeal_type`/`cdeal_day` 건). **`git add -A` 사고 없음.**

**`lib/blog.ts` 가시 본문 — PASS.** 해시 동일 + diff 17추가/1삭제 전부 `//` 주석.

## 3. 값 정확성 — **PASS**

transpile→require로 **실값을 출력**해 확인했다.

| 항목 | 이전 | 현재 | 기대 |
|---|---|---|---|
| 전국 총 지역수 | 114 | **115** | 115 ✓ |
| 인천 구 수 | 10 | **11** | 11 ✓ |
| `LAWD_TO_SIGUNGU` 키 수 | 114 | **115** | 115 ✓ |

- 인천 삽입순 = 제물포구 28125 → 영종구 28155 → 미추홀구 28177 → 연수구 28185 → 남동구 28200 → 부평구 28237 → 계양구 28245 → 서해구 28275 → 검단구 28290 → 강화군 28710 → 옹진군 28720. **설계 §4-2 표와 11/11 일치**, 코드 오름차순 위반 0.
- 전 9개 시/도 오름차순 위반 0.
- **중복 0** — 객체 리터럴이 중복 키를 조용히 흡수하므로 **원문 텍스트 기준으로도** 재확인(`grep -cE '^\s*"[0-9]{5}":'` → 115, `uniq -d` → 0).
- `LAWD_TO_SIGUNGU` 인천 11키 전부 항등 매핑. 비항등은 경기 17건뿐(변경 전후 동일).
- **양쪽 집합 완전 일치**(한쪽에만 있는 코드 0). 폐지 3개는 **양쪽 모두에서** 소멸.
- 이름↔코드 짝을 정본 `incheon-2026-reorg-codes-2026-08-05.md` §2-1과 대조 → **4/4 일치**.
- §4-4 머리 주석 반영됨, 가리키는 문서 2개 실재(경로 오타 0).

**엣지 입력** `getRegionByLawdCd`: `null`·`undefined`·`""`·`"0"`·`"abc"`·`"2811"`·`"281100"`·`"-28110"`·`"28110.0"`·`" 28110"` → **전부 `null`, throw 0건.**

## 4. 빌드 — **PASS**

```
npx tsc --noEmit   → exit 0
npm run build      → exit 0
✓ Generating static pages using 7 workers (30/30)
```
**정적 페이지 30개 유지 ✓**, 신규 에러·경고 0.

## 5. 회귀: 폐지 코드 딥링크 — **PASS** (설계 §3-3 예측이 참)

- `app/page.tsx:27-31` `resolveDeepLink` → `getRegionByLawdCd("28110")` = **`null`** → `return null`.
- `L40` `initialDeepLink=null` → `L52-54` `status` 초깃값 **`"idle"`**.
- `L122-123` `useEffect`가 `if (!initialDeepLink) return;`로 즉시 종료 → **fetch 0회, 에러·크래시 경로 없음.**
- HTTP 실측: `/`, `/?lawd_cd=28110`, `/?lawd_cd=28125` 모두 200이고 **md5 3개 동일**(27,529B) — 정적 프리렌더라 서버 측 분기 없음.

`lib/ranking.ts:54-55` — **PASS.** RSC 페이로드 파싱 결과 **고유 지역 코드 115개, 폐지 코드 0개.** `monthly_stats`에는 세 코드 행이 실존하는데 화면 페이로드에 없음 → `continue`가 실제로 걸러냄을 입증.

## 6. 로컬 렌더 (`npx next start -p 3117`) — **PASS**

**신설 4개 노출 ✓**
```
인천 제물포구 -> /?lawd_cd=28125     인천 영종구 -> /?lawd_cd=28155
인천 서해구   -> /?lawd_cd=28275     인천 검단구 -> /?lawd_cd=28290
```
실수치 동반(서해구 5억 3,200만원 / 검단구 5억 592만원 / 영종구 3억 3,614만원 / 제물포구 2억 2,460만원).

**폐지 3개 소멸 ✓ — 동명 타지역 오탐을 배제하는 방식으로 판정:**
```
"인천 중구" 0 · "인천 동구" 0 · "인천 서구" 0
[대조군] "부산 중구" 2 · "대전 서구" 1 · "서울 중구" 1   ← 정상 잔존
28110/28140/28260 문자열: /ranking 0 · / 0 · /sitemap.xml 0 · /blog 0
```
단순 `중구` grep이 아니라 `인천 `+구명 결합 및 `sidoShort` 필드 기준 RSC 레코드 파싱으로 확인. 인천 레코드 22건(11구 × 매매/전세) 전부 `sido:"인천광역시"`.

**`PendingRegions` 9 → 6 ✓** — 칩 6개 실측: 광주 광산구·남구·동구·북구·서구 + **인천 옹진군**.
→ 설계 §1-3이 "정체 불명"으로 남긴 잔여 1개는 **옹진군**으로 확정(28720 `monthly_stats` 최근 6행 전부 0/null). **9 = 광주 5 + 인천 폐지 3 + 옹진군 1**이었다. 설계 예측 6과 정확히 일치.

**모바일**: 데이터 전용 변경이라 레이아웃 클래스 변경 0. 신규 이름 최장 **4자(제물포구)** = 기존 최장(미추홀구)과 동일 → 줄바꿈/오버플로 신규 위험 없음. `RegionSelector`는 `localeCompare("ko")` 정렬이라 강화군→검단구→계양구→…→제물포구 순 표시(설계 §3-3 서술과 일치).

**프리워밍 인덱스**: `prewarm_progress.last_index = 49`. 새 목록에서 인천 블록 idx **64–74** → `49 < 64` → **스킵 0, 중복 0.** 설계 §5-3 "무조치" 권고 유효.

## 7. 숫자 정합성 (`monthly_stats` 읽기 전용 대조) — **PASS (표본 8/8)**

요구는 1개 이상이었으나 신설 4개 × 매매/전세 **전부** 대조했다. `deal_ym=202608`(화면 `refYm`과 동일).

| 코드 | 유형 | 화면(RSC) | DB | 일치 |
|---|---|---|---|---|
| 28125 제물포구 | 매매 / 전세 | 22,460·5건 / 17,000·3건 | 동일 | ✓✓ |
| 28155 영종구 | 매매 / 전세 | 33,614·7건 / 29,643·11건 | 동일 | ✓✓ |
| 28275 서해구 | 매매 / 전세 | 53,200·5건 / 37,722·16건 | 동일 | ✓✓ |
| 28290 검단구 | 매매 / 전세 | 50,592·15건 / 34,933·22건 | 동일 | ✓✓ |

**불일치 0건 — 라운드45 세종 유형 재발 없음.**

## 8. 추가 독립 검증: "고유 데이터 소실 0건" 반증 시도 — **PASS**

`deals` 잔존 4행 각각을 **11개 축**(단지명·거래유형·건물유형·연·월·일·면적·층·가격·보증금·월세) 완전일치로 승계 구에서 재조회:

```
28110 운서역대라수어썸에듀        2026-06-14 월세 84.9859㎡ 8층 → 28155 완전일치 1건
28260 신검단중앙역디에트르더에듀  2026-05-23 전세 59.8554㎡ 6층 → 28290 완전일치 1건
28260 신검단중앙역디에트르더에듀  2026-06-10 전세 59.8448㎡ 5층 → 28290 완전일치 1건
28260 신검단중앙역디에트르더에듀  2026-06-21 월세 59.8448㎡ 2층 → 28290 완전일치 1건
28140 → 0행
```
지리적 귀속도 교차 확인(운서역=영종도→영종구, 신검단중앙역→검단구). **고유 데이터 소실 0건 확정.** 폐지 3개의 `monthly_stats` 행은 DB에 잔존해 3줄 되돌리기로 복귀 가능(설계 §3-2 "비가역 아님" 근거 유지).

## 9. WARN (FAIL 아님 — 마스터 판단)

- **W1.** 인천 무관 문서 1건이 커밋에 혼입 — `blog-region-search-and-deals-table-howto-copy-2026-08-05.md`(82행, 라운드63 사후 정정 잔여물). diff 내 인천 문자열 0건, 런타임 영향 0이나 커밋 단위 회귀 판정 범위를 흐린다.
- **W2.** 스테일 `.next/lock` — 빌드 완료 후에도 락이 남아 QA 빌드가 즉시 실패했다. 실행 중 `next build` 프로세스 0개를 `Win32_Process`로 확인 후 제거·재실행(gitignore 산출물, 소스 무수정). 마스터 빌드가 정리 없이 종료된 흔적.
- **W3.** 신설 4개의 `refYm`이 **202608(부분월)** 이라 표본 3~15건으로 변화율이 산출된다(서해구 매매 5건, 제물포구 전세 3건). 115개 전 지역 공통 기존 동작이라 이번 변경의 결함은 아니나 특히 얇은 구간.
- **W4.** `.claude/settings.json`이 untracked이면서 `.gitignore` 미적용. 이번 커밋엔 미포함이나 다음에 `git add -A`가 들어가면 추적된다. **자매 공개 저장소 시크릿 사고와 동일 구조.**
- **W5.** 검증 대상이 QA 진행 중 변경·커밋됨(§1). **QA 착수 후에는 대상 트리를 동결하는 편이 안전하다.**

## 10. 미측정 (추정으로 채우지 않음)

1. `/api/data?lawd_cd=28110`의 200 응답 — **의도적 미실행.** 이 경로는 `fetch_cache_status`·`deals`·`monthly_stats`를 **쓰기** 때문에 읽기 전용 원칙상 호출하면 관측 대상을 오염시킨다.
2. `/api/admin/backfill-monthly-stats?lawd_cd=28110` → 400 여부(관리자 인증 경로).
3. 빌라/단독 축의 소급 재배치 여부.
4. 건축HUB의 신설 4개 구 서비스 여부 — `LAWD_TO_SIGUNGU` 항등 매핑은 여전히 **추정값**. 다만 **소비처 0개** 재확인(`grep -rn LAWD_TO_SIGUNGU` = 정의 1 + 주석 2, import 0).
5. 실제 브라우저 스크린샷 — 도구 불안정으로 HTML/RSC 파싱 + 코드 판독으로 대체.

## 11. 재현 명령

```bash
cd "C:\bitcoin_vdcode\realestate_claude code"
git --no-pager diff --numstat 8656aea HEAD -- '*.ts' '*.tsx'
git show --name-only --format="" HEAD | grep -E "db-cache|molit-api|schema.sql"   # 공집합이어야 정상
npx tsc --noEmit && npm run build          # exit 0, static 30/30
npx next start -p 3117 & curl -s localhost:3117/ranking > ranking.html
grep -c "28110\|28140\|28260" ranking.html # 0
```
